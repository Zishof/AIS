package ais.database.model;

// Generated Dec 12, 2009 3:35:45 PM by Hibernate Tools 3.2.4.CR1

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

/**
 * Statusabsensi generated by hbm2java
 *
 * <p><b>Master jenis status kehadiran/absensi pegawai</b> (mis. Hadir, Sakit, Cuti Tahunan, Izin,
 * Alpa) sekaligus aturan cutinya. Penulisan nama kelas TANPA spasi ({@code Statusabsensi}, bukan
 * {@code StatusAbsensi}) dipertahankan apa adanya mengikuti konvensi historis kelas ini -- BUKAN
 * kesalahan ketik yang perlu diperbaiki, karena nama tabel fisik ({@code public.statusabsensi})
 * dan seluruh kode yang mereferensikannya sudah mengikuti ejaan ini.</p>
 *
 * <p>Dua entity lain yang berelasi {@code @ManyToOne} langsung ke tabel ini lewat kolom FK
 * {@code statusabsensi}: {@link StatuskehadiranKaryawanHarian} (rekap kehadiran harian per
 * karyawan, versi aktif -- getter relasinya sendiri bermutasi, lihat javadoc di sana) dan
 * {@link Statuskehadiran_old} (versi lama/warisan). {@link #getMerupakanCuti()} pada kelas ini
 * yang menyimpulkan "apakah status ini termasuk cuti" berdasarkan {@code id} hardcoded (lihat di
 * bawah) dipakai kedua entity tersebut secara tidak langsung lewat status yang mereka rujuk.</p>
 *
 * <h3>Aturan cuti: hardcoded ID, bukan flag data</h3>
 * <p>{@link #getMerupakanCuti()} menyimpulkan apakah suatu status "termasuk cuti" dari daftar
 * {@code id} yang DITULIS LANGSUNG di kode ({@code 4, 21, 22, 23, 24, 7, 25, 26, 8, 20, 6}), bukan
 * dari kolom basis data. Ini berarti menambah jenis cuti baru di data TIDAK otomatis membuatnya
 * dikenali sebagai cuti oleh method ini kecuali id-nya juga ditambahkan ke daftar hardcoded
 * tersebut secara manual -- pola rapuh yang mudah terlewat saat data master bertambah.</p>
 */
@Entity
@org.hibernate.annotations.Entity(dynamicInsert = true, dynamicUpdate = true)
@Audited
@Table(schema = "public", name = "statusabsensi")
public class Statusabsensi extends GeneralValueObject {

	/**
	 * 
	 */
	private static final long serialVersionUID = 4929449510013007095L;

	/** Primary key baris status kehadiran, DIISI MANUAL (lihat catatan pada {@link #getId()}). */
	private Long id;
	/** Jejak "siapa yang menulis baris ini", diisi otomatis lewat {@link #setOleh(String)}. */
	private String oleh;
	/** Pendamping {@link #oleh}. Lihat {@link #getOlehId()}. */
	private String olehId;

	/**
	 * Mengembalikan rantai jejak pemanggil yang tersimpan bersama {@link #oleh}, diisi otomatis
	 * oleh {@code AuditTimestampInterceptor}.
	 *
	 * @return jejak pemanggil; boleh {@code null} bila belum pernah diisi
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menyetel jejak pemanggil {@link #getOlehId()}. Setter ini <b>mengabaikan diam-diam</b>
	 * nilai {@code null}/kosong sehingga jejak lama tidak dapat ditimpa nilai hampa.
	 *
	 * @param olehId jejak pemanggil baru; {@code null}/kosong diabaikan tanpa peringatan
	 */
	public void setOlehId(String olehId) {
		if (olehId == null || olehId.trim().isEmpty()) {
			return;
		}
		this.olehId = olehId;
	}

	/**
	 * Menyetel identitas pelaku penyimpanan baris ini. Nilai {@code null}/kosong diabaikan
	 * diam-diam agar jejak lama tidak tertimpa nilai hampa.
	 *
	 * @param oleh nama/identitas pelaku; {@code null}/kosong diabaikan tanpa peringatan
	 */
	public void setOleh(String oleh) {
		if (oleh == null || oleh.trim().isEmpty()) {
			return;
		}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan identitas pelaku yang terakhir menyimpan baris ini.
	 *
	 * @return nama/identitas pelaku; boleh {@code null} bila belum pernah diisi
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * <i>Callback</i> JPA {@link javax.persistence.PreUpdate} yang memperbarui
	 * {@link #getTanggal_dirubah()}/{@link #getOleh()}/{@link #getOlehId()} lewat
	 * {@code AuditTimestampInterceptor} pada setiap UPDATE baris.
	 */
	@javax.persistence.PreUpdate
	protected void onUpdate() {
		ais.database.hibernate.AuditTimestampInterceptor.ubah(this);
	}

	/** Stempel waktu terakhir baris diubah, diinisialisasi ke waktu server saat objek dibuat. */
	private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menyetel stempel waktu perubahan terakhir. Umumnya diurus otomatis oleh {@link #onUpdate()}.
	 *
	 * @param tanggal_dirubah stempel waktu baru
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan stempel waktu perubahan terakhir (presisi TIMESTAMP).
	 *
	 * @return waktu terakhir baris disimpan/diubah
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/** Representasi teks baris, berupa id digabung {@link #nama}. */
	public String toString() {
		return id + "-" + nama;
	}

	/** Kode singkat status (mis. "H", "S", "CT"). Lihat {@link #getKode()}. */
	private String kode;
	/** Nama status kehadiran (mis. "Hadir", "Sakit", "Cuti Tahunan"). Lihat {@link #getNama()}. */
	private String nama;
	/** Penanda status masih aktif dipakai. Lihat {@link #getAktif()}. */
	private Boolean aktif;
	/** Penanda status termasuk kategori cuti. Lihat {@link #getMerupakanCuti()}. */
	private Boolean merupakanCuti;
	/** Penanda status memotong jatah cuti tahunan. Lihat {@link #getMemotongJatahCuti()}. */
	private Boolean memotongJatahCuti;
	/** Penanda status dihitung sebagai ketidakhadiran pada rekap. Lihat {@link #getDihitungKetidakhadiran()}. */
	private Boolean dihitungKetidakhadiran;
	/** Minimal masa kerja (bulan) sebelum status ini boleh diambil. */
	private Integer bolehDiambilMinimalBulanKerja;
	/** Batas masa kerja (bulan) setelah mana status ini boleh diambil. */
	private Integer bolehDiambilSetelahBulanKerja;
	/** Maksimal masa kerja (bulan) status ini masih boleh diambil. */
	private Integer bolehDiambilMaksimalBulanKerja;

	/** Penanda hari libur ikut dihitung sebagai bagian durasi status ini. */
	private Boolean hariLiburDihitung;
	/** Durasi baku (hari) untuk cuti khusus. Lihat {@link #getDurasiBakuHari()}. */
	private Integer durasiBakuHari;

	/** Konstruktor tanpa argumen yang diwajibkan JPA/Hibernate. */
	public Statusabsensi() {
	}

	/**
	 * Konstruktor ringkas untuk membentuk referensi hanya dengan id, umum dipakai saat entity
	 * lain hanya perlu menunjuk status kehadiran tanpa memuat seluruh datanya dari basis data.
	 *
	 * @param id primary key status yang dirujuk
	 */
	public Statusabsensi(Long id) {
		this.id = id;
	}

	/**
	 * Konstruktor ringkas untuk membentuk status baru dengan kode dan nama sekaligus.
	 *
	 * @param kode kode singkat status
	 * @param nama nama status
	 */
	public Statusabsensi(String kode, String nama) {
		this.kode = kode;
		this.nama = nama;
	}

	/**
	 * Mengembalikan primary key baris status kehadiran.
	 *
	 * <p>Perhatikan bahwa anotasi {@code @GeneratedValue(strategy = IDENTITY)} DIKOMENTARI (tidak
	 * aktif): berbeda dari mayoritas entity lain di paket ini, id kelas ini TIDAK dihasilkan
	 * otomatis oleh basis data pada strategi ini -- kombinasi {@code insertable = false} tanpa
	 * {@code @GeneratedValue} aktif berarti nilai id diharapkan sudah ditentukan sebelum
	 * disimpan (konsisten dengan {@link #getMerupakanCuti()} yang mencocokkan id tertentu secara
	 * hardcoded -- id-id itu harus stabil/dikenal terlebih dahulu, bukan hasil auto-increment
	 * yang bisa berubah urutan).</p>
	 *
	 * @return id baris; {@code null} selama objek belum pernah disimpan
	 */
	@Id
	// @GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menyetel primary key baris status kehadiran.
	 *
	 * @param id nilai primary key
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Mengembalikan kode singkat status, dinormalisasi ke string kosong (bukan {@code null}) bila
	 * belum diisi, dengan spasi tepi dipangkas.
	 *
	 * @return kode status yang sudah di-<i>trim</i>; tidak pernah {@code null}
	 */
	@Column(name = "kode", nullable = false, length = 20)
	public String getKode() {
		return this.kode == null ? "" : this.kode.trim();
	}

	/**
	 * Menyetel kode singkat status.
	 *
	 * @param kode kode baru
	 */
	public void setKode(String kode) {
		this.kode = kode;
	}

	/**
	 * Mengembalikan nama status kehadiran, dengan spasi tepi dipangkas.
	 *
	 * @return nama status yang sudah di-<i>trim</i>; {@code null} bila field belum diisi
	 */
	@Column(name = "nama", nullable = false, length = 150)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Menyetel nama status kehadiran.
	 *
	 * @param nama nama baru
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Mengembalikan penanda apakah status ini masih aktif dipakai. Default {@code true} bila
	 * belum pernah disetel eksplisit -- fail-open untuk status master.
	 *
	 * @return {@code true} bila aktif (nilai bawaan); {@code false} hanya bila eksplisit disetel
	 */
	public Boolean getAktif() {
		return aktif == null ? true : aktif;
	}

	/**
	 * Menyetel penanda status aktif.
	 *
	 * @param aktif nilai baru; {@code null} berarti kembali ke bawaan ({@code true})
	 */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}

	/**
	 * Mengembalikan penanda apakah status kehadiran ini termasuk kategori "cuti".
	 *
	 * <p><b>Bukan kolom data biasa -- disimpulkan dari daftar {@code id} yang DITULIS LANGSUNG di
	 * kode</b> ({@code 4, 21, 22, 23, 24, 7, 25, 26, 8, 20, 6}), hanya dihitung sekali per objek
	 * (di-<i>cache</i> ke field {@link #merupakanCuti} begitu {@link #id} tersedia) dan hanya
	 * bila belum pernah disetel eksplisit lewat {@link #setMerupakanCuti(Boolean)}. Konsekuensi
	 * praktis: status cuti BARU yang ditambahkan ke data master TIDAK akan otomatis dikenali
	 * sebagai cuti oleh method ini kecuali id-nya juga ditambahkan ke daftar hardcoded ini secara
	 * manual -- dan sebaliknya, bila id salah satu status LAMA di daftar ini kelak dipakai ulang
	 * (mis. setelah baris lama dihapus dan id-nya dipakai baris baru yang bukan cuti), status
	 * baru itu akan keliru dianggap cuti.</p>
	 *
	 * @return {@code true} bila id termasuk daftar hardcoded (atau sudah disetel eksplisit
	 *         {@code true}); {@code false} bila tidak termasuk (atau {@link #id} masih
	 *         {@code null}, mis. objek belum disimpan)
	 */
	public Boolean getMerupakanCuti() {
		if (merupakanCuti == null && id != null) {
			merupakanCuti = false;
			Long[] statusabsensis = new Long[] { 4L, 21L, 22L, 23L, 24L, 7L, 25L, 26L, 8L, 20L, 6L };
			for (Long ids : statusabsensis) {
				if (ids.equals(id)) {
					merupakanCuti = true;
					break;
				}
			}
		}
		return merupakanCuti;
	}

	/**
	 * Menyetel penanda "termasuk cuti" secara eksplisit, mem-<i>bypass</i> penyimpulan hardcoded
	 * pada {@link #getMerupakanCuti()}.
	 *
	 * @param merupakanCuti nilai eksplisit baru; {@code null} mengembalikan getter ke logika
	 *                       penyimpulan berdasarkan {@link #id} (bila belum di-<i>cache</i>)
	 */
	public void setMerupakanCuti(Boolean merupakanCuti) {
		this.merupakanCuti = merupakanCuti;
	}

	/**
	 * Mengembalikan penanda apakah status ini memotong jatah cuti tahunan. Bila belum disetel
	 * eksplisit, ikut nilai {@link #getMerupakanCuti()} -- artinya bawaannya adalah "status cuti
	 * memotong jatah, status non-cuti tidak", kecuali diubah manual per status.
	 *
	 * @return {@code true} bila memotong jatah cuti
	 */
	public Boolean getMemotongJatahCuti() {
		return memotongJatahCuti == null ? getMerupakanCuti() : memotongJatahCuti;
	}

	/**
	 * Menyetel penanda pemotongan jatah cuti tahunan secara eksplisit.
	 *
	 * @param memotongJatahCuti nilai baru; {@code null} mengembalikan getter ke bawaan (ikut
	 *                          {@link #getMerupakanCuti()})
	 */
	public void setMemotongJatahCuti(Boolean memotongJatahCuti) {
		this.memotongJatahCuti = memotongJatahCuti;
	}

	/**
	 * Mengembalikan minimal masa kerja (bulan) sebelum status ini boleh diambil pegawai.
	 *
	 * @return minimal bulan kerja; {@code 0} (tidak ada syarat minimal) bila belum diisi
	 */
	public Integer getBolehDiambilMinimalBulanKerja() {
		return bolehDiambilMinimalBulanKerja == null ? 0 : bolehDiambilMinimalBulanKerja;
	}

	/**
	 * Menyetel minimal masa kerja (bulan) syarat pengambilan status ini.
	 *
	 * @param bolehDiambilMinimalBulanKerja nilai baru; {@code null} berarti tidak ada syarat
	 */
	public void setBolehDiambilMinimalBulanKerja(Integer bolehDiambilMinimalBulanKerja) {
		this.bolehDiambilMinimalBulanKerja = bolehDiambilMinimalBulanKerja;
	}

	/**
	 * Mengembalikan maksimal masa kerja (bulan) status ini masih boleh diambil.
	 *
	 * @return maksimal bulan kerja; {@code 0} (tidak ada batas maksimal) bila belum diisi
	 */
	public Integer getBolehDiambilMaksimalBulanKerja() {
		return bolehDiambilMaksimalBulanKerja == null ? 0 : bolehDiambilMaksimalBulanKerja;
	}

	/**
	 * Menyetel maksimal masa kerja (bulan) syarat pengambilan status ini.
	 *
	 * @param bolehDiambilMaksimalBulanKerja nilai baru; {@code null} berarti tidak ada batas
	 */
	public void setBolehDiambilMaksimalBulanKerja(Integer bolehDiambilMaksimalBulanKerja) {
		this.bolehDiambilMaksimalBulanKerja = bolehDiambilMaksimalBulanKerja;
	}

	/**
	 * Mengembalikan batas masa kerja (bulan) setelah mana status ini boleh diambil.
	 *
	 * @return batas bulan kerja; {@code 0} bila belum diisi
	 */
	public Integer getBolehDiambilSetelahBulanKerja() {
		return bolehDiambilSetelahBulanKerja == null ? 0 : bolehDiambilSetelahBulanKerja;
	}

	/**
	 * Menyetel batas masa kerja (bulan) syarat pengambilan status ini.
	 *
	 * @param bolehDiambilSetelahBulanKerja nilai baru
	 */
	public void setBolehDiambilSetelahBulanKerja(Integer bolehDiambilSetelahBulanKerja) {
		this.bolehDiambilSetelahBulanKerja = bolehDiambilSetelahBulanKerja;
	}

	/**
	 * Mengembalikan penanda apakah status ini dihitung sebagai ketidakhadiran pada rekap
	 * kehadiran. Default {@code true} bila belum disetel eksplisit.
	 *
	 * @return {@code true} bila dihitung sebagai ketidakhadiran (nilai bawaan)
	 */
	public Boolean getDihitungKetidakhadiran() {
		return dihitungKetidakhadiran == null ? true : dihitungKetidakhadiran;
	}

	/**
	 * Menyetel penanda dihitung-sebagai-ketidakhadiran.
	 *
	 * @param dihitungKetidakhadiran nilai baru; {@code null} berarti kembali ke bawaan
	 *                               ({@code true})
	 */
	public void setDihitungKetidakhadiran(Boolean dihitungKetidakhadiran) {
		this.dihitungKetidakhadiran = dihitungKetidakhadiran;
	}

	/**
	 * Mengembalikan penanda apakah hari libur ikut dihitung sebagai bagian durasi status ini
	 * (mis. saat menghitung lama cuti yang melewati akhir pekan/libur nasional). Default
	 * {@code false} bila belum disetel eksplisit.
	 *
	 * @return {@code true} bila hari libur ikut dihitung
	 */
	public Boolean getHariLiburDihitung() {
		return hariLiburDihitung == null ? false : hariLiburDihitung;
	}

	/**
	 * Menyetel penanda hari-libur-dihitung.
	 *
	 * @param hariLiburDihitung nilai baru; {@code null} berarti kembali ke bawaan ({@code false})
	 */
	public void setHariLiburDihitung(Boolean hariLiburDihitung) {
		this.hariLiburDihitung = hariLiburDihitung;
	}

	/**
	 * Durasi BAKU (hari) untuk cuti khusus — mis. Menikah 7, Menikahkan anak 3, Melahirkan 90,
	 * Istri melahirkan/keguguran 3, Orang tua meninggal 7, Keluarga inti meninggal 3, Khitanan anak 3.
	 *
	 * <p>{@code null} atau &lt;= 0 berarti TIDAK ada durasi baku (cuti biasa/izin, panjangnya bebas
	 * mengikuti tanggal yang dipilih). Bila diisi, form pengajuan memakainya sebagai batas maksimal
	 * lama cuti untuk status ini. Kolom aditif &amp; nullable; pembuatannya diserahkan ke Hibernate
	 * ({@code hbm2ddl.auto=update}). Karena entity ini {@code @Audited}, tabel audit
	 * {@code new_audit.statusabsensi__audit} diselaraskan oleh {@code ais.common.AturanCutiSchemaFix}.</p>
	 */
	@Column(name = "durasi_baku_hari", nullable = true)
	public Integer getDurasiBakuHari() {
		return durasiBakuHari == null ? 0 : durasiBakuHari;
	}

	/**
	 * Menyetel durasi baku (hari) untuk cuti khusus.
	 *
	 * @param durasiBakuHari nilai baru; {@code null}/{@code <= 0} berarti tidak ada durasi baku
	 */
	public void setDurasiBakuHari(Integer durasiBakuHari) {
		this.durasiBakuHari = durasiBakuHari;
	}

}
