package ais.database.model;

// Generated Dec 22, 2009 9:02:38 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

/**
 * StatusMahasiswa generated by hbm2java
 *
 * <p><b>Master jenis status keaktifan mahasiswa</b> (mis. Aktif, Cuti, Non&nbsp;Aktif, Lulus, Drop
 * Out) &mdash; satu baris tabel {@code public.status_mahasiswa} adalah satu nilai yang dapat dipilih
 * di kolom "status" pada berbagai layar akademik. Entity ini sendiri <b>tidak</b> menyimpan status
 * seorang mahasiswa pada suatu semester; ia hanya daftar pilihannya (lookup).</p>
 *
 * <h2>Relasi dengan {@link HistoryStatusMahasiswa} (diverifikasi dari kode)</h2>
 * <p>Sesuai catatan {@link HistoryStatusMahasiswa}, entity itulah <b>satu-satunya sumber
 * kebenaran</b> status per mahasiswa per semester &mdash; {@link Mahasiswa} sendiri tidak punya
 * kolom status. {@code HistoryStatusMahasiswa.ambilStatusMahasiswa(Integer)} adalah mesin yang
 * MEMILIH salah satu baris entity ini (lewat {@link HistoryStatusMahasiswa#getStatusMahasiswa()})
 * berdasarkan kaskade aturan (status terminal Lulus/Drop&nbsp;Out, override {@link
 * KelompokStatusMahasiswa}, pengajuan cuti, keaktifan SKS, blok pembayaran/batas studi). Entity ini
 * juga dipakai sebagai <b>status tujuan</b> pada {@link KelompokStatusMahasiswa} (SK kolektif yang
 * memaksa status banyak mahasiswa sekaligus pada rentang semester tertentu) &mdash; lihat javadoc
 * kelas tersebut untuk detail kaskade override-nya.</p>
 *
 * <h2>Peta anggota</h2>
 * <ul>
 *   <li><b>Identitas &amp; audit</b>: {@link #getId()}/{@link #setId(Long)},
 *       {@link #getOleh()}/{@link #setOleh(String)}, {@link #getOlehId()}/{@link #setOlehId(String)},
 *       {@link #getTanggal_dirubah()}/{@link #setTanggal_dirubah(Date)}, {@link #onUpdate()}.</li>
 *   <li><b>Deskriptif</b>: {@link #getNama()}/{@link #setNama(String)} (unik, wajib),
 *       {@link #getKodeEpsbed()}/{@link #setKodeEpsbed(String)} (kode pelaporan EPSBED/PDDikti),
 *       {@link #getAktif()}/{@link #setAktif(Boolean)} (flag tampil di combobox pemilihan).</li>
 * </ul>
 *
 * <p>{@link GeneralValueObject} bukan {@code @Entity}/{@code @MappedSuperclass} sehingga
 * {@code id}, {@code oleh}, {@code olehId}, {@code tanggal_dirubah} harus dideklarasikan ulang di
 * setiap entity turunan &mdash; keharusan teknis pola paket ini, bukan duplikasi yang perlu
 * dirapikan. Diaudit lewat Hibernate Envers ({@code @Audited}).</p>
 *
 * @see HistoryStatusMahasiswa
 * @see HistoryStatusMahasiswa#ambilStatusMahasiswa(Integer)
 * @see KelompokStatusMahasiswa
 * @see GeneralValueObject
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "public", name = "status_mahasiswa")

public class StatusMahasiswa extends GeneralValueObject {

	/**
	 * Penanda versi serialisasi Java. Tidak berdampak fungsional; nilainya khas per berkas hasil
	 * generator, tidak dipakai sebagai acuan identitas kelas.
	 */
	private static final long serialVersionUID = 6891282957373067916L;

	/** Kunci utama, lihat {@link #getId()}. */
	private Long id;

	/** Nama pengguna pengubah terakhir, lihat {@link #getOleh()}. */
	private String oleh;

	/** Id pengguna pengubah terakhir, lihat {@link #getOlehId()}. */
	private String olehId;

	/**
	 * Id pengguna pengubah terakhir.
	 *
	 * @return id pengguna pengubah terakhir; {@code null} bila belum pernah tercatat.
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menetapkan id pengguna pengubah terakhir. Setter penjaga: nilai {@code null}/kosong diabaikan
	 * diam-diam sehingga jejak audit yang sudah terisi tidak dapat dihapus lewat setter ini.
	 *
	 * @param olehId id pengguna; diabaikan bila {@code null} atau kosong.
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Menetapkan nama pengguna pengubah terakhir. Berperilaku sama dengan
	 * {@link #setOlehId(String)}: nilai {@code null}/kosong diabaikan diam-diam.
	 *
	 * @param oleh nama pengguna; diabaikan bila {@code null} atau kosong.
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Nama pengguna yang terakhir mengubah baris ini.
	 *
	 * @return nama pengguna pengubah terakhir; {@code null} bila belum pernah tercatat.
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Hook JPA {@code @PreUpdate} yang menyegarkan metadata audit sebelum baris ini di-{@code UPDATE},
	 * plus deklarasi field {@code tanggal_dirubah} yang menempel pada baris kode yang sama (bentuk
	 * padat warisan penyuntingan massal, dibiarkan apa adanya). Mendelegasikan ke
	 * {@code ais.database.hibernate.AuditTimestampInterceptor.ubah(this)}; dipanggil Hibernate,
	 * tidak pernah dipanggil manual.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menetapkan stempel waktu perubahan terakhir. Tidak menjaga terhadap {@code null}.
	 *
	 * @param tanggal_dirubah waktu perubahan terakhir; boleh {@code null}.
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Waktu terakhir baris ini diubah.
	 *
	 * @return waktu perubahan terakhir, dipetakan sebagai {@code TIMESTAMP}.
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Representasi teks baris ini dalam bentuk {@code "<id>-<nama>"}, dipakai antara lain oleh
	 * {@code KelompokStatusMahasiswa.toString()} saat merangkai status tujuan SK.
	 *
	 * @return string gabungan id dan nama status.
	 */
	public String toString() {
		return id + "-" + nama;
	}

	/** Nama/label status (mis. "Aktif", "Cuti"), lihat {@link #getNama()}. */
	private String nama;

	/** Kode pelaporan EPSBED/PDDikti, lihat {@link #getKodeEpsbed()}. */
	private String kodeEpsbed;

	/** Flag tampil-aktif di combobox pemilihan, lihat {@link #getAktif()}. */
	private Boolean aktif;

	/** Konstruktor tanpa argumen yang dibutuhkan Hibernate/JPA. */
	public StatusMahasiswa() {
	}

	/**
	 * Konstruktor pintas untuk merujuk baris yang sudah ada lewat id-nya saja (mis. saat membangun
	 * referensi {@code @ManyToOne} tanpa memuat seluruh baris).
	 *
	 * @param id kunci utama baris yang dirujuk.
	 */
	public StatusMahasiswa(Long id) {
		this.id = id;
	}

	/**
	 * Konstruktor pintas untuk membuat baris baru dengan nama status langsung terisi.
	 *
	 * @param nama nama/label status.
	 */
	public StatusMahasiswa(String nama) {
		this.nama = nama;
	}

	/**
	 * Kunci utama baris status (kolom {@code id}, {@code IDENTITY} &mdash; dibangkitkan database).
	 *
	 * @return id baris; {@code null} selama object belum pernah disimpan.
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menetapkan kunci utama. Praktis hanya dipanggil Hibernate.
	 *
	 * @param id kunci utama baris.
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Nama/label status (kolom {@code nama}, wajib &amp; unik). Dikembalikan sudah di-{@code trim()}.
	 *
	 * @return nama status; {@code null} bila belum diisi.
	 */
	@Column(name = "nama", nullable = false, unique = true, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Menetapkan nama/label status.
	 *
	 * @param nama nama status; wajib unik pada database.
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Menetapkan kode pelaporan EPSBED/PDDikti yang berpadanan dengan status ini.
	 *
	 * @param kodeEpsbed kode pelaporan; boleh {@code null}.
	 */
	public void setKodeEpsbed(String kodeEpsbed) {
		this.kodeEpsbed = kodeEpsbed;
	}

	/**
	 * Kode pelaporan EPSBED/PDDikti (kolom {@code kode_epsbed}), dikembalikan sudah di-{@code trim()}
	 * dan tidak pernah {@code null} (kosong bila belum diisi).
	 *
	 * @return kode pelaporan; string kosong bila belum diisi.
	 */
	@Column(name = "kode_epsbed")
	public String getKodeEpsbed() {
		return kodeEpsbed == null ? "" : kodeEpsbed.trim();
	}

	/**
	 * Flag apakah status ini masih ditawarkan di combobox pemilihan. Getter berpola
	 * <i>default-true</i>: {@code null} (baris lama/belum diisi) dibaca sebagai aktif.
	 *
	 * @return {@code true} bila aktif atau belum diisi; {@code false} hanya bila eksplisit dinonaktifkan.
	 */
	public Boolean getAktif() {
		return aktif == null ? true : aktif;
	}

	/**
	 * Menetapkan flag aktif/nonaktif status ini.
	 *
	 * @param aktif {@code true}/{@code null} untuk aktif, {@code false} untuk menyembunyikannya dari
	 *              combobox pemilihan.
	 */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}

}
