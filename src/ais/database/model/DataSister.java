package ais.database.model;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

/**
 * Bank generated by hbm2java
 *
 * <p><b>Cadangan JSON mentah hasil tarikan API SISTER Kemdikbud</b> &mdash; satu baris tabel
 * {@code public.data_sister} menyimpan satu blob JSON ({@link #getKeterangan()}, kolom
 * {@code text}) hasil pemanggilan satu endpoint/tabel referensi SISTER
 * ({@link #getNama()} &mdash; nama endpoint atau {@code "<endpoint>?id_sdm=<id>"}), dengan
 * {@link #getKode()} sebagai kunci baris per dosen/objek (biasanya {@code idSdm}). Ditulis oleh
 * {@code ais.common.DataSisterApi} (lihat {@code simpanArrayDataSister}/{@code simpanObjekDataSister}).</p>
 *
 * <h2>Relasi dengan paket {@code ais.database.model.sister} (diverifikasi dari kode, PENTING agar
 * tidak duplikasi)</h2>
 * <p>Entity ini <b>bukan bagian</b> dari paket {@code ais.database.model.sister} (88 entity
 * terstruktur seperti {@code RefSdmSister}, {@code DpProfilSister}, {@code TridPenelitianSister},
 * dst. &mdash; mesin sinkronisasi SISTER yang sudah didokumentasikan tuntas). Keduanya
 * <b>komplementer, bukan tumpang tindih</b>:</p>
 * <ul>
 *   <li>Entity terstruktur di paket {@code sister} menyimpan data yang <b>sudah diparse</b> ke
 *       kolom-kolom bertipe sesuai skema masing-masing endpoint (bila mapping-nya sudah dibuat).</li>
 *   <li><b>Entity ini ({@code DataSister}, di root paket model)</b> selalu menyimpan
 *       <b>cadangan JSON generik</b> dari respons API &mdash; menurut komentar {@code DataSisterApi},
 *       cadangan ini ditulis <b>SELALU</b> untuk endpoint yang responsnya berbentuk objek/paginasi
 *       (hanya dilewati untuk endpoint array yang punya entitas terstruktur, guna mencegah hitung
 *       ganda pada counter dasbor), dan menjadi satu-satunya penyimpanan untuk endpoint yang
 *       <b>belum</b> punya entity terstruktur.</li>
 * </ul>
 * <p>Dengan kata lain: {@code sister.*} = data siap pakai per skema; entity ini = arsip JSON mentah
 * untuk audit/observabilitas dan fallback endpoint yang belum dipetakan. {@link #getNama()} dipakai
 * juga oleh {@code DataSisterApi.hitungPerNamaTabel(...)} untuk grafik dasbor jumlah baris per
 * endpoint.</p>
 *
 * <h2>Peta anggota</h2>
 * <ul>
 *   <li><b>Identitas &amp; audit</b>: {@link #getId()}/{@link #setId(Long)},
 *       {@link #getOleh()}/{@link #setOleh(String)}, {@link #getOlehId()}/{@link #setOlehId(String)},
 *       {@link #getTanggal_dirubah()}/{@link #setTanggal_dirubah(Date)}, {@link #onUpdate()}.</li>
 *   <li><b>Isi cadangan</b>: {@link #getKode()}/{@link #setKode(String)} (kunci baris per
 *       dosen/objek, mis. {@code idSdm}), {@link #getNama()}/{@link #setNama(String)} (nama
 *       endpoint/tabel, wajib), {@link #getKeterangan()}/{@link #setKeterangan(String)} (blob JSON
 *       mentah), {@link #getAktif()}/{@link #setAktif(Boolean)}.</li>
 * </ul>
 *
 * @see ais.common.DataSisterApi
 * @see GeneralValueObject
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "public", name = "data_sister")
public class DataSister extends GeneralValueObject {

	/**
	 * Penanda versi serialisasi Java. Nilainya sama dengan sejumlah entity lain di paket ini
	 * (jejak salin-tempel generator); tidak berdampak fungsional.
	 */
	private static final long serialVersionUID = 2463821577548439808L;

	/** Kunci utama, lihat {@link #getId()}. */
	private Long id;

	/** Nama pengguna pengubah terakhir, lihat {@link #getOleh()}. */
	private String oleh;

	/** Id pengguna pengubah terakhir, lihat {@link #getOlehId()}. */
	private String olehId;

	/**
	 * Id pengguna pengubah terakhir.
	 *
	 * @return id pengguna pengubah terakhir; {@code null} bila belum pernah tercatat.
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menetapkan id pengguna pengubah terakhir. Setter penjaga: nilai {@code null}/kosong diabaikan
	 * diam-diam sehingga jejak audit yang sudah terisi tidak dapat dihapus lewat setter ini.
	 *
	 * @param olehId id pengguna; diabaikan bila {@code null} atau kosong.
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Menetapkan nama pengguna pengubah terakhir. Berperilaku sama dengan
	 * {@link #setOlehId(String)}: nilai {@code null}/kosong diabaikan diam-diam.
	 *
	 * @param oleh nama pengguna; diabaikan bila {@code null} atau kosong.
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Nama pengguna yang terakhir mengubah baris ini.
	 *
	 * @return nama pengguna pengubah terakhir; {@code null} bila belum pernah tercatat.
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Hook JPA {@code @PreUpdate} yang menyegarkan metadata audit sebelum baris ini di-{@code UPDATE},
	 * plus deklarasi field {@code tanggal_dirubah} yang menempel pada baris kode yang sama. Mendelegasikan
	 * ke {@code ais.database.hibernate.AuditTimestampInterceptor.ubah(this)}; dipanggil Hibernate saja.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menetapkan stempel waktu perubahan terakhir. Tidak menjaga terhadap {@code null}.
	 *
	 * @param tanggal_dirubah waktu perubahan terakhir; boleh {@code null}.
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Waktu terakhir baris ini diubah.
	 *
	 * @return waktu perubahan terakhir, dipetakan sebagai {@code TIMESTAMP}.
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Representasi teks baris ini dalam bentuk {@code "<id>-<nama>"}.
	 *
	 * @return string gabungan id dan nama.
	 */
	public String toString() {
		return id + "-" + nama;
	}

	/** Kunci baris per dosen/objek (mis. idSdm), lihat {@link #getKode()}. */
	private String kode;

	/** Nama endpoint/tabel SISTER asal data, lihat {@link #getNama()}. */
	private String nama;

	/** Blob JSON mentah hasil tarikan, lihat {@link #getKeterangan()}. */
	private String keterangan;

	/** Flag aktif, lihat {@link #getAktif()}. */
	private Boolean aktif;

	/** Konstruktor tanpa argumen yang dibutuhkan Hibernate/JPA. */
	public DataSister() {
	}

	/**
	 * Kunci utama baris (kolom {@code id}, {@code IDENTITY} &mdash; dibangkitkan database).
	 *
	 * @return id baris; {@code null} selama object belum pernah disimpan.
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menetapkan kunci utama. Praktis hanya dipanggil Hibernate.
	 *
	 * @param id kunci utama baris.
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Kunci baris per dosen/objek pada endpoint tertentu (biasanya {@code idSdm} dari SISTER),
	 * dikembalikan sudah di-{@code trim()}. Berbeda dari sebagian saudara lookup di paket ini,
	 * getter ini mengembalikan {@code null} (bukan string kosong) baik untuk field {@code null}
	 * maupun kosong.
	 *
	 * @return kode; {@code null} bila belum diisi atau kosong.
	 */
	public String getKode() {
		return kode == null || kode.isEmpty() ? null : kode.trim();
	}

	/**
	 * Menetapkan kunci baris per dosen/objek.
	 *
	 * @param kode kode; boleh {@code null}.
	 */
	public void setKode(String kode) {
		this.kode = kode;
	}

	/**
	 * Nama endpoint/tabel referensi SISTER asal data (kolom {@code nama}, bertipe {@code text},
	 * wajib) &mdash; dipakai juga sebagai dimensi pengelompokan pada grafik dasbor jumlah baris per
	 * endpoint. Dikembalikan sudah di-{@code trim()}.
	 *
	 * @return nama endpoint/tabel; {@code null} bila belum diisi.
	 */
	@Column(name = "nama", nullable = false, columnDefinition = "text")
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Menetapkan nama endpoint/tabel referensi SISTER asal data.
	 *
	 * @param nama nama endpoint/tabel.
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Blob JSON mentah hasil tarikan API SISTER (kolom {@code keterangan}, bertipe {@code text}),
	 * dikembalikan apa adanya tanpa {@code trim()} (penting agar JSON tidak berubah bentuk).
	 *
	 * @return blob JSON mentah; {@code null} bila belum diisi.
	 */
	@Column(name = "keterangan", nullable = true, columnDefinition = "text")
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * Menetapkan blob JSON mentah hasil tarikan.
	 *
	 * @param keterangan teks JSON; boleh {@code null}.
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * Flag aktif. Getter berpola <i>default-true</i>: {@code null} (baris lama/belum diisi) dibaca
	 * sebagai aktif.
	 *
	 * @return {@code true} bila aktif atau belum diisi; {@code false} hanya bila eksplisit dinonaktifkan.
	 */
	public Boolean getAktif() {
		return aktif == null ? true : aktif;
	}

	/**
	 * Menetapkan flag aktif/nonaktif baris ini.
	 *
	 * @param aktif {@code true}/{@code null} untuk aktif, {@code false} untuk nonaktif.
	 */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}

}
