package ais.database.model;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

/**
 * Entity Hibernate untuk tabel {@code public.jenis_aktfitas_mahasiswa} &mdash; master jenis
 * aktivitas mahasiswa (mis. untuk pelaporan feeder PDDIKTI/Kampus Merdeka).
 *
 * <p><b>Catatan penamaan:</b> nama class, tabel, dan field pada entity ini memuat dua salah eja
 * peninggalan kode asli yang DIPERTAHANKAN apa adanya (mengubahnya berarti migrasi skema/rename
 * lintas seluruh pemakai, di luar cakupan dokumentasi): "Aktfitas" (seharusnya "Aktivitas") pada
 * nama class/tabel, dan {@link #getKampusMerderka() kampusMerderka} (seharusnya "kampusMerdeka")
 * pada field boolean penanda Kampus Merdeka.
 *
 * <p>Javadoc class ini sebelumnya adalah stub salah-tempel ("Bank generated by hbm2java")
 * peninggalan generator hbm2java untuk entity lain; sudah dikoreksi di sini tanpa mengubah
 * perilaku.
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "public", name = "jenis_aktfitas_mahasiswa")

public class JenisAktfitasMahasiswa extends GeneralValueObject {

	/**
	 *
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	private Long id;
	/** Nama pelaku (audit shadow, lihat {@link GeneralValueObject}) yang membuat/mengubah baris ini. */
	private String oleh;
	/** Id pelaku (audit shadow) yang membuat/mengubah baris ini. */
	private String olehId;

	/** @return id pelaku terakhir yang mengubah baris ini, atau {@code null} bila belum pernah diisi. */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Mengisi id pelaku. Nilai kosong/blank diabaikan (fail-safe agar audit shadow tidak
	 * tertimpa string kosong secara tidak sengaja) &mdash; bukan validasi keamanan.
	 *
	 * @param olehId id pelaku; diabaikan jika {@code null} atau hanya berisi spasi
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Mengisi nama pelaku. Nilai kosong/blank diabaikan, sama seperti {@link #setOlehId(String)}.
	 *
	 * @param oleh nama pelaku; diabaikan jika {@code null} atau hanya berisi spasi
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/** @return nama pelaku terakhir yang mengubah baris ini, atau {@code null} bila belum pernah diisi. */
	public String getOleh() {
		return oleh;
	}

	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Mengubah stempel waktu perubahan terakhir secara manual. Nilai default sudah di-set ke
	 * waktu saat ini pada deklarasi field dan di-refresh otomatis oleh {@link #onUpdate()} pada
	 * setiap update; setter ini jarang perlu dipanggil langsung.
	 *
	 * @param tanggal_dirubah stempel waktu perubahan baru
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/** @return stempel waktu perubahan terakhir baris ini. */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/** @return representasi ringkas berupa {@code id-nama}, dipakai untuk debugging/log. */
	public String toString() {
		return id + "-" + nama;
	}

	private String kode;

	private String nama;
	private String keterangan;
	private Boolean aktif;
	private Boolean kampusMerderka;
	private Long feeder;

	/** Konstruktor kosong yang diperlukan Hibernate. */
	public JenisAktfitasMahasiswa() {
	}

	/** @return id baris (primary key, auto-generated identity di database). */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/** @param id id baris; kolom tidak insertable sehingga nilai ini diabaikan saat insert. */
	public void setId(Long id) {
		this.id = id;
	}

	/** @return kode jenis aktivitas, di-trim setiap dibaca; string kosong (bukan {@code null}) bila belum diisi. */
	public String getKode() {
		return kode == null ? "" : kode.trim();
	}

	/** @param kode kode jenis aktivitas untuk baris ini. */
	public void setKode(String kode) {
		this.kode = kode;
	}

	/** @return nama jenis aktivitas, di-trim setiap dibaca; wajib diisi (kolom {@code NOT NULL}). */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/** @param nama nama jenis aktivitas; disimpan apa adanya (trim terjadi saat dibaca lewat {@link #getNama()}). */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/** @return keterangan tambahan untuk jenis aktivitas ini, boleh {@code null}. */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/** @param keterangan keterangan tambahan untuk jenis aktivitas ini. */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * @return kode feeder PDDIKTI untuk jenis aktivitas ini; default {@code 1L} bila belum
	 * diisi. <b>Catatan:</b> pemanggilan pertama MENULIS default itu ke field {@link #feeder}
	 * (getter destruktif) &mdash; pola berulang di banyak entity pada codebase ini, bukan bug
	 * baru yang spesifik untuk kelas ini.
	 */
	public Long getFeeder() {
		if (feeder == null) {
			feeder = 1L;
		}
		return feeder;
	}

	/** @param feeder kode feeder PDDIKTI untuk jenis aktivitas ini. */
	public void setFeeder(Long feeder) {
		this.feeder = feeder;
	}

	/** @return status aktif jenis aktivitas ini; default {@code true} bila belum pernah diisi. */
	public Boolean getAktif() {
		return aktif == null ? true : aktif;
	}

	/** @param aktif status aktif jenis aktivitas ini. */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}

	/**
	 * @return apakah jenis aktivitas ini tergolong program Kampus Merdeka, boleh {@code null}
	 * bila belum pernah diisi (tidak ada default seperti field boolean lain di kelas ini). Nama
	 * getter/field memuat salah eja "Merderka" peninggalan kode asli, lihat catatan penamaan di
	 * Javadoc class.
	 */
	public Boolean getKampusMerderka() {
		return kampusMerderka;
	}

	/** @param kampusMerderka apakah jenis aktivitas ini tergolong program Kampus Merdeka. */
	public void setKampusMerderka(Boolean kampusMerderka) {
		this.kampusMerderka = kampusMerderka;
	}

	/**
	 * Setter tanpa efek (no-op) yang dipasangkan dengan {@link #getMerupakanKampusMerdeka()} agar
	 * pasangan getter/setter lengkap untuk binding komponen UI (mis. ZK) yang mensyaratkan kedua
	 * accessor pada satu "properti"; nilai yang dikirim TIDAK PERNAH disimpan ke mana pun.
	 *
	 * @param kampusMerdeka diabaikan sepenuhnya, hanya untuk kelengkapan kontrak bean.
	 */
	public void setMerupakanKampusMerdeka(String kampusMerdeka) {

	}

	/**
	 * @return label tampilan siap-pakai "Kampus Merdeka : YA" atau "Kampus Merdeka : TIDAK",
	 * diturunkan dari {@link #getKampusMerderka()} (properti derivasi murni, tidak dipetakan
	 * sebagai kolom tersendiri).
	 */
	public String getMerupakanKampusMerdeka() {
		return getKampusMerderka() != null && getKampusMerderka() ? "Kampus Merdeka : YA" : "Kampus Merdeka : TIDAK";
	}
}
