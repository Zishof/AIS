package ais.database.model.config;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

import ais.database.model.GeneralValueObject;

/**
 * Bank generated by hbm2java
 *
 * <p>Entity (tabel {@code config.schema_config} — satu-satunya entity di seluruh basis kode yang
 * dipetakan ke skema database {@code config}, bukan {@code public} atau skema domain lain) yang
 * pasangan kolom {@link #getNama()}/{@link #getSchema_yang_digunakan()}-nya menyiratkan rancangan
 * "nama konfigurasi → nama skema DB yang harus dipakai" — kemungkinan besar dimaksudkan sebagai
 * mekanisme pemilihan skema per konteks (mis. skema-per-institusi) pada masa rancangan awal AIS.</p>
 *
 * <p><b>TEMUAN AUDIT — entity YATIM/TIDUR (tidak pernah dipakai):</b> per pemeriksaan seluruh basis
 * kode pada saat javadoc ini ditulis, TIDAK ADA satu pun class Action/helper/listener di paket
 * mana pun yang mereferensikan {@code SchemaConfig} atau kolom {@code schema_yang_digunakan} —
 * satu-satunya rujukan di luar file ini adalah pemetaan Hibernate ({@code hibernate.cfg.xml}, entity
 * tetap ter-mapping dan tabelnya tetap ada di skema database). {@code svn log} tabel ini hanya
 * mencatat satu revisi (impor awal repositori), tanpa jejak pernah dipakai lalu dihapus. Juga TIDAK
 * ADA mekanisme multi-schema/multi-tenancy Hibernate ({@code hibernate.multiTenancy}, pergantian
 * skema saat runtime, dsb.) di mana pun pada basis kode — arsitektur multi-tenant AIS yang benar-benar
 * dipakai berbasis KOLOM (mis. {@code satuanKerja}/tenant key pada baris data), BUKAN skema-per-tenant.
 * Kesimpulan paling mungkin: ini adalah rancangan awal skema-per-konteks yang ditinggalkan sebelum
 * pernah diimplementasikan pada lapisan aplikasi, digantikan pendekatan scoping berbasis kolom yang
 * dipakai di seluruh modul lain.</p>
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "config", name = "schema_config")
public class SchemaConfig extends GeneralValueObject {

	/**
	 *
	 */
	private static final long serialVersionUID = 2463821577541439808L;
	/** Primary key (identity, auto-generated oleh database). */
	private Long id;
	/** Nama pengguna yang terakhir mengubah baris ini. Field audit shadow — lihat javadoc kelas induk sejenis. */
	private String oleh;
	/** Id pengguna yang terakhir mengubah baris ini. Field audit shadow — lihat javadoc kelas induk sejenis. */
	private String olehId;

	/** @return id pengguna yang terakhir mengubah baris ini, atau {@code null} bila belum pernah tercatat. */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Set id pengguna yang mengubah baris ini. NO-OP (nilai lama dipertahankan) bila {@code olehId} null
	 * atau kosong-setelah-trim — mencegah riwayat audit tertimpa nilai kosong secara tidak sengaja.
	 *
	 * @param olehId id pengguna; diabaikan bila null/kosong.
	 */
	public void setOlehId(String olehId) {
		if (olehId == null || olehId.trim().isEmpty()) {
			return;
		}
		this.olehId = olehId;
	}

	/**
	 * Set nama pengguna yang mengubah baris ini. NO-OP (nilai lama dipertahankan) bila {@code oleh} null
	 * atau kosong-setelah-trim — mencegah riwayat audit tertimpa nilai kosong secara tidak sengaja.
	 *
	 * @param oleh nama pengguna; diabaikan bila null/kosong.
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/** @return nama pengguna yang terakhir mengubah baris ini, atau {@code null} bila belum pernah tercatat. */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Callback JPA {@code @PreUpdate}: dipanggil otomatis oleh provider persistence tepat sebelum baris ini
	 * di-UPDATE, mendelegasikan ke {@link ais.database.hibernate.AuditTimestampInterceptor#ubah(Object)}
	 * untuk memperbarui {@link #tanggal_dirubah}. Tidak dipanggil manual dari kode aplikasi.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Set timestamp perubahan terakhir. Biasanya diisi otomatis oleh {@link #onUpdate()} / interceptor
	 * audit, bukan dipanggil manual oleh kode bisnis.
	 *
	 * @param tanggal_dirubah timestamp perubahan terakhir.
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/** @return timestamp perubahan terakhir; default nilai saat instance dibuat (belum tentu waktu commit DB). */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/** @return representasi singkat — nama konfigurasi ({@link #getNama()}), dipakai pada log/debug dan tampilan combo/bandbox. */
	public String toString() {
		return nama;
	}

	/** Nama/kunci konfigurasi skema, mis. dipakai memilih baris ini sebagai "config aktif". Lihat javadoc kelas untuk status yatim entity ini. */
	private String nama;
	/** Nama skema database yang (menurut rancangan) harus dipakai untuk konfigurasi bernama {@link #nama}. Tidak pernah dibaca kode mana pun — lihat javadoc kelas. */
	private String schema_yang_digunakan;

	/** Constructor default (dibutuhkan Hibernate); tidak menginisialisasi field apa pun secara eksplisit. */
	public SchemaConfig() {
	}

	/** @return primary key baris ini, atau {@code null} bila instance belum pernah dipersist. */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/** @param id primary key; biasanya hanya diisi framework persistence, bukan kode aplikasi. */
	public void setId(Long id) {
		this.id = id;
	}

	/** @return nama/kunci konfigurasi, di-trim; {@code null} bila field asli {@code null}. Kolom wajib diisi (nullable=false). */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/** @param nama nama/kunci konfigurasi. */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/** @param schema_yang_digunakan nama skema database yang dirujuk konfigurasi ini. */
	public void setSchema_yang_digunakan(String schema_yang_digunakan) {
		this.schema_yang_digunakan = schema_yang_digunakan;
	}

	/** @return nama skema database yang dirujuk konfigurasi ini, atau {@code null} bila belum diisi. */
	public String getSchema_yang_digunakan() {
		return schema_yang_digunakan;
	}

}
