package ais.database.model;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

/**
 * Entity Hibernate untuk tabel {@code public.jenis_sekolah_mahasiswa_baru} &mdash; master jenis
 * sekolah asal (mis. SMA, SMK, MA, sederajat) yang dipakai saat mendata riwayat pendidikan calon
 * mahasiswa baru pada modul PMB. Dipasangkan dengan {@link NamaSekolahAsal} dan
 * {@link JurusanSekolahMahasiswaBaru} untuk membentuk struktur asal sekolah calon mahasiswa.
 *
 * <p>Javadoc class ini sebelumnya adalah stub salah-tempel ("Bank generated by hbm2java")
 * peninggalan generator hbm2java untuk entity lain; sudah dikoreksi di sini tanpa mengubah
 * perilaku.
 *
 * @see NamaSekolahAsal
 * @see JurusanSekolahMahasiswaBaru
 */
@Entity
@org.hibernate.annotations.Entity(dynamicInsert = true, dynamicUpdate = true)
@Audited
@Table(schema = "public", name = "jenis_sekolah_mahasiswa_baru")
public class JenisSekolahMahasiswaBaru extends GeneralValueObject {

	/**
	 *
	 */
	private static final long serialVersionUID = 2463821577518439808L;
	private Long id;
	/** Nama pelaku (audit shadow, lihat {@link GeneralValueObject}) yang membuat/mengubah baris ini. */
	private String oleh;
	/** Id pelaku (audit shadow) yang membuat/mengubah baris ini. */
	private String olehId;

	/** @return id pelaku terakhir yang mengubah baris ini, atau {@code null} bila belum pernah diisi. */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Mengisi id pelaku. Nilai kosong/blank diabaikan (fail-safe agar audit shadow tidak
	 * tertimpa string kosong secara tidak sengaja) &mdash; bukan validasi keamanan.
	 *
	 * @param olehId id pelaku; diabaikan jika {@code null} atau hanya berisi spasi
	 */
	public void setOlehId(String olehId) {
		if (olehId == null || olehId.trim().isEmpty()) {
			return;
		}
		this.olehId = olehId;
	}

	/**
	 * Mengisi nama pelaku. Nilai kosong/blank diabaikan, sama seperti {@link #setOlehId(String)}.
	 *
	 * @param oleh nama pelaku; diabaikan jika {@code null} atau hanya berisi spasi
	 */
	public void setOleh(String oleh) {
		if (oleh == null || oleh.trim().isEmpty()) {
			return;
		}
		this.oleh = oleh;
	}

	/** @return nama pelaku terakhir yang mengubah baris ini, atau {@code null} bila belum pernah diisi. */
	public String getOleh() {
		return oleh;
	}

	/** Menandai baris berubah; dipanggil otomatis oleh Hibernate sebelum setiap update. */
	@javax.persistence.PreUpdate
	protected void onUpdate() {
		ais.database.hibernate.AuditTimestampInterceptor.ubah(this);
	}

	private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Mengubah stempel waktu perubahan terakhir secara manual. Nilai default sudah di-set ke
	 * waktu saat ini pada deklarasi field dan di-refresh otomatis oleh {@link #onUpdate()} pada
	 * setiap update; setter ini jarang perlu dipanggil langsung.
	 *
	 * @param tanggal_dirubah stempel waktu perubahan baru
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/** @return stempel waktu perubahan terakhir baris ini. */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/** @return representasi ringkas berupa {@code id-nama}, dipakai untuk debugging/log. */
	public String toString() {
		return id + "-" + nama;
	}

	private String nama;
	private String keterangan;
	private String kode;
	private Boolean aktif;

	/** Konstruktor kosong yang diperlukan Hibernate. */
	public JenisSekolahMahasiswaBaru() {
	}

	/** @return id baris (primary key, auto-generated identity di database). */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/** @param id id baris; kolom tidak insertable sehingga nilai ini diabaikan saat insert. */
	public void setId(Long id) {
		this.id = id;
	}

	/** @return nama jenis sekolah, di-trim setiap dibaca; wajib diisi (kolom {@code NOT NULL}). */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/** @param nama nama jenis sekolah; disimpan apa adanya (trim terjadi saat dibaca lewat {@link #getNama()}). */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/** @return keterangan tambahan untuk jenis sekolah ini, boleh {@code null}. */
	@Column(name = "keterangan")
	public String getKeterangan() {
		return this.keterangan;
	}

	/** @param keterangan keterangan tambahan untuk jenis sekolah ini. */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * @return kode jenis sekolah; bila belum diisi, jatuh kembali ke {@link #getId()} sebagai
	 * string (atau string kosong jika id juga belum ada) sehingga tetap ada nilai tampil walau
	 * kode belum pernah di-set secara eksplisit.
	 */
	public String getKode() {
		return kode == null || kode.trim().isEmpty() ? (getId() != null ? getId() + "" : "") : kode;
	}

	/** @param kodeEmis kode jenis sekolah (mis. kode EMIS) untuk baris ini. */
	public void setKode(String kodeEmis) {
		this.kode = kodeEmis;
	}

	/**
	 * @return status aktif jenis sekolah ini; default {@code true} bila belum pernah diisi.
	 * <b>Catatan:</b> pemanggilan pertama MENULIS default itu ke field {@link #aktif} (getter
	 * destruktif) &mdash; pola berulang di banyak entity flag aktif pada codebase ini, bukan bug
	 * baru yang spesifik untuk kelas ini.
	 */
	public Boolean getAktif() {
		if (aktif == null) {
			aktif = true;
		}
		return aktif;
	}

	/** @param aktif status aktif jenis sekolah ini. */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}
}
