package ais.database.model;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

/**
 * Bank generated by hbm2java
 *
 * <p><b>STATUS PEMAKAIAN — DUPLIKAT YATIM, LEBIH MATI dari versi {@code epsbed}
 * (diverifikasi ulang pada audit ini):</b> ada class lain dengan nama identik,
 * {@link ais.database.model.epsbed.EpsbedStatusPs} (paket {@code ais.database.model.epsbed}),
 * yang memetakan tabel bernama sama tapi skema BERBEDA ({@code epsbed.epsbed_status_ps},
 * bukan {@code public.epsbed_status_ps} seperti class ini). Class TERSEBUT sudah
 * dikonfirmasi entity yatim (tidak direferensikan Action/Helper/entity lain mana pun).
 * </p>
 * <p>
 * Class INI (versi {@code public}, paket root {@code ais.database.model}) statusnya
 * LEBIH PARAH: selain tidak direferensikan di mana pun secara Java (hanya class ini
 * sendiri yang muncul saat pencarian {@code EpsbedStatusPs} di luar javadoc/komentar
 * versi {@code epsbed}), class ini bahkan TIDAK TERDAFTAR di {@code hibernate.cfg.xml}
 * (hanya {@code ais.database.model.epsbed.EpsbedStatusPs} yang punya baris
 * {@code <mapping class="...">}), dan tidak ada {@code packagesToScan}/annotation-scan
 * yang bisa memungutnya secara implisit. Artinya class ini BUKAN SEKADAR tidak dipakai
 * kode Java — ia bahkan tidak pernah dikenal Hibernate/JPA sama sekali, sehingga
 * {@code @Entity}/{@code @Table} di sini murni dekoratif dan tabel {@code public.epsbed_status_ps}
 * (bila ada di database) sepenuhnya terputus dari ORM. Kemungkinan besar sisa refactor
 * pemindahan model epsbed ke paket {@code ais.database.model.epsbed} yang lupa dihapus.
 * JANGAN dianggap sumber data yang saling menggantikan dengan versi {@code epsbed} tanpa
 * verifikasi manual skema mana yang benar-benar berisi data di database produksi.
 * </p>
 *
 * @see ais.database.model.epsbed.EpsbedStatusPs
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "public", name = "epsbed_status_ps")

public class EpsbedStatusPs extends GeneralValueObject {

	/**
	 * 
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	private Long id;
	private String oleh;
	private String olehId;

	/**
	 * @return id akun yang membuat/mengubah baris ini.
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menyimpan id pembuat/pengubah. Nilai kosong/null diabaikan (tidak menimpa
	 * nilai lama) — write-guard satu-arah, konsisten dengan pola arsip lain.
	 *
	 * @param olehId id akun pembuat/pengubah.
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Menyimpan nama pembuat/pengubah. Nilai kosong/null diabaikan (tidak menimpa
	 * nilai lama) — write-guard satu-arah, konsisten dengan pola arsip lain.
	 *
	 * @param oleh nama akun pembuat/pengubah.
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * @return nama akun yang membuat/mengubah baris ini.
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Hook Envers/JPA: memperbarui timestamp audit shadow {@link #tanggal_dirubah}
	 * setiap kali baris ini di-update. Field ini adalah kebutuhan teknis, bukan bug.
	 * CATATAN: tidak akan pernah terpanggil di runtime karena class ini tidak
	 * terdaftar di {@code hibernate.cfg.xml} (lihat Javadoc kelas).
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * @param tanggal_dirubah waktu perubahan terakhir (audit shadow field).
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * @return waktu perubahan terakhir baris ini, diisi otomatis oleh {@link #onUpdate()}.
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * @return nama status program studi, dipakai sebagai representasi ringkas untuk log/debug.
	 */
	public String toString() {
		return nama;
	}

	private String nama;
	private String kode;

	/**
	 * Konstruktor kosong (dipakai Hibernate untuk instansiasi via reflection — meski
	 * class ini tidak pernah benar-benar dipetakan, lihat Javadoc kelas).
	 */
	public EpsbedStatusPs() {
	}

	/**
	 * @return id unik baris (surrogate key, auto-increment).
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * @param id id unik baris.
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * @return nama status program studi (versi pelaporan EPSBED lama), di-trim saat dibaca.
	 */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * @param nama nama status program studi.
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * @return kode status program studi (versi pelaporan EPSBED lama).
	 */
	@Column(name = "kode", nullable = false, length = 255)
	public String getKode() {
		return this.kode;
	}

	/**
	 * @param kode kode status program studi.
	 */
	public void setKode(String kode) {
		this.kode = kode;
	}

}
