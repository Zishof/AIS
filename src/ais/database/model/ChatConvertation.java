package ais.database.model;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

/**
 * Bank generated by hbm2java
 *
 * <p>
 * Entitas Hibernate untuk tabel {@code public.chat_convertation} (ejaan
 * "Convertation" adalah typo lama pada kode, dipertahankan apa adanya —
 * BUKAN "Conversation"). Dirancang sebagai <i>header</i> percakapan chat
 * berpasangan dengan {@link ChatMessage} (rancangan dua tabel: percakapan
 * &rarr; pesan): {@link #getFromUsername()}/{@link #getToUsername()} adalah
 * pasangan pengirim/penerima, {@link #getNama()} judul percakapan,
 * {@link #getKeterangan()} keterangan bebas. Relasi ke {@link ChatMessage}
 * satu arah lewat {@link ChatMessage#getChatConvertation()} — entitas ini
 * TIDAK punya koleksi balik ke pesan.
 * </p>
 * <p>
 * <b>Status pemakaian — ENTITY YATIM (tidak dipakai sama sekali):</b>
 * konsisten dengan analisis {@link ChatMessage} (rancangan pasangannya):
 * tidak ada satu pun {@code Action}/{@code Helper} yang membuat, menyimpan,
 * atau mengambil baris {@code ChatConvertation}. Rujukan yang ada hanyalah
 * baris {@code <mapping class="ais.database.model.ChatConvertation" />} di
 * {@code hibernate.cfg.xml} (skema tetap dipelihara Hibernate) dan satu entri
 * manifes {@code general_value_object_inventory} berstatus kandidat CRUD
 * generik yang masih nonaktif. Fitur chat yang benar-benar berjalan memakai
 * {@link Pesan} (model pesan sementara, dihapus/ditandai setelah dirender),
 * bukan pasangan {@code ChatConvertation}/{@link ChatMessage}. Karena tidak
 * pernah dipakai, TIDAK ADA filter kepemilikan percakapan yang perlu
 * diverifikasi di sini — tidak ada kode yang membaca baris siapa pun.
 * </p>
 *
 * @see ChatMessage
 * @see Pesan
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "public", name = "chat_convertation")

public class ChatConvertation extends GeneralValueObject {

	/** 
	 * 
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	private Long id;
	private String oleh;
	private String olehId;

	/**
	 * @return id akun yang membuat/mengubah baris ini.
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menyimpan id pembuat/pengubah. Nilai kosong/null diabaikan (tidak menimpa
	 * nilai lama) — write-guard satu-arah, konsisten dengan pola arsip lain.
	 *
	 * @param olehId id akun pembuat/pengubah.
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Menyimpan nama pembuat/pengubah. Nilai kosong/null diabaikan (tidak menimpa
	 * nilai lama) — write-guard satu-arah, konsisten dengan pola arsip lain.
	 *
	 * @param oleh nama akun pembuat/pengubah.
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * @return nama akun yang membuat/mengubah baris ini.
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Hook Envers/JPA: memperbarui timestamp audit shadow {@link #tanggal_dirubah}
	 * setiap kali baris ini di-update. Field ini adalah kebutuhan teknis, bukan bug.
	 * CATATAN: tidak akan pernah terpanggil di runtime karena entitas ini tidak
	 * pernah disimpan/diubah di mana pun (lihat status yatim pada Javadoc kelas).
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * @param tanggal_dirubah waktu perubahan terakhir (audit shadow field).
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * @return waktu perubahan terakhir baris ini, diisi otomatis oleh {@link #onUpdate()}.
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * @return representasi ringkas "{id}-{nama}", dipakai untuk keperluan log/debug.
	 */
	public String toString() {
		return id + "-" + nama;
	}

	private String nama;
	private String fromUsername;
	private String toUsername;
	private String keterangan;

	/**
	 * Konstruktor kosong (dipakai Hibernate untuk instansiasi via reflection —
	 * meski entitas ini tidak pernah benar-benar dipakai, lihat Javadoc kelas).
	 */
	public ChatConvertation() {
	}

	/**
	 * @return id unik percakapan (surrogate key, auto-increment).
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * @param id id unik percakapan.
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * @return judul percakapan, di-trim saat dibaca.
	 */
	@Column(name = "nama", columnDefinition = "text")
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * @param nama judul percakapan.
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * @return keterangan bebas percakapan ini.
	 */
	@Column(name = "keterangan", nullable = true, columnDefinition = "text")
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * @param keterangan keterangan bebas percakapan.
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * @return username pengirim (satu sisi pasangan percakapan) — batas
	 *         kepemilikan yang dimaksudkan pada desain ini, tapi tidak pernah
	 *         benar-benar dipakai/divalidasi (lihat status yatim pada Javadoc kelas).
	 */
	@Column(name = "from_username")
	public String getFromUsername() {
		return fromUsername;
	}

	/**
	 * @param fromUsername username pengirim.
	 */
	public void setFromUsername(String fromUsername) {
		this.fromUsername = fromUsername;
	}

	/**
	 * @return username penerima (sisi lain pasangan percakapan).
	 */
	@Column(name = "to_username")
	public String getToUsername() {
		return toUsername;
	}

	/**
	 * @param toUsername username penerima.
	 */
	public void setToUsername(String toUsername) {
		this.toUsername = toUsername;
	}

}
