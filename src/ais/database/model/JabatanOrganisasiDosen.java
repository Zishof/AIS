package ais.database.model;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

/**
 * Entitas master (lookup) untuk tabel {@code jabatan_organisasi_dosen}.
 *
 * <p>Menyimpan daftar nama peran/jabatan yang dapat dipegang seorang dosen di dalam
 * sebuah organisasi dosen (mis. "Ketua", "Sekretaris", "Bendahara", "Anggota"). Baris di
 * sini dipilih sebagai referensi oleh {@code OrganisasiDosenPunyaDosen#getJabatanOrganisasiDosen()}
 * untuk mencatat peran seorang dosen pada keanggotaan organisasi tertentu &mdash; lihat
 * javadoc kelas tersebut untuk perbedaan dengan cakupan/level organisasi
 * ({@link LevelOrganisasiDosen}, yang disimpan terpisah pada {@code OrganisasiDosen}).
 *
 * <p>Dikelola lewat CRUD master data sederhana di
 * {@code ais.action.master.JabatanOrganisasiDosenAction}; {@link #getNama()} bersifat unik
 * per baris sehingga tidak boleh ada dua jabatan dengan nama yang sama.
 *
 * <p>Diturunkan dari {@link GeneralValueObject} sehingga mewarisi field audit generik
 * (dibuat/diubah oleh siapa &amp; kapan) dari kelas induk; {@link #tanggal_dirubah} di
 * bawah adalah field lokal tambahan yang di-set otomatis lewat
 * {@code AuditTimestampInterceptor} pada setiap {@code @PreUpdate}, bukan bug &mdash;
 * pola ini berulang di banyak entitas master lain pada paket ini.
 *
 * Bank generated by hbm2java
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "public", name = "jabatan_organisasi_dosen")

public class JabatanOrganisasiDosen extends GeneralValueObject {

	/**
	 *
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	private Long id;
	/** Nama pelaku (audit shadow, lihat {@link GeneralValueObject}) yang membuat/mengubah baris ini. */
	private String oleh;
	/** Id pelaku (audit shadow) yang membuat/mengubah baris ini. */
	private String olehId;

	/** @return id pelaku terakhir yang mengubah baris ini, atau {@code null} bila belum pernah diisi. */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Mengisi id pelaku. Nilai kosong/blank diabaikan (fail-safe agar audit shadow tidak
	 * tertimpa string kosong secara tidak sengaja) &mdash; bukan validasi keamanan.
	 *
	 * @param olehId id pelaku; diabaikan jika {@code null} atau hanya berisi spasi
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Mengisi nama pelaku. Nilai kosong/blank diabaikan, sama seperti {@link #setOlehId(String)}.
	 *
	 * @param oleh nama pelaku; diabaikan jika {@code null} atau hanya berisi spasi
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/** @return nama pelaku terakhir yang mengubah baris ini, atau {@code null} bila belum pernah diisi. */
	public String getOleh() {
		return oleh;
	}

	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Mengubah stempel waktu perubahan terakhir secara manual. Nilai default sudah
	 * di-set ke waktu saat ini pada deklarasi field dan di-refresh otomatis oleh
	 * {@link #onUpdate()} pada setiap update; setter ini jarang perlu dipanggil langsung.
	 *
	 * @param tanggal_dirubah stempel waktu perubahan baru
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/** @return stempel waktu perubahan terakhir baris ini. */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/** @return representasi ringkas berupa {@code id-nama}, dipakai untuk debugging/log. */
	public String toString() {
		return id + "-" + nama;
	}

	private String nama;
	private String keterangan;

	/** Konstruktor kosong yang diperlukan Hibernate. */
	public JabatanOrganisasiDosen() {
	}

	/** @return id baris (primary key, auto-generated identity di database). */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/** @param id id baris; kolom tidak insertable sehingga nilai ini diabaikan saat insert. */
	public void setId(Long id) {
		this.id = id;
	}

	/** @return nama jabatan/peran organisasi dosen (unik, wajib diisi), sudah di-trim. */
	@Column(name = "nama", nullable = false, length = 255, unique = true)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/** @param nama nama jabatan/peran organisasi dosen. */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/** @return keterangan tambahan untuk jabatan ini, boleh {@code null}. */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/** @param keterangan keterangan tambahan untuk jabatan ini. */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

}
