package ais.database.model;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

/**
 * Entity Hibernate untuk tabel {@code public.nama_sekolah_asal} &mdash; master nama sekolah asal
 * (SD/SMP/SMA/perguruan tinggi) yang dipilih bebas teks saat mendata riwayat pendidikan calon
 * mahasiswa/pegawai, dilengkapi field {@link #getTingkat()} untuk mengelompokkan baris ke salah
 * satu jenjang baku {@link #SD}, {@link #SMP}, {@link #SMA}, atau {@link #PERGURUAN_TINGGI}.
 *
 * <p><b>Catatan:</b> konstanta jenjang ({@link #SD}, {@link #SMP}, {@link #SMA},
 * {@link #PERGURUAN_TINGGI}) dideklarasikan {@code public static} TANPA {@code final} &mdash;
 * secara teknis dapat ditimpa oleh kode lain di JVM yang sama, yang akan mengubah nilai
 * pembanding di seluruh pemakai kelas ini sekaligus. Tidak ada jalur yang diketahui menimpa
 * konstanta ini saat ini; dicatat sebagai risiko rapuh, bukan kerentanan yang tereksploitasi.
 *
 * <p>Javadoc class ini sebelumnya adalah stub salah-tempel ("Bank generated by hbm2java")
 * peninggalan generator hbm2java untuk entity lain; sudah dikoreksi di sini tanpa mengubah
 * perilaku.
 */
@Entity
@org.hibernate.annotations.Entity(dynamicInsert = true, dynamicUpdate = true)
@Audited
@Table(schema = "public", name = "nama_sekolah_asal")

public class NamaSekolahAsal extends GeneralValueObject {

	/** Label jenjang "Sekolah Dasar", nilai default {@link #getTingkat()} untuk kategori SD. */
	public static String SD = "Sekolah Dasar";
	/** Label jenjang "Sekolah Menengah Pertama". */
	public static String SMP = "Sekolah Menengah Pertama";
	/** Label jenjang "Sekolah Menengah Atas"; juga dipakai sebagai normalisasi untuk input "SMK" dan "MA". */
	public static String SMA = "Sekolah Menengah Atas";
	/** Label jenjang "Perguruan Tinggi". */
	public static String PERGURUAN_TINGGI = "Perguruan Tinggi";

	/**
	 *
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	private Long id;
	/** Nama pelaku (audit shadow, lihat {@link GeneralValueObject}) yang membuat/mengubah baris ini. */
	private String oleh;
	/** Id pelaku (audit shadow) yang membuat/mengubah baris ini. */
	private String olehId;

	/** @return id pelaku terakhir yang mengubah baris ini, atau {@code null} bila belum pernah diisi. */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Mengisi id pelaku. Nilai kosong/blank diabaikan (fail-safe agar audit shadow tidak
	 * tertimpa string kosong secara tidak sengaja) &mdash; bukan validasi keamanan.
	 *
	 * @param olehId id pelaku; diabaikan jika {@code null} atau hanya berisi spasi
	 */
	public void setOlehId(String olehId) {
		if (olehId == null || olehId.trim().isEmpty()) {
			return;
		}
		this.olehId = olehId;
	}

	/**
	 * Mengisi nama pelaku. Nilai kosong/blank diabaikan, sama seperti {@link #setOlehId(String)}.
	 *
	 * @param oleh nama pelaku; diabaikan jika {@code null} atau hanya berisi spasi
	 */
	public void setOleh(String oleh) {
		if (oleh == null || oleh.trim().isEmpty()) {
			return;
		}
		this.oleh = oleh;
	}

	/** @return nama pelaku terakhir yang mengubah baris ini, atau {@code null} bila belum pernah diisi. */
	public String getOleh() {
		return oleh;
	}

	/** Menandai baris berubah; dipanggil otomatis oleh Hibernate sebelum setiap update. */
	@javax.persistence.PreUpdate
	protected void onUpdate() {
		ais.database.hibernate.AuditTimestampInterceptor.ubah(this);
	}

	private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Mengubah stempel waktu perubahan terakhir secara manual. Nilai default sudah di-set ke
	 * waktu saat ini pada deklarasi field dan di-refresh otomatis oleh {@link #onUpdate()} pada
	 * setiap update; setter ini jarang perlu dipanggil langsung.
	 *
	 * @param tanggal_dirubah stempel waktu perubahan baru
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/** @return stempel waktu perubahan terakhir baris ini. */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/** @return representasi ringkas berupa {@code id-kode-nama}, dipakai untuk debugging/log. */
	public String toString() {
		return id + "-" + kode + "-" + nama;
	}

	private String kode;

	private String nama;
	private String tingkat;
	private String keterangan;
	private Boolean aktif;

	/** Konstruktor kosong yang diperlukan Hibernate. */
	public NamaSekolahAsal() {
	}

	/** @return id baris (primary key, auto-generated identity di database). */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/** @param id id baris; kolom tidak insertable sehingga nilai ini diabaikan saat insert. */
	public void setId(Long id) {
		this.id = id;
	}

	/** @return nama sekolah asal, di-trim setiap dibaca; wajib diisi (kolom {@code NOT NULL}). */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/** @param nama nama sekolah asal; disimpan apa adanya (trim terjadi saat dibaca lewat {@link #getNama()}). */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/** @return keterangan tambahan untuk sekolah asal ini, boleh {@code null}. */
	@Column(name = "keterangan", nullable = true, columnDefinition = "text")
	public String getKeterangan() {
		return this.keterangan;
	}

	/** @param keterangan keterangan tambahan untuk sekolah asal ini. */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * @return jenjang baku sekolah asal ini: {@link #SD}, {@link #SMP}, {@link #SMA}, atau
	 * {@link #PERGURUAN_TINGGI}. Bila belum diisi, default ke {@link #SMA}; input "SMK" dan "MA"
	 * (tanpa memandang huruf besar/kecil) juga dinormalisasi ke {@link #SMA}. <b>Catatan:</b>
	 * pemanggilan pertama MENULIS hasil normalisasi itu ke field {@link #tingkat} (getter
	 * destruktif) &mdash; pola berulang di banyak entity pada codebase ini, bukan bug baru yang
	 * spesifik untuk kelas ini.
	 */
	public String getTingkat() {
		if (tingkat == null) {
			tingkat = SMA;
		}
		if (tingkat.trim().equalsIgnoreCase("sma")) {
			tingkat = SMA;
		}
		if (tingkat.trim().equalsIgnoreCase("smk")) {
			tingkat = SMA;
		}
		if (tingkat.trim().equalsIgnoreCase("ma")) {
			tingkat = SMA;
		}
		return tingkat;
	}

	/** @param tingkat jenjang sekolah asal ini; dinormalisasi ke label baku saat berikutnya dibaca via {@link #getTingkat()}. */
	public void setTingkat(String tingkat) {
		this.tingkat = tingkat;
	}

	/** @return kode sekolah asal, di-trim setiap dibaca; string kosong (bukan {@code null}) bila belum diisi. */
	public String getKode() {
		return kode == null ? "" : kode.trim();
	}

	/** @param kode kode sekolah asal untuk baris ini. */
	public void setKode(String kode) {
		this.kode = kode;
	}

	/** @return status aktif sekolah asal ini; default {@code true} bila belum pernah diisi. */
	public Boolean getAktif() {
		return aktif == null ? true : aktif;
	}

	/** @param aktif status aktif sekolah asal ini. */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}

}
