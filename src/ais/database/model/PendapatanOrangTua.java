package ais.database.model;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

import ais.common.Common;

/**
 * Bank generated by hbm2java
 *
 * <p><b>Master rentang pendapatan orang tua/wali mahasiswa</b> &mdash; satu baris tabel
 * {@code public.pendapatan_orang_tua} mewakili satu rentang nominal pendapatan
 * ({@link #getMulaiDari()} sampai {@link #getSampai()}), mis. "Rp. 0 - Rp. 1.000.000". Dipakai
 * sebagai lookup pada biodata orang tua mahasiswa, berdampingan dengan {@link PekerjaanOrangTua}
 * dan {@link PendidikanOrangTua} sebagai satu kelompok master data biodata orang tua.</p>
 *
 * <h2>{@link #getNama()}: label turunan, bukan kolom bebas isi (diverifikasi dari kode)</h2>
 * <p>Berbeda dari kebanyakan master lookup lain di paket ini yang memetakan {@code nama} sebagai
 * kolom database wajib-isi manual, entity ini <b>tidak memetakan {@code nama} sebagai kolom
 * JPA</b>: {@link #getNama()} selalu <b>menghitung ulang dan menugaskan kembali</b> field
 * {@code nama} menjadi string terformat {@code "Rp. <mulaiDari> - Rp. <sampai>"} dari
 * {@link #getMulaiDari()}/{@link #getSampai()} setiap kali dipanggil &mdash; getter-mutasi yang,
 * tidak seperti sebagian pola serupa di paket ini, <b>selalu</b> menimpa nilai lama tanpa syarat
 * (bukan hanya saat {@code null}). Nilai apa pun yang ditulis lewat {@link #setNama(String)}
 * akan langsung tertimpa pada pemanggilan {@link #getNama()} berikutnya, sehingga setter
 * tersebut pada praktiknya tidak berguna untuk mengubah label secara permanen.</p>
 *
 * <h2>Peta anggota</h2>
 * <ul>
 *   <li><b>Identitas &amp; audit</b>: {@link #getId()}/{@link #setId(Long)},
 *       {@link #getOleh()}/{@link #setOleh(String)}, {@link #getOlehId()}/{@link #setOlehId(String)},
 *       {@link #getTanggal_dirubah()}/{@link #setTanggal_dirubah(Date)}, {@link #onUpdate()}.</li>
 *   <li><b>Deskriptif</b>: {@link #getKode()}/{@link #setKode(String)}, {@link #getNama()}/
 *       {@link #setNama(String)} (lihat catatan turunan di atas, dipakai {@link #toString()}),
 *       {@link #getKeterangan()}/{@link #setKeterangan(String)}.</li>
 *   <li><b>Rentang nominal</b>: {@link #getMulaiDari()}/{@link #setMulaiDari(Double)} (default
 *       0.0 bila belum diisi), {@link #getSampai()}/{@link #setSampai(Double)} (default 0.0
 *       bila belum diisi).</li>
 *   <li><b>Status</b>: {@link #getAktif()}/{@link #setAktif(Boolean)} (default {@code true} bila
 *       belum pernah diisi).</li>
 * </ul>
 *
 * @see PekerjaanOrangTua
 * @see PendidikanOrangTua
 * @see GeneralValueObject
 */
@Entity
@org.hibernate.annotations.Entity(dynamicInsert = true, dynamicUpdate = true)
@Audited
@Table(schema = "public", name = "pendapatan_orang_tua")

public class PendapatanOrangTua extends GeneralValueObject {

	/**
	 * Penanda versi serialisasi Java. Tidak berdampak fungsional.
	 */
	private static final long serialVersionUID = 2463821577548439808L;

	/** Kunci utama, lihat {@link #getId()}. */
	private Long id;

	/** Nama pengguna pengubah terakhir, lihat {@link #getOleh()}. */
	private String oleh;

	/** Id pengguna pengubah terakhir, lihat {@link #getOlehId()}. */
	private String olehId;

	/**
	 * Id pengguna pengubah terakhir.
	 *
	 * @return id pengguna pengubah terakhir; {@code null} bila belum pernah tercatat.
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menetapkan id pengguna pengubah terakhir. Setter penjaga: nilai {@code null}/kosong diabaikan
	 * diam-diam sehingga jejak audit yang sudah terisi tidak dapat dihapus lewat setter ini.
	 *
	 * @param olehId id pengguna; diabaikan bila {@code null} atau kosong.
	 */
	public void setOlehId(String olehId) {
		if (olehId == null || olehId.trim().isEmpty()) {
			return;
		}
		this.olehId = olehId;
	}

	/**
	 * Menetapkan nama pengguna pengubah terakhir. Berperilaku sama dengan
	 * {@link #setOlehId(String)}: nilai {@code null}/kosong diabaikan diam-diam.
	 *
	 * @param oleh nama pengguna; diabaikan bila {@code null} atau kosong.
	 */
	public void setOleh(String oleh) {
		if (oleh == null || oleh.trim().isEmpty()) {
			return;
		}
		this.oleh = oleh;
	}

	/**
	 * Nama pengguna yang terakhir mengubah baris ini.
	 *
	 * @return nama pengguna pengubah terakhir; {@code null} bila belum pernah tercatat.
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Hook JPA {@code @PreUpdate} yang menyegarkan metadata audit sebelum baris ini di-{@code UPDATE}.
	 * Mendelegasikan ke {@code ais.database.hibernate.AuditTimestampInterceptor.ubah(this)};
	 * dipanggil Hibernate saja.
	 */
	@javax.persistence.PreUpdate
	protected void onUpdate() {
		ais.database.hibernate.AuditTimestampInterceptor.ubah(this);
	}

	/** Stempel waktu perubahan terakhir, lihat {@link #getTanggal_dirubah()}. */
	private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menetapkan stempel waktu perubahan terakhir. Tidak menjaga terhadap {@code null}.
	 *
	 * @param tanggal_dirubah waktu perubahan terakhir; boleh {@code null}.
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Waktu terakhir baris ini diubah.
	 *
	 * @return waktu perubahan terakhir, dipetakan sebagai {@code TIMESTAMP}.
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Representasi teks baris ini: id digabung nama (label rentang pendapatan terformat).
	 *
	 * @return string {@code "<id>-<nama>"}.
	 */
	public String toString() {
		return id + "-" + nama;
	}

	/** Kode rentang pendapatan, lihat {@link #getKode()}. */
	private String kode;

	/** Label rentang pendapatan terformat (turunan, lihat {@link #getNama()}). */
	private String nama;

	/** Batas bawah rentang pendapatan, lihat {@link #getMulaiDari()}. */
	private Double mulaiDari;

	/** Batas atas rentang pendapatan, lihat {@link #getSampai()}. */
	private Double sampai;

	/** Keterangan bebas, lihat {@link #getKeterangan()}. */
	private String keterangan;

	/** Status aktif/nonaktif baris, lihat {@link #getAktif()}. */
	private Boolean aktif;

	/** Konstruktor tanpa argumen yang dibutuhkan Hibernate/JPA. */
	public PendapatanOrangTua() {
	}

	/**
	 * Kunci utama baris (kolom {@code id}, {@code IDENTITY} &mdash; dibangkitkan database).
	 *
	 * @return id baris; {@code null} selama object belum pernah disimpan.
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menetapkan kunci utama. Praktis hanya dipanggil Hibernate.
	 *
	 * @param id kunci utama baris.
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Keterangan bebas (kolom {@code keterangan}).
	 *
	 * @return keterangan; {@code null} bila belum diisi.
	 */
	@Column(name = "keterangan")
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * Menetapkan keterangan bebas.
	 *
	 * @param keterangan teks keterangan; boleh {@code null}.
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * Batas bawah rentang pendapatan (kolom {@code mulai_dari}). <b>Getter berdefault-mutasi</b>:
	 * bila belum pernah diisi, field ditugaskan {@code 0.0} sebelum dikembalikan (bukan hanya
	 * dikembalikan sebagai nilai literal).
	 *
	 * @return batas bawah rentang; {@code 0.0} bila belum pernah diisi.
	 */
	@Column(name = "mulai_dari")
	public Double getMulaiDari() {
		if (mulaiDari == null) {
			mulaiDari = 0.0;
		}
		return mulaiDari;
	}

	/**
	 * Menetapkan batas bawah rentang pendapatan.
	 *
	 * @param mulaiDari batas bawah; boleh {@code null} (akan dinormalisasi menjadi 0.0 oleh
	 *                  {@link #getMulaiDari()} berikutnya).
	 */
	public void setMulaiDari(Double mulaiDari) {
		this.mulaiDari = mulaiDari;
	}

	/**
	 * Batas atas rentang pendapatan (kolom {@code sampai}). <b>Getter berdefault-mutasi</b>: bila
	 * belum pernah diisi, field ditugaskan {@code 0.0} sebelum dikembalikan.
	 *
	 * @return batas atas rentang; {@code 0.0} bila belum pernah diisi.
	 */
	@Column(name = "sampai")
	public Double getSampai() {
		if (sampai == null) {
			sampai = 0.0;
		}
		return sampai;
	}

	/**
	 * Menetapkan batas atas rentang pendapatan.
	 *
	 * @param sampai batas atas; boleh {@code null} (akan dinormalisasi menjadi 0.0 oleh
	 *               {@link #getSampai()} berikutnya).
	 */
	public void setSampai(Double sampai) {
		this.sampai = sampai;
	}

	/**
	 * Kode rentang pendapatan (bukan kolom terpetakan JPA eksplisit, hanya field biasa; getter
	 * murni tanpa efek samping).
	 *
	 * @return kode rentang; {@code null} bila belum diisi.
	 */
	public String getKode() {
		return kode;
	}

	/**
	 * Menetapkan kode rentang pendapatan.
	 *
	 * @param kode kode rentang; boleh {@code null}.
	 */
	public void setKode(String kode) {
		this.kode = kode;
	}

	/**
	 * Label rentang pendapatan. <b>Getter-mutasi tanpa syarat</b>: setiap dipanggil, field
	 * {@code nama} <b>selalu</b> ditimpa dengan string terformat
	 * {@code "Rp. <mulaiDari> - Rp. <sampai>"} (memakai {@code Common.numberFormat}) dari nilai
	 * {@link #getMulaiDari()}/{@link #getSampai()} saat itu, sehingga nilai apa pun yang pernah
	 * ditulis lewat {@link #setNama(String)} tidak pernah benar-benar terbaca kembali.
	 *
	 * @return label rentang pendapatan terformat, mis. {@code "Rp. 0 - Rp. 1.000.000"}.
	 */
	public String getNama() {
		nama = "Rp. " + Common.numberFormat.get().format(getMulaiDari()) + " - Rp. "
				+ Common.numberFormat.get().format(getSampai());
		return nama;
	}

	/**
	 * Menetapkan label rentang pendapatan secara manual. Nilai ini akan langsung ditimpa oleh
	 * {@link #getNama()} pada pemanggilan berikutnya (lihat catatan getter-mutasi di atas).
	 *
	 * @param nama label rentang pendapatan; boleh {@code null}.
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Status aktif/nonaktif baris ini pada layar pemilihan.
	 *
	 * @return {@code true} bila aktif atau belum pernah diisi (default aktif); nilai tersimpan
	 *         apa adanya bila sudah pernah diisi eksplisit.
	 */
	public Boolean getAktif() {
		return aktif == null ? true : aktif;
	}

	/**
	 * Menetapkan status aktif/nonaktif.
	 *
	 * @param aktif status aktif baru; boleh {@code null} (diperlakukan sebagai aktif oleh
	 *              {@link #getAktif()}).
	 */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}
}
