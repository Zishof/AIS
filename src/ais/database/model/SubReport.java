package ais.database.model;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

/**
 * Entity <b>SUB REPORT</b> — daftar berkas <i>subreport</i> ({@code .jasper}) yang boleh disisipkan
 * ke dalam laporan JasperReports utama. Dipetakan ke tabel {@code public.sub_report}.
 *
 * <p>Header berkas ini masih menyisakan komentar asli <i>"Bank generated by hbm2java"</i> dari
 * generator Hibernate Tools — sisa <i>copy-paste</i> template, tidak ada hubungannya dengan entity
 * {@code Bank}.</p>
 *
 * <h2>Peran dalam sistem</h2>
 * <p>Struktur dan pola pemakaiannya identik dengan {@link PendukungReport}: baris entity ini murni
 * metadata (nama, keterangan, status aktif), sedangkan berkas fisiknya disimpan lewat
 * {@link ais.database.model.file.LampiranLain}, ditautkan memakai <i>owner id + owner class name</i>
 * ({@code LampiranLain.ambil(getId(), SubReport.class.getName())}). {@code ais.action.report.Report}
 * mengiterasi seluruh baris {@code SubReport} yang {@link #getAktif() aktif} dan berlampiran
 * {@code .jasper}, lalu mendaftarkannya sebagai <i>subreport</i> bagi laporan induk memakai
 * {@link #getKode()} (format {@code SUB_RPT_00000001}) sebagai kunci parameter.</p>
 *
 * <p>Perbedaan dengan {@link PendukungReport}: subreport adalah berkas laporan {@code .jasper} yang
 * DIEKSEKUSI sebagai bagian laporan (mis. tabel detail bersarang), sedangkan berkas pendukung pada
 * {@link PendukungReport} umumnya berupa aset non-laporan (gambar, dsb) yang disisipkan apa
 * adanya.</p>
 *
 * @see PendukungReport
 * @see ais.database.model.file.LampiranLain
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "public", name = "sub_report")
public class SubReport extends GeneralValueObject {

	/**
	 * Versi serialisasi Java untuk kompatibilitas antar build kelas ini.
	 */
	private static final long serialVersionUID = 2463821577548439808L;

	/** Kunci utama, di-<i>generate</i> database ({@code IDENTITY}). */
	private Long id;

	/** Nama pengguna terakhir yang mengubah baris ini; diisi otomatis lewat {@link #onUpdate()}. */
	private String oleh;

	/** Id pengguna terakhir yang mengubah baris ini; diisi otomatis lewat {@link #onUpdate()}. */
	private String olehId;

	/**
	 * Mengembalikan id pengguna pengubah terakhir.
	 *
	 * @return id pengguna pengubah terakhir, atau {@code null} bila belum pernah di-<i>update</i>
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menetapkan id pengguna pengubah terakhir. Nilai {@code null} atau kosong (hanya spasi)
	 * diabaikan diam-diam agar jejak audit tidak bisa dikosongkan lewat pemanggilan ini.
	 *
	 * @param olehId id pengguna pengubah; diabaikan bila {@code null} atau kosong
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Menetapkan nama pengguna pengubah terakhir. Sama seperti {@link #setOlehId(String)}, nilai
	 * {@code null} atau kosong diabaikan.
	 *
	 * @param oleh nama pengguna pengubah; diabaikan bila {@code null} atau kosong
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan nama pengguna terakhir yang mengubah baris ini.
	 *
	 * @return nama pengguna pengubah terakhir, atau {@code null} bila belum pernah di-<i>update</i>
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * <i>Callback</i> JPA {@code @PreUpdate}: mendelegasikan ke
	 * {@code ais.database.hibernate.AuditTimestampInterceptor.ubah(this)} untuk mengisi
	 * {@link #setTanggal_dirubah(Date)}, {@link #setOleh(String)}, {@link #setOlehId(String)} dari
	 * konteks pengguna aktif sebelum baris ini di-{@code UPDATE}.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menetapkan waktu perubahan terakhir baris ini.
	 *
	 * @param tanggal_dirubah waktu perubahan
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan waktu perubahan terakhir baris ini. Nilai awalnya adalah waktu objek dibuat.
	 *
	 * @return waktu perubahan terakhir
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Representasi teks baris: {@code id-nama}.
	 *
	 * @return teks ringkas berisi id dan nama
	 */
	public String toString() {
		return id + "-" + nama;
	}

	/** Kode parameter laporan; dihitung ulang setiap dipanggil oleh {@link #getKode()}, BUKAN kolom persisten. */
	private String kode;

	/** Nama tampilan berkas subreport. Wajib diisi, maksimal 255 karakter. */
	private String nama;

	/** Keterangan/deskripsi bebas subreport. */
	private String keterangan;

	/** Status aktif; {@code null} diperlakukan sebagai {@code true} (aktif) oleh {@link #getAktif()}. */
	private Boolean aktif;

	/**
	 * Konstruktor kosong yang diwajibkan Hibernate/JPA.
	 */
	public SubReport() {
	}

	/**
	 * Mengembalikan kunci utama baris. Kolom {@code id} bersifat {@code insertable = false} karena
	 * nilainya dihasilkan database ({@code IDENTITY}).
	 *
	 * @return id baris, atau {@code null} bila baris belum disimpan
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menetapkan kunci utama. Normalnya hanya dipanggil Hibernate setelah {@code INSERT}.
	 *
	 * @param id kunci utama baris
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Mengembalikan kode parameter laporan, format {@code SUB_RPT_00000001} (id diratakan-kiri
	 * dengan nol menjadi 8 digit). <b>Bukan kolom database</b> — nilainya dihitung ulang dari
	 * {@link #getId()} setiap kali method ini dipanggil, dan field {@code kode} ditulis balik
	 * sekadar sebagai variabel kerja sementara. Kode inilah yang dipakai
	 * {@code ais.action.report.Report} sebagai nama parameter JasperReports untuk merujuk subreport
	 * ini.
	 *
	 * @return kode parameter laporan turunan {@link #getId()}
	 */
	public String getKode() {

		kode = "0000000000000000" + getId();
		kode = "SUB_RPT_" + kode.substring(kode.length() - 8);

		return kode.trim();
	}

	/**
	 * Menetapkan field kerja {@code kode}. <b>Tidak berpengaruh</b> pada nilai yang dikembalikan
	 * {@link #getKode()}, karena getter tersebut selalu menghitung ulang dari {@link #getId()}.
	 *
	 * @param kode nilai yang akan ditimpa pada pemanggilan {@link #getKode()} berikutnya
	 */
	public void setKode(String kode) {
		this.kode = kode;
	}

	/**
	 * Mengembalikan nama subreport, sudah di-{@code trim}. Kolom {@code nama} wajib diisi, maksimal
	 * 255 karakter.
	 *
	 * @return nama tanpa spasi di ujung, atau {@code null} bila belum diisi
	 */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Menetapkan nama subreport.
	 *
	 * @param nama nama tampilan
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Mengembalikan keterangan/deskripsi bebas subreport.
	 *
	 * @return isi keterangan, bisa {@code null}
	 */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * Menetapkan keterangan/deskripsi bebas.
	 *
	 * @param keterangan teks keterangan
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * Mengembalikan status aktif. Nilai {@code null} (belum pernah diisi) diperlakukan sebagai
	 * {@code true} — baris subreport dianggap aktif secara default. Hanya baris aktif yang diikutkan
	 * {@code ais.action.report.Report} ke dalam laporan.
	 *
	 * @return {@code true} bila aktif atau belum diisi, {@code false} bila dinonaktifkan eksplisit
	 */
	public Boolean getAktif() {
		return aktif == null ? true : aktif;
	}

	/**
	 * Menetapkan status aktif.
	 *
	 * @param aktif status aktif baru
	 */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}

}
