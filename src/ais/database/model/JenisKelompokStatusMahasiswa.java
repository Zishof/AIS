package ais.database.model;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

/**
 * Bank generated by hbm2java
 *
 * <p><b>Master klasifikasi SK kelompok status mahasiswa</b> (mis. "SK Cuti Bersama",
 * "SK Aktif Kembali", "SK Non Aktif Kolektif") &mdash; satu baris tabel
 * {@code public.jenis_kelompok_status_mahasiswa} adalah satu pilihan pada kolom
 * {@link KelompokStatusMahasiswa#getJenisKelompokStatusMahasiswa()}. Entity ini murni lookup
 * deskriptif: sesuai catatan lengkap pada javadoc {@link KelompokStatusMahasiswa}, klasifikasi ini
 * tidak memengaruhi logika bisnis apa pun (tidak ada percabangan berdasarkan jenis) &mdash; hanya
 * ditampilkan sebagai kolom "Jenis" pada grid dan kolom ekspor layar master.</p>
 *
 * <h2>Relasi (diverifikasi dari kode)</h2>
 * <p>{@link JenisKelompokStatusMahasiswa} <b>bukan</b> saudara sejajar {@link StatusMahasiswa},
 * melainkan master klasifikasi milik {@link KelompokStatusMahasiswa} (SK kolektif yang menimpa
 * status keaktifan &mdash; {@link StatusMahasiswa} &mdash; banyak mahasiswa sekaligus pada rentang
 * semester tertentu). Layarnya dimuat sebagai tab kedua ("Manajemen Status") pada layar master yang
 * sama dengan {@code KelompokStatusMahasiswaAction}
 * ({@code /pages/master/jenis_kelompok_status_mahasiswa.zul}). Kolom relasi ini
 * {@code nullable = true} di database namun diwajibkan oleh layar simpan {@code KelompokStatusMahasiswa}.</p>
 *
 * <h2>Peta anggota</h2>
 * <ul>
 *   <li><b>Identitas &amp; audit</b>: {@link #getId()}/{@link #setId(Long)},
 *       {@link #getOleh()}/{@link #setOleh(String)}, {@link #getOlehId()}/{@link #setOlehId(String)},
 *       {@link #getTanggal_dirubah()}/{@link #setTanggal_dirubah(Date)}, {@link #onUpdate()}.</li>
 *   <li><b>Deskriptif</b>: {@link #getKode()}/{@link #setKode(String)},
 *       {@link #getNama()}/{@link #setNama(String)} (wajib),
 *       {@link #getKeterangan()}/{@link #setKeterangan(String)},
 *       {@link #getAktif()}/{@link #setAktif(Boolean)} (flag tampil di combobox pemilihan).</li>
 * </ul>
 *
 * @see KelompokStatusMahasiswa
 * @see KelompokStatusMahasiswa#getJenisKelompokStatusMahasiswa()
 * @see StatusMahasiswa
 * @see GeneralValueObject
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "public", name = "jenis_kelompok_status_mahasiswa")
public class JenisKelompokStatusMahasiswa extends GeneralValueObject {

	/**
	 * Penanda versi serialisasi Java. Nilainya sama dengan {@link KelompokStatusMahasiswa} dan
	 * sejumlah entity lain di paket ini (jejak salin-tempel generator); tidak berdampak fungsional,
	 * jangan dijadikan acuan identitas kelas.
	 */
	private static final long serialVersionUID = 2463821577548439808L;

	/** Kunci utama, lihat {@link #getId()}. */
	private Long id;

	/** Nama pengguna pengubah terakhir, lihat {@link #getOleh()}. */
	private String oleh;

	/** Id pengguna pengubah terakhir, lihat {@link #getOlehId()}. */
	private String olehId;

	/**
	 * Id pengguna pengubah terakhir.
	 *
	 * @return id pengguna pengubah terakhir; {@code null} bila belum pernah tercatat.
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menetapkan id pengguna pengubah terakhir. Setter penjaga: nilai {@code null}/kosong diabaikan
	 * diam-diam sehingga jejak audit yang sudah terisi tidak dapat dihapus lewat setter ini.
	 *
	 * @param olehId id pengguna; diabaikan bila {@code null} atau kosong.
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Menetapkan nama pengguna pengubah terakhir. Berperilaku sama dengan
	 * {@link #setOlehId(String)}: nilai {@code null}/kosong diabaikan diam-diam.
	 *
	 * @param oleh nama pengguna; diabaikan bila {@code null} atau kosong.
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Nama pengguna yang terakhir mengubah baris ini.
	 *
	 * @return nama pengguna pengubah terakhir; {@code null} bila belum pernah tercatat.
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Hook JPA {@code @PreUpdate} yang menyegarkan metadata audit sebelum baris ini di-{@code UPDATE},
	 * plus deklarasi field {@code tanggal_dirubah} yang menempel pada baris kode yang sama. Mendelegasikan
	 * ke {@code ais.database.hibernate.AuditTimestampInterceptor.ubah(this)}; dipanggil Hibernate saja.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menetapkan stempel waktu perubahan terakhir. Tidak menjaga terhadap {@code null}.
	 *
	 * @param tanggal_dirubah waktu perubahan terakhir; boleh {@code null}.
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Waktu terakhir baris ini diubah.
	 *
	 * @return waktu perubahan terakhir, dipetakan sebagai {@code TIMESTAMP}.
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Representasi teks baris ini dalam bentuk {@code "<id>-<nama>"}.
	 *
	 * @return string gabungan id dan nama.
	 */
	public String toString() {
		return id + "-" + nama;
	}

	/** Kode ringkas jenis kelompok, lihat {@link #getKode()}. */
	private String kode;

	/** Nama/label jenis kelompok, lihat {@link #getNama()}. */
	private String nama;

	/** Keterangan bebas, lihat {@link #getKeterangan()}. */
	private String keterangan;

	/** Flag tampil-aktif di combobox pemilihan, lihat {@link #getAktif()}. */
	private Boolean aktif;

	/** Konstruktor tanpa argumen yang dibutuhkan Hibernate/JPA. */
	public JenisKelompokStatusMahasiswa() {
	}

	/**
	 * Kunci utama baris (kolom {@code id}, {@code IDENTITY} &mdash; dibangkitkan database).
	 *
	 * @return id baris; {@code null} selama object belum pernah disimpan.
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menetapkan kunci utama. Praktis hanya dipanggil Hibernate.
	 *
	 * @param id kunci utama baris.
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Kode ringkas jenis kelompok. Tidak beranotasi {@code @Column} eksplisit (mengikuti penamaan
	 * default). Berbeda dari sebagian saudara lookup di paket ini, getter ini secara eksplisit
	 * menjaga nilai {@code null}/kosong menjadi string kosong (bukan {@code null}) sebelum
	 * {@code trim()}.
	 *
	 * @return kode; string kosong bila belum diisi (tidak pernah {@code null}).
	 */
	public String getKode() {
		return kode == null || kode.isEmpty() ? "" : kode.trim();
	}

	/**
	 * Menetapkan kode ringkas jenis kelompok.
	 *
	 * @param kode kode; boleh {@code null}.
	 */
	public void setKode(String kode) {
		this.kode = kode;
	}

	/**
	 * Nama/label jenis kelompok (kolom {@code nama}, wajib). Dikembalikan sudah di-{@code trim()}.
	 *
	 * @return nama; {@code null} bila belum diisi.
	 */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Menetapkan nama/label jenis kelompok.
	 *
	 * @param nama nama jenis kelompok.
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Keterangan bebas (kolom {@code keterangan}, {@code nullable}), dikembalikan apa adanya tanpa
	 * {@code trim()} maupun koersi {@code null}&rarr;kosong.
	 *
	 * @return keterangan; {@code null} bila belum diisi.
	 */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * Menetapkan keterangan bebas.
	 *
	 * @param keterangan teks keterangan; boleh {@code null}.
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * Flag apakah jenis kelompok ini masih ditawarkan di combobox pemilihan. Getter berpola
	 * <i>default-true</i>: {@code null} (baris lama/belum diisi) dibaca sebagai aktif.
	 *
	 * @return {@code true} bila aktif atau belum diisi; {@code false} hanya bila eksplisit dinonaktifkan.
	 */
	public Boolean getAktif() {
		return aktif == null ? true : aktif;
	}

	/**
	 * Menetapkan flag aktif/nonaktif jenis kelompok ini.
	 *
	 * @param aktif {@code true}/{@code null} untuk aktif, {@code false} untuk menyembunyikannya dari
	 *              combobox pemilihan.
	 */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}

}
