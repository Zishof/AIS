package ais.database.model.recruitment;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;




import org.hibernate.Session;
import org.hibernate.criterion.Restrictions;
import org.hibernate.envers.Audited;



import ais.database.hibernate.HibernateUtil;
import ais.common.KarirConfigUtil;
import ais.database.model.GeneralValueObject;

/**
 * Master KELOMPOK (grup/kategori tampilan) untuk parameter-tambahan dinamis calon pegawai — SATU
 * baris mewakili satu grup yang mengelompokkan sejumlah {@code ParameterTambahan} pada formulir
 * pendaftaran (mis. kelompok bernama "VII. Form Tambahan"). Kelas ini SENDIRI BUKAN turunan {@code
 * ais.database.model.ParameterTambahanAstract} dan tidak menyimpan nilai isian apa pun — ia hanya
 * dirujuk sebagai KUNCI PENGELOMPOKAN saat entity {@link CalonPegawai} membangun namespace
 * {@code jenis} untuk lampiran parameter-tambahan (lihat {@code
 * CalonPegawai.populateParameterTambahan(java.util.List)}):
 * <pre>{@code
 * String jenis = LampiranLain.resolveJenisParameterTambahan(CalonPegawai.class, getId(),
 *         kelompokParameterTambahanCalonPegawai.getId() + "->" + parameterTambahan.getId());
 * }</pre>
 *
 * <p><b>Konfirmasi perluasan {@code task_484d4bd0} (instance ke-10, SUDAH memakai mekanisme
 * aman).</b> Kunci komposit {@code (kelompokId, parameterTambahanId)} yang digabung jadi satu string
 * ({@code kelompokId + "->" + parameterTambahanId}) adalah persis pola yang RENTAN tabrakan namespace
 * bila digabung mentah-mentah ke {@code jenis} — pola yang sudah ditemukan berulang di {@code
 * LampiranLain}/{@code ParameterTambahanAstract} pada 9+ entity lain (Pengaduan, CutiDanIzin,
 * Mahasiswa, dll., task {@code task_484d4bd0}). Namun titik pemakaian aktual pada modul rekrutmen
 * (dalam {@code CalonPegawai.populateParameterTambahan(...)}) SUDAH memanggil {@code
 * LampiranLain.resolveJenisParameterTambahan(...)} — helper hasil perbaikan batch sebelumnya
 * (r83920-83971) yang menormalisasi/menamespace-kan {@code jenis} dengan aman — BUKAN konkatenasi
 * mentah tanpa pelindung. Modul rekrutmen dengan demikian menjadi instance ke-10 dari pola
 * pengelompokan {@code kelompok->parameter}, tetapi sudah lahir dengan mekanisme yang sudah
 * ditambal; tidak ada tindakan tambahan yang diperlukan di sini.</p>
 *
 * <p><b>Auto-seed satu baris default</b> lewat {@link #checkCreateDefault()} — lihat javadoc method
 * tersebut untuk detail risiko duplikasi.</p>
 *
 * Bank generated by hbm2java
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "public", name = "kelompok_parameter_tambahan_calon_pegawai")



public class KelompokParameterTambahanCalonPegawai extends GeneralValueObject {

	/**
	 * Versi serialisasi tetap. Nilai literal ini disalin apa adanya dari boilerplate hbm2java yang
	 * sama dan dipakai berulang di banyak entity recruitment lain (bukan unik per kelas) — tidak
	 * masalah karena Java hanya mencocokkan {@code serialVersionUID} pada saat deserialisasi objek
	 * dari kelas yang SAMA.
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	/** Primary key baris (identity, di-generate database). */
	private Long id;
	/** Nama penampil pengguna yang terakhir mengubah baris ini (audit ringan, bukan FK). */
	private String oleh;
	/** Id pengguna yang terakhir mengubah baris ini (audit ringan, pasangan {@link #oleh}). */
	private String olehId;

	/** @return id pengguna (audit) yang terakhir mengubah baris ini. */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Set id pengguna audit; nilai kosong/blank diabaikan (fail-safe) agar baris tidak kehilangan
	 * jejak audit sebelumnya akibat pemanggilan dengan argumen kosong.
	 *
	 * @param olehId id pengguna yang mengubah; {@code null}/kosong tidak melakukan apa-apa
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Set nama pengguna audit; nilai kosong/blank diabaikan (fail-safe), simetris dengan
	 * {@link #setOlehId(String)}.
	 *
	 * @param oleh nama pengguna yang mengubah; {@code null}/kosong tidak melakukan apa-apa
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/** @return nama pengguna (audit) yang terakhir mengubah baris ini. */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Hook lifecycle JPA yang dipanggil sebelum {@code UPDATE}; mendelegasikan pencatatan timestamp
	 * perubahan ke {@link ais.database.hibernate.AuditTimestampInterceptor#ubah(Object)} — field audit
	 * shadow ini adalah KEHARUSAN TEKNIS interceptor Hibernate, bukan sisa kode yang bisa dihapus.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     /** Tanggal terakhir baris ini dirubah; diinisialisasi ke waktu sekarang saat objek dibuat, lalu dimutakhirkan oleh {@link #onUpdate()}. */
private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/** @param tanggal_dirubah timestamp perubahan terakhir (biasanya diisi otomatis oleh {@link #onUpdate()}). */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/** @return timestamp perubahan terakhir baris ini. */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/** @return representasi ringkas untuk log/debug: {@code "<id>-<nama>"}. */
	public String toString() {
		return id + "-" + nama;
	}

	/** Nama/label kelompok (mis. "VII. Form Tambahan"), tampil sebagai header pengelompokan pada formulir. */
	private String nama;
	/** Catatan/deskripsi bebas untuk kelompok ini. */
	private String keterangan;
	/** Penanda kelompok bawaan sistem (hasil {@link #checkCreateDefault()}); lihat catatan getter destruktif di {@link #getDefaultData()}. */
	private Boolean defaultData;
	/** Flag aktif/tidak; lihat catatan getter destruktif di {@link #getAktif()}. */
	private Boolean aktif;
	/** Urutan tampil kelompok pada formulir; lihat catatan getter destruktif di {@link #getNomorUrut()}. */
	private Integer nomorUrut;

	/**
	 * Ambil (atau buat bila belum ada) SATU kelompok bawaan sistem bernama "VII. Form Tambahan",
	 * ditandai {@code defaultData = true}. Query pencarian memakai
	 * {@code Restrictions.eq("defaultData", true)} tanpa {@code setMaxResults} sebelum diambil
	 * {@code uniqueResult()} — bila lebih dari satu baris kebetulan sudah ber-{@code defaultData =
	 * true} (mis. hasil race dua pemanggilan bersamaan, lihat di bawah), Hibernate melempar {@code
	 * NonUniqueResultException} yang akan tertangkap oleh {@code catch (Exception e)} dan dibungkus
	 * ulang sebagai {@code RuntimeException} — method ini TIDAK diam-diam memilih salah satu baris.
	 *
	 * <p><b>Tidak ada constraint unik atas {@code defaultData = true}.</b> Dua pemanggilan yang
	 * beririsan waktu (mis. dua request bersamaan yang sama-sama menemukan hasil query kosong) dapat
	 * membuat DUA baris ber-{@code defaultData = true} secara bersamaan — pola risiko yang sama
	 * dengan {@link VerifikasiKelengkapanCalonPegawai#reloadDefault()} dan berbagai kasus duplikasi
	 * lain yang sudah ditambal di modul lain lewat index unik/counter atomik.</p>
	 *
	 * @return kelompok default (baris lama bila sudah ada, atau baris baru yang baru saja disimpan).
	 * @throws RuntimeException dibungkus dari exception apa pun saat query/simpan (termasuk saat
	 *         {@code uniqueResult()} menemukan lebih dari satu baris default); transaksi aktif
	 *         di-rollback terlebih dahulu bila memungkinkan.
	 */
	public static KelompokParameterTambahanCalonPegawai checkCreateDefault() {
		Session session = null;
		try {
			session = HibernateUtil.currentNativeSession();
			KelompokParameterTambahanCalonPegawai kelompokParameterTambahanCalonPegawai = (KelompokParameterTambahanCalonPegawai) session
					.createCriteria(KelompokParameterTambahanCalonPegawai.class)
					.add(Restrictions.eq("defaultData", true)).setMaxResults(1).uniqueResult();
			if (kelompokParameterTambahanCalonPegawai == null) {
				kelompokParameterTambahanCalonPegawai = new KelompokParameterTambahanCalonPegawai();
				kelompokParameterTambahanCalonPegawai.setDefaultData(true);
				kelompokParameterTambahanCalonPegawai.setNama("VII. Form Tambahan");
				kelompokParameterTambahanCalonPegawai.setKeterangan("VII. Form Tambahan");
				session.getTransaction().begin();
				session.save(kelompokParameterTambahanCalonPegawai);
				session.getTransaction().commit();
			}
			return kelompokParameterTambahanCalonPegawai;
		} catch (Exception e) {
			try { if (session != null && session.getTransaction() != null && session.getTransaction().isActive()) session.getTransaction().rollback(); } catch (Exception ex) { ais.common.ErrorAuditUtil.record(ex, "auto-audit(empty-catch) src/ais/database/model/recruitment/KelompokParameterTambahanCalonPegawai.java:108");}
			throw new RuntimeException(e);
		} finally {
			KarirConfigUtil.closeNativeSession(session);
		}
	}

	/** Konstruktor default (dibutuhkan Hibernate); semua field bernilai default/null. */
	public KelompokParameterTambahanCalonPegawai() {
	}

	/** @return primary key baris ini, atau {@code null} bila belum tersimpan. */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/** @param id primary key baris; biasanya hanya diisi Hibernate, bukan kode aplikasi. */
	public void setId(Long id) {
		this.id = id;
	}

	/** @return nama kelompok, hasil di-{@code trim()}; {@code null} bila belum diisi. */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/** @param nama nama/label kelompok; disimpan apa adanya (trimming dilakukan di getter). */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/** @return catatan/deskripsi kelompok; bisa {@code null}. */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/** @param keterangan catatan/deskripsi bebas untuk kelompok ini. */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * Penanda kelompok bawaan sistem (dibuat oleh {@link #checkCreateDefault()}).
	 *
	 * <p><b>GETTER DESTRUKTIF — memutasi field, bukan sekadar membaca.</b> Berbeda dengan pola
	 * default aman di file sejenis paket ini (mis. {@link VerifikasiKelengkapanCalonPegawai#getAktif()},
	 * yang HANYA mengevaluasi ternary tanpa menulis field), getter ini benar-benar meng-{@code assign}
	 * {@code this.defaultData = false} ke field bila sebelumnya {@code null}. Pada entity yang SEDANG
	 * dikelola Hibernate ({@code dynamicUpdate = true}, {@code @Audited}), pemanggilan getter ini pada
	 * baris lama yang belum pernah punya kolom {@code default_data} ter-set (nilai DB {@code NULL})
	 * mengubah state in-memory entity tersebut menjadi "dirty" HANYA KARENA DIBACA — bila sesi
	 * Hibernate melakukan flush setelahnya (mis. di akhir request/transaksi yang sama, dipicu operasi
	 * lain), perubahan ini bisa ikut ter-{@code UPDATE} ke database beserta revisi baru di riwayat
	 * Envers, padahal tidak ada perubahan data yang benar-benar dimaksudkan pengguna. Getter ini
	 * dipanggil dari renderer daftar admin ({@code
	 * KelompokParameterTambahanCalonPegawaiAction}, dalam konteks render baris/tombol hapus) — konteks
	 * yang secara semantik seharusnya READ-ONLY.
	 *
	 * @return {@code true} bila kelompok ini adalah kelompok bawaan sistem; {@code false} bila bukan.
	 */
	public Boolean getDefaultData() {
		if (defaultData == null) {
			defaultData = false;
		}
		return defaultData;
	}

	/** @param defaultData penanda kelompok bawaan sistem baru. */
	public void setDefaultData(Boolean defaultData) {
		this.defaultData = defaultData;
	}

	/**
	 * Status aktif kelompok.
	 *
	 * <p><b>GETTER DESTRUKTIF — sama seperti {@link #getDefaultData()}</b>: bila {@link #aktif}
	 * bernilai {@code null}, getter ini meng-{@code assign} {@code this.aktif = true} ke field
	 * sebelum mengembalikannya, bukan sekadar mengevaluasi ternary tanpa efek samping. Risiko dan
	 * konteks pemanggilan (renderer daftar admin, checkbox aktif) sama seperti dijelaskan di {@link
	 * #getDefaultData()}.
	 *
	 * @return {@code true} bila kelompok ini aktif; {@code false} bila dinonaktifkan.
	 */
	public Boolean getAktif() {
		if (aktif == null) {
			aktif = true;
		}
		return aktif;
	}

	/** @param aktif status aktif baru. */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}

	/**
	 * Urutan tampil kelompok pada formulir.
	 *
	 * <p><b>GETTER DESTRUKTIF — sama seperti {@link #getDefaultData()}</b>: bila {@link #nomorUrut}
	 * bernilai {@code null}, getter ini meng-{@code assign} {@code this.nomorUrut = 1} ke field
	 * SEBELUM baris {@code return nomorUrut == null ? 1 : nomorUrut;} dijalankan — baris ternary
	 * tersebut karenanya TIDAK PERNAH mengevaluasi cabang {@code null} (sudah keburu di-assign di
	 * blok {@code if} sebelumnya); kode redundan yang tidak berbahaya sendiri, namun menandakan
	 * assignment di blok {@code if} tersebut memang disengaja/tidak disadari sebagai efek samping.
	 * Risiko dan konteks pemanggilan sama seperti dijelaskan di {@link #getDefaultData()}.
	 *
	 * @return urutan tampil kelompok; {@code 1} bila belum pernah diisi.
	 */
	public Integer getNomorUrut() {
		if (nomorUrut == null) {
			nomorUrut = 1;
		}
		return nomorUrut == null ? 1 : nomorUrut;
	}

	/** @param nomorUrut urutan tampil baru kelompok ini pada formulir. */
	public void setNomorUrut(Integer nomorUrut) {
		this.nomorUrut = nomorUrut;
	}

	/**
	 * Bandingkan urutan tampil untuk pengurutan koleksi (mis. {@code TreeSet<KelompokParameterTambahanCalonPegawai>}).
	 * Memanggil {@link #getNomorUrut()} pada KEDUA sisi — sebagai konsekuensi tidak langsung dari
	 * getter destruktif di atas, mengurutkan koleksi berisi baris ber-{@code nomorUrut} null akan ikut
	 * menuliskan default {@code 1} ke field masing-masing baris tersebut.
	 *
	 * @param arg0 objek pembanding; HARUS berupa {@code KelompokParameterTambahanCalonPegawai}
	 *             ({@code ClassCastException} bila bukan — tidak ada pengecekan tipe).
	 * @return hasil {@code Integer.compareTo(Integer)} atas {@code nomorUrut} kedua sisi.
	 */
	@Override
	public int compareTo(GeneralValueObject arg0) {
		return getNomorUrut().compareTo(((KelompokParameterTambahanCalonPegawai) arg0).getNomorUrut());
	}
}
