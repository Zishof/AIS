package ais.database.model.recruitment;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;




import org.hibernate.envers.Audited;



import ais.database.model.GeneralValueObject;

/**
 * Master parameter/kriteria verifikasi calon pegawai — SATU baris mewakili SATU kriteria yang dapat
 * dipasangkan (many-to-many) ke satu atau lebih gelombang pendaftaran lewat entity join
 * {@link GelombangPendaftaranPegawaiPunyaParameterVerifikasiCalonPegawai}. Kelas ini TIDAK sama
 * dengan {@link VerifikasiKelengkapanCalonPegawai}: yang terakhir adalah master "syarat kelengkapan
 * berkas" (dokumen wajib/opsional yang diverifikasi lewat {@link CalonPegawaiPunyaVerifikasiBerkas}/
 * {@link CalonPegawaiPunyaDokumen}), sedangkan {@code ParameterVerifikasiCalonPegawai} adalah master
 * berdiri sendiri yang hanya dirujuk lewat join many-to-many tersebut — tidak punya baris detail
 * per-calon di paket ini (baris hasil/nilai per calon, bila ada, dikelola di luar 13 file paket
 * {@code recruitment} ini, kemungkinan pada klaster ujian/gelombang).
 *
 * <p>Dikelola lewat layar admin {@code
 * ais.action.master.recruitment.ParameterVerifikasiCalonPegawaiAction}, termasuk fitur upload data
 * massal ({@code Common.uploadData(...)}) dan validasi nama duplikat ({@code
 * checkNamaParameterVerifikasiCalonPegawai()} pada action tersebut).</p>
 *
 * Bank generated by hbm2java
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "public", name = "parameter_verifikasi_calon_pegawai")



public class ParameterVerifikasiCalonPegawai extends GeneralValueObject {

	/**
	 * Versi serialisasi tetap. Nilai literal ini disalin apa adanya dari boilerplate hbm2java yang
	 * sama dan dipakai berulang di banyak entity recruitment lain (bukan unik per kelas) — tidak
	 * masalah karena Java hanya mencocokkan {@code serialVersionUID} pada saat deserialisasi objek
	 * dari kelas yang SAMA.
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	/** Primary key baris (identity, di-generate database). */
	private Long id;
	/** Nama penampil pengguna yang terakhir mengubah baris ini (audit ringan, bukan FK). */
	private String oleh;
	/** Id pengguna yang terakhir mengubah baris ini (audit ringan, pasangan {@link #oleh}). */
	private String olehId;

	/** @return id pengguna (audit) yang terakhir mengubah baris ini. */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Set id pengguna audit; nilai kosong/blank diabaikan (fail-safe) agar baris tidak kehilangan
	 * jejak audit sebelumnya akibat pemanggilan dengan argumen kosong.
	 *
	 * @param olehId id pengguna yang mengubah; {@code null}/kosong tidak melakukan apa-apa
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Set nama pengguna audit; nilai kosong/blank diabaikan (fail-safe), simetris dengan
	 * {@link #setOlehId(String)}.
	 *
	 * @param oleh nama pengguna yang mengubah; {@code null}/kosong tidak melakukan apa-apa
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/** @return nama pengguna (audit) yang terakhir mengubah baris ini. */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Hook lifecycle JPA yang dipanggil sebelum {@code UPDATE}; mendelegasikan pencatatan timestamp
	 * perubahan ke {@link ais.database.hibernate.AuditTimestampInterceptor#ubah(Object)} — field audit
	 * shadow ini adalah KEHARUSAN TEKNIS interceptor Hibernate, bukan sisa kode yang bisa dihapus.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     /** Tanggal terakhir baris ini dirubah; diinisialisasi ke waktu sekarang saat objek dibuat, lalu dimutakhirkan oleh {@link #onUpdate()}. */
private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/** @param tanggal_dirubah timestamp perubahan terakhir (biasanya diisi otomatis oleh {@link #onUpdate()}). */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/** @return timestamp perubahan terakhir baris ini. */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/** @return representasi ringkas untuk log/debug: {@code "<id>-<nama>"}. */
	public String toString() {
		return id + "-" + nama;
	}

	/** Nama/label kriteria verifikasi (wajib diisi, maks. 255 karakter). */
	private String nama;
	/** Catatan/deskripsi bebas untuk kriteria ini. */
	private String keterangan;
	/** Flag aktif/tidak; menentukan apakah kriteria ini masih ditawarkan untuk dipasangkan ke gelombang baru. */
	private Boolean aktif;
	/** Urutan tampil pada daftar/form (angka lebih kecil tampil lebih dulu). */
	private Integer nomorUrut;

	/** Konstruktor default (dibutuhkan Hibernate); semua field bernilai default/null. */
	public ParameterVerifikasiCalonPegawai() {
	}

	/** @return primary key baris ini, atau {@code null} bila belum tersimpan. */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/** @param id primary key baris; biasanya hanya diisi Hibernate, bukan kode aplikasi. */
	public void setId(Long id) {
		this.id = id;
	}

	/** @return nama kriteria, hasil di-{@code trim()}; {@code null} bila belum diisi. */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/** @param nama nama/label kriteria verifikasi; disimpan apa adanya (trimming dilakukan di getter). */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/** @return catatan/deskripsi kriteria; bisa {@code null}. */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/** @param keterangan catatan/deskripsi bebas untuk kriteria ini. */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * Status aktif. Default fail-open: nilai {@code null} (belum pernah diset) dibaca sebagai
	 * {@code true} (aktif), bukan {@code false} — pola berulang di banyak master data pada codebase
	 * ini (baris lama yang belum pernah punya kolom ini di-set tetap dianggap aktif).
	 *
	 * @return {@code true} bila kriteria ini aktif/masih ditawarkan; {@code false} bila sengaja dinonaktifkan.
	 */
	public Boolean getAktif() {
		return aktif == null ? true : aktif;
	}

	/** @param aktif status aktif baru; {@code null} akan dibaca sebagai aktif oleh {@link #getAktif()}. */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}

	/**
	 * Urutan tampil. Default: {@code 1} bila belum pernah diset — TIDAK ada penjaga keunikan nomor
	 * urut antar baris; nilai yang sama pada beberapa baris hanya memengaruhi urutan tampil (tidak
	 * menimbulkan error), sehingga aman namun bisa membuat urutan tampil ambigu bila banyak baris
	 * memakai nilai default yang sama.
	 *
	 * @return nomor urut tampil; {@code 1} bila belum diisi.
	 */
	public Integer getNomorUrut() {
		return nomorUrut == null ? 1 : nomorUrut;
	}

	/** @param nomorUrut urutan tampil baru pada daftar/form. */
	public void setNomorUrut(Integer nomorUrut) {
		this.nomorUrut = nomorUrut;
	}

}
