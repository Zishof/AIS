package ais.database.model.sekolah;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

import ais.database.model.GeneralValueObject;

/**
 * Entity MASTER <b>pekerjaan orang tua siswa</b> — katalog jenis pekerjaan ayah/ibu/wali yang
 * dipakai sebagai daftar pilihan pada bagian "data keluarga" formulir master siswa. Satu baris
 * tabel {@code sekolah.pekerjaan_ortu_siswa} mewakili satu jenis pekerjaan ("Petani", "PNS/TNI/Polri",
 * "Wiraswasta", "Buruh", dan seterusnya).
 *
 * <p><i>Catatan komentar generator.</i> Javadoc lama berbunyi "Bank generated by hbm2java" — itu
 * salah tempel dari {@code ais.database.model.Bank} yang tersebar ke ratusan berkas model di repo
 * ini (sumber aslinya sudah ditelusuri pada batch dokumentasi sebelumnya). Berkas ini memang lahir
 * dari hbm2java, tetapi tidak ada hubungannya dengan entity Bank.</p>
 *
 * <h3>Isi domain (terverifikasi dari kode, bukan dugaan)</h3>
 *
 * <p>Tabel ini <b>tidak</b> punya daftar literal miliknya sendiri. Pada instalasi baru
 * {@code ais.common.InitDataHelper} menyalin seluruh baris {@link ais.database.model.Pekerjaan}
 * (skema {@code public}, katalog sisi perguruan tinggi) ke sini <i>hanya bila tabel siswa masih
 * kosong</i> ({@code COUNT(*) == 0}). Daftar {@code Pekerjaan} itu sendiri di-seed literal di
 * {@code InitDataHelper} memakai nomenklatur PDDikti/Feeder:</p>
 * <table border="1">
 *   <tr><th>id Feeder</th><th>{@code nama}</th></tr>
 *   <tr><td>1</td><td>Tidak bekerja</td></tr>
 *   <tr><td>2</td><td>Nelayan</td></tr>
 *   <tr><td>3</td><td>Petani</td></tr>
 *   <tr><td>4</td><td>Peternak</td></tr>
 *   <tr><td>5</td><td>PNS/TNI/Polri</td></tr>
 *   <tr><td>6</td><td>Karyawan Swasta</td></tr>
 *   <tr><td>7</td><td>Pedagang Kecil</td></tr>
 *   <tr><td>8</td><td>Pedagang Besar</td></tr>
 *   <tr><td>9</td><td>Wiraswasta</td></tr>
 *   <tr><td>10</td><td>Wirausaha</td></tr>
 *   <tr><td>11</td><td>Buruh</td></tr>
 *   <tr><td>12</td><td>Pensiunan</td></tr>
 *   <tr><td>98</td><td>Sudah Meninggal</td></tr>
 *   <tr><td>99</td><td>Lainnya</td></tr>
 * </table>
 *
 * <p><b>Kode Feeder hilang dalam penyalinan.</b> Blok seed menulis
 * {@code pekerjaanOrtuSiswa.setKode(pekerjaan.getKode())}, padahal {@link ais.database.model.Pekerjaan}
 * <b>tidak mendeklarasikan properti {@code kode} sama sekali</b> — {@code getKode()} yang terpanggil
 * adalah milik {@link GeneralValueObject}, yang bukan {@code @Entity}/{@code @MappedSuperclass}
 * sehingga fieldnya tidak pernah dipetakan maupun diisi Hibernate. Akibatnya {@code getKode()} di
 * sisi sumber <b>selalu</b> mengembalikan {@code null} dan seluruh baris hasil seed di sini masuk
 * dengan {@code kode} kosong. Nomor Feeder (1..99) yang sebenarnya tersimpan di properti
 * {@code Pekerjaan.feeder} tidak pernah ikut. Bandingkan dengan {@link AlatTransportasiSiswa} yang
 * blok seed-nya sengaja memakai {@code getFeeder() + ""} sehingga kodenya terbawa — di sini pola
 * itu tidak diikuti. Konsekuensi praktis: kolom "Kode" pada layar master selalu kosong sampai
 * operator mengisinya manual, dan tidak ada jalur otomatis untuk memetakan balik ke kode Feeder.</p>
 *
 * <p>Seperti kerabatnya, syarat {@code COUNT(*) == 0} berarti bila operator sempat membuat satu
 * baris apa pun sebelum seed berjalan, seluruh daftar baku 14 jenis di atas <b>tidak pernah</b>
 * masuk.</p>
 *
 * <h3>Peran dalam model data</h3>
 *
 * <p>Entity ini adalah sisi "satu" dari <b>tiga</b> relasi {@code @ManyToOne} sekaligus pada
 * {@link Siswa} — satu-satunya entity yang menunjuk ke sini:</p>
 * <ul>
 *   <li>{@link Siswa#getPekerjaanAyah()} → kolom {@code sekolah.siswa.pekerjaan_ortu_ayah_id}</li>
 *   <li>{@link Siswa#getPekerjaanIbu()} → kolom {@code sekolah.siswa.pekerjaan_ortu_ibu_id}</li>
 *   <li>{@link Siswa#getPekerjaanWali()} → kolom {@code sekolah.siswa.pekerjaan_ortu_wali_id}</li>
 * </ul>
 * <p>Ketiganya {@code nullable} dan ber-{@code cascade = {PERSIST, MERGE}}. Tidak ada koleksi balik
 * ({@code @OneToMany}) di kelas ini, sehingga dari sisi katalog tidak ada cara mengetahui berapa
 * siswa yang memakai suatu jenis pekerjaan selain lewat query manual. Perlu dicatat bahwa ketiga
 * getter di {@code Siswa} memanggil {@code check(...)} milik {@link GeneralValueObject} (resolusi
 * proxy lazy dengan penulisan balik ke field) — pola "getter write-back" itu ada di sisi
 * {@code Siswa}, <b>bukan</b> di kelas ini; seluruh getter kelas ini bebas efek samping.</p>
 *
 * <p><b>Jangkauan pemakaian sudah diverifikasi menyeluruh.</b> Penulis kolom FK hanyalah formulir
 * master siswa {@code ais.action.master.sekolah.SiswaAction} (tiga combobox dibangun di sekitar
 * baris 3609/3720/3820, nilainya disimpan di sekitar baris 4113 dan 4186). Pembacanya hanya
 * {@code SiswaAction} sendiri dan panel profil {@code ais.action.master.helper.profile.ProfileSiswa}
 * ("Pekerjaan Ayah/Ibu/Wali"). <b>Tidak ada</b> laporan, dasbor, ekspor Excel, atau endpoint API di
 * sisi sekolah yang membaca ketiga kolom itu — {@code DashboardMahasiswaPekerjaanOrtu} yang namanya
 * mirip memakai katalog sisi PT ({@code Pekerjaan}/{@code PekerjaanOrangTua}), sama sekali bukan
 * entity ini. Jadi data pekerjaan orang tua siswa dikumpulkan, tetapi belum pernah dilaporkan
 * (kembar temuan pada {@link AlatTransportasiSiswa}).</p>
 *
 * <h3>PSB/PPDB memakai katalog yang berbeda, dan penyalinannya gagal senyap</h3>
 *
 * <p>{@link CalonSiswa} juga punya properti {@code pekerjaanAyah}/{@code pekerjaanIbu}/
 * {@code pekerjaanWali}, tetapi bertipe {@link ais.database.model.Pekerjaan} (tabel
 * {@code public.pekerjaan}), bukan kelas ini. Formulir {@code PPDB1}/{@code PPDB2} dan
 * {@code CalonSiswaAction} mengisi katalog PT tersebut, sedangkan formulir siswa mengisi katalog
 * sekolah di sini. Keduanya berisi nama-nama yang sama persis (karena satu disalin dari yang lain)
 * tetapi dengan id yang berbeda.</p>
 *
 * <p>Saat calon siswa dipromosikan menjadi siswa, {@code ais.common.CommonPSB} menyalin field
 * secara reflektif dengan mencocokkan <b>nama properti</b> Hibernate:
 * {@code classMetadataSiswa.setPropertyValue(siswa, p, val, EntityMode.POJO)} untuk setiap nama
 * yang ada di kedua entity. Nama {@code pekerjaanAyah}/{@code pekerjaanIbu}/{@code pekerjaanWali}
 * ada di kedua sisi sehingga lolos penyaringan, tetapi <b>tipenya berbeda</b> — memasukkan sebuah
 * {@code Pekerjaan} ke setter yang menuntut {@code PekerjaanOrtuSiswa} melempar exception, yang
 * ditelan {@code try/catch} di loop itu dan hanya dicatat lewat {@code ErrorAuditUtil}. Efeknya:
 * setiap promosi calon siswa yang datanya terisi menghasilkan tiga entri error di log audit, dan
 * pekerjaan orang tua <b>tidak pernah terbawa</b> — harus diisi ulang manual di formulir siswa.
 * (Bila nilainya {@code null} tidak ada exception, jadi kegagalan justru terjadi tepat ketika
 * datanya ada.) Perbedaan semantik lain: {@code CalonSiswa.getPekerjaanWali()} jatuh ke
 * {@code getPekerjaanAyah()} bila wali kosong, sedangkan {@link Siswa#getPekerjaanWali()} tidak
 * punya fallback seperti itu.</p>
 *
 * <h3>Layar dan penjagaan hak akses</h3>
 *
 * <p>Layar CRUD-nya {@code /pages/master/sekolah/pekerjaan_ortu_siswa.zul}, di-apply oleh
 * {@code ais.action.master.sekolah.PekerjaanOrtuSiswaAction}. Action tersebut memanggil
 * {@code Common.doCheckSecurity()} di {@code doBeforeCompose} dan <b>menjaga seluruh aksi mutasi
 * dengan benar</b>: tombol Tambah dibatasi {@code CREATE}, tombol Ubah/Hapus per baris memakai
 * {@code setVisible(edit)}/{@code setVisible(delete)}, checkbox "Aktif" di grid dimatikan dengan
 * {@code setDisabled(!edit)}, dan tombol Upload hanya tampil bila pemakai memegang
 * CREATE+UPDATE+DELETE sekaligus. Tidak ada kontrol grid yang lolos gerbang — layar ini tidak
 * punya kontrol nomor urut sama sekali, sehingga temuan {@code JenisAktiftasHarianDefault}
 * (Intbox tanpa gerbang) tidak berlaku di sini.</p>
 *
 * <p><b>Namun berlaku pola "pewarisan hak lewat menu induk", dalam varian paling murni — dan lewat
 * pintu yang persis sama dengan kembarannya.</b> Layar ini <b>tidak memiliki entri menu sendiri</b>:
 * {@code ais.common.MenuInitializer} tidak menyebut {@code pekerjaan_ortu_siswa.zul} sama sekali
 * (kerabat {@code status_awal_siswa.zul} punya entri, {@code pekerjaan_ortu_siswa.zul} tidak), dan
 * penelusuran seluruh {@code .java}/{@code .zul}/{@code .jsp}/{@code .sql}/{@code .xml} hanya
 * menemukan satu rujukan: daftar tab {@code subZuls} di
 * {@code ais.action.master.KonfigurasiTampilanSiswaAction} (tab "Pekerjaan", disisipkan lewat
 * {@code MyInclude}). Karena {@code CommonPrivilages.checkPrevilages(...)} menguji hak terhadap
 * menu halaman yang sedang dibuka — bukan halaman yang di-{@code include} — maka hak yang
 * benar-benar diperiksa <b>selalu</b> hak atas menu Konfigurasi Tampilan Siswa. Siapa pun yang
 * boleh mengubah konfigurasi tampilan formulir siswa dengan sendirinya memperoleh
 * CREATE/UPDATE/DELETE penuh atas katalog ini <i>dan</i> enam katalog master siswa lain yang
 * menjadi tab bersamanya (Jenis Tinggal, Alat Transportasi, Penghasilan, Pendidikan, Status Awal,
 * Status Keluar). Ini tab yang sama dan pintu yang sama dengan {@link AlatTransportasiSiswa} serta
 * {@link JenisTinggalSiswa}.</p>
 *
 * <p>Ada satu permukaan lain: generator CRUD generik
 * {@code /WEB-INF/baru/modul/pagesmastersekolahpekerjaanortusiswazul/index.jsp} memanggil
 * {@code ais.common.DynamicJspCrudGenerator.generate(PekerjaanOrtuSiswa.class)}; kelas itu
 * menggerbangi tombolnya dengan {@code CommonPrivilages.checkPrevilages(...)} yang sama (jadi
 * mewarisi keterbatasan yang sama, bukan lubang baru). Sebaliknya berkas UI baru
 * {@code /WEB-INF/new/sekolah/services/pekerjaan_ortu_siswa_service.jsp} dan
 * {@code uiux/pekerjaan_ortu_siswa.jsp} hanyalah <i>scaffold</i> hasil generator yang meneruskan ke
 * {@code dispatcher.jsp} tanpa akses data apa pun — <b>verifikasi negatif</b>, bukan endpoint
 * pra-otentikasi.</p>
 *
 * <p><b>Tidak ada kolom tenant.</b> Entity ini tidak punya FK ke sekolah maupun yayasan — katalog
 * bersifat GLOBAL per instalasi, sama seperti seluruh kerabat katalog data pribadi siswa. Seluruh
 * sekolah dalam satu instalasi berbagi baris yang sama, sehingga penyuntingan atau penghapusan oleh
 * operator satu sekolah langsung terasa di sekolah lain. Ini bukan "fail-open cakupan tenant"
 * (tidak ada cakupan yang bocor), melainkan memang desain katalog global.</p>
 *
 * <h3>Hal non-obvious</h3>
 * <ul>
 *   <li><b>Deklarasi ulang {@code id}/{@code oleh}/{@code olehId}/{@code tanggal_dirubah} BUKAN
 *       bug — itu keharusan teknis.</b> {@link GeneralValueObject} adalah POJO abstrak biasa, bukan
 *       {@code @Entity} maupun {@code @MappedSuperclass}, sehingga Hibernate sama sekali tidak
 *       memetakan properti induknya. Setiap subclass wajib mendeklarasikan ulang field dan accessor
 *       yang ingin dipersistensikan. Hal yang sama berlaku untuk {@code kode}, {@code nama}, dan
 *       {@code keterangan}: field induk bernama sama tetap ada tapi {@code private} dan permanen
 *       kosong. Justru <i>karena</i> {@link ais.database.model.Pekerjaan} lupa mendeklarasikan
 *       ulang {@code kode}, blok seed di atas menyalin {@code null}.</li>
 *   <li><b>{@link #getKeterangan()} DIPETAKAN dengan benar di sini</b> — {@code @Column(name =
 *       "keterangan", nullable = true)} terpasang, jadi isian keterangan benar-benar tersimpan. Ini
 *       <b>verifikasi negatif</b> untuk pola "{@code getKeterangan()} tidak dipetakan sama sekali"
 *       yang ditemukan pada {@code StatusAwalSiswa}; katalog ini tidak terkena. Satu-satunya sisa
 *       efek pewarisan: karena override di sini boleh mengembalikan {@code null} (sedangkan
 *       {@code GeneralValueObject.getKeterangan()} berjanji tidak pernah {@code null}), cabang
 *       {@code keterangan} pada {@code compareTo()} induk bisa terlewat.</li>
 *   <li><b>{@code getNomorUrut()} tidak di-override</b>, sehingga selalu mengembalikan {@code null}
 *       (field {@code nomorUrut} milik induk tidak dipetakan dan tidak pernah diisi siapa pun).
 *       {@code GeneralValueObject.compareTo()} karena itu melewati kunci {@code nomorUrut} dan
 *       {@code nim}, lalu jatuh ke {@code nama}. Ini <b>verifikasi negatif</b> untuk pola
 *       "{@code getNomorUrut()} non-null yang meruntuhkan {@code TreeSet}" — katalog ini tidak
 *       terkena, dan tidak ada {@code TreeSet} berkunci entity ini di repo.</li>
 *   <li><b>Pengamatan mentah soal kolom {@code aktif} (sengaja tanpa kesimpulan).</b> Antar-agen
 *       masih ada penjelasan yang bertentangan tentang pola "kolom {@code aktif} tak pernah
 *       ditulis"; yang berikut ini adalah fakta yang bisa dibaca langsung dari kode, bukan
 *       simpulan:
 *       <ol>
 *         <li>Satu-satunya pemanggil {@link #setAktif(Boolean)} di seluruh repo adalah listener
 *             {@code onCheck} checkbox "Aktif" pada grid layar master. {@code onSave()},
 *             jalur unggah Excel, dan blok seed {@code InitDataHelper} tidak pernah menyentuhnya —
 *             field {@code aktif} pada object baru bernilai {@code null} di memori.</li>
 *         <li>Anotasi {@code @Id} berada pada {@link #getId()}, jadi entity ini memakai
 *             <i>property access</i>; {@link #getAktif()} tidak dianotasi sehingga terpetakan
 *             implisit ke kolom {@code aktif}; getter tersebut meng-<i>coalesce</i> {@code null}
 *             menjadi {@code true}. Kelas juga memakai {@code dynamicInsert = true}.</li>
 *         <li>Kedua pembaca memakai filter yang <b>berbeda</b>:
 *             {@code PekerjaanOrtuSiswaAction.initCriteria(...)} toleran NULL
 *             ({@code Restrictions.or(isNull("aktif"), eq("aktif", true))}), sedangkan ketiga
 *             combobox di {@code SiswaAction} menyaring ketat
 *             ({@code Restrictions.eq("aktif", true)}).</li>
 *       </ol>
 *       Asimetri (3) itulah yang membuat entity ini menjadi <b>probe empiris yang baik</b>: bila
 *       nilai yang benar-benar tersimpan adalah NULL, baris baru akan <i>tampak</i> di layar master
 *       tetapi <i>tidak muncul</i> di ketiga kombo formulir siswa; bila yang tersimpan
 *       {@code true}, keduanya konsisten. Perbedaan itu langsung terlihat di UI, jadi sebaiknya
 *       diverifikasi terhadap database sungguhan sebelum dijadikan dasar perbaikan kode.
 *       Catatan tambahan: checkbox grid diinisialisasi {@code setChecked(getAktif())} yang selalu
 *       {@code true} pada baris baru, sehingga klik pertama menulis {@code false} dan baru klik
 *       kedua menulis {@code true} secara eksplisit.</li>
 *   <li><b>Keunikan {@code nama} hanya ditegakkan di aplikasi</b>, lewat
 *       {@code PekerjaanOrtuSiswaAction.checkNamaAgama()} — nama method itu sisa salin-tempel dari
 *       layar Agama, isinya {@code rowCount} dengan {@code Restrictions.eq("nama", ...)} plus
 *       pengecualian id sendiri. Tidak ada {@code unique = true} di kolom mana pun, dan jalur
 *       unggah Excel maupun generator CRUD generik tidak memanggil pemeriksaan itu — duplikat masih
 *       mungkin masuk lewat kedua jalur tersebut. {@code kode} tidak diperiksa unik sama
 *       sekali.</li>
 *   <li><b>Jalur unggah Excel hanya memetakan tiga kolom</b> ({@code id}, {@code nama},
 *       {@code kode}); {@code keterangan} dan {@code aktif} tidak pernah diisi dari berkas. Setiap
 *       baris di-commit dalam transaksi sendiri dan {@code HibernateUtil.closeSession()} dipanggil
 *       di dalam loop, sehingga kegagalan di tengah berkas meninggalkan impor separuh jalan tanpa
 *       rollback. Berkas sementara ditulis ke {@code /temp/} pada webapp memakai nama berkas asli
 *       dari pemakai — pola yang sama dipakai di 170+ titik unggah lain di repo dan sudah tercakup
 *       audit unggah-berkas yang ada, bukan temuan khusus entity ini.</li>
 *   <li><b>{@code keterangan} tidak ikut tercetak.</b> Tombol cetak/ekspor layar master memakai
 *       {@code Common.cetakData(this, "id", "nama", "kode", "aktif")} sehingga kolom Keterangan yang
 *       tampil di grid tidak pernah ikut ke hasil cetak. {@code keterangan} juga tidak dipakai
 *       formulir siswa: kombo dibangun dengan {@code Common.insertCombo(..., "nama", ...)} yang
 *       hanya menampilkan {@code nama} (berbeda dari {@link AlatTransportasiSiswa} yang memakai
 *       varian kombo dengan deskripsi).</li>
 *   <li><b>{@link #setOleh(String)}/{@link #setOlehId(String)} menolak nilai kosong secara
 *       senyap</b> — nilai jejak audit tidak pernah bisa dikosongkan kembali setelah terisi.</li>
 *   <li><b>{@link #toString()} membaca field {@code nama} langsung</b>, bukan lewat
 *       {@link #getNama()}, sehingga hasilnya <i>tidak</i> di-{@code trim()} — berbeda dari nilai
 *       yang dipersistensikan. Format {@code "<id>-<nama>"} ini juga menimpa format standar
 *       {@code "kode - nama"} milik {@link GeneralValueObject}.</li>
 *   <li><b>{@code serialVersionUID} identik</b> dengan {@link AlatTransportasiSiswa} dan
 *       {@code AlatTransportasiMahasiswa} ({@code 2463821577548439808L}) — penanda bahwa berkas ini
 *       satu keluarga salin-tempel, bukan penanda identitas kelas.</li>
 *   <li>Kelas dianotasi {@link Audited}, jadi setiap perubahan direkam Envers.
 *       {@code dynamicInsert}/{@code dynamicUpdate} aktif sehingga hanya kolom yang berubah yang
 *       ikut dalam pernyataan SQL. Berbeda dengan beberapa layar master lain, grid layar ini tidak
 *       menampilkan tautan riwayat revisi.</li>
 * </ul>
 *
 * <h3>Pengelompokan anggota kelas</h3>
 * <ul>
 *   <li><b>Identitas &amp; konstruktor</b> — {@link #PekerjaanOrtuSiswa()}, {@link #getId()},
 *       {@link #setId(Long)}, {@link #toString()}.</li>
 *   <li><b>Isi katalog</b> — {@link #getKode()}, {@link #setKode(String)}, {@link #getNama()},
 *       {@link #setNama(String)}, {@link #getKeterangan()}, {@link #setKeterangan(String)}.</li>
 *   <li><b>Penanda aktif</b> — {@link #getAktif()}, {@link #setAktif(Boolean)}.</li>
 *   <li><b>Jejak audit</b> — {@link #getOleh()}, {@link #setOleh(String)}, {@link #getOlehId()},
 *       {@link #setOlehId(String)}, {@link #getTanggal_dirubah()},
 *       {@link #setTanggal_dirubah(Date)}, serta kait {@code onUpdate()}.</li>
 * </ul>
 *
 * <p>Tidak ada satu pun method di kelas ini yang mengandung logika bisnis, query, atau efek samping
 * di luar yang dijelaskan di atas — seluruh perilaku "pintar" ada di
 * {@code PekerjaanOrtuSiswaAction}, {@code SiswaAction}, dan {@code InitDataHelper}.</p>
 *
 * @see ais.database.model.GeneralValueObject
 * @see ais.database.model.Pekerjaan
 * @see Siswa#getPekerjaanAyah()
 * @see Siswa#getPekerjaanIbu()
 * @see Siswa#getPekerjaanWali()
 * @see AlatTransportasiSiswa
 * @see JenisTinggalSiswa
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "sekolah", name = "pekerjaan_ortu_siswa")
public class PekerjaanOrtuSiswa extends GeneralValueObject {

	/**
	 * Versi serialisasi. Nilainya <b>identik</b> dengan {@link AlatTransportasiSiswa} dan
	 * {@code ais.database.model.AlatTransportasiMahasiswa} karena berkas ini satu keluarga
	 * salin-tempel; jangan dijadikan penanda identitas kelas.
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	/** Kunci primer, dipetakan lewat {@link #getId()}. Deklarasi ulang wajib karena induk tidak dipetakan. */
	private Long id;
	/** Nama pengguna terakhir yang mengubah baris; diisi interceptor audit lewat {@link #setOleh(String)}. */
	private String oleh;
	/** Id pengguna terakhir yang mengubah baris; diisi interceptor audit lewat {@link #setOlehId(String)}. */
	private String olehId;

	/**
	 * Mengembalikan id pengguna terakhir yang mengubah baris ini.
	 *
	 * <p>Tidak dianotasi {@code @Column} sehingga terpetakan implisit. Tanpa efek samping.</p>
	 *
	 * @return id pengguna, atau {@code null} bila belum pernah diisi
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menetapkan id pengguna terakhir yang mengubah baris ini. Dipanggil
	 * {@code ais.database.hibernate.AuditTimestampInterceptor} lewat kait {@link #onUpdate()},
	 * bukan oleh kode layar.
	 *
	 * <p><b>Non-obvious:</b> nilai {@code null} atau string kosong/spasi <b>diabaikan secara
	 * senyap</b> (nilai lama dipertahankan), sehingga jejak audit tidak dapat dikosongkan kembali
	 * setelah pernah terisi.</p>
	 *
	 * @param olehId id pengguna; diabaikan bila {@code null}/kosong
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Menetapkan nama pengguna terakhir yang mengubah baris ini. Sama seperti
	 * {@link #setOlehId(String)}, pemanggilnya adalah interceptor audit.
	 *
	 * <p><b>Non-obvious:</b> nilai {@code null} atau kosong/spasi diabaikan secara senyap.</p>
	 *
	 * @param oleh nama pengguna; diabaikan bila {@code null}/kosong
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan nama pengguna terakhir yang mengubah baris ini.
	 *
	 * <p>Tidak dianotasi {@code @Column} sehingga terpetakan implisit. Tanpa efek samping.</p>
	 *
	 * @return nama pengguna, atau {@code null} bila belum pernah diisi
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Kait {@code @PreUpdate} — dipanggil Hibernate/JPA <b>otomatis sebelum setiap UPDATE</b>
	 * terhadap baris ini, lalu meneruskan ke
	 * {@code ais.database.hibernate.AuditTimestampInterceptor.ubah(this)} yang mengisi
	 * {@code oleh}/{@code olehId}/{@code tanggal_dirubah} dari konteks pengguna aktif. Tidak pernah
	 * dipanggil manual dari kode aplikasi, dan <b>tidak berjalan pada INSERT</b> — tidak ada kait
	 * {@code @PrePersist} di kelas ini, sehingga baris hasil seed maupun hasil unggah Excel tidak
	 * pernah punya jejak {@code oleh}/{@code olehId}.
	 *
	 * <p><b>Perhatian pemeliharaan:</b> baris di bawah ini juga mendeklarasikan field
	 * {@code tanggal_dirubah} pada baris fisik yang sama (gaya asli berkas, dipertahankan). Nilai
	 * awalnya diambil dari {@code ais.ui.util.WaktuUtil.getDate()} sehingga object baru sudah
	 * bertanggal sejak dibuat, bukan {@code null}.</p>
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menetapkan waktu perubahan terakhir baris ini.
	 *
	 * <p>Umumnya diisi otomatis oleh {@code AuditTimestampInterceptor} lewat {@link #onUpdate()};
	 * pemanggilan manual hanya dilakukan pada jalur migrasi/impor data.</p>
	 *
	 * @param tanggal_dirubah waktu perubahan baru
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan waktu perubahan terakhir baris ini (kolom {@code tanggal_dirubah},
	 * {@link TemporalType#TIMESTAMP}).
	 *
	 * @return waktu perubahan terakhir; tidak pernah {@code null} pada object yang baru dibuat
	 *         karena field diinisialisasi {@code WaktuUtil.getDate()}
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Representasi teks baris ini dalam bentuk {@code "<id>-<nama>"}.
	 *
	 * <p><b>Non-obvious:</b> method ini membaca field {@code nama} <b>langsung</b>, bukan lewat
	 * {@link #getNama()}, sehingga hasilnya tidak di-{@code trim()} dan bisa berbeda dari nilai yang
	 * dipersistensikan. Bila {@code id} masih {@code null} (object belum disimpan) hasilnya
	 * berawalan {@code "null-"}. Format ini juga menimpa format standar {@code "kode - nama"} milik
	 * {@link GeneralValueObject#toString()}.</p>
	 *
	 * @return gabungan id dan nama
	 */
	public String toString() {
		return id + "-" + nama;
	}

	/**
	 * Kode ringkas jenis pekerjaan. <b>Selalu kosong pada baris hasil seed</b> (lihat Javadoc kelas:
	 * sumber salinannya tidak memetakan {@code kode}); hanya terisi bila operator mengetiknya
	 * manual atau mengunggahnya lewat Excel.
	 */
	private String kode;
	/** Nama jenis pekerjaan — satu-satunya kolom wajib dan label yang tampil di ketiga kombo formulir siswa. */
	private String nama;
	/** Keterangan bebas; tampil sebagai kolom ketiga grid layar master, tidak dipakai formulir siswa maupun cetakan. */
	private String keterangan;
	/** Penanda aktif; {@code null} diperlakukan sebagai aktif oleh {@link #getAktif()}. */
	private Boolean aktif;

	/**
	 * Konstruktor tanpa argumen. Wajib ada untuk Hibernate dan dipakai langsung oleh
	 * {@code PekerjaanOrtuSiswaAction.onAdd(...)}, jalur unggah Excel pada action yang sama, serta
	 * blok seed {@code InitDataHelper}. Tidak melakukan inisialisasi apa pun selain default field
	 * ({@code tanggal_dirubah} sudah terisi waktu saat ini dari inisialisasi field).
	 */
	public PekerjaanOrtuSiswa() {
	}

	/**
	 * Mengembalikan kunci primer baris ini.
	 *
	 * <p>Kolom {@code id} dianotasi {@code insertable = false} karena nilainya dibangkitkan database
	 * ({@link javax.persistence.GenerationType#IDENTITY}). Nilai {@code null} berarti object belum
	 * pernah disimpan — kondisi inilah yang dipakai layar master untuk membedakan judul "Tambah
	 * Pekerjaan Orang Tua" dan "Ubah Pekerjaan Orang Tua", untuk memilih antara {@code save} dan
	 * {@code load+update} di {@code onSave()}, serta untuk mengecualikan diri sendiri saat memeriksa
	 * duplikasi nama.</p>
	 *
	 * <p>Letak {@code @Id} pada getter inilah yang menentukan seluruh entity memakai
	 * <i>property access</i> — Hibernate membaca dan menulis setiap kolom lewat accessor, bukan
	 * lewat field.</p>
	 *
	 * @return id baris, atau {@code null} bila belum tersimpan
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menetapkan kunci primer baris ini. Umumnya hanya dipanggil Hibernate saat memuat/menyimpan;
	 * kode aplikasi tidak pernah menetapkannya manual.
	 *
	 * @param id kunci primer baru
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Mengembalikan kode ringkas jenis pekerjaan.
	 *
	 * <p>Tidak dianotasi {@code @Column} sehingga terpetakan implisit ke kolom {@code kode}. Isinya
	 * bebas dan boleh kosong — layar master tidak mewajibkan maupun memeriksa keunikannya, dan
	 * jalur seed awal selalu meninggalkannya {@code null} (lihat Javadoc kelas). Dipakai sebagai
	 * kolom pertama grid layar master, kriteria pencarian {@code ilike} pada layar itu, serta salah
	 * satu kolom cetak/ekspor.</p>
	 *
	 * @return kode jenis pekerjaan, atau {@code null}
	 */
	public String getKode() {
		return kode;
	}

	/**
	 * Menetapkan kode ringkas jenis pekerjaan. Tanpa validasi, tanpa normalisasi.
	 *
	 * <p>Dipanggil dari {@code PekerjaanOrtuSiswaAction.onSave()} (isi Textbox "Kode"), dari jalur
	 * unggah Excel (kolom ke-3 lembar kerja), dan dari blok seed {@code InitDataHelper} — yang pada
	 * kasus terakhir selalu meneruskan {@code null}.</p>
	 *
	 * @param kode kode baru; boleh {@code null}/kosong
	 */
	public void setKode(String kode) {
		this.kode = kode;
	}

	/**
	 * Mengembalikan nama jenis pekerjaan, sudah di-{@code trim()}.
	 *
	 * <p><b>Efek samping: tidak ada</b> — getter ini <b>tidak</b> menulis balik hasil {@code trim()}
	 * ke field, jadi bukan instance pola "getter write-back/destruktif". Namun karena entity memakai
	 * <i>property access</i>, nilai yang dipersistensikan Hibernate adalah hasil {@code trim()} ini,
	 * sedangkan {@link #toString()} membaca field mentah.</p>
	 *
	 * <p>Kolom wajib ({@code nullable = false}, panjang 255). Nilai inilah yang menjadi label
	 * pilihan pada ketiga kombo "Pekerjaan Ayah"/"Pekerjaan Ibu"/"Pekerjaan Wali" di formulir master
	 * siswa, kunci pemeriksaan duplikasi, kunci pengurutan daftar, sekaligus teks yang tampil di
	 * panel profil siswa.</p>
	 *
	 * @return nama jenis pekerjaan tanpa spasi tepi, atau {@code null} bila field kosong
	 */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Menetapkan nama jenis pekerjaan. Tanpa validasi maupun {@code trim()} — pemeriksaan "harus
	 * diisi" dan "sudah ada di database" dilakukan di {@code PekerjaanOrtuSiswaAction.onSave()} dan
	 * {@code checkNamaAgama()}, bukan di sini; jalur unggah Excel dan generator CRUD generik
	 * melewati kedua pemeriksaan itu.
	 *
	 * @param nama nama jenis pekerjaan baru
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Mengembalikan keterangan bebas baris ini.
	 *
	 * <p><b>Dipetakan dengan benar</b> ke kolom {@code keterangan} (verifikasi negatif untuk pola
	 * "{@code getKeterangan()} tidak dipetakan"; lihat Javadoc kelas). Nilai ini hanya ditampilkan
	 * sebagai kolom ketiga grid layar master dan diisi lewat Textbox "Keterangan" pada dialog
	 * tambah/ubah — tidak dipakai formulir siswa dan tidak ikut dalam cetak/ekspor.</p>
	 *
	 * <p><b>Non-obvious:</b> override ini boleh mengembalikan {@code null}, sedangkan
	 * {@code GeneralValueObject.getKeterangan()} berjanji tidak pernah {@code null}; akibatnya
	 * cabang {@code keterangan} pada {@code compareTo()} induk dapat terlewat untuk entity ini.</p>
	 *
	 * @return keterangan, atau {@code null}
	 */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * Menetapkan keterangan bebas baris ini. Tanpa validasi.
	 *
	 * <p>Validasi "Keterangan harus diisi" pernah ada di {@code onSave()} tetapi kini dikomentari,
	 * jadi kolom ini benar-benar opsional.</p>
	 *
	 * @param keterangan keterangan baru; boleh {@code null}/kosong
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * Mengembalikan penanda aktif baris ini, dengan {@code null} dianggap <b>aktif</b>.
	 *
	 * <p>Tidak dianotasi {@code @Column} sehingga terpetakan implisit ke kolom {@code aktif}. Karena
	 * entity memakai <i>property access</i>, Hibernate juga membaca nilai kolom lewat method ini.
	 * Pengamatan lengkap (dan kontradiksi antar-analisis yang masih terbuka) soal apa yang
	 * sesungguhnya tersimpan untuk baris yang tak pernah menyentuh {@link #setAktif(Boolean)}
	 * dicatat di Javadoc kelas — bagian "Pengamatan mentah soal kolom {@code aktif}".</p>
	 *
	 * <p>Dua pembacanya memakai filter berbeda: layar master toleran NULL
	 * ({@code isNull OR eq(true)}), ketiga kombo formulir siswa ketat ({@code eq(true)}).</p>
	 *
	 * @return {@code true} bila aktif atau belum pernah ditentukan; {@code false} hanya bila pernah
	 *         dinonaktifkan secara eksplisit. Tidak pernah {@code null}
	 */
	public Boolean getAktif() {
		return aktif == null ? true : aktif;
	}

	/**
	 * Menetapkan penanda aktif baris ini. Tanpa validasi.
	 *
	 * <p>Satu-satunya pemanggil di seluruh repo adalah listener {@code onCheck} checkbox "Aktif"
	 * pada grid layar master, yang langsung diikuti {@code Common.refreshSaveOrUpdate(...)} — jadi
	 * perubahan tersimpan seketika tanpa tombol Simpan. Checkbox tersebut dimatikan
	 * ({@code setDisabled(!edit)}) bila pemakai tidak memegang hak {@code UPDATE}. Menonaktifkan
	 * sebuah baris tidak memutus FK siswa yang sudah menunjuk ke sana — baris lama tetap tersimpan
	 * dan tetap tampil di panel profil, hanya tidak lagi bisa dipilih di formulir.</p>
	 *
	 * @param aktif {@code true}/{@code false}; {@code null} akan dibaca kembali sebagai aktif oleh
	 *              {@link #getAktif()}
	 */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}

}
