package ais.database.model.sekolah;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;




import org.hibernate.envers.Audited;



import ais.database.model.GeneralValueObject;

/**
 * Katalog master <b>skala (tingkat) kegiatan kesiswaan</b> &mdash; label yang dipakai untuk
 * menyatakan seberapa luas jangkauan sebuah kegiatan/prestasi siswa, dipetakan ke tabel
 * {@code sekolah.skala_kegiatan_kesiswaan}.
 *
 * <p><b>Catatan generator:</b> komentar bawaan di atas kelas ini semula berbunyi
 * <i>"Bank generated by hbm2java"</i>. Nama "Bank" adalah sisa salin-tempel dari kelas lain saat
 * berkas dibangkitkan Hibernate Tools (16 April 2010) dan <b>tidak</b> berhubungan dengan domain
 * perbankan; teks aslinya sengaja dicatat di sini agar jejak asal-usul berkas tidak hilang.</p>
 *
 * <h2>Makna "skala" yang TERVERIFIKASI dari kode</h2>
 * <p>Isi katalog tidak dibatasi enumerasi apa pun di kelas ini &mdash; kolom {@code nama} adalah
 * teks bebas yang diisi admin lewat layar master. Satu-satunya kumpulan nilai contoh yang ada di
 * repo adalah blok seed {@code InitDataHelper.initDataKegiatanKesiswan()}, dan isinya
 * <b>campur aduk</b>: selain skala sebenarnya ("Fak./Jur", "Institut", "Regional", "Nasional",
 * "Internasional") terdapat pula durasi kegiatan ("1 TAHUN (12 bulan)", "3 - 5 hari"), flag
 * tampilan ("DITAMPILKAN DI KAMPUS", "TIDAK DITAMPILKAN"), peran kepenulisan ("Penerjemah",
 * "Pengarang perorangan", "Editor buku") sampai jabatan kepanitiaan ("KETUA",
 * "PENGURUS/PANITIA INTI", "ANGGOTA PENGURUS/PANITIA LAINNYA"). Blok itu klon baris-per-baris dari
 * seed sisi perguruan tinggi ({@code SkalaKegiatanKemahasiswaan}) &mdash; termasuk istilah PT
 * "Fak./Jur" dan "Institut" yang tidak relevan untuk jenjang sekolah. Jadi <b>jangan</b> memakai
 * nama-nama itu sebagai definisi domain; label yang benar-benar dipakai sebuah instalasi
 * sepenuhnya ditentukan admin sekolah.</p>
 * <p><b>Penting:</b> {@code initDataKegiatanKesiswan()} tidak pernah dipanggil dari mana pun di
 * repo (satu-satunya kemunculan namanya adalah deklarasi method itu sendiri). Konsekuensinya tabel
 * ini <b>kosong pada instalasi baru</b> dan seluruh isinya harus diketik manual lewat layar master.
 * Yang terdaftar di {@code InitData.initClasses(...)} hanyalah pra-muat cache
 * ({@code InitDataHelper.initData(Class)}), bukan pengisian data.</p>
 *
 * <h2>Cakupan dan multi-tenant</h2>
 * <p>Entity ini <b>tidak</b> punya kolom {@code sekolah} maupun {@code yayasan}: katalognya
 * berlaku untuk SELURUH instalasi. Satu admin sekolah yang menyunting/menghapus baris di sini
 * mengubah pilihan yang dilihat semua sekolah dan yayasan lain pada instalasi yang sama.</p>
 *
 * <h2>Siapa memakai katalog ini</h2>
 * <ul>
 *   <li>{@link KegiatanKesiswaan#getSkalaKegiatanKesiswaan()} &mdash; FK {@code nullable}
 *   ({@code skala_kegiatan_kesiswaan}) pada baris kegiatan; diisi lewat kombo "Skala *" di
 *   {@code KegiatanKesiswaanAction}.</li>
 *   <li>{@link KegiatanKesiswaanPunyaSiswa#getSkalaKegiatanKesiswaan()} &mdash; FK per peserta.
 *   Getter di sana <b>menulis balik</b>: bila nilai baris masih {@code null} ia menyalin skala dari
 *   {@code kegiatanKesiswaan} induknya ke field, sehingga nilai warisan itu ikut tersimpan pada
 *   flush berikutnya (Hibernate memakai property access).</li>
 *   <li>{@link DetailKelompokKegiatanKesiswaan#getSkalaKegiatanKesiswaans()} &mdash; relasi
 *   {@code @ManyToMany} lewat tabel {@code detail_kelompok_has_skala_kegiatan_kesiswaan}; inilah
 *   yang membatasi skala mana saja yang boleh muncul di kombo layar Kegiatan Kesiswaan (kombo
 *   TIDAK menampilkan seluruh isi katalog, hanya yang tercentang pada detail kelompok terpilih).</li>
 *   <li>{@link NilaiKegiatanKesiswaan} &mdash; rubrik poin; salah satu sisi kunci gabungan
 *   {@code kodeUnik} berformat {@code detailKelompokId-skalaId-jabatanId}.</li>
 *   <li>{@code DashboardRekapKegiatanKesiswaanBerdasarSkala} &mdash; rekap spreadsheet yang
 *   mengelompokkan peserta kegiatan per skala.</li>
 * </ul>
 *
 * <h2>Layar pengelola dan hak akses</h2>
 * <p>Master ini dikelola {@code /pages/master/sekolah/skala_kegiatan_kesiswaan.zul} dengan controller
 * {@code ais.action.master.sekolah.SkalaKegiatanKesiswaanAction}. Halaman itu <b>tidak punya entri
 * menu sendiri</b> di {@code MenuSnapshotData}: satu-satunya jalan masuk adalah rantai tab tiga
 * tingkat &mdash; menu "Kegiatan Kesiswaan" &rarr; {@code kegiatan_kesiswaan.zul} &rarr; tab
 * {@code kelompok_kegiatan_kesiswaan.zul} &rarr; tab {@code skala_kegiatan_kesiswaan.zul}. Karena
 * {@code CommonPrivilages.checkPrevilages(...)} menghitung hak dari {@code Common.getCurrentMenu()}
 * (menu induk yang sedang aktif), hak CREATE/UPDATE/DELETE atas menu "Kegiatan Kesiswaan"
 * otomatis berlaku juga untuk katalog ini.</p>
 * <p>Sisi positifnya, {@code SkalaKegiatanKesiswaanAction} sendiri memanggil
 * {@code Common.doCheckSecurity()} dan menggerbangi tombol Tambah/Ubah/Hapus serta checkbox
 * "Aktif" dengan hak yang benar. <b>Kekecualian</b>: {@code Intbox} nomor urut pada grid daftar
 * dibuat tanpa {@code setDisabled(!edit)} dan {@code onChange}-nya langsung memanggil
 * {@code Common.refreshUpdate(...)} &mdash; pemegang hak BACA saja tetap dapat mengubah
 * {@code nomorUrut} baris katalog.</p>
 *
 * <h2>Pengelompokan anggota kelas</h2>
 * <ul>
 *   <li><b>Identitas &amp; audit:</b> {@link #getId()}/{@link #setId(Long)},
 *   {@link #getOleh()}/{@link #setOleh(String)}, {@link #getOlehId()}/{@link #setOlehId(String)},
 *   {@link #getTanggal_dirubah()}/{@link #setTanggal_dirubah(Date)}, {@code onUpdate()}.</li>
 *   <li><b>Isi katalog:</b> {@link #getNama()}/{@link #setNama(String)},
 *   {@link #getKeterangan()}/{@link #setKeterangan(String)}.</li>
 *   <li><b>Kendali tampil:</b> {@link #getAktif()}/{@link #setAktif(Boolean)},
 *   {@link #getNomorUrut()}/{@link #setNomorUrut(Integer)}.</li>
 *   <li><b>Utilitas:</b> {@link #toString()} dan konstruktor tanpa argumen.</li>
 * </ul>
 * <p>Tidak ada satu pun method dengan logika bisnis di kelas ini: seluruh aturan (validasi nama
 * unik, urutan, penyaringan aktif) hidup di Action/Helper pemanggil.</p>
 *
 * <h2>Hal non-obvious yang perlu diketahui</h2>
 * <ul>
 *   <li><b>Deklarasi ulang {@code id}/{@code oleh}/{@code olehId}/{@code tanggal_dirubah} bukan
 *   bug.</b> {@link GeneralValueObject} adalah POJO abstrak biasa &mdash; bukan {@code @Entity}
 *   maupun {@code @MappedSuperclass} &mdash; sehingga Hibernate tidak memetakan properti induknya
 *   sama sekali. Setiap entity turunan wajib mendeklarasikan ulang kolom yang ingin dipetakan.
 *   Berbeda dengan beberapa entity sejenis (mis. {@code StatusAwalSiswa}), di sini
 *   {@link #getKeterangan()} <b>ikut dideklarasikan ulang dan dipetakan</b> ({@code @Column}),
 *   jadi isian keterangan benar-benar tersimpan.</li>
 *   <li><b>{@link #getNama()} memangkas spasi saat dibaca, {@link #setNama(String)} tidak saat
 *   ditulis.</b> {@code SkalaKegiatanKesiswaanAction.onSave()} menyimpan nilai {@code Textbox} apa
 *   adanya, sedangkan {@code checkNamaSkalaKegiatanKesiswaan()} membandingkan dengan
 *   {@code Restrictions.eq("nama", <nilai ter-trim>)} langsung ke kolom. Baris yang terlanjur
 *   tersimpan dengan spasi di ujung karena itu lolos dari pemeriksaan duplikat, walau di layar
 *   terlihat identik.</li>
 *   <li><b>{@link #getNomorUrut()} tidak pernah mengembalikan {@code null}</b> (default {@code 1}),
 *   sementara formulir Tambah/Ubah di layar master <b>tidak menyediakan isian nomor urut sama
 *   sekali</b> &mdash; {@code onSave()} hanya menulis {@code nama} dan {@code keterangan}. Semua
 *   baris yang dibuat lewat formulir itu karena itu ber-{@code nomorUrut} {@code NULL} di database
 *   dan dianggap bernilai 1 oleh getter. Karena
 *   {@code GeneralValueObject.compareTo(...)} membandingkan {@code nomorUrut} lebih dulu dan
 *   mengembalikan {@code 0} bila sama, setiap {@code TreeSet<SkalaKegiatanKesiswaan>} akan
 *   <b>menciutkan</b> baris-baris itu menjadi satu elemen saja. Titik nyata yang terkena:
 *   {@code NilaiKegiatanKesiswaanAction.NilaiKegiatanKesiswaanRenderer.render()} membungkus
 *   {@code detailKelompok.getSkalaKegiatanKesiswaans()} ke dalam {@code TreeSet} sebelum merender
 *   kolom rubrik. Ironisnya {@code Intbox} nomor urut yang tanpa gerbang hak (lihat di atas) adalah
 *   satu-satunya cara memperbaiki kondisi ini dari UI.</li>
 *   <li><b>{@link #getAktif()} mengembalikan {@code true} bila field {@code null}.</b> Satu-satunya
 *   penulis kolom ini adalah checkbox "Aktif" pada grid daftar; {@code onSave()} tidak pernah
 *   menyentuhnya. <i>Observasi mentah, tanpa kesimpulan:</i> kedua pembaca yang menyaring
 *   ({@code DetailKelompokKegiatanKesiswaanHelper} dan {@code NilaiKegiatanKesiswaanAction})
 *   memakai bentuk toleran-NULL {@code Restrictions.or(isNull("aktif"), eq("aktif", true))},
 *   sehingga baris ber-{@code aktif} {@code NULL} tetap ikut terjaring di kedua tempat itu.</li>
 *   <li><b>{@code aktif} dan {@code nomorUrut} tidak memakai {@code @Column} eksplisit</b>, berbeda
 *   dari {@code id}/{@code nama}/{@code keterangan}. Nama kolom fisiknya mengikuti strategi
 *   penamaan default Hibernate pada instalasi.</li>
 *   <li><b>Skema tabel keluarga ini terpecah dua.</b> Entity ini, {@code JabatanKegiatanKesiswaan},
 *   {@code NilaiKegiatanKesiswaan} dan {@code KegiatanKesiswaan} berada di skema {@code sekolah},
 *   sedangkan {@code KelompokKegiatanKesiswaan} dan {@code DetailKelompokKegiatanKesiswaan} berada
 *   di skema {@code public}. Pemetaan {@code sekolah} pada kelas ini <b>konsisten</b> dengan SQL
 *   native rekap ({@code sekolah.kegiatan_kesiswaan_punya_siswa} dsb.), jadi bukan salah-salin
 *   skema; pembelahan itu ada di kelas saudaranya. Tabel gabungan
 *   {@code detail_kelompok_has_skala_kegiatan_kesiswaan} sendiri dideklarasikan tanpa skema.</li>
 *   <li><b>{@code @Audited}</b>: setiap perubahan direkam Envers ke tabel audit; tombol "Revisi"
 *   pada grid daftar ({@code RevisiHelper.createNewRevisi(SkalaKegiatanKesiswaan.class, ...)})
 *   membaca riwayat itu &mdash; dan di layar ini kelas audit yang dipakai sudah benar.</li>
 *   <li><b>{@code dynamicInsert}/{@code dynamicUpdate}</b> aktif: Hibernate hanya menyertakan kolom
 *   yang benar-benar berubah pada perintah INSERT/UPDATE.</li>
 * </ul>
 *
 * @see GeneralValueObject
 * @see KegiatanKesiswaan
 * @see KegiatanKesiswaanPunyaSiswa
 * @see DetailKelompokKegiatanKesiswaan
 * @see NilaiKegiatanKesiswaan
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "sekolah", name = "skala_kegiatan_kesiswaan")



public class SkalaKegiatanKesiswaan extends GeneralValueObject {

	/**
	 * Versi serialisasi Java. Nilainya dibangkitkan otomatis dan tidak boleh diubah tanpa alasan:
	 * object entity AIS ikut diserialkan ke cache in-memory/MapDB dan dibawa lintas request ZK.
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	/**
	 * Kunci primer baris katalog. Dideklarasikan ulang di sini karena {@link GeneralValueObject}
	 * bukan {@code @MappedSuperclass} sehingga properti induknya tidak dipetakan Hibernate.
	 */
	private Long id;
	/** Nama pengguna yang terakhir menyunting baris ini; diisi jalur audit, lihat {@link #setOleh(String)}. */
	private String oleh;
	/** Id pengguna yang terakhir menyunting baris ini; diisi jalur audit, lihat {@link #setOlehId(String)}. */
	private String olehId;

	/**
	 * Mengembalikan id pengguna yang terakhir mengubah baris katalog ini.
	 *
	 * @return id pengguna pengubah terakhir, atau {@code null} bila belum pernah terisi
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menyetel id pengguna pengubah terakhir, dengan <b>validasi non-trivial</b>: nilai
	 * {@code null} atau string kosong/hanya spasi <b>diabaikan diam-diam</b> (method langsung
	 * {@code return} tanpa mengubah apa pun). Akibatnya jejak audit yang sudah terisi tidak pernah
	 * bisa dikosongkan lewat setter ini &mdash; disengaja agar jalur simpan yang berjalan tanpa
	 * sesi login (batch/penjadwal) tidak menghapus informasi pengubah sebelumnya.
	 *
	 * @param olehId id pengguna pengubah; diabaikan bila {@code null} atau kosong/spasi
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Menyetel nama pengguna pengubah terakhir, dengan <b>validasi non-trivial</b> yang sama
	 * seperti {@link #setOlehId(String)}: nilai {@code null} atau kosong/spasi diabaikan diam-diam
	 * sehingga jejak audit lama tidak tertimpa nilai kosong.
	 *
	 * @param oleh nama pengguna pengubah; diabaikan bila {@code null} atau kosong/spasi
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan nama pengguna yang terakhir mengubah baris katalog ini.
	 *
	 * @return nama pengguna pengubah terakhir, atau {@code null} bila belum pernah terisi
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Callback JPA {@code @PreUpdate} yang menyegarkan stempel waktu audit tepat sebelum Hibernate
	 * menjalankan UPDATE untuk baris ini.
	 *
	 * <p>Implementasinya mendelegasikan ke {@code AuditTimestampInterceptor.ubah(this)}, yang
	 * mengisi {@code tanggal_dirubah} beserta {@code oleh}/{@code olehId} dari konteks pengguna
	 * yang sedang aktif. Method ini <b>tidak pernah dipanggil manual</b> &mdash; hanya oleh
	 * provider JPA &mdash; dan hanya untuk UPDATE, bukan INSERT (nilai awal
	 * {@code tanggal_dirubah} datang dari inisialisasi field di bawah).</p>
	 *
	 * <p><b>Perhatikan penataan baris:</b> deklarasi field {@code tanggal_dirubah} berada pada
	 * <b>baris fisik yang sama</b> dengan method ini (gaya asli berkas, dipertahankan apa adanya).
	 * Field itu diinisialisasi ke {@code ais.ui.util.WaktuUtil.getDate()} &mdash; jam server yang
	 * sudah dikoreksi konfigurasi zona waktu AIS, bukan {@code new Date()} langsung &mdash;
	 * sehingga baris baru selalu punya stempel waktu walau belum pernah di-UPDATE.</p>
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menyetel stempel waktu perubahan terakhir. Tanpa validasi.
	 *
	 * <p>Dipakai jalur audit ({@code AuditTimestampInterceptor}); pemanggil biasa tidak perlu
	 * menyentuhnya karena {@code onUpdate()} sudah mengurusnya otomatis.</p>
	 *
	 * @param tanggal_dirubah stempel waktu baru
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan stempel waktu perubahan terakhir baris katalog ini.
	 *
	 * @return waktu perubahan terakhir; tidak pernah {@code null} untuk object yang dibuat lewat
	 *         konstruktor karena field-nya diinisialisasi saat deklarasi
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Representasi teks baris katalog berformat {@code "<id>-<nama>"}.
	 *
	 * <p>Sengaja <b>tidak</b> memakai format {@code "kode - nama"} milik
	 * {@link GeneralValueObject#toString()} karena entity ini tidak punya kolom kode. Perhatikan
	 * bahwa {@code nama} dibaca langsung dari field, bukan lewat {@link #getNama()}, jadi hasilnya
	 * <b>tidak ter-trim</b> dan bisa berbeda dari yang tampil di layar bila nilai tersimpan
	 * memiliki spasi di ujung.</p>
	 *
	 * @return gabungan id dan nama; bagian id berbunyi {@code "null"} untuk object yang belum
	 *         disimpan
	 */
	public String toString() {
		return id + "-" + nama;
	}

	/** Label skala yang tampil di kombo, checkbox pemilihan, dan judul kolom rekap. */
	private String nama;
	/** Penjelasan bebas untuk admin; hanya ditampilkan di grid daftar layar master. */
	private String keterangan;
	/** Penanda baris masih boleh dipilih; lihat catatan pada {@link #getAktif()}. */
	private Boolean aktif;
	/** Nomor urut tampil; kunci pengurutan pertama {@code compareTo}, lihat {@link #getNomorUrut()}. */
	private Integer nomorUrut;

	/**
	 * Konstruktor tanpa argumen yang diwajibkan Hibernate/JPA sekaligus dipakai layar master saat
	 * menekan tombol "Tambah" ({@code SkalaKegiatanKesiswaanAction.onAdd(...)}). Seluruh field
	 * dibiarkan pada nilai awalnya; hanya {@code tanggal_dirubah} yang langsung terisi.
	 */
	public SkalaKegiatanKesiswaan() {
	}

	/**
	 * Mengembalikan kunci primer baris katalog.
	 *
	 * <p>Kolomnya dipetakan {@code insertable = false}: nilainya dibangkitkan database
	 * ({@code IDENTITY}) dan tidak pernah ikut dikirim pada INSERT. Nilai {@code null} berarti
	 * object belum pernah disimpan &mdash; pembeda yang dipakai layar master untuk memilih judul
	 * "Tambah" vs "Ubah" dan untuk menentukan bentuk pemeriksaan nama duplikat.</p>
	 *
	 * @return id baris, atau {@code null} bila belum tersimpan
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menyetel kunci primer. Tanpa validasi.
	 *
	 * <p>Umumnya hanya dipanggil Hibernate saat memuat/menyimpan baris; kode aplikasi tidak perlu
	 * memanggilnya.</p>
	 *
	 * @param id id baris
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Mengembalikan label skala, <b>sudah dipangkas spasi depan-belakang</b>.
	 *
	 * <p>Pemangkasan terjadi saat baca saja &mdash; {@link #setNama(String)} menyimpan nilai apa
	 * adanya. Lihat catatan "hal non-obvious" pada dokumentasi kelas mengenai efeknya terhadap
	 * pemeriksaan nama duplikat di {@code SkalaKegiatanKesiswaanAction}.</p>
	 *
	 * <p>Nilai ini juga dipakai sebagai <b>alias kolom SQL native</b> pada rekap
	 * {@code DashboardRekapKegiatanKesiswaan} (lihat peringatan keamanan di sana), sebagai label
	 * checkbox pemilihan di {@code DetailKelompokKegiatanKesiswaanHelper}, dan sebagai judul kolom
	 * lembar Excel unggah/unduh rubrik {@code NilaiKegiatanKesiswaanAction}.</p>
	 *
	 * @return label skala tanpa spasi di ujung, atau {@code null} bila belum diisi
	 */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Menyetel label skala. Tanpa validasi dan <b>tanpa pemangkasan spasi</b>.
	 *
	 * <p>Keunikan nama tidak ditegakkan oleh constraint database melainkan hanya oleh
	 * {@code SkalaKegiatanKesiswaanAction.checkNamaSkalaKegiatanKesiswaan()} sebelum menyimpan.</p>
	 *
	 * @param nama label skala baru
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Mengembalikan keterangan bebas baris katalog.
	 *
	 * <p>Berbeda dari beberapa entity katalog sejenis, properti ini benar-benar dipetakan
	 * ({@code @Column(name = "keterangan")}) sehingga isiannya bertahan di database. Nilainya
	 * dikembalikan apa adanya &mdash; termasuk {@code null} &mdash; jadi pemanggil perlu
	 * memeriksanya sendiri; grid daftar layar master melewatkannya langsung ke
	 * {@code org.zkoss.zul.Label}, yang menerima {@code null}.</p>
	 *
	 * @return keterangan, atau {@code null} bila belum diisi
	 */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * Menyetel keterangan bebas. Tanpa validasi.
	 *
	 * @param keterangan keterangan baru
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * Mengembalikan penanda aktif, dengan <b>normalisasi</b>: field yang masih {@code null}
	 * dilaporkan sebagai {@code true}.
	 *
	 * <p>Karena Hibernate memakai property access, nilai hasil normalisasi inilah yang dibaca
	 * mekanisme dirty-checking; nilai kolom di database sendiri tetap bisa {@code NULL} untuk baris
	 * yang belum pernah tersentuh checkbox "Aktif".</p>
	 *
	 * <p><i>Observasi mentah, tanpa kesimpulan:</i> satu-satunya penulis kolom ini di jalur UI
	 * adalah listener {@code onCheck} checkbox "Aktif" pada grid daftar; {@code onSave()} formulir
	 * Tambah/Ubah tidak pernah menyentuhnya. Dua pembaca yang menyaring
	 * ({@code DetailKelompokKegiatanKesiswaanHelper} dan {@code NilaiKegiatanKesiswaanAction})
	 * sama-sama memakai bentuk toleran-NULL
	 * {@code Restrictions.or(Restrictions.isNull("aktif"), Restrictions.eq("aktif", true))}.</p>
	 *
	 * @return {@code true} bila baris masih boleh dipilih; tidak pernah {@code null}
	 */
	public Boolean getAktif() {
		return aktif == null ? true : aktif;
	}

	/**
	 * Menyetel penanda aktif. Tanpa validasi; nilai {@code null} diterima apa adanya dan akan
	 * dibaca kembali sebagai {@code true} oleh {@link #getAktif()}.
	 *
	 * <p>Dipanggil dari listener {@code onCheck} checkbox "Aktif" di grid daftar, yang segera
	 * menyusulnya dengan {@code Common.refreshSaveOrUpdate(...)}. Checkbox tersebut dinonaktifkan
	 * bila pengguna tidak punya hak UPDATE.</p>
	 *
	 * @param aktif penanda aktif baru
	 */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}

	/**
	 * Mengembalikan nomor urut tampil, dengan <b>normalisasi</b>: field yang masih {@code null}
	 * dilaporkan sebagai {@code 1}.
	 *
	 * <p><b>Konsekuensi penting.</b> {@code GeneralValueObject.compareTo(...)} membandingkan
	 * {@code nomorUrut} sebagai kunci pertama dan berhenti di situ bila kedua nilai tidak
	 * {@code null}. Karena getter ini tidak pernah mengembalikan {@code null}, dua baris yang
	 * sama-sama ber-{@code nomorUrut} {@code NULL} selalu dibandingkan sebagai <b>sama</b>
	 * ({@code 0}). Formulir Tambah/Ubah layar master tidak menyediakan isian nomor urut sama
	 * sekali, sehingga hampir semua baris yang dibuat lewat UI berada dalam kondisi itu. Setiap
	 * {@code TreeSet<SkalaKegiatanKesiswaan>} karena itu hanya menyimpan satu di antaranya &mdash;
	 * lihat {@code NilaiKegiatanKesiswaanAction.NilaiKegiatanKesiswaanRenderer.render()} yang
	 * membungkus koleksi skala sebuah detail kelompok ke {@code TreeSet} sebelum merender kolom
	 * rubrik. Pemakaian berbasis {@code List} + {@code Collections.sort(...)} (mis. kombo di
	 * {@code KegiatanKesiswaanAction}) tidak kehilangan baris, hanya urutannya menjadi tidak
	 * deterministik.</p>
	 *
	 * @return nomor urut tampil; tidak pernah {@code null}
	 */
	public Integer getNomorUrut() {
		return nomorUrut == null ? 1 : nomorUrut;
	}

	/**
	 * Menyetel nomor urut tampil. Tanpa validasi (nilai negatif atau duplikat tidak ditolak).
	 *
	 * <p>Satu-satunya pemanggil di jalur UI adalah listener {@code onChange} pada {@code Intbox}
	 * nomor urut di grid daftar layar master, yang langsung menyusulnya dengan
	 * {@code Common.refreshUpdate(...)}. Berbeda dari checkbox "Aktif" di baris yang sama,
	 * {@code Intbox} itu <b>tidak</b> dinonaktifkan ketika pengguna tanpa hak UPDATE.</p>
	 *
	 * @param nomorUrut nomor urut baru; {@code null} berarti kembali ke perilaku default
	 *                  {@link #getNomorUrut()} (dianggap {@code 1})
	 */
	public void setNomorUrut(Integer nomorUrut) {
		this.nomorUrut = nomorUrut;
	}

}
