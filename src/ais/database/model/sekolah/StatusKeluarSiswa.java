package ais.database.model.sekolah;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

import ais.database.model.GeneralValueObject;

/**
 * <h3>Status Keluar Siswa</h3>
 *
 * <p>Katalog master (tabel {@code sekolah.status_keluar_siswa}) berisi <b>jenis/alasan seorang
 * siswa berhenti berstatus siswa aktif</b> — misalnya Lulus, Mutasi/Pindah, Mengundurkan Diri,
 * Dikeluarkan, atau Putus Sekolah. Entitas ini hanyalah tabel referensi: satu {@code kode},
 * satu {@code nama}, satu {@code keterangan}, dan saklar {@code aktif}. Tidak ada aturan bisnis,
 * query statis, koleksi turunan, maupun relasi keluar sama sekali.</p>
 *
 * <p><b>Isi barisnya tidak ditentukan di kode ini.</b> Verifikasi atas seluruh penulis di basis
 * kode menunjukkan tidak ada satu pun daftar nilai yang di-<i>hardcode</i>. Katalog dibentuk oleh
 * auto-seed {@code InitDataHelper} yang — hanya bila tabel masih kosong — <b>menyalin seluruh baris
 * {@code ais.database.model.StatusKeluar}</b> (katalog padanan milik modul perguruan tinggi):
 * {@code nama} disalin apa adanya dan {@code kode} diisi dari {@code StatusKeluar.getFeeder()}
 * (kode PDDikti Feeder) yang dikonversi ke {@link String}. Jadi daftar status keluar siswa pada
 * satu instalasi persis mengikuti daftar status keluar mahasiswa di instalasi yang sama, lalu bebas
 * disunting operator lewat layarnya.</p>
 *
 * <h4>Siapa memakai entitas ini (terverifikasi menyeluruh)</h4>
 * <p>Hanya enam berkas di seluruh basis kode menyebut tipe ini:</p>
 * <ul>
 *   <li>{@link Siswa#getStatusKeluar()} — relasi {@code @ManyToOne} lewat kolom
 *       {@code sekolah.siswa.status_keluar_siswa} ({@code nullable}). <b>Inilah pemakaian
 *       utamanya</b>: {@code NULL} berarti "siswa masih aktif". Satu-satunya penulisnya adalah
 *       formulir biodata siswa di {@code SiswaAction} (label combo-nya bahkan berbunyi
 *       "== Siswa ini Belum Keluar / Masih Aktif ==" untuk pilihan kosong).</li>
 *   <li>{@link KelompokStatusKeluarSiswa#getStatusKeluar()} — relasi {@code @ManyToOne}
 *       {@code NOT NULL} lewat kolom {@code status_keluar_siswa}. <b>Arah relasinya penting</b>:
 *       kelompok-lah yang menunjuk ke katalog ini, bukan sebaliknya. Entity ini <b>tidak memiliki
 *       koleksi balik</b> ke kelompok, sehingga dari sebuah baris status keluar tidak ada cara
 *       menelusuri kelompok mana saja yang memakainya kecuali lewat query eksplisit. Perlu
 *       ditegaskan pula bahwa {@link KelompokStatusKeluarSiswa} <b>bukan</b> wadah yang berisi
 *       baris-baris entity ini: kelompok adalah batch siswa yang semuanya keluar dengan satu status
 *       yang sama, dan — sebagaimana didokumentasikan pada berkas itu — status milik kelompok
 *       <i>tidak pernah</i> disalin ke {@code Siswa.statusKeluar}. Kedua kolom pada
 *       {@code sekolah.siswa} ({@code status_keluar_siswa} dan
 *       {@code kelompok_status_keluar_siswa}) hidup terpisah.</li>
 *   <li>{@code DasboardKelulusanSiswa} — memuat seluruh baris katalog ini sebagai isi combo
 *       "Semua Status Keluar" dan memakainya sebagai dimensi penyaring rekap kelulusan.</li>
 *   <li>{@code StatusKeluarSiswaAction} — layar CRUD-nya
 *       ({@code /pages/master/sekolah/status_keluar_siswa.zul}).</li>
 *   <li>{@code InitData}/{@code InitDataHelper} — pendaftaran kelas dan auto-seed di atas.</li>
 * </ul>
 *
 * <h4>Struktur anggota berkas ini</h4>
 * <ul>
 *   <li><b>Jejak audit ringan</b> — {@link #getOleh()}/{@link #getOlehId()} (identitas pengubah
 *       terakhir, dengan setter yang menolak nilai kosong) dan {@link #getTanggal_dirubah()} yang
 *       disegarkan callback {@link #onUpdate()}.</li>
 *   <li><b>Identitas</b> — {@link #getId()} ({@code IDENTITY}), dua konstruktor, dan
 *       {@link #toString()}.</li>
 *   <li><b>Atribut deskriptif</b> — {@link #getKode()}, {@link #getNama()} (wajib diisi &amp;
 *       divalidasi unik di layar), dan {@link #getKeterangan()}.</li>
 *   <li><b>Saklar tampil</b> — {@link #getAktif()}.</li>
 * </ul>
 *
 * <h4>Hal non-obvious</h4>
 * <ul>
 *   <li><b>Komentar generator palsu.</b> Javadoc kelas versi lama berbunyi
 *       "{@code Bank generated by hbm2java}" — sisa salin-tempel dari berkas lain, bukan keterangan
 *       yang benar untuk kelas ini (pola salah-salin yang sama sudah ditelusuri pada
 *       {@code JenisGuru} dan {@code JenisSekolah}). Komentar baris
 *       "{@code // Generated Apr 16, 2010 ... by Hibernate Tools}" di atas {@code import}
 *       dipertahankan apa adanya sebagai jejak sejarah.</li>
 *   <li><b>Pewarisan properti audit bukan bug.</b> {@code oleh}, {@code olehId},
 *       {@code tanggal_dirubah}, {@code id}, {@code nama}, {@code keterangan}, dan {@code aktif}
 *       sengaja <b>dideklarasikan ulang</b> di kelas ini walaupun namanya sama dengan milik
 *       {@link ais.database.model.GeneralValueObject}. Induk itu POJO abstrak biasa — bukan
 *       {@code @Entity} maupun {@code @MappedSuperclass} — sehingga Hibernate tidak memetakan
 *       properti induk sama sekali; deklarasi ulang adalah keharusan teknis agar kolomnya
 *       tersimpan.</li>
 *   <li><b>{@link #getKeterangan()} DI SINI dipetakan — berbeda dari tetangganya.</b> Pola
 *       "keterangan tidak dipetakan" yang ditemukan pada {@code StatusAwalSiswa} (di mana
 *       {@code keterangan} hanya ada di kelas induk sehingga isian layar hilang setiap request)
 *       <b>tidak berlaku pada berkas ini</b>: field {@code keterangan} dideklarasikan ulang dan
 *       getter-nya beranotasi {@code @Column(name = "keterangan", nullable = true)}. Isian
 *       Keterangan pada layar benar-benar tersimpan dan benar-benar tampil di grid. Yang tetap
 *       berlaku hanyalah <i>pembalikan kontrak</i>-nya: getter mengembalikan {@code ""} untuk
 *       {@code NULL} (lihat dokumentasi getter).</li>
 *   <li><b>{@code kode} dan {@code aktif} tanpa {@code @Column}.</b> Dua-duanya dipetakan implisit
 *       ke kolom bernama sama. Perhatikan {@code kode} sama sekali tidak dibatasi panjang maupun
 *       keunikannya di level pemetaan; keunikan yang divalidasi layar adalah {@code nama}, bukan
 *       {@code kode}.</li>
 *   <li><b>Diaudit Envers.</b> Kelas ini {@code @Audited} (berbeda dari
 *       {@link KelompokStatusKeluarSiswa} yang sengaja tidak diaudit), sehingga setiap perubahan
 *       katalog meninggalkan revisi pada tabel {@code _aud} pasangannya. Konsekuensinya: hindari
 *       memicu {@code UPDATE} yang tidak perlu, karena tiap flush menghasilkan baris revisi baru.
 *       {@code dynamicInsert}/{@code dynamicUpdate} aktif sehingga hanya kolom yang benar-benar
 *       berubah yang dikirim.</li>
 *   <li><b>Tidak ada penanda pemilik (tenant).</b> Tidak ada kolom {@code sekolah} maupun
 *       {@code yayasan}: katalog ini <b>global untuk seluruh instalasi</b>. Karena itu tidak ada
 *       pola "fail-open cakupan tenant" yang bisa dilanggar di sini — tetapi sebaliknya, menghapus
 *       atau mengganti nama satu baris berdampak ke seluruh sekolah/yayasan dalam satu basis data
 *       sekaligus, termasuk memutus FK {@code siswa.status_keluar_siswa} yang sudah menunjuk
 *       baris itu.</li>
 *   <li><b>Pola {@code getNomorUrut()} non-null TIDAK ada di sini.</b> Kelas ini tidak
 *       meng-override {@code getNomorUrut()}, jadi cabang pertama
 *       {@code GeneralValueObject.compareTo(...)} tidak pernah aktif dan bug penciutan
 *       {@code TreeSet} yang ditemukan pada batch sebelumnya tidak berlaku. Pengurutan jatuh ke
 *       {@code nama}, dan tidak ada satu pun koleksi ber-{@code TreeSet} yang menampung entity
 *       ini.</li>
 *   <li><b>Tidak ada getter write-back/destruktif.</b> Seluruh getter di berkas ini murni baca
 *       (paling jauh menormalkan nilai kembalian) dan tidak satu pun menulis balik ke field —
 *       berbeda dari {@code StatusAwalSiswa.getNama()}/{@code getPindahan()} atau
 *       {@code KelompokStatusKeluarSiswa.toString()}.</li>
 * </ul>
 *
 * <h4>Kuirk pada layar pengelolanya ({@code StatusKeluarSiswaAction})</h4>
 * <ul>
 *   <li><b>Penyaring {@code aktif} tidak konsisten antar layar.</b> Grid master memakai bentuk
 *       toleran-{@code NULL} ({@code isNull("aktif") OR eq("aktif", true)}) dan dasbor kelulusan
 *       tidak menyaring sama sekali, sedangkan dua combo di {@code SiswaAction} (filter pencarian
 *       dan formulir biodata) menyaring KETAT dengan {@code Restrictions.eq("aktif", true)} — dan
 *       {@code = true} tidak pernah cocok dengan {@code NULL}. Lihat {@link #getAktif()} untuk
 *       batas berlakunya pola "kolom aktif tak pernah ditulis" pada berkas ini.</li>
 *   <li><b>Validasi keunikan nama membandingkan nilai yang sudah di-{@code trim}, tetapi yang
 *       disimpan tidak.</b> {@code onSave()} memvalidasi
 *       {@code Restrictions.eq("nama", nama.getValue().trim())} lalu menyimpan
 *       {@code setNama(nama.getValue())} tanpa {@code trim}. Nama {@code " Lulus"} karenanya lolos
 *       validasi walau {@code "Lulus"} sudah ada, dan baris berspasi itu tersimpan apa adanya —
 *       hanya <i>tampak</i> rapi karena {@link #getNama()} men-{@code trim} saat dibaca.</li>
 *   <li><b>Kolom "feeder" hantu pada tombol Cetak/Upload.</b> Layar meneruskan daftar kolom
 *       {@code {"id", "kode", "nama", "keterangan", "aktif", "feeder"}} ke
 *       {@code Common.cetakData(...)} dan {@code Common.uploadData(...)}, padahal entity ini
 *       <b>tidak punya properti {@code feeder}</b> (yang punya adalah {@code StatusKeluar} milik
 *       modul PT, yang nilainya justru diseed ke {@code kode} di sini). Kolom itu ditelan
 *       {@code catch} di {@code CommonDownloadUpload} sehingga hanya menjadi kolom kosong/terlewat,
 *       bukan galat.</li>
 *   <li><b>Pewarisan hak lewat menu induk.</b> Berkas {@code status_keluar_siswa.zul} disisipkan
 *       sebagai tab ke-8 ("Status Keluar") pada layar Konfigurasi Siswa
 *       ({@code KonfigurasiTampilanSiswaAction}). Karena
 *       {@code CommonPrivilages.checkPrevilages(...)} menguji hak <i>menu yang sedang aktif</i>,
 *       pengguna yang berhak {@code CREATE}/{@code UPDATE}/{@code DELETE} atas menu Konfigurasi
 *       Siswa otomatis memperoleh CRUD penuh atas katalog global ini. Ini instance berulang dari
 *       pola "pewarisan hak lewat menu induk" yang sudah dikenal proyek. Di luar itu layar ini
 *       tergolong <b>contoh POSITIF</b>: tombol Tambah digerbangi {@code CREATE}, tombol
 *       Ubah/Hapus per baris digerbangi {@code UPDATE}/{@code DELETE}, checkbox "Aktif" di grid
 *       memakai {@code setDisabled(!edit)}, dan tombol Upload menuntut ketiga hak sekaligus.</li>
 * </ul>
 *
 * @see Siswa#getStatusKeluar()
 * @see KelompokStatusKeluarSiswa#getStatusKeluar()
 * @see ais.database.model.StatusKeluar
 * @see ais.database.model.GeneralValueObject
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "sekolah", name = "status_keluar_siswa")
public class StatusKeluarSiswa extends GeneralValueObject {

	/**
	 * Penanda versi serialisasi Java. Nilai ini dipakai ulang (ditambah satu) oleh
	 * {@link KelompokStatusKeluarSiswa} — jejak bahwa berkas itu digandakan dari berkas ini.
	 * Jangan diubah agar sesi ZK/HTTP yang sudah ter-serialisasi tetap dapat dipulihkan.
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	/** Kunci utama, diisi basis data melalui kolom {@code IDENTITY}. Lihat {@link #getId()}. */
	private Long id;
	/** Nama tampil pengguna yang terakhir mengubah baris ini. Lihat {@link #getOleh()}. */
	private String oleh;
	/** Pengenal (id) pengguna yang terakhir mengubah baris ini. Lihat {@link #getOlehId()}. */
	private String olehId;

	/**
	 * Mengembalikan pengenal (id) pengguna yang terakhir mengubah baris ini.
	 *
	 * @return id pengubah terakhir, atau {@code null} bila belum pernah diisi.
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menyetel pengenal (id) pengguna pengubah terakhir.
	 *
	 * <p><b>Perilaku non-obvious:</b> nilai {@code null} maupun string kosong/spasi
	 * <b>diabaikan diam-diam</b> (method langsung {@code return} tanpa mengubah apa pun), sehingga
	 * jejak audit yang sudah ada tidak pernah terhapus oleh pemanggil yang mengirim nilai kosong.
	 * Konsekuensinya: properti ini tidak dapat dikosongkan lewat setter.</p>
	 *
	 * <p>Pemanggil nyatanya hanya {@code AuditTimestampInterceptor} lewat {@link #onUpdate()}.</p>
	 *
	 * @param olehId id pengguna pengubah; diabaikan bila {@code null} atau kosong.
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Menyetel nama tampil pengguna pengubah terakhir.
	 *
	 * <p>Sama seperti {@link #setOlehId(String)}: nilai {@code null} atau kosong/spasi diabaikan
	 * diam-diam sehingga jejak audit sebelumnya tidak tertimpa nilai hampa.</p>
	 *
	 * @param oleh nama pengguna pengubah; diabaikan bila {@code null} atau kosong.
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan nama tampil pengguna yang terakhir mengubah baris ini.
	 *
	 * @return nama pengubah terakhir, atau {@code null} bila belum pernah diisi.
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Callback JPA {@code @PreUpdate}: dipanggil kontainer persistensi tepat sebelum perintah
	 * {@code UPDATE} dikirim ke basis data.
	 *
	 * <p>Mendelegasikan ke {@code AuditTimestampInterceptor.ubah(this)} yang menyegarkan
	 * {@link #getTanggal_dirubah()} serta {@link #getOleh()}/{@link #getOlehId()} dari konteks
	 * pengguna aktif. <b>Tidak dipanggil pada penyimpanan pertama</b> ({@code INSERT}) — pada kasus
	 * itu nilai {@code tanggal_dirubah} berasal dari inisialisasi field.</p>
	 *
	 * <p><b>Efek samping:</b> memutasi state entitas terkelola. Jangan dipanggil manual dari kode
	 * aplikasi.</p>
	 */
	@javax.persistence.PreUpdate
	protected void onUpdate() {
		ais.database.hibernate.AuditTimestampInterceptor.ubah(this);
	}

	/**
	 * Cap waktu perubahan terakhir. Diinisialisasi ke waktu server saat objek dibuat sehingga baris
	 * baru pun selalu memiliki nilai; sesudah itu disegarkan oleh {@link #onUpdate()}.
	 */
	private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menyetel cap waktu perubahan terakhir.
	 *
	 * @param tanggal_dirubah cap waktu yang dikehendaki. Umumnya diisi otomatis oleh
	 *                        {@link #onUpdate()}; pengisian manual hanya relevan bagi alur
	 *                        impor/migrasi yang ingin mempertahankan cap waktu asal.
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan cap waktu perubahan terakhir (presisi {@code TIMESTAMP}).
	 *
	 * @return cap waktu perubahan terakhir; tidak pernah {@code null} untuk objek yang dibuat lewat
	 *         konstruktor karena field-nya sudah diinisialisasi.
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Representasi teks singkat berbentuk {@code "<id>-<nama>"}, mis. {@code "3-Lulus"}.
	 *
	 * <p>Membaca field {@code nama} secara langsung (bukan lewat {@link #getNama()}), sehingga
	 * <b>tidak</b> ikut men-{@code trim} spasi dan dapat menghasilkan {@code "3-null"} untuk objek
	 * yang belum diisi. Method ini murni baca — tidak ada efek samping maupun inisialisasi proxy
	 * lazy, karena entity ini tidak punya relasi keluar sama sekali.</p>
	 *
	 * <p>Terpakai antara lain saat objek ini dipasang sebagai {@code value} sebuah
	 * {@code Comboitem} lalu dicetak ke log/pesan galat. Untuk tampilan layar gunakan
	 * {@link #getNama()}.</p>
	 *
	 * @return gabungan id dan nama status keluar.
	 */
	public String toString() {
		return id + "-" + nama;
	}

	/**
	 * Kode ringkas status keluar. Diisi auto-seed dari {@code StatusKeluar.getFeeder()} (kode
	 * PDDikti Feeder) atau diketik operator. Lihat {@link #getKode()}.
	 */
	private String kode;
	/** Nama status keluar yang tampil ke pengguna; wajib diisi &amp; unik. Lihat {@link #getNama()}. */
	private String nama;
	/** Catatan bebas tentang status keluar ini. Lihat {@link #getKeterangan()}. */
	private String keterangan;
	/** Saklar tampil/pakai baris ini. Lihat {@link #getAktif()}. */
	private Boolean aktif;

	/**
	 * Konstruktor tanpa argumen yang diwajibkan JPA/Hibernate untuk instansiasi entitas, dan dipakai
	 * layar {@code StatusKeluarSiswaAction} saat operator menekan tombol "Tambah".
	 *
	 * <p>Seluruh properti dibiarkan {@code null} kecuali {@link #getTanggal_dirubah()} yang sudah
	 * terisi lewat inisialisasi field. Perlu dicatat {@link #getKeterangan()} dan
	 * {@link #getAktif()} tetap mengembalikan nilai (bukan {@code null}) meski belum pernah
	 * disetel — lihat dokumentasi masing-masing getter.</p>
	 */
	public StatusKeluarSiswa() {
	}

	/**
	 * Konstruktor pintasan yang hanya menetapkan kunci utama.
	 *
	 * <p>Berguna untuk membentuk referensi ringan ke baris yang sudah ada tanpa memuatnya dari basis
	 * data. <b>Peringatan:</b> objek hasil konstruktor ini {@code nama}-nya {@code null}, sehingga
	 * bila dipakai langsung sebagai nilai combo/label akan tampil kosong. Saat ini tidak ada
	 * pemanggil di basis kode — konstruktor ini peninggalan template hbm2java.</p>
	 *
	 * @param id kunci utama baris yang direferensikan.
	 */
	public StatusKeluarSiswa(Long id) {
		this.id = id;
	}

	/**
	 * Mengembalikan kunci utama baris ini.
	 *
	 * <p>Dihasilkan basis data dengan strategi {@code IDENTITY} sehingga <b>berurutan dan mudah
	 * ditebak</b>; kolomnya {@code insertable = false} karena nilainya tidak pernah dikirim
	 * aplikasi. Nilai {@code null} menandakan objek baru yang belum pernah disimpan — layar
	 * pengelola memakai tepat pemeriksaan ini untuk memilih judul "Tambah" atau "Ubah" dan untuk
	 * memutuskan apakah validasi keunikan nama perlu mengecualikan baris yang sedang disunting.</p>
	 *
	 * @return kunci utama, atau {@code null} untuk objek yang belum tersimpan.
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menyetel kunci utama.
	 *
	 * <p>Dipakai Hibernate saat memuat/menyimpan baris. Jangan disetel manual dari kode aplikasi:
	 * mengubah id objek terkelola membuat Hibernate memperlakukannya sebagai baris lain.</p>
	 *
	 * @param id kunci utama baru.
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Mengembalikan kode ringkas status keluar.
	 *
	 * <p>Dipetakan implisit ke kolom {@code kode} (tanpa {@code @Column}), jadi tanpa batas panjang
	 * maupun {@code unique} di level pemetaan. Pada instalasi hasil auto-seed nilainya adalah kode
	 * PDDikti Feeder milik {@code StatusKeluar} padanannya, disimpan sebagai teks. Ditampilkan
	 * sebagai kolom pertama grid master dan dapat dicari (ilike, {@code ANYWHERE}) lewat kotak
	 * "Kode".</p>
	 *
	 * @return kode status keluar, atau {@code null} bila belum diisi (tidak dinormalkan).
	 */
	public String getKode() {
		return kode;
	}

	/**
	 * Menyetel kode ringkas status keluar.
	 *
	 * @param kode kode baru; boleh {@code null}. Tidak divalidasi dan tidak dipaksa unik.
	 */
	public void setKode(String kode) {
		this.kode = kode;
	}

	/**
	 * Mengembalikan nama status keluar yang tampil ke pengguna (mis. "Lulus", "Pindah").
	 *
	 * <p><b>Menormalkan saat baca:</b> hasilnya selalu di-{@code trim}. Karena
	 * {@link #setNama(String)} <i>tidak</i> men-{@code trim}, nilai yang tersimpan di basis data
	 * bisa saja berspasi di ujung sementara seluruh layar menampilkannya rapi — dan validasi
	 * keunikan di {@code StatusKeluarSiswaAction.onSave()} yang membandingkan versi ter-{@code
	 * trim} terhadap kolom mentah karenanya dapat meloloskan nama yang secara visual duplikat.</p>
	 *
	 * <p>Kolomnya {@code nullable = false} dengan panjang 255; layar mewajibkan isian ini. Nilai ini
	 * juga menjadi label combo di {@code SiswaAction} dan {@code DasboardKelulusanSiswa}, kunci
	 * pengurutan default grid, serta kunci urut ketiga
	 * {@code GeneralValueObject.compareTo(...)}.</p>
	 *
	 * @return nama status keluar yang sudah di-{@code trim}, atau {@code null} bila belum diisi.
	 */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Menyetel nama status keluar.
	 *
	 * <p>Tidak melakukan {@code trim} maupun validasi — keduanya dilakukan layar pengelola sebelum
	 * memanggil method ini. Menulis {@code null} akan menyebabkan kegagalan {@code NOT NULL} saat
	 * flush.</p>
	 *
	 * @param nama nama status keluar yang baru.
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Mengembalikan keterangan bebas mengenai status keluar ini.
	 *
	 * <p><b>Membalik kontrak {@code null}:</b> nilai {@code NULL} di basis data dikembalikan sebagai
	 * string kosong, sehingga method ini <b>tidak pernah</b> mengembalikan {@code null}. Dua akibat
	 * yang perlu diketahui:</p>
	 * <ul>
	 *   <li>Karena Hibernate memakai <i>property access</i> (anotasi berada di getter), nilai yang
	 *       dikirim ke kolom pada {@code INSERT}/{@code UPDATE} adalah hasil getter — yakni
	 *       {@code ""}, bukan {@code NULL}. Kolom {@code keterangan} praktis tidak pernah berisi
	 *       {@code NULL} untuk baris yang pernah disimpan aplikasi ini, sehingga query
	 *       {@code keterangan IS NULL} tidak dapat diandalkan untuk mencari baris tanpa
	 *       keterangan.</li>
	 *   <li>Cabang {@code keterangan} pada {@code GeneralValueObject.compareTo(...)} selalu
	 *       memenuhi syarat non-null, sehingga menjadi kunci urut terakhir yang benar-benar
	 *       terpakai bila {@code nomorUrut}/{@code nim}/{@code nama} tidak tersedia.</li>
	 * </ul>
	 *
	 * <p><b>Verifikasi penting:</b> berbeda dari {@code StatusAwalSiswa} — di mana
	 * {@code keterangan} hanya ada pada kelas induk yang tidak dipetakan sehingga isian layar hilang
	 * setiap request — di sini field dan getter dideklarasikan ulang dengan {@code @Column}, jadi
	 * <b>keterangan benar-benar tersimpan</b>. Kolom "Keterangan" pada grid master pun terisi.</p>
	 *
	 * @return keterangan, atau {@code ""} bila belum diisi; tidak pernah {@code null}.
	 */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan == null ? "" : this.keterangan;
	}

	/**
	 * Menyetel keterangan bebas.
	 *
	 * <p>Menerima {@code null} tanpa protes, tetapi nilai itu akan terbaca kembali sebagai
	 * {@code ""} lewat {@link #getKeterangan()} dan tersimpan sebagai {@code ""} pada flush
	 * berikutnya.</p>
	 *
	 * @param keterangan catatan tambahan; boleh {@code null}.
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * Mengembalikan penanda aktif, dengan {@code NULL} diperlakukan sebagai {@code true}.
	 *
	 * <p>Dipetakan implisit ke kolom {@code aktif}. Baris yang tidak aktif dianggap sudah tidak
	 * boleh dipilih lagi untuk siswa baru, tetapi <b>tidak</b> memutus data lama: siswa yang
	 * terlanjur memakainya tetap menunjuk baris tersebut.</p>
	 *
	 * <p><b>Penyaring pembacanya tidak seragam</b> — perlu diketahui sebelum menyentuh alur ini:</p>
	 * <ul>
	 *   <li>Grid master ({@code StatusKeluarSiswaAction.initCriteria}) memakai bentuk
	 *       toleran-{@code NULL}: {@code isNull("aktif") OR eq("aktif", true)}.</li>
	 *   <li>{@code DasboardKelulusanSiswa} tidak menyaring sama sekali — baris nonaktif pun tetap
	 *       muncul di combo filter dasbor.</li>
	 *   <li>Dua combo di {@code SiswaAction} (filter pencarian siswa dan formulir biodata) menyaring
	 *       KETAT dengan {@code Restrictions.eq("aktif", true)}.</li>
	 * </ul>
	 *
	 * <p><b>Batas berlakunya pola "kolom aktif tak pernah ditulis":</b> prasyarat pertamanya memang
	 * ada — {@code StatusKeluarSiswaAction.onSave()} hanya menulis {@code kode}, {@code nama}, dan
	 * {@code keterangan}, tidak pernah memanggil {@link #setAktif(Boolean)}, dan auto-seed
	 * {@code InitDataHelper} pun tidak. Namun karena pemetaan memakai <i>property access</i>,
	 * Hibernate membaca nilai lewat getter ini pada saat flush, sehingga baris baru tetap tersimpan
	 * dengan {@code aktif = true} dan <b>tetap terlihat</b> oleh kedua combo yang ketat. Risiko
	 * nyata yang tersisa hanyalah baris <i>warisan</i> yang bernilai {@code NULL} di kolomnya
	 * (mis. hasil penambahan kolom lewat migrasi skema, karena aplikasi berjalan dengan
	 * {@code hbm2ddl.auto=none}): baris seperti itu tampak tercentang di grid master namun tidak
	 * pernah muncul di formulir siswa sampai checkbox "Aktif" di-toggle dua kali. Kesimpulan
	 * verifikasi untuk berkas ini: <b>pola tersebut tidak berlaku untuk baris yang dibuat lewat
	 * aplikasi</b>, hanya untuk data warisan.</p>
	 *
	 * @return {@code true} bila baris dianggap aktif (termasuk saat kolomnya masih {@code NULL}),
	 *         {@code false} hanya bila pernah dinonaktifkan eksplisit; tidak pernah {@code null}.
	 */
	public Boolean getAktif() {
		return aktif == null ? true : aktif;
	}

	/**
	 * Menetapkan penanda aktif.
	 *
	 * <p>Satu-satunya pemanggil adalah listener {@code onCheck} checkbox "Aktif" pada grid master,
	 * yang langsung menyusulinya dengan {@code Common.refreshSaveOrUpdate(...)} sehingga perubahan
	 * tersimpan seketika tanpa menekan tombol Simpan. Checkbox itu digerbangi hak {@code UPDATE}
	 * ({@code setDisabled(!edit)}).</p>
	 *
	 * <p><b>Efek samping tak langsung:</b> karena entity ini {@code @Audited}, setiap pencentangan
	 * menghasilkan satu revisi Envers baru.</p>
	 *
	 * @param aktif {@code true}/{@code false}; {@code null} akan terbaca kembali sebagai
	 *              {@code true} lewat {@link #getAktif()}.
	 */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}

}
