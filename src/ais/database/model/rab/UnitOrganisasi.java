package ais.database.model.rab;

// Generated Dec 19, 2009 10:58:09 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;




import org.hibernate.envers.Audited;

import ais.database.model.GeneralValueObject;



/**
 * Satuan generated by hbm2java
 *
 * <p>
 * <b>Master unit organisasi</b> pada modul RAB (Rencana Anggaran Belanja / perencanaan anggaran).
 * Dalam nomenklatur penganggaran pemerintahan Indonesia, "unit organisasi" adalah tingkatan di
 * bawah kementerian/lembaga yang memegang bagian anggaran tersendiri (mis. direktorat jenderal,
 * badan, sekretariat jenderal). Di AIS, entity ini menjadi <b>dimensi pengelompokan</b> yang
 * dilekatkan pada sebuah {@code Workspace} perencanaan anggaran sehingga rencana dan realisasi
 * dapat dipilah per unit.
 * </p>
 *
 * <h2>Entity paling ramping di paket {@code rab}</h2>
 * <p>
 * Di luar identitas dan trio field audit bayangan, kelas ini hanya memiliki DUA kolom data:
 * {@link #getNama()} dan {@link #getKeterangan()}. Ia sengaja tidak membawa hal-hal yang biasa
 * menyertai master AIS lain, dan ketiadaan itu perlu disadari karena berdampak pada perilaku:
 * </p>
 * <ul>
 * <li><b>Tidak ada kolom {@code kode}</b> — berbeda dari {@link SatuanKerja}, {@link Kppn}, dan
 * {@code SatuanLokasi} yang semuanya berkode. Identitas fungsional unit sepenuhnya bertumpu pada
 * teks {@code nama}, sehingga pencocokan saat impor dan pelaporan bersifat berbasis nama.</li>
 * <li><b>Tidak ada flag aktif</b> ({@code defaultItem}/{@code aktif}) — unit organisasi tidak dapat
 * dinonaktifkan tanpa dihapus; satu-satunya cara menyembunyikannya dari pemilih adalah
 * menghapusnya, yang berisiko memutus referensi historis dari {@code Workspace} lama.</li>
 * <li><b>Tidak ada relasi apa pun</b> — tidak ada {@code parent} (jadi <b>flat</b>, bukan
 * berjenjang seperti {@link SatuanKerja}), tidak ada {@code satuanLokasi}, dan tidak ada FK
 * keluar sama sekali. Kelas ini bahkan tidak meng-{@code import} anotasi relasi JPA.</li>
 * <li><b>Tidak ada {@code toString()}</b> — satu-satunya entity di antara empat entity master
 * {@code rab} yang tidak menyediakannya, sehingga komponen ZK yang menampilkan objek ini secara
 * langsung akan memunculkan bentuk baku {@code Object.toString()} berupa nama kelas dan hash.
 * Layar dan pemilih yang menampilkannya harus memanggil {@link #getNama()} secara eksplisit —
 * lihat renderer di {@code ais.action.master.rab.UnitOrganisasiAction}.</li>
 * <li><b>Tidak ada {@code deep} maupun {@code jmlDipakai}</b> — tidak ada pengurutan berbasis
 * frekuensi pemakaian maupun indentasi pohon.</li>
 * </ul>
 *
 * <h2>Cakupan tenant — perhatikan</h2>
 * <p>
 * Entity ini <b>tidak memiliki sumbu tenant sama sekali</b>: tidak ada {@code yayasan},
 * {@code sekolah}, maupun {@code satuanKerja}. Akibatnya whitelist
 * {@code GenericCrudAutoEntityAdapter.scopeBindings()} — yang mencoba enam nama properti tetap
 * {@code yayasan}/{@code sekolah}/{@code program}/{@code fakultas}/{@code jurusan}/
 * {@code satuanKerja} dan melewatkan diam-diam yang tidak ada — <b>tidak akan memasang pembatas
 * apa pun</b> untuk kelas ini, sehingga seluruh unit organisasi dari semua tenant terlihat oleh
 * setiap pengguna yang dapat mengakses layarnya.
 * </p>
 * <p>
 * Berbeda dari {@link Kppn} yang berisi data publik kantor negara, daftar unit organisasi di sini
 * <b>bersifat internal institusi</b> (nama direktorat/bagian/unit kerja). Pada instalasi
 * multi-tenant yang benar-benar berbagi satu basis data, ini berarti nama-nama unit satu institusi
 * dapat terbaca institusi lain. Nilai kebocorannya rendah (hanya nama dan keterangan, tanpa angka
 * anggaran), tetapi keterbatasannya nyata dan merupakan varian dari pola "filter tenant lemah/hilang
 * pada master tanpa sumbu tenant" yang sudah tercatat berulang di inisiatif dokumentasi ini. Bila
 * suatu saat pemisahan diperlukan, solusinya adalah menambahkan field {@code satuanKerja} (yang
 * otomatis dikenali whitelist tersebut), bukan menambal di lapisan Action satu per satu.
 * </p>
 *
 * <h2>Siapa yang memakainya</h2>
 * <p>
 * Pemetaan tabelnya {@code rab.unit_organisasi} pada schema {@code rab}, dengan tabel audit Envers
 * berpasangan {@code new_audit.unit_organisasi__audit}. Di seluruh pohon sumber, tepat SATU entity
 * merujuk kelas ini: field {@code unitOrganisasi} pada {@code ais.database.model.rab.Workspace}
 * (wadah rencana anggaran). Dari titik itu, dimensi unit organisasi merambat ke:
 * </p>
 * <ul>
 * <li>Layar workspace dan revisinya: {@code WorkspaceRevisiAction},
 * {@code WorkspaceRevisiBulananAction}, {@code WorkspaceProjectRevisiAction}, dan
 * {@code WorkspaceTreeModel}.</li>
 * <li>Importer lembar kerja: {@code RabImporter} dan {@code RabBulananImporter}, serta
 * {@code RabUtil}.</li>
 * <li>Belasan laporan format1/rab: {@code LaporanPerencanaan},
 * {@code LaporanPerencanaanTiapBulan}, {@code LaporanPerencanaanTriWulan},
 * {@code LaporanRealisasi}, {@code LaporanRealisasiPerTanggal},
 * {@code LaporanRealisasiTiapBulan}, {@code LaporanRealisasiTriWulan},
 * {@code LaporanRencanaDanRealisasiTriWulan}, {@code LaporanJadwalRencanaAnggaran},
 * {@code LaporanJadwalRencanaAnggaranPerUnit}, dan {@code RabReportHelper} — di sinilah nilai
 * praktis entity ini terlihat, yaitu sebagai sumbu pengelompokan baris laporan anggaran.</li>
 * <li>{@code JadwalRealisasiUmumAction} untuk penjadwalan realisasi.</li>
 * </ul>
 * <p>
 * Pengelolaannya dilakukan lewat {@code ais.action.master.rab.UnitOrganisasiAction} (dengan
 * {@code UnitOrganisasiDao} dan {@code UnitOrganisasiRenderer} tersendiri), yang antara lain
 * memasang pemeriksaan {@code checkNamaUnitOrganisasi()}. Perlu dicatat bahwa penjagaan keunikan
 * nama itu berada di lapisan Action, BUKAN di model maupun basis data — kolom {@code nama} tidak
 * beranotasi {@code unique}, sehingga jalur lain (importer, CRUD generik, penyemaian) masih dapat
 * melahirkan unit organisasi bernama kembar yang akan mengacaukan pengelompokan laporan.
 * </p>
 *
 * <h2>Pola arsitektur khas AIS yang muncul di kelas ini</h2>
 * <ul>
 * <li><b>Field audit bayangan</b> ({@code oleh}, {@code olehId}, {@code tanggal_dirubah},
 * {@link #onUpdate()}) diduplikasi karena induk {@link GeneralValueObject} BUKAN {@code @Entity}
 * dan tidak dapat mewariskan pemetaan kolom. KEHARUSAN TEKNIS, bukan salin-tempel ceroboh.</li>
 * <li><b>Tanpa getter destruktif.</b> Seluruh getter di kelas ini murni membaca dan bebas efek
 * samping — konsekuensi wajar dari ketiadaan relasi sehingga tidak ada proxy lazy yang perlu
 * diresolusi {@code check(...)}.</li>
 * <li><b>{@code serialVersionUID} kembar</b> dengan entity {@code rab} lain karena berasal dari
 * template generator hbm2java yang sama.</li>
 * </ul>
 *
 * @see SatuanKerja sumbu tenant utama yang TIDAK dirujuk kelas ini
 * @see ais.action.master.rab.UnitOrganisasiAction layar master pengelola entity ini
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "rab", name = "unit_organisasi")



public class UnitOrganisasi extends GeneralValueObject {

	/**
	 * Nomor versi serialisasi Java. Nilai {@code -8738027816264807168L} SENGAJA identik dengan
	 * {@link SatuanKerja}, {@link Kppn}, {@link Pejabat}, dan {@code SatuanLokasi} karena semuanya
	 * lahir dari template hbm2java yang sama. Kesamaan antar kelas berbeda tidak bermasalah —
	 * {@code serialVersionUID} hanya dibandingkan antar versi dari kelas yang SAMA.
	 */
	private static final long serialVersionUID = -8738027816264807168L;
	/** Kunci primer numerik, {@code IDENTITY} (auto-increment basis data). Lihat {@link #getId()}. */
	private Long id;

	/**
	 * Nama tampil pengguna yang terakhir mengubah baris ini (field audit bayangan). Diisi oleh
	 * {@code AuditTimestampInterceptor} melalui {@link #setOleh(String)}.
	 */
	private String oleh;
	/**
	 * Identitas/username pengguna yang terakhir mengubah baris ini (field audit bayangan). Diisi
	 * oleh {@code AuditTimestampInterceptor} melalui {@link #setOlehId(String)}.
	 */
	private String olehId;

	/**
	 * Mengembalikan identitas (username/ID) pengguna terakhir yang menyunting baris ini.
	 *
	 * @return identitas penyunting terakhir, atau {@code null} bila belum pernah diisi
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menetapkan identitas pengguna penyunting terakhir, dengan <b>penjagaan anti-penghapusan</b>:
	 * argumen {@code null} atau string kosong/berisi spasi saja diabaikan diam-diam sehingga nilai
	 * lama dipertahankan. Pola seragam di seluruh entity AIS agar jejak audit tidak hilang hanya
	 * karena sebuah proses batch atau importer menyerahkan objek tanpa konteks pengguna.
	 *
	 * @param olehId identitas penyunting; diabaikan bila {@code null} atau hanya berisi spasi
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Menetapkan nama tampil pengguna penyunting terakhir, dengan penjagaan anti-penghapusan yang
	 * sama seperti {@link #setOlehId(String)}.
	 *
	 * @param oleh nama penyunting; diabaikan bila {@code null} atau hanya berisi spasi
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan nama tampil pengguna terakhir yang menyunting baris ini.
	 *
	 * @return nama penyunting terakhir, atau {@code null} bila belum pernah diisi
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Kait daur hidup JPA {@code @PreUpdate} yang mendelegasikan pengisian jejak audit ke
	 * {@code ais.database.hibernate.AuditTimestampInterceptor.ubah(this)} tepat sebelum Hibernate
	 * menerbitkan pernyataan {@code UPDATE}.
	 *
	 * <p>
	 * Interceptor membaca konteks pengguna aktif dari sesi ZK/HTTP lalu mengisi
	 * {@link #setOleh(String)}, {@link #setOlehId(String)}, dan
	 * {@link #setTanggal_dirubah(java.util.Date)}. Karena kait ini hanya menempel pada
	 * {@code @PreUpdate} — bukan {@code @PrePersist} — jejak audit pada operasi INSERT bergantung
	 * pada pemanggil yang mengisi field secara eksplisit; asimetri ini berlaku seragam di seluruh
	 * entity AIS.
	 * </p>
	 * <p>
	 * Mekanisme ini TERPISAH dari Hibernate Envers ({@link Audited}). Envers menulis salinan penuh
	 * setiap revisi ke {@code new_audit.unit_organisasi__audit}, sedangkan trio {@code oleh}/
	 * {@code olehId}/{@code tanggal_dirubah} hanya menyimpan potret "siapa terakhir" pada baris
	 * hidup agar layar daftar dapat menampilkannya tanpa menyentuh tabel audit.
	 * </p>
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}
	/**
	 * Cap waktu perubahan terakhir (field audit bayangan). Diinisialisasi ke waktu server saat objek
	 * dibuat melalui {@code ais.ui.util.WaktuUtil.getDate()} agar baris baru tidak pernah membawa
	 * {@code null}.
	 */
	private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menetapkan cap waktu perubahan terakhir. Berbeda dari {@link #setOleh(String)}, setter ini
	 * TIDAK menjaga nilai kosong — {@code null} akan benar-benar tersimpan.
	 *
	 * @param tanggal_dirubah cap waktu perubahan terakhir
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan cap waktu perubahan terakhir, dipetakan sebagai {@code TIMESTAMP}.
	 *
	 * @return cap waktu perubahan terakhir; praktis tidak pernah {@code null} untuk objek yang dibuat
	 *         lewat konstruktor kelas ini
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/** Nama unit organisasi — satu-satunya identitas fungsional. Lihat {@link #getNama()}. */
	private String nama;
	/** Keterangan bebas. Lihat {@link #getKeterangan()}. */
	private String keterangan;

	/**
	 * Konstruktor tanpa argumen yang WAJIB ada agar Hibernate/JPA dapat meng-instansiasi entity lewat
	 * refleksi saat memuat baris dari basis data, dan agar komponen ZK dapat membuat objek kosong
	 * untuk form tambah-baru pada {@code UnitOrganisasiAction}.
	 *
	 * <p>
	 * Objek hasil konstruktor ini sudah membawa {@code tanggal_dirubah} berisi waktu server saat
	 * instansiasi; {@code nama} dan {@code keterangan} masih {@code null}.
	 * </p>
	 */
	public UnitOrganisasi() {
	}

	/**
	 * Konstruktor pintasan yang mengisi nama unit organisasi. Karena kelas ini tidak memiliki kolom
	 * {@code kode} maupun relasi apa pun, konstruktor ini sudah cukup untuk membentuk objek yang
	 * lengkap secara fungsional — cukup ditambah {@code keterangan} bila diperlukan.
	 *
	 * <p>
	 * Dipakai terutama oleh importer lembar kerja RAB ({@code RabImporter},
	 * {@code RabBulananImporter}) yang membentuk unit organisasi dari nama pada kolom lembar kerja.
	 * Karena keunikan nama hanya dijaga di {@code UnitOrganisasiAction.checkNamaUnitOrganisasi()}
	 * dan bukan oleh basis data, pemanggil dari jalur impor bertanggung jawab memeriksa sendiri
	 * apakah unit bernama sama sudah ada sebelum menyimpan — bila tidak, akan lahir duplikat yang
	 * memecah pengelompokan pada laporan {@code LaporanPerencanaan} dan kerabatnya.
	 * </p>
	 *
	 * <p>
	 * <b>Hati-hati:</b> kelas ini TIDAK memiliki konstruktor yang menerima {@code Long id}, sehingga
	 * referensi ringan berbasis kunci primer harus dibentuk manual
	 * ({@code UnitOrganisasi u = new UnitOrganisasi(); u.setId(id);}).
	 * </p>
	 *
	 * @param nama nama unit organisasi
	 */
	public UnitOrganisasi(String nama) {
		this.nama = nama;
	}

	/**
	 * Mengembalikan kunci primer unit organisasi.
	 *
	 * <p>
	 * Dipetakan {@code IDENTITY} (auto-increment basis data), sehingga nilainya baru terisi SETELAH
	 * baris tertulis. Nilai {@code null} adalah penanda baku bahwa objek belum tersimpan.
	 * </p>
	 *
	 * @return kunci primer, atau {@code null} bila entity belum pernah disimpan
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menetapkan kunci primer. Umumnya hanya dipanggil Hibernate, atau kode yang membentuk referensi
	 * ringan secara manual karena kelas ini tidak menyediakan konstruktor berbasis id.
	 *
	 * @param id kunci primer
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Mengembalikan nama unit organisasi, dipangkas spasinya.
	 *
	 * <p>
	 * Karena kelas ini tidak memiliki kolom {@code kode} maupun {@code toString()}, nilai inilah
	 * <b>satu-satunya identitas yang dilihat pengguna</b> — di pemilih pada layar workspace, di
	 * renderer {@code UnitOrganisasiAction.UnitOrganisasiRenderer}, dan sebagai judul kelompok pada
	 * belasan laporan RAB. Pemangkasan spasi di sini penting agar nama yang tertulis dengan spasi
	 * berlebih dari lembar kerja impor tetap tampil rapi dan tetap cocok saat dibandingkan.
	 * </p>
	 * <p>
	 * Kolom dipetakan {@code nullable = false} sepanjang 255 karakter, namun penjagaan itu ada di
	 * lapisan basis data — method ini tetap dapat mengembalikan {@code null} untuk objek yang belum
	 * disimpan. Perhatikan bahwa <b>keunikan nama tidak dijamin</b>: kolom tidak beranotasi
	 * {@code unique} dan pemeriksaannya hanya ada di {@code UnitOrganisasiAction}, sehingga jalur
	 * impor, CRUD generik, dan penyemaian masih dapat melahirkan nama kembar.
	 * </p>
	 *
	 * @return nama unit organisasi yang sudah dipangkas spasinya, atau {@code null} bila belum diisi
	 */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Menetapkan nama unit organisasi. Nilai diterima apa adanya; pemangkasan spasi terjadi saat
	 * dibaca lewat {@link #getNama()}.
	 *
	 * @param nama nama unit organisasi
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Mengembalikan keterangan bebas unit organisasi apa adanya (tanpa pemangkasan spasi maupun
	 * normalisasi {@code null}).
	 *
	 * @return keterangan, atau {@code null} bila tidak diisi
	 */
	public String getKeterangan() {
		return keterangan;
	}

	/**
	 * Menetapkan keterangan bebas unit organisasi.
	 *
	 * @param keterangan teks keterangan
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

}
