package ais.database.model.rab;

// Generated Dec 19, 2009 10:58:09 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;




import org.hibernate.envers.Audited;

import ais.database.model.GeneralValueObject;



///**
// * Satuan generated by hbm2java
// */
/**
 * Katalog GLOBAL "jenis parameter" pada modul RAB — dipetakan ke tabel {@code
 * rab.jenis_parameter}, satu tabel untuk seluruh konteks yang membutuhkan atribut dinamis (tidak
 * di-scope per Proyek/Workspace). Setiap entri mendefinisikan nama atribut ({@link #nama}) dan
 * tipe datanya ({@link #typedata}, berisi nama kelas Java lengkap seperti {@code
 * java.lang.String}) yang menentukan kolom nilai mana pada baris "punya jenis parameter" yang
 * relevan dipakai pemanggil.
 *
 * <p><b>Dua jalur pemakaian berbeda.</b> Katalog ini dirujuk dari dua arah yang tidak simetris:</p>
 * <ol>
 *   <li>{@link TugasPunyaJenisParameter} merujuk entri di sini secara <b>langsung</b> — satu
 *   {@link Tugas} memasang nilai langsung ke definisi katalog global.</li>
 *   <li>{@code WorkspacePunyaJenisParameter} juga merujuk entri di sini secara langsung, untuk
 *   menginstansiasi satu entri katalog pada satu {@code Workspace} tertentu; {@code
 *   AcaraPunyaJenisParameter} kemudian merujuk INSTANSI tersebut (bukan {@code JenisParameter}
 *   ini secara langsung) untuk melaporkan nilai per {@link Acara}.</li>
 * </ol>
 * <p>Dengan kata lain, katalog ini benar-benar dipakai bersama (satu tabel, satu definisi tipe)
 * oleh domain Tugas dan domain Workspace/Acara, tetapi jarak relasinya ke entity pelapor nilai
 * berbeda: satu langkah untuk Tugas dan Workspace, dua langkah untuk Acara.</p>
 *
 * @see TugasPunyaJenisParameter
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "rab", name = "jenis_parameter")



public class JenisParameter extends GeneralValueObject {

	/**
	 * Versi serialisasi tetap untuk kompatibilitas antar-build; nilainya disalin dari template
	 * entity lain di modul ini dan tidak mencerminkan riwayat perubahan struktur kelas.
	 */
	private static final long serialVersionUID = -8738027816264807168L;
	/** Primary key baris {@code rab.jenis_parameter}, dibangkitkan otomatis ({@code IDENTITY}). */
	private Long id;

	/** @see #getOleh() */
	private String oleh;private String olehId;public String getOlehId() {return olehId;}public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}this.olehId = olehId;}

	/**
	 * Menyetel nama pelaku perubahan. Nilai {@code null} atau blank diabaikan secara senyap.
	 *
	 * @param oleh nama pelaku perubahan; diabaikan bila {@code null} atau blank
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan nama pelaku perubahan terakhir.
	 *
	 * @return nama pelaku, atau {@code null} bila belum diset
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Hook {@code @PreUpdate} Hibernate: memperbarui {@link #tanggal_dirubah} otomatis sebelum
	 * baris ini di-{@code UPDATE}. Jangan dipanggil manual dari kode aplikasi.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menyetel waktu perubahan terakhir. Biasanya hanya dipanggil oleh {@link #onUpdate()}.
	 *
	 * @param tanggal_dirubah waktu perubahan terakhir
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan waktu perubahan terakhir baris ini.
	 *
	 * @return waktu perubahan terakhir; default konstruksi object adalah waktu saat ini
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/** Nama atribut dinamis yang didefinisikan entri ini; wajib, maksimum 255 karakter. */
	private String nama;
	/**
	 * Nama kelas Java lengkap yang menentukan tipe data atribut ini (mis. {@code
	 * java.lang.String}, {@code java.lang.Integer}) — dipakai pemanggil untuk memutuskan kolom
	 * nilai mana ({@code jenisParameterValue}, {@code ...ValueInteger}, {@code ...ValueDouble},
	 * {@code ...ValueDate}, {@code ...ValueTime}) yang relevan pada baris "punya jenis parameter"
	 * terkait; default {@code String.class.getName()}.
	 */
	private String typedata = String.class.getName();
	/** Deskripsi/keterangan bebas untuk entri katalog ini; opsional. */
	private String keterangan;

	/** Konstruktor default (wajib untuk entity Hibernate); seluruh field memakai nilai default. */
	public JenisParameter() {
	}

	/**
	 * Membangun instance baru dengan nama langsung terisi; field lain memakai nilai default kelas.
	 *
	 * @param nama nama atribut dinamis
	 */
	public JenisParameter(String nama) {
		this.nama = nama;
	}

	/**
	 * Mengembalikan primary key baris ini.
	 *
	 * @return {@link #id}, atau {@code null} bila baris belum tersimpan
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menyetel primary key secara manual — normalnya hanya dipakai lapisan persistence.
	 *
	 * @param id primary key baru
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Mengembalikan nama atribut dinamis, di-{@code trim()} dari whitespace di kedua sisi.
	 *
	 * @return nama yang sudah di-trim, atau {@code null} bila {@link #nama} belum diset
	 */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Menyetel nama atribut dinamis. Tidak melakukan trim di sisi setter — pembersihan whitespace
	 * baru terjadi saat dibaca lewat {@link #getNama()}.
	 *
	 * @param nama nama baru
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Mengembalikan deskripsi/keterangan bebas entri katalog ini.
	 *
	 * @return {@link #keterangan}, atau {@code null} bila belum diisi
	 */
	public String getKeterangan() {
		return keterangan;
	}

	/**
	 * Menyetel deskripsi/keterangan bebas entri katalog ini.
	 *
	 * @param keterangan teks keterangan baru
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * Mengembalikan nama kelas Java lengkap yang menentukan tipe data atribut ini.
	 *
	 * @return {@link #typedata}; default {@code String.class.getName()}
	 */
	public String getTypedata() {
		return typedata;
	}

	/**
	 * Menyetel nama kelas Java lengkap yang menentukan tipe data atribut ini.
	 *
	 * @param typedata nama kelas baru (mis. {@code java.lang.Integer})
	 */
	public void setTypedata(String typedata) {
		this.typedata = typedata;
	}

}
