package ais.database.model;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

/**
 * Entity <b>MEMORY INFO</b> — satu <i>snapshot</i> periodik penggunaan memori JVM server aplikasi,
 * diambil dari {@link Runtime#maxMemory()}/{@link Runtime#totalMemory()}/{@link Runtime#freeMemory()}.
 * Dipetakan ke tabel {@code public.memory_info}.
 *
 * <p>Header berkas ini masih menyisakan komentar asli <i>"Bank generated by hbm2java"</i> dari
 * generator Hibernate Tools — sisa <i>copy-paste</i> template, tidak ada hubungannya dengan entity
 * {@code Bank}.</p>
 *
 * <h2>Peran dalam sistem</h2>
 * <p>Baris baru dibuat oleh {@code ais.action.master.helper.UserOnlineCounter} secara berkala: nilai
 * {@link Runtime#maxMemory()}, {@link Runtime#totalMemory()} (dicatat sebagai
 * {@link #getAllocatedMemory() allocatedMemory}), dan {@link Runtime#freeMemory()} diambil, lalu
 * {@link #getTotalFreeMemory() totalFreeMemory} dihitung sebagai
 * {@code freeMemory + (maxMemory - allocatedMemory)} — estimasi memori yang MASIH BISA dipakai JVM
 * termasuk ruang heap yang belum dialokasikan. Data ini juga dipakai
 * {@code UserOnlineCounter} sebagai ambang untuk memicu <i>restart</i> paksa server saat tekanan
 * memori tinggi. Dibaca kembali lewat layar {@code ais.action.master.MemoryInfoAction} sebagai grafik
 * pemantauan kesehatan server.</p>
 *
 * <h2>HASIL VERIFIKASI KEAMANAN (diminta karena entity ini berpotensi menyimpan info sistem sensitif)</h2>
 * <p>Seluruh field data ({@link #getMaxMemory()}, {@link #getAllocatedMemory()},
 * {@link #getFreeMemory()}, {@link #getTotalFreeMemory()}) bertipe numerik ({@link Long}, byte
 * memori JVM) — TIDAK ADA path berkas/direktori server, nama host, alamat IP, versi perangkat lunak,
 * atau kredensial apa pun yang tersimpan di entity ini. {@link #getNama() nama} hanyalah gabungan
 * teks dari keempat angka tersebut (lihat kuirk pada method itu). Risiko yang ada murni bersifat
 * <i>information disclosure</i> RINGAN: pengungkapan kapasitas/tekanan memori server ke pengguna
 * yang bisa membuka layar {@code MemoryInfoAction} — layar ini tidak memasang gerbang hak akses
 * tambahan di kelas action-nya sendiri, mengandalkan gerbang standar tingkat menu (registrasi menu +
 * {@code RolePrivilage}) seperti layar admin lain di aplikasi ini, bukan celah baru yang unik pada
 * entity ini.</p>
 *
 * @see ais.action.master.helper.UserOnlineCounter
 */
@Entity
@org.hibernate.annotations.Entity(dynamicInsert = true, dynamicUpdate = true)
@Audited
@Table(schema = "public", name = "memory_info")
public class MemoryInfo extends GeneralValueObject {

	/**
	 * Versi serialisasi Java untuk kompatibilitas antar build kelas ini.
	 */
	private static final long serialVersionUID = 2463821577548439808L;

	/** Kunci utama, di-<i>generate</i> database ({@code IDENTITY}). */
	private Long id;

	/** Nama pengguna terakhir yang mengubah baris ini; diisi otomatis lewat {@link #onUpdate()}. */
	private String oleh;

	/** Id pengguna terakhir yang mengubah baris ini; diisi otomatis lewat {@link #onUpdate()}. */
	private String olehId;

	/**
	 * Mengembalikan id pengguna pengubah terakhir.
	 *
	 * @return id pengguna pengubah terakhir, atau {@code null} bila belum pernah di-<i>update</i>
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menetapkan id pengguna pengubah terakhir. Nilai {@code null} atau kosong (hanya spasi)
	 * diabaikan diam-diam agar jejak audit tidak bisa dikosongkan lewat pemanggilan ini.
	 *
	 * @param olehId id pengguna pengubah; diabaikan bila {@code null} atau kosong
	 */
	public void setOlehId(String olehId) {
		if (olehId == null || olehId.trim().isEmpty()) {
			return;
		}
		this.olehId = olehId;
	}

	/**
	 * Menetapkan nama pengguna pengubah terakhir. Sama seperti {@link #setOlehId(String)}, nilai
	 * {@code null} atau kosong diabaikan.
	 *
	 * @param oleh nama pengguna pengubah; diabaikan bila {@code null} atau kosong
	 */
	public void setOleh(String oleh) {
		if (oleh == null || oleh.trim().isEmpty()) {
			return;
		}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan nama pengguna terakhir yang mengubah baris ini.
	 *
	 * @return nama pengguna pengubah terakhir, atau {@code null} bila belum pernah di-<i>update</i>
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * <i>Callback</i> JPA {@code @PreUpdate}: mendelegasikan ke
	 * {@code ais.database.hibernate.AuditTimestampInterceptor.ubah(this)} untuk mengisi
	 * {@link #setTanggal_dirubah(Date)}, {@link #setOleh(String)}, {@link #setOlehId(String)} dari
	 * konteks pengguna aktif sebelum baris ini di-{@code UPDATE}.
	 */
	@javax.persistence.PreUpdate
	protected void onUpdate() {
		ais.database.hibernate.AuditTimestampInterceptor.ubah(this);
	}

	/** Waktu perubahan terakhir; nilai awalnya waktu objek dibuat. */
	private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menetapkan waktu perubahan terakhir baris ini.
	 *
	 * @param tanggal_dirubah waktu perubahan
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan waktu perubahan terakhir baris ini.
	 *
	 * @return waktu perubahan terakhir
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Representasi teks baris: {@code id-maxMemory-allocatedMemory-freeMemory-totalFreeMemory}.
	 *
	 * @return teks ringkas seluruh angka memori pada snapshot ini
	 */
	public String toString() {
		return id + "-" + maxMemory + "-" + allocatedMemory + "-" + freeMemory + "-" + totalFreeMemory;
	}

	/** Field kerja nama tampilan; selalu ditimpa ulang oleh {@link #getNama()}, lihat kuirk pada method itu. */
	private String nama;

	/** Batas maksimum memori heap JVM ({@link Runtime#maxMemory()}), dalam byte. */
	private Long maxMemory;

	/** Memori heap yang sudah dialokasikan JVM ({@link Runtime#totalMemory()}), dalam byte. */
	private Long allocatedMemory;

	/** Memori bebas di dalam heap yang sudah dialokasikan ({@link Runtime#freeMemory()}), dalam byte. */
	private Long freeMemory;

	/** Estimasi total memori yang masih bisa dipakai JVM: {@code freeMemory + (maxMemory - allocatedMemory)}. */
	private Long totalFreeMemory;

	/**
	 * Konstruktor kosong yang diwajibkan Hibernate/JPA.
	 */
	public MemoryInfo() {
	}

	/**
	 * Mengembalikan kunci utama baris. Kolom {@code id} bersifat {@code insertable = false} karena
	 * nilainya dihasilkan database ({@code IDENTITY}).
	 *
	 * @return id baris, atau {@code null} bila baris belum disimpan
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menetapkan kunci utama. Normalnya hanya dipanggil Hibernate setelah {@code INSERT}.
	 *
	 * @param id kunci utama baris
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Mengembalikan nama tampilan snapshot ini.
	 *
	 * <p><b>Kuirk getter-menulis-balik:</b> nilai kembalian SELALU dihitung ulang dari
	 * {@link #toString()} dan ditulis balik ke field {@code nama}, MENGABAIKAN nilai apa pun yang
	 * pernah ditetapkan lewat {@link #setNama(String)}. Kolom ini bukan kolom database ber-anotasi
	 * ({@code nama} tidak punya {@code @Column} khusus di sini, dipetakan lewat properti default),
	 * sehingga isinya di database selalu berupa gabungan keempat angka memori, tidak pernah nilai
	 * bebas yang di-set manual.</p>
	 *
	 * @return teks gabungan {@link #toString()} baris ini
	 */
	public String getNama() {
		nama = toString();
		return nama;
	}

	/**
	 * Menetapkan field kerja {@code nama}. <b>Tidak berpengaruh</b> pada nilai yang dikembalikan
	 * {@link #getNama()}, karena getter tersebut selalu menghitung ulang dari {@link #toString()}.
	 *
	 * @param nama nilai yang akan ditimpa pada pemanggilan {@link #getNama()} berikutnya
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Mengembalikan memori heap yang sudah dialokasikan JVM saat snapshot diambil.
	 *
	 * @return {@link Runtime#totalMemory()} dalam byte, bisa {@code null} bila belum diisi
	 */
	public Long getAllocatedMemory() {
		return allocatedMemory;
	}

	/**
	 * Menetapkan memori heap yang sudah dialokasikan JVM.
	 *
	 * @param allocatedMemory nilai baru dalam byte
	 */
	public void setAllocatedMemory(Long allocatedMemory) {
		this.allocatedMemory = allocatedMemory;
	}

	/**
	 * Mengembalikan batas maksimum memori heap JVM.
	 *
	 * @return {@link Runtime#maxMemory()} dalam byte, bisa {@code null} bila belum diisi
	 */
	public Long getMaxMemory() {
		return maxMemory;
	}

	/**
	 * Menetapkan batas maksimum memori heap JVM.
	 *
	 * @param maxMemory nilai baru dalam byte
	 */
	public void setMaxMemory(Long maxMemory) {
		this.maxMemory = maxMemory;
	}

	/**
	 * Mengembalikan memori bebas di dalam heap yang sudah dialokasikan.
	 *
	 * @return {@link Runtime#freeMemory()} dalam byte, bisa {@code null} bila belum diisi
	 */
	public Long getFreeMemory() {
		return freeMemory;
	}

	/**
	 * Menetapkan memori bebas di dalam heap yang sudah dialokasikan.
	 *
	 * @param freeMemory nilai baru dalam byte
	 */
	public void setFreeMemory(Long freeMemory) {
		this.freeMemory = freeMemory;
	}

	/**
	 * Mengembalikan estimasi total memori yang masih bisa dipakai JVM (termasuk ruang heap yang
	 * belum dialokasikan).
	 *
	 * @return {@code freeMemory + (maxMemory - allocatedMemory)} dalam byte, bisa {@code null} bila
	 *         belum diisi
	 */
	public Long getTotalFreeMemory() {
		return totalFreeMemory;
	}

	/**
	 * Menetapkan estimasi total memori yang masih bisa dipakai JVM.
	 *
	 * @param totalFreeMemory nilai baru dalam byte
	 */
	public void setTotalFreeMemory(Long totalFreeMemory) {
		this.totalFreeMemory = totalFreeMemory;
	}

}
