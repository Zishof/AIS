package ais.database.model.koperasi;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

import ais.database.model.GeneralValueObject;

/**
 * Bank generated by hbm2java
 *
 * <p><b>Katalog TIPE anggota koperasi</b> — dipetakan ke {@code koperasi.tipe_anggota_koperasi}.
 * Satu baris merepresentasikan satu SUB-KATEGORI keanggotaan yang membawa HAK/PRIVILESE
 * transaksi keras: plafon utang ({@link #getMaksimalBolehUtang()}), limit transaksi
 * harian/mingguan/bulanan, pembatasan cara pembayaran (termasuk default &amp; larangan cara
 * pembayaran lain), kewajiban kontak (HP/email), dan cakupan toko
 * ({@link #getBerlakuSemuaToko()}/{@link #getDaftarToko()}). Dirujuk
 * {@link AnggotaKoperasi#getTipeAnggotaKoperasi()}; sub-kategorinya cenderung mengikuti PERAN
 * institusional (Mahasiswa/Dosen/Siswa/Guru/Pegawai/Umum — lihat konstanta
 * {@code ConstantValues.MAHASISWA} dkk. yang dipakai default getter tsb) alih-alih tingkatan
 * loyalitas bebas.</p>
 *
 * <p><b>Beda dengan {@link JenisAnggotaKoperasi} (jangan disamakan meski nama mirip).</b> Kedua
 * kelas ini adalah DUA DIMENSI klasifikasi anggota yang independen dan SAMA-SAMA dirujuk
 * langsung oleh {@code AnggotaKoperasi} (satu anggota punya SATU jenis DAN SATU tipe sekaligus,
 * bukan hierarki tipe→jenis). {@link JenisAnggotaKoperasi} berfokus pada pengalaman
 * dompet/loyalitas (istilah &amp; visibilitas saldo/cashback, saldo minimum, aturan frekuensi
 * belanja) dan TIDAK punya field limit transaksi nominal atau plafon utang — lihat Javadoc
 * kelas tsb untuk perbandingan lengkap. Kedua kelas JUGA sama-sama punya field
 * {@code wajibPin}/{@code daftarCaraPembayaranWajibPin}/{@code wajibVerifikasiBiometricWajah}/
 * {@code wajibVerifikasiBiometricFingerprint} — duplikasi struktur yang disengaja (dua sumber
 * aturan independen); pemanggil yang mengecek kewajiban PIN/biometrik SEHARUSNYA memeriksa
 * KEDUANYA (jenis DAN tipe anggota) dan menggabungkan hasilnya secara OR.</p>
 *
 * @see JenisAnggotaKoperasi
 * @see AnggotaKoperasi
 */
@Entity
@org.hibernate.annotations.Entity(dynamicInsert = true, dynamicUpdate = true)
@Audited
@Table(schema = "koperasi", name = "tipe_anggota_koperasi")
public class TipeAnggotaKoperasi extends GeneralValueObject {

	/**
	 *
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	/** Primary key baris tipe anggota ini, di-generate database (IDENTITY). */
	private Long id;
	/** Nama pengguna yang terakhir membuat/mengubah baris ini (audit shadow field — lihat {@link #onUpdate()}). */
	private String oleh;
	/** Id pengguna yang terakhir membuat/mengubah baris ini (audit shadow field, pasangan {@link #oleh}). */
	private String olehId;

	/** @return {@link #olehId} — id pengguna audit terakhir yang mengubah baris ini. */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Meng-set {@link #olehId}; dilewati (no-op) bila {@code olehId} {@code null} atau string
	 * kosong/berisi spasi saja, sehingga nilai audit lama tidak pernah tertimpa nilai kosong.
	 *
	 * @param olehId id pengguna yang melakukan perubahan
	 */
	public void setOlehId(String olehId) {
		if (olehId == null || olehId.trim().isEmpty()) {
			return;
		}
		this.olehId = olehId;
	}

	/**
	 * Meng-set {@link #oleh}; dilewati (no-op) bila {@code oleh} {@code null} atau string
	 * kosong/berisi spasi saja, sehingga nilai audit lama tidak pernah tertimpa nilai kosong.
	 *
	 * @param oleh nama pengguna yang melakukan perubahan
	 */
	public void setOleh(String oleh) {
		if (oleh == null || oleh.trim().isEmpty()) {
			return;
		}
		this.oleh = oleh;
	}

	/** @return {@link #oleh} — nama pengguna audit terakhir yang mengubah baris ini. */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Callback JPA {@code @PreUpdate}: dipanggil otomatis oleh Hibernate tepat sebelum baris ini
	 * di-UPDATE, mendelegasikan pembaruan timestamp audit ({@link #tanggal_dirubah}) ke
	 * {@link ais.database.hibernate.AuditTimestampInterceptor#ubah(Object)}. Tidak dipanggil manual
	 * dari kode aplikasi.
	 */
	@javax.persistence.PreUpdate
	protected void onUpdate() {
		ais.database.hibernate.AuditTimestampInterceptor.ubah(this);
	}

	/** Timestamp terakhir baris ini diubah (audit); nilai awal saat konstruksi objek adalah waktu sekarang, diperbarui otomatis lewat {@link #onUpdate()} saat UPDATE. */
	private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * @param tanggal_dirubah nilai timestamp audit baru; dipanggil manual maupun otomatis oleh
	 *                         {@link #onUpdate()}
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/** @return {@link #tanggal_dirubah} — timestamp terakhir baris ini diubah. */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/** @return representasi teks baris ini, yaitu {@link #nama} tipe anggota (dipakai kombo/pencarian di layar ZK). */
	public String toString() {
		return nama;
	}

	/** Kode singkat tipe anggota ini. */
	private String kode;
	/** Nama tipe anggota ini (mis. "Mahasiswa", "Dosen", "Pegawai"); juga dipakai {@link #toString()}. */
	private String nama;
	/** Keterangan/catatan bebas mengenai tipe anggota ini. */
	private String keterangan;
	/** Penanda aktif/nonaktif; {@code null} diperlakukan {@code true} (aktif) — lihat {@link #getAktif()}. */
	private Boolean aktif;
	/** Plafon utang; lihat penjelasan lengkap pada {@link #getMaksimalBolehUtang()}. */
	private Double maksimalBolehUtang;
	/** Daftar ID cara pembayaran terizinkan; lihat penjelasan lengkap pada {@link #getDaftarCaraPembayaranYangBolehDiPilih()}. */
	private String daftarCaraPembayaranYangBolehDiPilih;
	/** ID cara pembayaran default anggota tipe ini; lihat {@link #getCaraPembayaranDefaultId()}. */
	private Long caraPembayaranDefaultId;
	/** Penanda pembatasan keras "tidak boleh cara pembayaran lain" di luar {@link #daftarCaraPembayaranYangBolehDiPilih}; {@code null} diperlakukan {@code false} — lihat {@link #getTidakBolehCaraPembayaranLain()}. */
	private Boolean tidakBolehCaraPembayaranLain;
	/** Batas nominal transaksi harian; lihat {@link #getMaksimalTransaksiHarian()}. */
	private Double maksimalTransaksiHarian;
	/** Batas nominal transaksi mingguan; {@code null} diperlakukan {@code 0.0} (tanpa batas) — lihat {@link #getMaksimalTransaksiMingguan()}. */
	private Double maksimalTransaksiMingguan;
	/** Batas nominal transaksi bulanan; {@code null} diperlakukan {@code 0.0} (tanpa batas) — lihat {@link #getMaksimalTransaksiBulanan()}. */
	private Double maksimalTransaksiBulanan;
	/** Penanda wajib entry PIN pembeli sebelum transaksi POS anggota tipe ini tersimpan; {@code null} diperlakukan {@code false} — lihat {@link #getWajibPin()}. */
	private Boolean wajibPin;
	/** Daftar ID cara pembayaran yang secara khusus mewajibkan PIN ketika {@link #wajibPin} aktif; lihat {@link #getDaftarCaraPembayaranWajibPin()}. */
	private String daftarCaraPembayaranWajibPin;
	/** Penanda wajib verifikasi biometrik wajah sebelum pembayaran saldo POS anggota tipe ini; {@code null} diperlakukan {@code false} — independen dari {@link #wajibVerifikasiBiometricFingerprint}. */
	private Boolean wajibVerifikasiBiometricWajah;
	/** Penanda wajib verifikasi biometrik sidik jari sebelum pembayaran saldo POS anggota tipe ini; {@code null} diperlakukan {@code false} — independen dari {@link #wajibVerifikasiBiometricWajah}. */
	private Boolean wajibVerifikasiBiometricFingerprint;
	/** Penanda wajib mengisi nomor HP saat data member disimpan; {@code null} jatuh ke default berbasis nama tipe — lihat {@link #getWajibHp()}/{@link #defaultWajibHp(String)}. */
	private Boolean wajibHp;
	/** Penanda wajib mengisi email saat data member disimpan; {@code null} diperlakukan {@code false} — lihat {@link #getWajibEmail()}. */
	private Boolean wajibEmail;
	/** Cakupan aturan transaksi tipe ini ke seluruh toko atau terbatas; {@code null} diperlakukan {@code true} — lihat {@link #getBerlakuSemuaToko()}. */
	private Boolean berlakuSemuaToko;
	/** Daftar ID toko tempat aturan tipe ini berlaku bila {@link #berlakuSemuaToko} {@code false}; lihat {@link #getDaftarToko()}. */
	private String daftarToko;

	/** Konstruktor default (dibutuhkan Hibernate); tidak menginisialisasi field apa pun secara eksplisit selain default deklarasi field. */
	public TipeAnggotaKoperasi() {
	}

	/** @return {@link #id} — primary key baris tipe anggota ini. */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/** @param id primary key baru; normalnya di-generate database, jarang di-set manual dari kode aplikasi. */
	public void setId(Long id) {
		this.id = id;
	}

	/** @return {@link #kode} — kode singkat tipe anggota ini, boleh {@code null}. */
	public String getKode() {
		return kode;
	}

	/** @param kode kode singkat tipe anggota baru. */
	public void setKode(String kode) {
		this.kode = kode;
	}

	/** @return {@link #nama} — nama tipe anggota ini, di-trim; {@code null} bila belum di-set. */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/** @param nama nama tipe anggota baru. */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/** @return {@link #keterangan} — catatan bebas mengenai tipe anggota ini, boleh {@code null}. */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/** @param keterangan catatan bebas baru. */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/** @return {@link #aktif}, atau {@code true} bila belum pernah di-set (default aktif). */
	public Boolean getAktif() {
		return aktif == null ? true : aktif;
	}

	/** @param aktif penanda aktif/nonaktif baru untuk tipe anggota ini. */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}

	/**
	 * Batas maksimal hutang (piutang toko) yang boleh ditumpuk seorang member dgn tipe ini, bila
	 * ybs bertransaksi memakai cara pembayaran yg ditandai {@link CaraPembayaranKoperasi#getMasukSebagaiHutang()}.
	 * {@code 0} (default) berarti tipe ini TIDAK diizinkan berhutang sama sekali.
	 */
	@Column(name = "maksimal_boleh_utang")
	public Double getMaksimalBolehUtang() {
		return maksimalBolehUtang == null ? 0.0 : maksimalBolehUtang;
	}

	/** @param maksimalBolehUtang plafon utang baru untuk tipe anggota ini — lihat penjelasan lengkap pada {@link #getMaksimalBolehUtang()}. */
	public void setMaksimalBolehUtang(Double maksimalBolehUtang) {
		this.maksimalBolehUtang = maksimalBolehUtang;
	}

	/**
	 * Daftar ID cara pembayaran yang boleh dipakai tipe member ini. Bentuk
	 * tersimpan dinormalisasi menjadi ",1,2," agar aman dicocokkan dengan LIKE
	 * tanpa salah mencocokkan ID 1 dengan 11. Nilai kosong berarti tipe tidak
	 * menambah pembatasan (aturan Jenis Member tetap berlaku).
	 */
	@Column(name = "daftar_cara_pembayaran_yang_boleh_di_pilih")
	public String getDaftarCaraPembayaranYangBolehDiPilih() {
		if (daftarCaraPembayaranYangBolehDiPilih == null
				|| daftarCaraPembayaranYangBolehDiPilih.replace(",", "").trim().isEmpty()) {
			return "";
		}
		String nilai = daftarCaraPembayaranYangBolehDiPilih.trim();
		if (!nilai.startsWith(",")) nilai = "," + nilai;
		if (!nilai.endsWith(",")) nilai = nilai + ",";
		return nilai;
	}

	/** @param nilai daftar ID cara pembayaran baru (bentuk mentah, dinormalisasi saat dibaca lewat {@link #getDaftarCaraPembayaranYangBolehDiPilih()}). */
	public void setDaftarCaraPembayaranYangBolehDiPilih(String nilai) {
		this.daftarCaraPembayaranYangBolehDiPilih = nilai;
	}

	/** @return {@link #caraPembayaranDefaultId} — ID cara pembayaran yang dipilih otomatis/default untuk anggota tipe ini, boleh {@code null}. */
	@Column(name = "cara_pembayaran_default_id")
	public Long getCaraPembayaranDefaultId() {
		return caraPembayaranDefaultId;
	}

	/** @param caraPembayaranDefaultId ID cara pembayaran default baru. */
	public void setCaraPembayaranDefaultId(Long caraPembayaranDefaultId) {
		this.caraPembayaranDefaultId = caraPembayaranDefaultId;
	}

	/**
	 * @return {@link #tidakBolehCaraPembayaranLain}, atau {@code false} bila belum pernah di-set.
	 *         Bila {@code true}, anggota tipe ini HANYA boleh memakai cara pembayaran yang
	 *         terdaftar di {@link #getDaftarCaraPembayaranYangBolehDiPilih()} — pembatasan lebih
	 *         keras dari sekadar daftar kosong (daftar kosong berarti tanpa pembatasan sama
	 *         sekali, lihat Javadoc getter tsb).
	 */
	@Column(name = "tidak_boleh_cara_pembayaran_lain")
	public Boolean getTidakBolehCaraPembayaranLain() {
		return tidakBolehCaraPembayaranLain == null ? Boolean.FALSE : tidakBolehCaraPembayaranLain;
	}

	/** @param nilai penanda baru pembatasan keras cara pembayaran — lihat {@link #getTidakBolehCaraPembayaranLain()}. */
	public void setTidakBolehCaraPembayaranLain(Boolean nilai) {
		this.tidakBolehCaraPembayaranLain = nilai;
	}

	/** Nilai 0 berarti tanpa batas transaksi untuk periode terkait. */
	@Column(name = "maksimal_transaksi_harian")
	public Double getMaksimalTransaksiHarian() {
		return maksimalTransaksiHarian == null ? 0.0 : maksimalTransaksiHarian;
	}

	/** @param nilai batas nominal transaksi harian baru — lihat {@link #getMaksimalTransaksiHarian()}. */
	public void setMaksimalTransaksiHarian(Double nilai) {
		this.maksimalTransaksiHarian = nilai;
	}

	/** @return {@link #maksimalTransaksiMingguan}, atau {@code 0.0} bila belum pernah di-set (tanpa batas transaksi mingguan). */
	@Column(name = "maksimal_transaksi_mingguan")
	public Double getMaksimalTransaksiMingguan() {
		return maksimalTransaksiMingguan == null ? 0.0 : maksimalTransaksiMingguan;
	}

	/** @param nilai batas nominal transaksi mingguan baru. */
	public void setMaksimalTransaksiMingguan(Double nilai) {
		this.maksimalTransaksiMingguan = nilai;
	}

	/** @return {@link #maksimalTransaksiBulanan}, atau {@code 0.0} bila belum pernah di-set (tanpa batas transaksi bulanan). */
	@Column(name = "maksimal_transaksi_bulanan")
	public Double getMaksimalTransaksiBulanan() {
		return maksimalTransaksiBulanan == null ? 0.0 : maksimalTransaksiBulanan;
	}

	/** @param nilai batas nominal transaksi bulanan baru. */
	public void setMaksimalTransaksiBulanan(Double nilai) {
		this.maksimalTransaksiBulanan = nilai;
	}

	/** @return {@link #wajibPin}, atau {@code false} bila belum pernah di-set (default tidak wajib). */
	@Column(name = "wajib_pin")
	public Boolean getWajibPin() {
		return wajibPin == null ? Boolean.FALSE : wajibPin;
	}

	/** @param nilai penanda baru kewajiban entry PIN pembeli sebelum transaksi POS tersimpan. */
	public void setWajibPin(Boolean nilai) {
		this.wajibPin = nilai;
	}

	/**
	 * Cara pembayaran yang secara khusus memerlukan PIN. Kosong berarti seluruh
	 * cara pembayaran memerlukan PIN ketika {@link #getWajibPin()} aktif, agar
	 * konfigurasi lama tetap kompatibel.
	 */
	@Column(name = "daftar_cara_pembayaran_wajib_pin")
	public String getDaftarCaraPembayaranWajibPin() {
		if (daftarCaraPembayaranWajibPin == null
				|| daftarCaraPembayaranWajibPin.replace(",", "").trim().isEmpty()) return "";
		String nilai = daftarCaraPembayaranWajibPin.trim();
		if (!nilai.startsWith(",")) nilai = "," + nilai;
		if (!nilai.endsWith(",")) nilai = nilai + ",";
		return nilai.replace(",,", ",");
	}

	/** @param nilai daftar ID cara pembayaran baru yang wajib PIN (bentuk mentah, dinormalisasi saat dibaca lewat {@link #getDaftarCaraPembayaranWajibPin()}). */
	public void setDaftarCaraPembayaranWajibPin(String nilai) {
		this.daftarCaraPembayaranWajibPin = nilai;
	}

	/** @return {@link #wajibVerifikasiBiometricWajah}, atau {@code false} bila belum pernah di-set (default tidak wajib). */
	@Column(name = "wajib_verifikasi_biometric_wajah")
	public Boolean getWajibVerifikasiBiometricWajah() {
		return wajibVerifikasiBiometricWajah == null ? Boolean.FALSE : wajibVerifikasiBiometricWajah;
	}

	/** @param nilai penanda baru kewajiban verifikasi biometrik wajah sebelum pembayaran saldo POS. */
	public void setWajibVerifikasiBiometricWajah(Boolean nilai) {
		this.wajibVerifikasiBiometricWajah = nilai;
	}

	/** @return {@link #wajibVerifikasiBiometricFingerprint}, atau {@code false} bila belum pernah di-set (default tidak wajib). */
	@Column(name = "wajib_verifikasi_biometric_fingerprint")
	public Boolean getWajibVerifikasiBiometricFingerprint() {
		return wajibVerifikasiBiometricFingerprint == null ? Boolean.FALSE : wajibVerifikasiBiometricFingerprint;
	}

	/** @param nilai penanda baru kewajiban verifikasi biometrik sidik jari sebelum pembayaran saldo POS. */
	public void setWajibVerifikasiBiometricFingerprint(Boolean nilai) {
		this.wajibVerifikasiBiometricFingerprint = nilai;
	}

	/**
	 * Default kebijakan "wajib No. HP" diturunkan dari NAMA tipe (permintaan bisnis):
	 * Pegawai/Dosen/Guru/Umum wajib mengisi nomor HP saat data member disimpan,
	 * Mahasiswa/Siswa (dan tipe lain) tidak. Berlaku hanya selama kolom belum pernah
	 * diisi eksplisit (nilai DB null) -- begitu admin menyetel lewat form Tipe Member,
	 * nilai eksplisitlah yang menang. Pola null->default sama dgn {@link #getAktif()}.
	 */
	public static boolean defaultWajibHp(String nama) {
		if (nama == null) return false;
		String n = nama.trim().toLowerCase();
		return n.contains("pegawai") || n.contains("dosen") || n.contains("guru")
				|| n.contains("umum");
	}

	/**
	 * @return {@link #wajibHp} bila sudah pernah di-set eksplisit; jika belum ({@code null}),
	 *         dihitung dari {@link #defaultWajibHp(String)} berdasarkan {@link #getNama()} —
	 *         lihat Javadoc method tsb untuk daftar nama tipe yang default-nya wajib.
	 */
	@Column(name = "wajib_hp")
	public Boolean getWajibHp() {
		return wajibHp == null ? Boolean.valueOf(defaultWajibHp(getNama())) : wajibHp;
	}

	/** @param wajibHp penanda eksplisit baru kewajiban No. HP; menimpa default berbasis nama tipe — lihat {@link #getWajibHp()}. */
	public void setWajibHp(Boolean wajibHp) {
		this.wajibHp = wajibHp;
	}

	/** Email TIDAK wajib utk semua tipe secara default (permintaan bisnis). */
	@Column(name = "wajib_email")
	public Boolean getWajibEmail() {
		return wajibEmail == null ? Boolean.FALSE : wajibEmail;
	}

	/** @param wajibEmail penanda baru kewajiban mengisi email saat data member disimpan. */
	public void setWajibEmail(Boolean wajibEmail) {
		this.wajibEmail = wajibEmail;
	}

	/**
	 * Cakupan aturan transaksi tipe member. Nilai lama/null sengaja dianggap
	 * berlaku ke semua toko agar deployment kolom baru tidak mengubah perilaku
	 * tipe member yang sudah ada.
	 */
	@Column(name = "berlaku_semua_toko")
	public Boolean getBerlakuSemuaToko() {
		return berlakuSemuaToko == null ? Boolean.TRUE : berlakuSemuaToko;
	}

	/** @param berlakuSemuaToko penanda baru cakupan aturan transaksi tipe ini ke seluruh toko atau terbatas — lihat {@link #getBerlakuSemuaToko()}. */
	public void setBerlakuSemuaToko(Boolean berlakuSemuaToko) {
		this.berlakuSemuaToko = berlakuSemuaToko;
	}

	/** Daftar ID toko dinormalisasi menjadi bentuk ",1,2,". */
	@Column(name = "daftar_toko")
	public String getDaftarToko() {
		if (daftarToko == null || daftarToko.replace(",", "").trim().isEmpty()) return "";
		String nilai = daftarToko.trim();
		if (!nilai.startsWith(",")) nilai = "," + nilai;
		if (!nilai.endsWith(",")) nilai = nilai + ",";
		return nilai.replace(",,", ",");
	}

	/** @param daftarToko daftar ID toko baru tempat aturan tipe ini berlaku (bentuk mentah, dinormalisasi saat dibaca lewat {@link #getDaftarToko()}). */
	public void setDaftarToko(String daftarToko) {
		this.daftarToko = daftarToko;
	}

	/** Benar bila seluruh kebijakan transaksi tipe ini efektif di toko tersebut. */
	public boolean berlakuUntukToko(Long tokoId) {
		if (Boolean.TRUE.equals(getBerlakuSemuaToko())) return true;
		return tokoId != null && getDaftarToko().contains("," + tokoId + ",");
	}
}
