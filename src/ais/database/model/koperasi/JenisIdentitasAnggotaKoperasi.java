package ais.database.model.koperasi;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

import ais.database.model.GeneralValueObject;

/**
 * Bank generated by hbm2java
 *
 * <p><b>Katalog JENIS IDENTITAS anggota koperasi</b> — dipetakan ke
 * {@code koperasi.jenis_identitas_anggota_koperasi}. Entity ini murni katalog REFERENSI
 * kecil: satu baris merepresentasikan satu jenis dokumen identitas yang bisa dipilih anggota
 * koperasi saat pendaftaran (mis. KTP, KTM/Kartu Tanda Mahasiswa, SIM, Paspor, Kartu Pelajar),
 * dirujuk lewat {@link AnggotaKoperasi#getJenisIdentitasAnggotaKoperasi()}/
 * {@code CalonAnggotaKoperasi} untuk menentukan JENIS nomor identitas yang disimpan pada
 * field nomor identitas anggota terkait — kelas ini sendiri hanya menyimpan katalog nama/kode
 * jenisnya, BUKAN nomor identitas anggota itu sendiri.</p>
 *
 * <p>Tidak seperti {@link JenisAnggotaKoperasi}/{@link TipeAnggotaKoperasi}, kelas ini TIDAK
 * membawa aturan bisnis (tidak ada limit transaksi, gerbang PIN/biometrik, atau field
 * privilese apa pun) — murni daftar pilihan (lookup table) untuk kombo di layar pendaftaran
 * anggota.</p>
 *
 * @see AnggotaKoperasi
 * @see CalonAnggotaKoperasi
 */
@Entity
@org.hibernate.annotations.Entity(dynamicInsert = true, dynamicUpdate = true)
@Audited
@Table(schema = "koperasi", name = "jenis_identitas_anggota_koperasi")
public class JenisIdentitasAnggotaKoperasi extends GeneralValueObject {

	/**
	 *
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	/** Primary key baris jenis identitas ini, di-generate database (IDENTITY). */
	private Long id;
	/** Nama pengguna yang terakhir membuat/mengubah baris ini (audit shadow field — lihat {@link #onUpdate()}). */
	private String oleh;
	/** Id pengguna yang terakhir membuat/mengubah baris ini (audit shadow field, pasangan {@link #oleh}). */
	private String olehId;

	/** @return {@link #olehId} — id pengguna audit terakhir yang mengubah baris ini. */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Meng-set {@link #olehId}; dilewati (no-op) bila {@code olehId} {@code null} atau string
	 * kosong/berisi spasi saja, sehingga nilai audit lama tidak pernah tertimpa nilai kosong.
	 *
	 * @param olehId id pengguna yang melakukan perubahan
	 */
	public void setOlehId(String olehId) {
		if (olehId == null || olehId.trim().isEmpty()) {
			return;
		}
		this.olehId = olehId;
	}

	/**
	 * Meng-set {@link #oleh}; dilewati (no-op) bila {@code oleh} {@code null} atau string
	 * kosong/berisi spasi saja, sehingga nilai audit lama tidak pernah tertimpa nilai kosong.
	 *
	 * @param oleh nama pengguna yang melakukan perubahan
	 */
	public void setOleh(String oleh) {
		if (oleh == null || oleh.trim().isEmpty()) {
			return;
		}
		this.oleh = oleh;
	}

	/** @return {@link #oleh} — nama pengguna audit terakhir yang mengubah baris ini. */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Callback JPA {@code @PreUpdate}: dipanggil otomatis oleh Hibernate tepat sebelum baris ini
	 * di-UPDATE, mendelegasikan pembaruan timestamp audit ({@link #tanggal_dirubah}) ke
	 * {@link ais.database.hibernate.AuditTimestampInterceptor#ubah(Object)}. Tidak dipanggil manual
	 * dari kode aplikasi.
	 */
	@javax.persistence.PreUpdate
	protected void onUpdate() {
		ais.database.hibernate.AuditTimestampInterceptor.ubah(this);
	}

	/** Timestamp terakhir baris ini diubah (audit); nilai awal saat konstruksi objek adalah waktu sekarang, diperbarui otomatis lewat {@link #onUpdate()} saat UPDATE. */
	private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * @param tanggal_dirubah nilai timestamp audit baru; dipanggil manual maupun otomatis oleh
	 *                         {@link #onUpdate()}
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/** @return {@link #tanggal_dirubah} — timestamp terakhir baris ini diubah. */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/** @return representasi teks baris ini, yaitu {@link #nama} jenis identitas (dipakai kombo/pencarian di layar ZK). */
	public String toString() {
		return nama;
	}

	/** Kode singkat jenis identitas ini. */
	private String kode;
	/** Nama jenis identitas ini (mis. "KTP", "KTM", "SIM", "Paspor"); juga dipakai {@link #toString()}. */
	private String nama;
	/** Keterangan/catatan bebas mengenai jenis identitas ini. */
	private String keterangan;
	/** Penanda aktif/nonaktif; {@code null} diperlakukan {@code true} (aktif) — lihat {@link #getAktif()}. */
	private Boolean aktif;

	/** Konstruktor default (dibutuhkan Hibernate); tidak menginisialisasi field apa pun secara eksplisit selain default deklarasi field. */
	public JenisIdentitasAnggotaKoperasi() {
	}

	/** @return {@link #id} — primary key baris jenis identitas ini. */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/** @param id primary key baru; normalnya di-generate database, jarang di-set manual dari kode aplikasi. */
	public void setId(Long id) {
		this.id = id;
	}

	/** @return {@link #kode} — kode singkat jenis identitas ini, boleh {@code null}. */
	public String getKode() {
		return kode;
	}

	/** @param kode kode singkat jenis identitas baru. */
	public void setKode(String kode) {
		this.kode = kode;
	}

	/** @return {@link #nama} — nama jenis identitas ini, di-trim; {@code null} bila belum di-set. */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/** @param nama nama jenis identitas baru. */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/** @return {@link #keterangan} — catatan bebas mengenai jenis identitas ini, boleh {@code null}. */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/** @param keterangan catatan bebas baru. */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/** @return {@link #aktif}, atau {@code true} bila belum pernah di-set (default aktif). */
	public Boolean getAktif() {
		return aktif == null ? true : aktif;
	}

	/** @param aktif penanda aktif/nonaktif baru untuk jenis identitas ini. */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}


}
