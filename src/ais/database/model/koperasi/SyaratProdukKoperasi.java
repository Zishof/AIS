package ais.database.model.koperasi;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

import ais.database.model.GeneralValueObject;

/**
 * Bank generated by hbm2java
 *
 * <p>Entity master <b>katalog syarat/persyaratan produk koperasi</b> (mis. "KTP", "Slip Gaji",
 * "Kartu Anggota") &mdash; tabel {@code koperasi.syarat_produk_koperasi}. Satu baris di sini adalah
 * satu item persyaratan yang dapat ditempelkan ke banyak {@link ProdukKoperasi} sekaligus lewat
 * relasi {@code @ManyToMany} {@code ProdukKoperasi.getSyaratProdukKoperasis()} (tabel penghubung
 * {@code koperasi.produk_koperasi_punya_syarat}); kelas ini sendiri tidak menyimpan referensi balik
 * ke produk mana pun, arah kepemilikan relasi sepenuhnya berada di {@link ProdukKoperasi}.</p>
 *
 * <h3>Bendera {@link #getWajib()}/{@link #getVerifikasi()} bersifat deskriptif, <b>tidak
 * ditegakkan</b> saat transaksi</h3>
 * <p>Secara semantik, {@link #getWajib()} menandai apakah syarat ini wajib dipenuhi anggota dan
 * {@link #getVerifikasi()} menandai apakah pemenuhannya perlu diverifikasi petugas. Kedua field ini
 * diisi/ditampilkan di layar master ({@code ais.action.master.koperasi.SyaratProdukKoperasiAction})
 * dan dipilih per-produk di layar produk ({@code ais.action.master.koperasi.ProdukKoperasiAction},
 * lewat {@code getSyaratProdukKoperasis()}/{@code setSyaratProdukKoperasis()}) &mdash; namun
 * penelusuran pada modul transaksi koperasi ({@code ais.action.master.koperasi.TransaksiKoperasiAction},
 * termasuk alur posting jurnal dan pembayaran anggota) <b>tidak menemukan satu pun</b> pembacaan
 * {@code getSyaratProdukKoperasis()}/{@code getWajib()}/{@code getVerifikasi()} yang menggerbang
 * (block/validasi) pembuatan transaksi. Dengan kata lain, menandai sebuah syarat sebagai wajib+wajib
 * verifikasi di layar master, dan memilihnya untuk sebuah produk, saat ini <b>tidak mencegah</b>
 * anggota bertransaksi pada produk itu tanpa memenuhi syarat tersebut — relasi ini murni katalog
 * informatif untuk kelengkapan data produk, bukan mekanisme kontrol akses/validasi aktif. Ini bukan
 * pola entity yatim (relasinya benar-benar terpakai dan tersimpan), melainkan fitur yang terlihat
 * seperti gerbang validasi tapi belum diimplementasikan sebagai gerbang.</p>
 *
 * <h3>{@link #getVerifikasi()} adalah getter semi-destruktif bersyarat</h3>
 * <p>Getter ini bukan sekadar default-lazy: bila {@link #getWajib()} bernilai {@code false} (syarat
 * tidak wajib), ia <b>memaksa</b> {@link #verifikasi} in-memory menjadi {@code false} sebelum
 * menormalkan dan mengembalikannya — menimpa nilai {@code true} yang mungkin pernah di-set eksplisit
 * lewat {@link #setVerifikasi(Boolean)} untuk baris yang kolom {@code wajib}-nya belakangan diubah
 * menjadi {@code false}. Efeknya sama seperti pola getter-menulis-balik pada entity
 * {@code ParameterTambahan*} lain di codebase ini (lihat catatan pada
 * {@code ParameterTambahanPengajuanTransaksiPegawai#getNomorUrut()}), hanya di sini pemicunya
 * bersyarat pada field saudara ({@link #wajib}), bukan tanpa syarat.</p>
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "koperasi", name = "syarat_produk_koperasi")
public class SyaratProdukKoperasi extends GeneralValueObject {

	/**
	 * ID versi serialisasi Java untuk kompatibilitas {@link java.io.Serializable} lintas build.
	 * Nilai generator konstan, kebetulan identik dengan {@code serialVersionUID} pada banyak entity
	 * lain hasil hbm2java di codebase ini &mdash; penomoran generator, bukan indikasi relasi apa pun.
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	/** ID baris (primary key), diisi otomatis oleh DB (kolom {@code IDENTITY}) — lihat {@link #getId()}. */
	private Long id;
	/** Nama akun/username pembuat-atau-pengubah terakhir baris ini; lihat {@link #getOleh()}. */
	private String oleh;
	/** ID pengguna pembuat-atau-pengubah terakhir baris ini; lihat {@link #getOlehId()}. */
	private String olehId;

	/**
	 * Mengembalikan ID pengguna yang terakhir membuat/mengubah baris syarat ini.
	 *
	 * @return ID pengguna audit terakhir, atau {@code null} bila belum pernah diisi
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menyimpan ID pengguna audit. Guard di awal method membuat setter ini <b>diam-diam menolak</b>
	 * nilai {@code null} atau string kosong/whitespace — memanggilnya dengan nilai semacam itu
	 * adalah no-op senyap yang mempertahankan {@link #olehId} lama, bukan mengosongkannya. Pola
	 * guard identik dipakai di {@link #setOleh(String)} dan di banyak entity {@code GeneralValueObject}
	 * lain pada codebase ini.
	 *
	 * @param olehId ID pengguna baru; diabaikan bila kosong
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Menyimpan nama/username audit. Guard di awal method membuat setter ini <b>diam-diam menolak</b>
	 * nilai {@code null} atau string kosong/whitespace, mempertahankan {@link #oleh} lama alih-alih
	 * mengosongkannya — sama seperti {@link #setOlehId(String)}.
	 *
	 * @param oleh nama/username baru; diabaikan bila kosong
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan nama/username pengguna yang terakhir membuat/mengubah baris syarat ini.
	 *
	 * @return nama/username audit terakhir, atau {@code null} bila belum pernah diisi
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Callback JPA {@code @PreUpdate}: dipanggil otomatis oleh Hibernate tepat sebelum baris ini
	 * di-{@code UPDATE}, mendelegasikan pencatatan stempel waktu perubahan ke
	 * {@link ais.database.hibernate.AuditTimestampInterceptor#ubah(GeneralValueObject)}, yang
	 * memutakhirkan {@link #tanggal_dirubah}. Tidak dipanggil pada {@code INSERT} pertama — field
	 * diinisialisasi saat konstruksi lewat {@link ais.ui.util.WaktuUtil#getDate()}.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menyimpan stempel waktu terakhir baris ini dirubah.
	 *
	 * @param tanggal_dirubah stempel waktu baru
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan stempel waktu terakhir baris ini dirubah, dipetakan sebagai kolom
	 * {@code @Temporal(TIMESTAMP)}. Diinisialisasi ke waktu-sekarang saat object dikonstruksi dan
	 * dimutakhirkan otomatis oleh {@link #onUpdate()} setiap kali baris di-{@code UPDATE}.
	 *
	 * @return stempel waktu perubahan terakhir
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Representasi teks singkat baris syarat ini, berformat {@code "<id>-<nama>"} (mis.
	 * {@code "4-KTP"}). Dipakai lewat pemanggilan implisit {@code toString()} pada log, combobox
	 * rendering, dan pesan debug.
	 *
	 * @return teks {@code "id-nama"}; bagian {@code id} bisa muncul sebagai {@code "null"} untuk
	 *         object yang belum tersimpan
	 */
	public String toString() {
		return id + "-" + nama;
	}

	/** Nama syarat/persyaratan, tercetak di daftar checkbox pemilihan syarat pada layar produk; lihat {@link #getNama()}. */
	private String nama;
	/** Bendera wajib-dipenuhi; lihat {@link #getWajib()} untuk perilaku default dan keterkaitannya dengan {@link #verifikasi}. */
	private Boolean wajib;
	/** Bendera aktif/nonaktif syarat; lihat {@link #getAktif()}. */
	private Boolean aktif;
	/** Bendera wajib-diverifikasi; lihat {@link #getVerifikasi()} untuk perilaku semi-destruktifnya. */
	private Boolean verifikasi;
	/** Keterangan/deskripsi opsional syarat; lihat {@link #getKeterangan()}. */
	private String keterangan;

	/** Konstruktor kosong wajib Hibernate/JavaBean; field diisi belakangan lewat setter atau reflection. */
	public SyaratProdukKoperasi() {
	}

	/**
	 * Primary key baris syarat, dipetakan {@code IDENTITY} (auto-increment DB) sehingga
	 * {@code insertable = false} — nilainya baru terisi <b>setelah</b> {@code INSERT} berhasil.
	 *
	 * @return ID baris, atau {@code null} untuk object baru yang belum tersimpan
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menyimpan ID baris. Dipanggil Hibernate saat hidrasi dari DB; jarang dipanggil aplikasi
	 * secara langsung karena kolomnya {@code insertable = false}.
	 *
	 * @param id ID baru
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Nama syarat/persyaratan (mis. "KTP", "Slip Gaji"), kolom wajib ({@code nullable = false},
	 * bertipe {@code text} tanpa batas panjang tetap). Nilai yang dikembalikan selalu di-{@code
	 * trim()}.
	 *
	 * @return nama syarat, sudah di-trim; {@code null} bila field mentah {@code null}
	 */
	@Column(name = "nama", nullable = false, columnDefinition = "text")
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Menyimpan nama syarat apa adanya (tanpa trim) — perataan spasi baru terjadi saat dibaca lewat
	 * {@link #getNama()}.
	 *
	 * @param nama nama syarat baru
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Keterangan/deskripsi bebas untuk syarat ini, kolom opsional ({@code nullable = true}, bertipe
	 * {@code text}). Berbeda dari {@link #getNama()}, nilainya dikembalikan apa adanya <b>tanpa</b>
	 * {@code trim()}.
	 *
	 * @return keterangan syarat, bisa {@code null}
	 */
	@Column(name = "keterangan", nullable = true, columnDefinition = "text")
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * Menyimpan keterangan syarat.
	 *
	 * @param keterangan teks keterangan baru
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * Menandai apakah syarat ini sedang aktif dan boleh dipilih saat membuat/mengubah relasi syarat
	 * pada {@link ProdukKoperasi}. Getter mengaplikasikan default lazy {@code true} bila field
	 * mentah masih {@code null} — konsisten dengan konvensi "aktif secara default" pada banyak
	 * entity master lain di codebase ini.
	 *
	 * @return {@code true} bila aktif (default); {@code false} bila dinonaktifkan eksplisit
	 */
	public Boolean getAktif() {
		return aktif == null ? true : aktif;
	}

	/**
	 * Menyimpan bendera aktif/nonaktif syarat.
	 *
	 * @param aktif bendera baru
	 */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}

	/**
	 * Menandai apakah syarat ini wajib dipenuhi anggota. Getter mengaplikasikan default lazy
	 * {@code true} bila field mentah masih {@code null}. <b>Catatan penting:</b> flag ini murni
	 * deskriptif di level entity/UI (dipakai untuk menandai checkbox "Wajib" di layar master dan
	 * memengaruhi hasil {@link #getVerifikasi()}) &mdash; tidak ada validasi di modul transaksi
	 * koperasi yang menggerbang pembuatan transaksi berdasarkan nilai ini; lihat javadoc kelas
	 * untuk uraian lengkap.
	 *
	 * @return {@code true} bila wajib (default); {@code false} bila opsional
	 */
	public Boolean getWajib() {
		return wajib == null ? true : wajib;
	}

	/**
	 * Menyimpan bendera wajib-dipenuhi.
	 *
	 * @param wajib bendera baru
	 */
	public void setWajib(Boolean wajib) {
		this.wajib = wajib;
	}

	/**
	 * Menandai apakah pemenuhan syarat ini perlu diverifikasi petugas. Berbeda dari getter
	 * default-lazy sederhana lain di kelas ini, method ini <b>bersyarat semi-destruktif</b>: ia
	 * lebih dulu memanggil {@link #getWajib()}, dan bila hasilnya {@code false} (syarat tidak
	 * wajib), field {@link #verifikasi} in-memory <b>dipaksa</b> menjadi {@code false} &mdash;
	 * menimpa nilai {@code true} apa pun yang sebelumnya di-set lewat {@link #setVerifikasi(Boolean)},
	 * termasuk untuk baris yang field {@link #wajib}-nya baru saja diubah menjadi {@code false} oleh
	 * pemanggil lain pada object yang sama sebelum getter ini dipanggil. Setelah pemaksaan
	 * (bersyarat) tersebut, hasil dinormalkan dengan default lazy {@code true} bila masih
	 * {@code null}. Seperti {@link #getWajib()}, flag ini murni deskriptif — tidak ditegakkan
	 * sebagai validasi transaksi; lihat javadoc kelas.
	 *
	 * @return {@code false} bila {@link #getWajib()} {@code false}; jika tidak, {@code true}
	 *         (default) atau nilai eksplisit yang tersimpan
	 */
	public Boolean getVerifikasi() {
		if (!getWajib()) {
			verifikasi = false;
		}
		return verifikasi == null ? true : verifikasi;
	}

	/**
	 * Menyimpan bendera wajib-diverifikasi. Nilai yang di-set di sini dapat ditimpa kembali menjadi
	 * {@code false} pada pemanggilan {@link #getVerifikasi()} berikutnya bila {@link #getWajib()}
	 * saat itu bernilai {@code false} — lihat catatan pada getter untuk detail.
	 *
	 * @param verifikasi bendera baru
	 */
	public void setVerifikasi(Boolean verifikasi) {
		this.verifikasi = verifikasi;
	}

}
