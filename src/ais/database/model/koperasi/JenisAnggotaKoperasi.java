package ais.database.model.koperasi;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

import ais.database.model.GeneralValueObject;

/**
 * Bank generated by hbm2java
 *
 * <p><b>Katalog JENIS anggota koperasi</b> — dipetakan ke {@code koperasi.jenis_anggota_koperasi}.
 * Satu baris di sini merepresentasikan satu KATEGORI LUAS keanggotaan (mis. "Reguler",
 * "Premium") yang dipilih {@link AnggotaKoperasi#getJenisAnggotaKoperasi()}/
 * {@link CalonAnggotaKoperasi} untuk menentukan bagaimana SALDO/DOMPET dan CASHBACK member
 * ditampilkan &amp; berperilaku di layar POS/e-Kantin, PLUS aturan kepatuhan belanja rutin
 * (target frekuensi belanja per periode, batas pelanggaran) dan gerbang keamanan transaksi
 * (wajib PIN, wajib verifikasi biometrik wajah/sidik jari) sebelum saldo boleh dipakai.</p>
 *
 * <p><b>Beda dengan {@link TipeAnggotaKoperasi} (jangan disamakan meski nama mirip).</b>
 * Kedua kelas ini adalah DUA DIMENSI klasifikasi anggota yang independen dan SAMA-SAMA
 * dirujuk langsung oleh {@code AnggotaKoperasi} (satu anggota punya SATU jenis DAN SATU tipe
 * sekaligus, bukan hierarki jenis→tipe):</p>
 * <ul>
 * <li><b>{@code JenisAnggotaKoperasi}</b> (kelas ini) berfokus pada PENGALAMAN
 * DOMPET/LOYALITAS: istilah &amp; visibilitas saldo/cashback, saldo minimum, aturan frekuensi
 * belanja, serta gerbang PIN/biometrik. TIDAK punya field limit transaksi nominal atau limit
 * utang eksplisit.</li>
 * <li><b>{@link TipeAnggotaKoperasi}</b> membawa HAK/PRIVILESE transaksi yang lebih keras:
 * plafon utang ({@link TipeAnggotaKoperasi#getMaksimalBolehUtang()}), limit transaksi
 * harian/mingguan/bulanan, cara pembayaran default/terbatas, kewajiban kontak (HP/email), dan
 * cakupan toko ({@code berlakuSemuaToko}/{@code daftarToko}). Sub-kategorinya cenderung
 * mengikuti PERAN institusional (Mahasiswa/Dosen/Siswa/Guru/Pegawai/Umum — lihat
 * {@code ConstantValues.MAHASISWA} dkk. yang dipakai default {@code AnggotaKoperasi
 * .getTipeAnggotaKoperasi()}), sementara {@code JenisAnggotaKoperasi} lebih bebas ditentukan
 * institusi (mis. tingkatan member "Reguler" vs "Premium").</li>
 * </ul>
 * <p>Kedua kelas JUGA sama-sama punya field {@code wajibPin}/{@code daftarCaraPembayaranWajibPin}/
 * {@code wajibVerifikasiBiometricWajah}/{@code wajibVerifikasiBiometricFingerprint} —
 * duplikasi struktur yang disengaja (dua sumber aturan independen); pemanggil yang mengecek
 * kewajiban PIN/biometrik SEHARUSNYA memeriksa KEDUANYA (jenis DAN tipe anggota) dan
 * menggabungkan hasilnya secara OR, bukan hanya salah satu, agar tidak melewatkan aturan yang
 * disetel institusi di sisi lain.</p>
 *
 * <p><b>Tidak ada field hak/privilase transaksi bernilai nominal di sini</b> — validasi
 * plafon utang &amp; limit transaksi murni tanggung jawab {@link TipeAnggotaKoperasi}; kelas
 * ini hanya membatasi CARA PEMBAYARAN yang boleh dipilih
 * ({@link #getDaftarCaraPembayaranYangBolehDiPilih()}) dan saldo minimum yang harus disisakan
 * ({@link #getMinimalSaldo()}).</p>
 *
 * @see TipeAnggotaKoperasi
 * @see AnggotaKoperasi
 * @see CalonAnggotaKoperasi
 */
@Entity
@org.hibernate.annotations.Entity(dynamicInsert = true, dynamicUpdate = true)
@Audited
@Table(schema = "koperasi", name = "jenis_anggota_koperasi")
public class JenisAnggotaKoperasi extends GeneralValueObject {

	/**
	 *
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	/** Primary key baris jenis anggota ini, di-generate database (IDENTITY). */
	private Long id;
	/** Nama pengguna yang terakhir membuat/mengubah baris ini (audit shadow field — lihat {@link #onUpdate()}). */
	private String oleh;
	/** Id pengguna yang terakhir membuat/mengubah baris ini (audit shadow field, pasangan {@link #oleh}). */
	private String olehId;

	/** @return {@link #olehId} — id pengguna audit terakhir yang mengubah baris ini. */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Meng-set {@link #olehId}; dilewati (no-op) bila {@code olehId} {@code null} atau string
	 * kosong/berisi spasi saja, sehingga nilai audit lama tidak pernah tertimpa nilai kosong.
	 *
	 * @param olehId id pengguna yang melakukan perubahan
	 */
	public void setOlehId(String olehId) {
		if (olehId == null || olehId.trim().isEmpty()) {
			return;
		}
		this.olehId = olehId;
	}

	/**
	 * Meng-set {@link #oleh}; dilewati (no-op) bila {@code oleh} {@code null} atau string
	 * kosong/berisi spasi saja, sehingga nilai audit lama tidak pernah tertimpa nilai kosong.
	 *
	 * @param oleh nama pengguna yang melakukan perubahan
	 */
	public void setOleh(String oleh) {
		if (oleh == null || oleh.trim().isEmpty()) {
			return;
		}
		this.oleh = oleh;
	}

	/** @return {@link #oleh} — nama pengguna audit terakhir yang mengubah baris ini. */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Callback JPA {@code @PreUpdate}: dipanggil otomatis oleh Hibernate tepat sebelum baris ini
	 * di-UPDATE, mendelegasikan pembaruan timestamp audit ({@link #tanggal_dirubah}) ke
	 * {@link ais.database.hibernate.AuditTimestampInterceptor#ubah(Object)}. Tidak dipanggil manual
	 * dari kode aplikasi.
	 */
	@javax.persistence.PreUpdate
	protected void onUpdate() {
		ais.database.hibernate.AuditTimestampInterceptor.ubah(this);
	}

	/** Timestamp terakhir baris ini diubah (audit); nilai awal saat konstruksi objek adalah waktu sekarang, diperbarui otomatis lewat {@link #onUpdate()} saat UPDATE. */
	private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * @param tanggal_dirubah nilai timestamp audit baru; dipanggil manual maupun otomatis oleh
	 *                         {@link #onUpdate()}
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/** @return {@link #tanggal_dirubah} — timestamp terakhir baris ini diubah. */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/** @return representasi teks baris ini, yaitu {@link #nama} jenis anggota (dipakai kombo/pencarian di layar ZK). */
	public String toString() {
		return nama;
	}

	/** Kode singkat jenis anggota ini (dipakai sebagai prefix nomor anggota — lihat pemakaian di {@code AnggotaKoperasi} penomoran otomatis). */
	private String kode;
	/** Nama jenis anggota ini (mis. "Reguler", "Premium"); juga dipakai {@link #toString()}. */
	private String nama;
	/** Keterangan/catatan bebas mengenai jenis anggota ini. */
	private String keterangan;
	/** Penanda aktif/nonaktif; {@code null} diperlakukan {@code true} (aktif) — lihat {@link #getAktif()}. */
	private Boolean aktif;
	/** Penanda apakah jenis ini muncul terpilih/tersorot secara default pada kombo pemilihan; {@code null} diperlakukan {@code true} — lihat {@link #getDipilih()}. */
	private Boolean dipilih;
	/** Izin admin melakukan entry top-up saldo manual untuk anggota jenis ini; {@code null} diperlakukan {@code false} — lihat {@link #getBolehEntryTopupOlehAdmin()}. */
	private Boolean bolehEntryTopupOlehAdmin;
	/** Daftar ID cara pembayaran yang boleh dipilih anggota jenis ini; disimpan dinormalisasi bentuk {@code ,1,2,} — lihat {@link #getDaftarCaraPembayaranYangBolehDiPilih()}. */
	private String daftarCaraPembayaranYangBolehDiPilih;
	/** Istilah tampilan untuk "sisa saldo" pada layar member (mis. diganti "Saldo Kas"); {@code null}/kosong memakai default — lihat {@link #getIstilahSisaSaldo()}. */
	private String istilahSisaSaldo;
	/** Penanda tampilkan/sembunyikan sisa saldo di layar member; {@code null} diperlakukan {@code true} — lihat {@link #getTampilkanSisaSaldo()}. */
	private Boolean tampilkanSisaSaldo;
	/** Istilah tampilan untuk "cashback" pada layar member; {@code null}/kosong memakai default — lihat {@link #getIstilahCashback()}. */
	private String istilahCashback;
	/** Penanda tampilkan/sembunyikan cashback di layar member; {@code null} diperlakukan {@code true} — lihat {@link #getTampilkanCashback()}. */
	private Boolean tampilkanCashback;
	/** Saldo minimum yang wajib disisakan pada dompet anggota jenis ini setelah transaksi; {@code null} diperlakukan {@code 0.0} — lihat {@link #getMinimalSaldo()}. */
	private Double minimalSaldo;

	// PENAMBAHAN VARIABEL BARU UNTUK ATURAN FREKUENSI BELANJA
	/** Penanda apakah anggota jenis ini WAJIB mematuhi target frekuensi belanja rutin; {@code null} diperlakukan {@code false} — lihat {@link #getWajibBelanjaRutin()}. */
	private Boolean wajibBelanjaRutin;
	/** Target jumlah transaksi belanja per periode evaluasi yang harus dipenuhi anggota jenis ini bila {@link #wajibBelanjaRutin} aktif; {@code null} diperlakukan {@code 0}. */
	private Integer targetFrekuensiBelanja;
	/** Jumlah maksimum pelanggaran (periode gagal memenuhi target frekuensi belanja) yang ditoleransi sebelum tindakan lanjutan diambil terhadap anggota; {@code null} diperlakukan {@code 0}. */
	private Integer maksimalPelanggaran;

	// Wajib verifikasi PIN pembeli di Layar Pelanggan (layar kedua POS) sebelum transaksi anggota
	// jenis ini boleh disimpan. Default TIDAK wajib (false) -- lihat AnggotaKoperasi.pin.
	/** Penanda wajib entry PIN pembeli sebelum transaksi POS anggota jenis ini tersimpan; {@code null} diperlakukan {@code false} — lihat {@link #getWajibPin()}. DIAUDIT (Envers). */
	private Boolean wajibPin;
	/** Daftar ID cara pembayaran yang secara khusus mewajibkan PIN ketika {@link #wajibPin} aktif; kosong berarti SELURUH cara pembayaran wajib PIN — lihat {@link #getDaftarCaraPembayaranWajibPin()}. */
	private String daftarCaraPembayaranWajibPin;
	// Gerbang pembayaran saldo pada POS Desktop/Android. Keduanya independen:
	// institusi boleh mewajibkan wajah, fingerprint, atau kedua-duanya.
	/** Penanda wajib verifikasi biometrik wajah sebelum pembayaran saldo POS anggota jenis ini; {@code null} diperlakukan {@code false} — independen dari {@link #wajibVerifikasiBiometricFingerprint}. */
	private Boolean wajibVerifikasiBiometricWajah;
	/** Penanda wajib verifikasi biometrik sidik jari sebelum pembayaran saldo POS anggota jenis ini; {@code null} diperlakukan {@code false} — independen dari {@link #wajibVerifikasiBiometricWajah}. */
	private Boolean wajibVerifikasiBiometricFingerprint;

	/** Konstruktor default (dibutuhkan Hibernate); tidak menginisialisasi field apa pun secara eksplisit selain default deklarasi field. */
	public JenisAnggotaKoperasi() {
	}

	/** @return {@link #id} — primary key baris jenis anggota ini. */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/** @param id primary key baru; normalnya di-generate database, jarang di-set manual dari kode aplikasi. */
	public void setId(Long id) {
		this.id = id;
	}

	/** @return {@link #kode} — kode singkat jenis anggota ini, boleh {@code null}. */
	public String getKode() {
		return kode;
	}

	/** @param kode kode singkat jenis anggota baru. */
	public void setKode(String kode) {
		this.kode = kode;
	}

	/** @return {@link #nama} — nama jenis anggota ini, di-trim; {@code null} bila belum di-set. */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/** @param nama nama jenis anggota baru. */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/** @return {@link #keterangan} — catatan bebas mengenai jenis anggota ini, boleh {@code null}. */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/** @param keterangan catatan bebas baru. */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/** @return {@link #aktif}, atau {@code true} bila belum pernah di-set (default aktif). */
	public Boolean getAktif() {
		return aktif == null ? true : aktif;
	}

	/** @param aktif penanda aktif/nonaktif baru untuk jenis anggota ini. */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}

	/** @return {@link #dipilih}, atau {@code true} bila belum pernah di-set (default terpilih). */
	public Boolean getDipilih() {
		return dipilih == null ? true : dipilih;
	}

	/** @param dipilih penanda terpilih/default baru untuk jenis anggota ini pada kombo pemilihan. */
	public void setDipilih(Boolean dipilih) {
		this.dipilih = dipilih;
	}

	/** @return {@link #bolehEntryTopupOlehAdmin}, atau {@code false} bila belum pernah di-set (default TIDAK diizinkan). */
	@Column(name = "boleh_entry_topup_oleh_admin", nullable = true)
	public Boolean getBolehEntryTopupOlehAdmin() {
		return bolehEntryTopupOlehAdmin == null ? false : bolehEntryTopupOlehAdmin;
	}

	/** @param bolehEntryTopupOlehAdmin penanda baru izin admin melakukan entry top-up saldo manual. */
	public void setBolehEntryTopupOlehAdmin(Boolean bolehEntryTopupOlehAdmin) {
		this.bolehEntryTopupOlehAdmin = bolehEntryTopupOlehAdmin;
	}

	/**
	 * Daftar ID cara pembayaran yang boleh dipakai anggota jenis ini. Getter ini BUKAN getter
	 * murni: setiap pemanggilan menormalkan ULANG {@link #daftarCaraPembayaranYangBolehDiPilih}
	 * dan MENIMPA field-nya sebagai efek samping sebelum mengembalikan nilai (getter destruktif),
	 * memakai deretan {@code replaceAll(",,", ",")} berulang untuk melipat koma ganda menjadi
	 * tunggal. Bentuk akhir dinormalisasi menjadi {@code ,1,2,} (diawali &amp; diakhiri koma)
	 * agar aman dicocokkan lewat {@code LIKE '%,1,%'} tanpa salah mencocokkan ID {@code 1} dengan
	 * ID {@code 11}. Nilai kosong/hanya-koma (termasuk {@code ","}, {@code ",,"}, {@code ",,,"})
	 * dinormalkan menjadi string kosong, yang berarti TIDAK ADA pembatasan cara pembayaran untuk
	 * jenis anggota ini (seluruh cara pembayaran aktif diizinkan).
	 *
	 * @return daftar ID cara pembayaran terizinkan, dinormalisasi bentuk {@code ,id,id,}, atau
	 *         string kosong bila tidak ada pembatasan
	 */
	@Column(name = "daftar_cara_pembayaran_yang_boleh_di_pilih", nullable = true)
	public String getDaftarCaraPembayaranYangBolehDiPilih() {
		daftarCaraPembayaranYangBolehDiPilih = (daftarCaraPembayaranYangBolehDiPilih == null
				|| daftarCaraPembayaranYangBolehDiPilih.trim().equalsIgnoreCase(",") ? ""
						: "," + daftarCaraPembayaranYangBolehDiPilih.trim() + ",")
				.replaceAll(",,", ",").replaceAll(",,", ",").replaceAll(",,", ",");

		if (daftarCaraPembayaranYangBolehDiPilih.equals(",")) {
			daftarCaraPembayaranYangBolehDiPilih = "";
		} else if (daftarCaraPembayaranYangBolehDiPilih.equals(",,")) {
			daftarCaraPembayaranYangBolehDiPilih = "";
		} else if (daftarCaraPembayaranYangBolehDiPilih.equals(",,,")) {
			daftarCaraPembayaranYangBolehDiPilih = "";
		}

		if (daftarCaraPembayaranYangBolehDiPilih == null) {
			daftarCaraPembayaranYangBolehDiPilih = "";
		}

		return daftarCaraPembayaranYangBolehDiPilih;
	}

	/** @param daftarCaraPembayaranYangBolehDiPilih daftar ID cara pembayaran baru (bentuk mentah, dinormalisasi saat dibaca lewat {@link #getDaftarCaraPembayaranYangBolehDiPilih()}). */
	public void setDaftarCaraPembayaranYangBolehDiPilih(String daftarCaraPembayaranYangBolehDiPilih) {
		this.daftarCaraPembayaranYangBolehDiPilih = daftarCaraPembayaranYangBolehDiPilih;
	}

	/** @return {@link #tampilkanCashback}, atau {@code true} bila belum pernah di-set (default tampil). */
	@Column(name = "tampilkan_cashback")
	public Boolean getTampilkanCashback() {
		return tampilkanCashback == null ? true : tampilkanCashback;
	}

	/** @param tampilkanCashback penanda tampilkan/sembunyikan cashback baru. */
	public void setTampilkanCashback(Boolean tampilkanCashback) {
		this.tampilkanCashback = tampilkanCashback;
	}

	/** @return {@link #istilahSisaSaldo}, atau {@code "Saldo Kas"} bila belum pernah di-set/kosong. */
	@Column(name = "istilah_sisa_saldo")
	public String getIstilahSisaSaldo() {
		return istilahSisaSaldo == null || istilahSisaSaldo.trim().isEmpty() ? "Saldo Kas" : istilahSisaSaldo;
	}

	/** @param istilahSisaSaldo istilah tampilan baru untuk sisa saldo. */
	public void setIstilahSisaSaldo(String istilahSisaSaldo) {
		this.istilahSisaSaldo = istilahSisaSaldo;
	}

	/** @return {@link #tampilkanSisaSaldo}, atau {@code true} bila belum pernah di-set (default tampil). */
	@Column(name = "tampilkan_sisa_saldo")
	public Boolean getTampilkanSisaSaldo() {
		return tampilkanSisaSaldo == null ? true : tampilkanSisaSaldo;
	}

	/** @param tampilkanSisaSaldo penanda tampilkan/sembunyikan sisa saldo baru. */
	public void setTampilkanSisaSaldo(Boolean tampilkanSisaSaldo) {
		this.tampilkanSisaSaldo = tampilkanSisaSaldo;
	}

	/** @return {@link #istilahCashback}, atau {@code "Cashback"} bila belum pernah di-set/kosong. */
	@Column(name = "istilah_cashback")
	public String getIstilahCashback() {
		return istilahCashback == null || istilahCashback.trim().isEmpty() ? "Cashback" : istilahCashback;
	}

	/** @param istilahCashback istilah tampilan baru untuk cashback. */
	public void setIstilahCashback(String istilahCashback) {
		this.istilahCashback = istilahCashback;
	}

	/** @return {@link #minimalSaldo}, atau {@code 0.0} bila belum pernah di-set (tanpa saldo minimum). */
	@Column(name = "minimal_saldo")
	public Double getMinimalSaldo() {
		return minimalSaldo == null ? 0.0 : minimalSaldo;
	}

	/** @param minimalSaldo saldo minimum baru yang wajib disisakan pada dompet anggota. */
	public void setMinimalSaldo(Double minimalSaldo) {
		this.minimalSaldo = minimalSaldo;
	}

	// ==========================================================
	// GETTER SETTER ATURAN WAJIB BELANJA
	// ==========================================================

	/** @return {@link #wajibBelanjaRutin}, atau {@code false} bila belum pernah di-set (default tidak wajib). */
	@Column(name = "wajib_belanja_rutin")
	public Boolean getWajibBelanjaRutin() {
		return wajibBelanjaRutin == null ? false : wajibBelanjaRutin;
	}

	/** @param wajibBelanjaRutin penanda baru kewajiban mematuhi target frekuensi belanja rutin. */
	public void setWajibBelanjaRutin(Boolean wajibBelanjaRutin) {
		this.wajibBelanjaRutin = wajibBelanjaRutin;
	}

	/**
	 * Wajib entry PIN pembeli (di Layar Pelanggan) sebelum transaksi POS anggota jenis ini
	 * tersimpan. DIAUDIT (Envers) -- kolom tabel audit ditambah via InitIndex.java.
	 */
	@Column(name = "wajib_pin")
	public Boolean getWajibPin() {
		return wajibPin == null ? false : wajibPin;
	}

	/** @param wajibPin penanda baru kewajiban entry PIN pembeli sebelum transaksi POS tersimpan. */
	public void setWajibPin(Boolean wajibPin) {
		this.wajibPin = wajibPin;
	}

	/** Kosong = seluruh metode wajib PIN ketika aturan wajib PIN aktif. */
	@Column(name = "daftar_cara_pembayaran_wajib_pin")
	public String getDaftarCaraPembayaranWajibPin() {
		if (daftarCaraPembayaranWajibPin == null
				|| daftarCaraPembayaranWajibPin.replace(",", "").trim().isEmpty()) return "";
		String nilai = daftarCaraPembayaranWajibPin.trim();
		if (!nilai.startsWith(",")) nilai = "," + nilai;
		if (!nilai.endsWith(",")) nilai = nilai + ",";
		return nilai.replace(",,", ",");
	}

	/** @param nilai daftar ID cara pembayaran baru yang wajib PIN (bentuk mentah, dinormalisasi saat dibaca lewat {@link #getDaftarCaraPembayaranWajibPin()}). */
	public void setDaftarCaraPembayaranWajibPin(String nilai) {
		this.daftarCaraPembayaranWajibPin = nilai;
	}

	/** @return {@link #wajibVerifikasiBiometricWajah}, atau {@code false} bila belum pernah di-set (default tidak wajib). */
	@Column(name = "wajib_verifikasi_biometric_wajah")
	public Boolean getWajibVerifikasiBiometricWajah() {
		return wajibVerifikasiBiometricWajah == null ? false : wajibVerifikasiBiometricWajah;
	}

	/** @param value penanda baru kewajiban verifikasi biometrik wajah sebelum pembayaran saldo POS. */
	public void setWajibVerifikasiBiometricWajah(Boolean value) {
		this.wajibVerifikasiBiometricWajah = value;
	}

	/** @return {@link #wajibVerifikasiBiometricFingerprint}, atau {@code false} bila belum pernah di-set (default tidak wajib). */
	@Column(name = "wajib_verifikasi_biometric_fingerprint")
	public Boolean getWajibVerifikasiBiometricFingerprint() {
		return wajibVerifikasiBiometricFingerprint == null ? false : wajibVerifikasiBiometricFingerprint;
	}

	/** @param value penanda baru kewajiban verifikasi biometrik sidik jari sebelum pembayaran saldo POS. */
	public void setWajibVerifikasiBiometricFingerprint(Boolean value) {
		this.wajibVerifikasiBiometricFingerprint = value;
	}

	/** @return {@link #targetFrekuensiBelanja}, atau {@code 0} bila belum pernah di-set (tanpa target). */
	@Column(name = "target_frekuensi_belanja")
	public Integer getTargetFrekuensiBelanja() {
		return targetFrekuensiBelanja == null ? 0 : targetFrekuensiBelanja;
	}

	/** @param targetFrekuensiBelanja target jumlah transaksi belanja per periode baru. */
	public void setTargetFrekuensiBelanja(Integer targetFrekuensiBelanja) {
		this.targetFrekuensiBelanja = targetFrekuensiBelanja;
	}

	/** @return {@link #maksimalPelanggaran}, atau {@code 0} bila belum pernah di-set (tanpa toleransi pelanggaran). */
	@Column(name = "maksimal_pelanggaran")
	public Integer getMaksimalPelanggaran() {
		return maksimalPelanggaran == null ? 0 : maksimalPelanggaran;
	}

	/** @param maksimalPelanggaran jumlah maksimum pelanggaran target frekuensi belanja yang ditoleransi. */
	public void setMaksimalPelanggaran(Integer maksimalPelanggaran) {
		this.maksimalPelanggaran = maksimalPelanggaran;
	}
}
