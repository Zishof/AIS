package ais.database.model.koperasi;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.Session;
import org.hibernate.criterion.Restrictions;
import org.hibernate.envers.Audited;

import ais.database.hibernate.HibernateUtil;
import ais.database.model.GeneralValueObject;

/**
 * Bank generated by hbm2java
 *
 * <p>Entity master <b>kategori (seksi) field kustom</b> untuk modul <i>koperasi</i> &mdash; tabel
 * {@code koperasi.Kelompok_parameter_tambahan_produk_koperasi} (perhatikan huruf besar {@code K}
 * awal pada nama tabel fisik, tidak konsisten dengan konvensi huruf kecil pada tabel lain di
 * codebase ini, tapi tidak berdampak fungsional karena Hibernate/PostgreSQL memetakan nama apa
 * adanya sesuai anotasi {@code @Table}). Satu baris di sini adalah satu <b>judul seksi</b> yang
 * tercetak sebagai label pemisah antar-kelompok isian dinamis pada formulir jenis transaksi
 * koperasi ({@link ais.database.model.koperasi.JenisTransaksiKoperasi}); bukan field/isian itu
 * sendiri. Field/isian sesungguhnya hidup di {@link ais.database.model.ParameterTambahan} dan
 * dipasang ke seksi ini lewat baris penghubung {@link ParameterTambahanProdukKoperasi} (kolom FK
 * {@code kelompok_parameter_tambahan_produk_koperasi} pada tabel
 * {@code koperasi.parameter_tambahan_produk_koperasi}).</p>
 *
 * <h3>Rantai konfigurasi &mdash; 4 lapis, khusus untuk modul koperasi</h3>
 * <ol>
 *   <li>{@link ais.database.model.ParameterTambahan} &mdash; definisi field kustom (label, tipe
 *   input, pilihan, bendera {@code aktif}, wajib-diisi, wajib-lampiran, dsb).</li>
 *   <li>{@link ParameterTambahanProdukKoperasi} &mdash; baris penghubung yang menempelkan sebuah
 *   {@code ParameterTambahan} ke <b>kategori ini</b>.</li>
 *   <li><b>Kelas ini</b> &mdash; kategori/seksi, dengan bendera {@link #getAktif()} dan
 *   {@link #getNomorUrut()} sebagai urutan seksi pada layar.</li>
 *   <li>{@code JenisTransaksiKoperasi.getKelompokParameterTambahanProdukKoperasis()} (relasi
 *   {@code @ManyToMany}, diatur lewat checkbox pada
 *   {@code ais.action.master.koperasi.JenisTransaksiKoperasiAction.initKelompokParameterTambahanProdukKoperasi})
 *   &mdash; lapis terakhir: kategori baru <b>tidak otomatis</b> muncul di formulir transaksi. Admin
 *   harus mencentang kategori itu per jenis transaksi koperasi pada layar master "Jenis Transaksi
 *   Koperasi".</li>
 * </ol>
 * <p>Pola "kelompok/kategori dinamis + baris penghubung ke {@code ParameterTambahan} + registrasi
 * per-jenis lewat {@code @ManyToMany}" ini berulang secara konsisten di banyak modul AIS (pegawai,
 * mahasiswa, siswa, alumni, payroll, dst.) &mdash; lihat javadoc
 * {@link ais.database.model.KelompokParameterTambahanPengajuan} untuk uraian rantai 4 lapisnya yang
 * berlaku sama di sini, dan javadoc
 * {@code ais.database.model.payroll.KelompokParameterTambahanPengajuanTransaksiPegawai} untuk
 * contoh varian yang <b>terkena</b> cacat salin-tempel kunci-atribut-ZK {@code task_fe6517bf}.</p>
 *
 * <h3>Verifikasi eksplisit: bug kunci-atribut-ZK {@code task_fe6517bf} TIDAK ditemukan di klaster
 * koperasi ini</h3>
 * <p>Registrasi {@code @ManyToMany} pada
 * {@code JenisTransaksiKoperasiAction.initKelompokParameterTambahanProdukKoperasi} memakai
 * referensi object langsung lewat closure Java (variabel final
 * {@code selectedKelompokParameterTambahanProdukKoperasi} bertipe {@code Set<KelompokParameterTambahanProdukKoperasi>},
 * dimutasi langsung oleh listener {@code onClick} checkbox) &mdash; <b>bukan</b> lewat pola
 * {@code row.setAttribute(kunciString, object)}/{@code row.getAttribute(kunciString)} yang dipakai
 * modul lain (mis. {@code BiodataMahasiswa}, {@code CatatanPegawai}, {@code PerbaikanAsset}, yang
 * semuanya memakai literal string {@code "parameterTambahan"}/{@code "kelompokParameterTambahan..."}
 * pada {@code row.getAttribute(...)} di dalam method render dinamisnya). Penelusuran menyeluruh atas
 * ketiga file yang memakai kelas ini/pasangannya ({@code KelompokParameterTambahanProdukKoperasiAction},
 * {@code ParameterTambahanProdukKoperasiAction}, {@code JenisTransaksiKoperasiAction}) tidak
 * menemukan satu pun pasangan {@code setAttribute}/{@code getAttribute} dengan kunci string yang
 * berpotensi tertukar modul &mdash; satu-satunya pemanggilan {@code arg0.setAttribute("checkbox", ...)}
 * pada renderer {@code KelompokParameterTambahanProdukKoperasiAction} tidak pernah dibaca kembali
 * lewat {@code getAttribute} di file manapun (kemungkinan sisa idiom yang tidak dipakai, bukan bug
 * kunci-tertukar). Kesimpulannya: pola bug salin-tempel kunci/tipe atribut baris ZK yang menimpa
 * modul payroll ({@code task_fe6517bf}) <b>secara arsitektur tidak dapat terjadi</b> di klaster
 * koperasi ini karena implementasinya memakai referensi object bertipe kuat, bukan lookup atribut
 * berkunci-string yang rawan salah modul. Ini verifikasi negatif, bukan indikasi tidak ada risiko
 * lain (lihat javadoc {@link ParameterTambahanProdukKoperasi} untuk catatan nama kolom FK yang
 * justru konsisten di modul ini, kontras dengan sepupu payroll-nya).</p>
 *
 * <h3>Tidak ada validasi anti-data-yatim di level entity/DB</h3>
 * <p>Kelas ini <b>tidak</b> mendeklarasikan constraint unik apa pun (nama boleh berulang, tidak ada
 * index unik pada kombinasi apa pun) dan <b>tidak</b> memiliki referensi balik wajib ke dokumen
 * induk mana pun &mdash; ia adalah master data berdiri sendiri, dapat eksis tanpa satu pun
 * {@link ParameterTambahanProdukKoperasi} yang menunjuknya (kelompok "yatim", kosong isi) maupun
 * tanpa dicentang di {@code JenisTransaksiKoperasi} manapun (kelompok tidak pernah tampil).
 * Sebaliknya, {@link ParameterTambahanProdukKoperasi} <b>mewajibkan</b> {@code ParameterTambahan}
 * (kolom {@code nullable = false}) tetapi <b>tidak mewajibkan</b> kelompok ini (kolom
 * {@code nullable = true}).</p>
 *
 * <p>Seluruh field audit ({@link #getOleh()}, {@link #getOlehId()}, {@link #getTanggal_dirubah()})
 * adalah <b>shadow field</b> yang wajib dideklarasikan ulang di sini secara teknis: kelas dasar
 * {@link GeneralValueObject} <b>bukan</b> {@code @MappedSuperclass}, sehingga field-field itu di
 * sana hanya state Java biasa tanpa pemetaan kolom &mdash; ini bukan duplikasi yang bisa dihapus,
 * melainkan keharusan agar Hibernate memetakan kolom {@code oleh}/{@code oleh_id}/{@code tanggal_dirubah}
 * pada tabel kelompok ini.</p>
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "koperasi", name = "Kelompok_parameter_tambahan_produk_koperasi")
public class KelompokParameterTambahanProdukKoperasi extends GeneralValueObject {

	/**
	 * ID versi serialisasi Java untuk kompatibilitas {@link java.io.Serializable} lintas build.
	 * Nilai generator konstan, kebetulan identik dengan {@code serialVersionUID} pada banyak entity
	 * lain hasil hbm2java di codebase ini &mdash; penomoran generator, bukan indikasi relasi apa pun.
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	/** ID baris (primary key), diisi otomatis oleh DB (kolom {@code IDENTITY}) — lihat {@link #getId()}. */
	private Long id;
	/** Nama akun/username pembuat-atau-pengubah terakhir baris ini; lihat {@link #getOleh()}. */
	private String oleh;
	/** ID pengguna pembuat-atau-pengubah terakhir baris ini; lihat {@link #getOlehId()}. */
	private String olehId;

	/**
	 * Mengembalikan ID pengguna yang terakhir membuat/mengubah baris kelompok ini.
	 *
	 * @return ID pengguna audit terakhir, atau {@code null} bila belum pernah diisi
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menyimpan ID pengguna audit. Guard di awal method membuat setter ini <b>diam-diam menolak</b>
	 * nilai {@code null} atau string kosong/whitespace — memanggilnya dengan nilai semacam itu
	 * adalah no-op senyap yang mempertahankan {@link #olehId} lama, bukan mengosongkannya. Pola
	 * guard identik dipakai di {@link #setOleh(String)} dan di kelas pasangan
	 * {@link ParameterTambahanProdukKoperasi}.
	 *
	 * @param olehId ID pengguna baru; diabaikan bila kosong
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Menyimpan nama/username audit. Guard di awal method membuat setter ini <b>diam-diam menolak</b>
	 * nilai {@code null} atau string kosong/whitespace, mempertahankan {@link #oleh} lama — sama
	 * seperti {@link #setOlehId(String)}.
	 *
	 * @param oleh nama/username baru; diabaikan bila kosong
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan nama/username pengguna yang terakhir membuat/mengubah baris kelompok ini.
	 *
	 * @return nama/username audit terakhir, atau {@code null} bila belum pernah diisi
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Callback JPA {@code @PreUpdate}: dipanggil otomatis oleh Hibernate tepat sebelum baris
	 * kelompok ini di-{@code UPDATE}, mendelegasikan pencatatan stempel waktu perubahan ke
	 * {@link ais.database.hibernate.AuditTimestampInterceptor#ubah(GeneralValueObject)}, yang pada
	 * gilirannya memutakhirkan {@link #tanggal_dirubah}. Tidak dipanggil pada {@code INSERT}
	 * pertama — field diinisialisasi saat konstruksi lewat {@link ais.ui.util.WaktuUtil#getDate()}.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menyimpan stempel waktu terakhir baris ini dirubah.
	 *
	 * @param tanggal_dirubah stempel waktu baru
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan stempel waktu terakhir baris ini dirubah, dipetakan sebagai kolom
	 * {@code @Temporal(TIMESTAMP)}. Diinisialisasi ke waktu-sekarang saat object dikonstruksi dan
	 * dimutakhirkan otomatis oleh {@link #onUpdate()} setiap kali baris di-{@code UPDATE}.
	 *
	 * @return stempel waktu perubahan terakhir
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Representasi teks singkat baris kelompok ini, berformat {@code "<id>-<nama>"} (mis.
	 * {@code "1-Form Tambahan"}). Dipakai lewat pemanggilan implisit {@code toString()} pada log,
	 * combobox rendering, dan pesan debug; <b>tidak</b> di-override lewat {@link #compareTo} meski
	 * keduanya memakai {@code nama} — lihat catatan pada {@link #compareTo(GeneralValueObject)}
	 * yang justru mengurutkan berdasarkan {@link #getNomorUrut()}, bukan {@code nama}.
	 *
	 * @return teks {@code "id-nama"}; bagian {@code id} bisa muncul sebagai {@code "null"} untuk
	 *         object yang belum tersimpan
	 */
	public String toString() {
		return id + "-" + nama;
	}

	/** Nama kelompok/seksi, tercetak sebagai judul pemisah di formulir transaksi koperasi; lihat {@link #getNama()}. */
	private String nama;
	/** Keterangan/deskripsi opsional kelompok; lihat {@link #getKeterangan()}. */
	private String keterangan;
	/**
	 * Penanda kelompok bawaan (default) yang di-auto-seed oleh {@link #checkCreateDefault()};
	 * lihat {@link #getDefaultData()}.
	 */
	private Boolean defaultData;
	/** Bendera aktif/nonaktif kelompok — hanya kelompok aktif yang ditampilkan; lihat {@link #getAktif()}. */
	private Boolean aktif;
	/** Urutan tampil kelompok relatif terhadap kelompok lain; lihat {@link #getNomorUrut()}. */
	private Integer nomorUrut;

	/**
	 * Menjamin tersedianya <b>satu</b> baris kelompok bawaan bernama {@code "Form Tambahan"}
	 * ({@link #defaultData} = {@code true}), membuatnya bila belum ada, lalu mengembalikannya.
	 *
	 * <p><b>Pola auto-seed dengan commit langsung ke DB.</b> Method ini mencari baris dengan
	 * {@code defaultData = true} lewat {@code Criteria}; bila tidak ditemukan, ia membangun
	 * instance baru, mengisi {@code nama}/{@code keterangan} = {@code "Form Tambahan"}, lalu
	 * <b>membuka dan meng-commit transaksinya sendiri</b> ({@code session.getTransaction().begin()}
	 * / {@code commit()}) sebelum menutup session lewat {@code session.disconnect()/close()} dan
	 * {@link HibernateUtil#closeSession()}. Ini adalah pola auto-seed yang sama seperti pada
	 * {@link ais.database.model.KelompokParameterTambahanPengajuan#checkCreateDefault()} dan
	 * sepupu {@code KelompokParameterTambahanGajiPegawai.checkCreateDefault()}: dipanggil dari
	 * layar master ({@code ParameterTambahanProdukKoperasiAction.doAfterCompose}, sebelum combobox
	 * kategori diisi) semata-mata agar dropdown kategori tidak pernah kosong pada instalasi baru —
	 * bukan bug, melainkan keharusan UX yang sengaja menembus lapisan transaksi UI normal.</p>
	 *
	 * <p><b>Efek samping penting:</b> karena transaksinya dibuka/ditutup sendiri dan session
	 * ditutup di akhir, pemanggilan method ini <b>di tengah</b> transaksi/session Hibernate lain
	 * yang sedang berjalan berisiko mengganggu unit-of-work pemanggil. Method ini juga <b>tidak
	 * idempoten terhadap race</b>: dua pemanggilan konkuren yang sama-sama menemukan hasil
	 * {@code uniqueResult() == null} dapat masing-masing membuat baris "Form Tambahan" sendiri
	 * (tidak ada constraint unik pada {@code defaultData} atau {@code nama} di level DB/entity —
	 * lihat catatan level-kelas soal ketiadaan validasi anti-duplikat).</p>
	 *
	 * @return baris kelompok bawaan yang sudah ada, atau baris baru yang baru saja dibuat dan
	 *         di-commit
	 */
	public static KelompokParameterTambahanProdukKoperasi checkCreateDefault() {
		Session session = HibernateUtil.currentNativeSession();
		KelompokParameterTambahanProdukKoperasi kelompokParameterTambahanCatatanAdministrasi = (KelompokParameterTambahanProdukKoperasi) session
				.createCriteria(KelompokParameterTambahanProdukKoperasi.class).add(Restrictions.eq("defaultData", true))
				.setMaxResults(1).uniqueResult();
		if (kelompokParameterTambahanCatatanAdministrasi == null) {
			kelompokParameterTambahanCatatanAdministrasi = new KelompokParameterTambahanProdukKoperasi();
			kelompokParameterTambahanCatatanAdministrasi.setDefaultData(true);
			kelompokParameterTambahanCatatanAdministrasi.setNama("Form Tambahan");
			kelompokParameterTambahanCatatanAdministrasi.setKeterangan("Form Tambahan");
			session.getTransaction().begin();
			session.save(kelompokParameterTambahanCatatanAdministrasi);
			session.getTransaction().commit();
		}
		// session.disconnect();
		if (session.isOpen()) {session.disconnect();session.close();}
		HibernateUtil.closeSession();
		return kelompokParameterTambahanCatatanAdministrasi;
	}

	/** Konstruktor kosong wajib Hibernate/JavaBean; field diisi belakangan lewat setter atau reflection. */
	public KelompokParameterTambahanProdukKoperasi() {
	}

	/**
	 * Primary key baris kelompok, dipetakan {@code IDENTITY} (auto-increment DB) sehingga
	 * {@code insertable = false} — nilainya baru terisi <b>setelah</b> {@code INSERT} berhasil.
	 *
	 * @return ID baris, atau {@code null} untuk object baru yang belum tersimpan
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menyimpan ID baris. Dipanggil Hibernate saat hidrasi dari DB, atau manual saat memuat ulang
	 * proxy by-id; jarang dipanggil aplikasi secara langsung karena kolomnya {@code insertable = false}.
	 *
	 * @param id ID baru
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Nama kelompok/seksi, tercetak sebagai judul pemisah antar-kelompok isian dinamis di layar
	 * transaksi koperasi. Kolom wajib ({@code nullable = false}, maks 255 karakter); nilai yang
	 * dikembalikan selalu di-{@code trim()} agar spasi tepi yang tersimpan tidak bocor ke
	 * tampilan/combobox.
	 *
	 * @return nama kelompok, sudah di-trim; {@code null} bila field mentah {@code null}
	 */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Menyimpan nama kelompok apa adanya (tanpa trim) — perataan spasi baru terjadi saat dibaca
	 * lewat {@link #getNama()}.
	 *
	 * @param nama nama kelompok baru
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Keterangan/deskripsi bebas untuk kelompok ini, kolom opsional ({@code nullable = true}, tanpa
	 * batas panjang eksplisit). Berbeda dari {@link #getNama()}, nilainya dikembalikan apa adanya
	 * <b>tanpa</b> {@code trim()}.
	 *
	 * @return keterangan kelompok, bisa {@code null}
	 */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * Menyimpan keterangan kelompok.
	 *
	 * @param keterangan teks keterangan baru
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * Menandai apakah baris ini adalah kelompok bawaan hasil auto-seed {@link #checkCreateDefault()}.
	 * Getter mengaplikasikan default lazy {@code false} bila field mentah masih {@code null} (baris
	 * lama sebelum kolom ini ada, atau baris yang dibuat manual tanpa mengisi field ini) — pola
	 * default-lazy yang konsisten dengan {@link #getAktif()} dan banyak entity bertipe
	 * {@code Boolean} lain di codebase ini. <b>Tidak</b> dipetakan {@code @Column} eksplisit;
	 * Hibernate memakai konvensi penamaan bawaannya (properti {@code defaultData} &rarr; kolom
	 * {@code default_data}) untuk field ini.
	 *
	 * @return {@code true} bila kelompok bawaan; {@code false} (setelah dinormalisasi) bila bukan
	 */
	public Boolean getDefaultData() {
		if (defaultData == null) {
			defaultData = false;
		}
		return defaultData;
	}

	/**
	 * Menyimpan bendera kelompok-bawaan. Dipanggil {@link #checkCreateDefault()} saat membuat baris
	 * seed; jarang/tidak pernah di-set eksplisit lewat UI admin biasa.
	 *
	 * @param defaultData bendera baru
	 */
	public void setDefaultData(Boolean defaultData) {
		this.defaultData = defaultData;
	}

	/**
	 * Menandai apakah kelompok ini sedang aktif dan boleh ditampilkan di formulir transaksi
	 * koperasi. Getter mengaplikasikan default lazy {@code true} bila field mentah masih
	 * {@code null} — konsisten dengan konvensi "aktif secara default" pada banyak entity master
	 * lain di codebase (mis. {@link ais.database.model.ParameterTambahan#getAktif()}). Kelompok
	 * nonaktif tetap tersimpan di DB dan tetap bisa dirujuk baris {@link ParameterTambahanProdukKoperasi}
	 * yang ada; renderer {@code KelompokParameterTambahanProdukKoperasiAction} menampilkan checkbox
	 * "Aktif" yang langsung menulis balik field ini via {@code Common.refreshSaveOrUpdate} setiap
	 * kali dicentang/dilepas.
	 *
	 * @return {@code true} bila aktif (default); {@code false} bila dinonaktifkan eksplisit
	 */
	public Boolean getAktif() {
		if (aktif == null) {
			aktif = true;
		}
		return aktif;
	}

	/**
	 * Menyimpan bendera aktif/nonaktif kelompok.
	 *
	 * @param aktif bendera baru
	 */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}

	/**
	 * Urutan tampil kelompok ini relatif terhadap kelompok lain pada layar transaksi koperasi
	 * (angka lebih kecil tampil lebih dulu). Getter mengaplikasikan default lazy {@code 1} bila
	 * field mentah masih {@code null} — dipakai langsung oleh {@link #compareTo(GeneralValueObject)}
	 * untuk mengurutkan koleksi kelompok. Ekspresi kondisional pada baris {@code return} bersifat
	 * mubazir: normalisasi sudah tuntas dua baris di atasnya, sehingga cabang
	 * {@code nomorUrut == null} pada {@code return} tidak pernah tercapai lagi — pola idiom yang
	 * berulang di banyak getter serupa pada codebase ini (bukan bug, sekadar sisa refactor yang
	 * tidak dirapikan).
	 *
	 * @return urutan tampil, minimal {@code 1} (default)
	 */
	public Integer getNomorUrut() {
		if (nomorUrut == null) {
			nomorUrut = 1;
		}
		return nomorUrut == null ? 1 : nomorUrut;
	}

	/**
	 * Menyimpan urutan tampil kelompok. Renderer {@code KelompokParameterTambahanProdukKoperasiAction}
	 * memakai {@code Intbox} yang langsung menulis balik field ini via
	 * {@code Common.refreshSaveOrUpdate} setiap kali nilainya diubah pengguna.
	 *
	 * @param nomorUrut urutan tampil baru
	 */
	public void setNomorUrut(Integer nomorUrut) {
		this.nomorUrut = nomorUrut;
	}

	/**
	 * Mengurutkan dua kelompok berdasarkan {@link #getNomorUrut()} secara menaik. Dipakai
	 * Hibernate/koleksi terurut untuk menentukan urutan render kelompok di layar. Ber-{@code cast}
	 * langsung ke tipe konkret kelas ini tanpa pengecekan {@code instanceof}; memanggilnya dengan
	 * {@code arg0} bertipe {@link GeneralValueObject} lain akan melempar {@link ClassCastException}
	 * saat runtime, bukan gagal senyap — kontras positif dibanding risiko bug attribute-key pada
	 * modul lain (lihat catatan verifikasi di javadoc kelas).
	 *
	 * @param arg0 kelompok pembanding, harus bertipe {@code KelompokParameterTambahanProdukKoperasi}
	 * @return negatif/nol/positif sesuai kontrak {@link Comparable#compareTo}, berbasis selisih
	 *         {@link #getNomorUrut()}
	 * @throws ClassCastException bila {@code arg0} bukan instance kelas ini
	 */
	@Override
	public int compareTo(GeneralValueObject arg0) {
		return getNomorUrut().compareTo(((KelompokParameterTambahanProdukKoperasi) arg0).getNomorUrut());
	}
}
