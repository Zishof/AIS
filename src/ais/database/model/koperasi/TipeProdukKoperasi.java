package ais.database.model.koperasi;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

import ais.database.model.GeneralValueObject;

/**
 * Bank generated by hbm2java
 *
 * <p>Entity master <b>tipe/jenis produk koperasi</b> (mis. "Simpanan", "Pinjaman") &mdash; tabel
 * {@code koperasi.tipe_produk_koperasi}. Setiap {@link ProdukKoperasi} wajib merujuk satu baris di
 * sini lewat {@code ProdukKoperasi.getTipeProdukKoperasi()} (kolom FK {@code tipe_produk_koperasi},
 * {@code nullable = false}); kelas ini sendiri tidak menyimpan produk apa pun, hanya klasifikasi
 * dan satu keputusan akuntansi penting: {@link #getJenis()} arah penambahan/pengurangan saldo.</p>
 *
 * <h3>{@link #getJenis()} menentukan arah debet/kredit jurnal transaksi koperasi</h3>
 * <p>Konstanta {@link #PENAMBAHAN} ({@code 1}) dan {@link #PENGURANGAN} ({@code -1}) bukan sekadar
 * label tampilan &mdash; nilainya dipakai langsung sebagai penentu arah jurnal akuntansi otomatis di
 * {@code ais.action.master.koperasi.TransaksiKoperasiAction} (metode posting transaksi simpan-pinjam):
 * {@code arah = tk.getProdukKoperasi().getTipeProdukKoperasi().getJenis()}, lalu
 * {@code boolean masuk = TipeProdukKoperasi.PENAMBAHAN.equals(arah)} menentukan akun mana yang
 * di-debet dan mana yang di-kredit (uang masuk ke kas vs keluar dari kas). Jenis produk
 * "Pinjaman" (pencairan pinjaman mengurangi kas/menambah piutang) harus bernilai
 * {@link #PENGURANGAN}; produk simpanan (uang masuk) harus {@link #PENAMBAHAN}. Salah konfigurasi
 * pada baris tipe produk manapun akan membalik arah debet/kredit seluruh transaksi yang memakai
 * produk-produk di bawah tipe itu &mdash; berdampak langsung pada kebenaran neraca, bukan sekadar
 * tampilan.</p>
 *
 * <h3>Fallback tebak-nama saat {@code jenis} belum diisi</h3>
 * <p>Layar master ({@code ais.action.master.koperasi.TipeProdukKoperasiAction}) sudah menyediakan
 * combobox eksplisit "Jenis" (Penambahan/Pengurangan) yang mengisi kolom ini langsung saat
 * baris dibuat/diubah lewat UI normal. Namun {@link #getJenis()} tetap memiliki jalur fallback untuk
 * baris lama/legacy yang kolomnya masih {@code null}: ia menebak berdasarkan <b>kecocokan nama
 * persis</b> (case-insensitive) dengan literal {@code "Pinjaman"} &mdash; bukan {@code contains}
 * atau prefix match. Bila admin memberi nama tipe produk pinjaman sesuatu yang lain (mis.
 * "Pinjaman Anggota", "Kredit", atau terjemahan lain), fallback ini akan salah menyimpulkan
 * {@link #PENAMBAHAN} untuk tipe yang seharusnya {@link #PENGURANGAN}, membalik arah jurnal secara
 * senyap untuk instalasi yang baris tipe-produknya belum pernah disentuh lewat combobox "Jenis" di
 * atas. Ini bukan bug baru (mekanisme combobox eksplisit sudah menutup celah untuk baris baru), tapi
 * risiko residual pada data historis/instalasi lama yang perlu diwaspadai saat audit konfigurasi
 * akuntansi koperasi.</p>
 *
 * <p><b>Efek samping getter:</b> seperti banyak getter default-lazy lain di codebase ini,
 * {@link #getJenis()} <b>menulis balik</b> hasil tebakannya ke field {@link #jenis} in-memory saat
 * dipanggil pertama kali dengan field masih {@code null} &mdash; bukan hanya menghitung nilai
 * sementara. Nilai yang ditebak baru benar-benar tersimpan ke DB bila object ini kemudian di-flush
 * lewat session Hibernate yang sedang berjalan.</p>
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "koperasi", name = "tipe_produk_koperasi")
public class TipeProdukKoperasi extends GeneralValueObject {

	/**
	 * Nilai {@link #jenis} yang menandai tipe produk sebagai <b>penambahan</b> saldo/kas (mis.
	 * simpanan) &mdash; transaksi dengan tipe produk ini men-debet akun kas dan mengkredit akun
	 * produk. Dipakai sebagai pembanding langsung ({@code equals}) pada logika posting jurnal di
	 * {@code ais.action.master.koperasi.TransaksiKoperasiAction}; lihat javadoc kelas untuk uraian
	 * lengkap dampaknya terhadap arah debet/kredit.
	 */
	public static final Integer PENAMBAHAN = 1;
	/**
	 * Nilai {@link #jenis} yang menandai tipe produk sebagai <b>pengurangan</b> saldo/kas (mis.
	 * pinjaman yang dicairkan) &mdash; transaksi dengan tipe produk ini men-debet akun produk dan
	 * mengkredit akun kas, kebalikan dari {@link #PENAMBAHAN}.
	 */
	public static final Integer PENGURANGAN = -1;

	/**
	 * ID versi serialisasi Java untuk kompatibilitas {@link java.io.Serializable} lintas build.
	 * Nilai generator konstan, kebetulan identik dengan {@code serialVersionUID} pada banyak entity
	 * lain hasil hbm2java di codebase ini &mdash; penomoran generator, bukan indikasi relasi apa pun.
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	/** ID baris (primary key), diisi otomatis oleh DB (kolom {@code IDENTITY}) — lihat {@link #getId()}. */
	private Long id;
	/** Nama akun/username pembuat-atau-pengubah terakhir baris ini; lihat {@link #getOleh()}. */
	private String oleh;
	/** ID pengguna pembuat-atau-pengubah terakhir baris ini; lihat {@link #getOlehId()}. */
	private String olehId;

	/**
	 * Mengembalikan ID pengguna yang terakhir membuat/mengubah baris tipe produk ini.
	 *
	 * @return ID pengguna audit terakhir, atau {@code null} bila belum pernah diisi
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menyimpan ID pengguna audit. Guard di awal method membuat setter ini <b>diam-diam menolak</b>
	 * nilai {@code null} atau string kosong/whitespace — memanggilnya dengan nilai semacam itu
	 * adalah no-op senyap yang mempertahankan {@link #olehId} lama, bukan mengosongkannya. Pola
	 * guard identik dipakai di {@link #setOleh(String)} dan di banyak entity {@code GeneralValueObject}
	 * lain pada codebase ini.
	 *
	 * @param olehId ID pengguna baru; diabaikan bila kosong
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Menyimpan nama/username audit. Guard di awal method membuat setter ini <b>diam-diam menolak</b>
	 * nilai {@code null} atau string kosong/whitespace, mempertahankan {@link #oleh} lama alih-alih
	 * mengosongkannya — sama seperti {@link #setOlehId(String)}.
	 *
	 * @param oleh nama/username baru; diabaikan bila kosong
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan nama/username pengguna yang terakhir membuat/mengubah baris tipe produk ini.
	 *
	 * @return nama/username audit terakhir, atau {@code null} bila belum pernah diisi
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Callback JPA {@code @PreUpdate}: dipanggil otomatis oleh Hibernate tepat sebelum baris ini
	 * di-{@code UPDATE}, mendelegasikan pencatatan stempel waktu perubahan ke
	 * {@link ais.database.hibernate.AuditTimestampInterceptor#ubah(GeneralValueObject)}, yang
	 * memutakhirkan {@link #tanggal_dirubah}. Tidak dipanggil pada {@code INSERT} pertama — field
	 * diinisialisasi saat konstruksi lewat {@link ais.ui.util.WaktuUtil#getDate()}.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menyimpan stempel waktu terakhir baris ini dirubah.
	 *
	 * @param tanggal_dirubah stempel waktu baru
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan stempel waktu terakhir baris ini dirubah, dipetakan sebagai kolom
	 * {@code @Temporal(TIMESTAMP)}. Diinisialisasi ke waktu-sekarang saat object dikonstruksi dan
	 * dimutakhirkan otomatis oleh {@link #onUpdate()} setiap kali baris di-{@code UPDATE}.
	 *
	 * @return stempel waktu perubahan terakhir
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Representasi teks singkat baris tipe produk ini, berformat {@code "<id>-<nama>"} (mis.
	 * {@code "2-Pinjaman"}). Dipakai lewat pemanggilan implisit {@code toString()} pada log,
	 * combobox rendering, dan pesan debug.
	 *
	 * @return teks {@code "id-nama"}; bagian {@code id} bisa muncul sebagai {@code "null"} untuk
	 *         object yang belum tersimpan
	 */
	public String toString() {
		return id + "-" + nama;
	}

	/**
	 * Kode singkat tipe produk, opsional. Berbeda dari field lain di kelas ini, field ini
	 * <b>tidak</b> dipetakan lewat anotasi {@code @Column} pada getter-nya — Hibernate memetakannya
	 * memakai konvensi penamaan bawaan (properti {@code kode} → kolom {@code kode}); lihat
	 * {@link #getKode()}.
	 */
	private String kode;

	/** Nama tipe produk, tercetak di combobox pemilihan tipe pada layar produk koperasi; lihat {@link #getNama()}. */
	private String nama;
	/** Keterangan/deskripsi opsional tipe produk; lihat {@link #getKeterangan()}. */
	private String keterangan;
	/** Bendera aktif/nonaktif tipe produk; lihat {@link #getAktif()}. */
	private Boolean aktif;
	/**
	 * Arah penambahan/pengurangan saldo untuk transaksi yang memakai produk bertipe ini, bernilai
	 * {@link #PENAMBAHAN} atau {@link #PENGURANGAN}. Lihat {@link #getJenis()} untuk perilaku
	 * fallback saat field ini {@code null} dan dampaknya terhadap arah debet/kredit jurnal.
	 */
	private Integer jenis;

	/** Konstruktor kosong wajib Hibernate/JavaBean; field diisi belakangan lewat setter atau reflection. */
	public TipeProdukKoperasi() {
	}

	/**
	 * Primary key baris tipe produk, dipetakan {@code IDENTITY} (auto-increment DB) sehingga
	 * {@code insertable = false} — nilainya baru terisi <b>setelah</b> {@code INSERT} berhasil.
	 *
	 * @return ID baris, atau {@code null} untuk object baru yang belum tersimpan
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menyimpan ID baris. Dipanggil Hibernate saat hidrasi dari DB; jarang dipanggil aplikasi
	 * secara langsung karena kolomnya {@code insertable = false}.
	 *
	 * @param id ID baru
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Kode singkat tipe produk. Getter menormalkan {@code null} menjadi string kosong dan
	 * men-{@code trim()} hasilnya — berbeda dari kebanyakan getter String lain di kelas ini
	 * ({@link #getKeterangan()}) yang membiarkan {@code null} apa adanya; pemanggil tidak perlu
	 * null-check terpisah untuk field ini.
	 *
	 * @return kode tipe produk yang sudah di-trim, tidak pernah {@code null} (string kosong bila
	 *         belum diisi)
	 */
	public String getKode() {
		return kode == null ? "" : kode.trim();
	}

	/**
	 * Menyimpan kode tipe produk apa adanya (tanpa trim) — perataan spasi baru terjadi saat dibaca
	 * lewat {@link #getKode()}.
	 *
	 * @param kode kode baru
	 */
	public void setKode(String kode) {
		this.kode = kode;
	}

	/**
	 * Nama tipe produk (mis. "Simpanan", "Pinjaman"), kolom wajib ({@code nullable = false},
	 * bertipe {@code text} tanpa batas panjang tetap). Nilai yang dikembalikan selalu di-{@code
	 * trim()}. <b>Nama persis</b> field ini (case-insensitive) dibandingkan oleh fallback
	 * {@link #getJenis()} terhadap literal {@code "Pinjaman"} untuk menebak arah jurnal saat kolom
	 * {@code jenis} belum diisi — lihat javadoc kelas untuk penjelasan lengkap risikonya.
	 *
	 * @return nama tipe produk, sudah di-trim; {@code null} bila field mentah {@code null}
	 */
	@Column(name = "nama", nullable = false, columnDefinition = "text")
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Menyimpan nama tipe produk apa adanya (tanpa trim) — perataan spasi baru terjadi saat dibaca
	 * lewat {@link #getNama()}.
	 *
	 * @param nama nama tipe produk baru
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Keterangan/deskripsi bebas untuk tipe produk ini, kolom opsional ({@code nullable = true},
	 * bertipe {@code text}). Berbeda dari {@link #getNama()}, nilainya dikembalikan apa adanya
	 * <b>tanpa</b> {@code trim()}.
	 *
	 * @return keterangan tipe produk, bisa {@code null}
	 */
	@Column(name = "keterangan", nullable = true, columnDefinition = "text")
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * Menyimpan keterangan tipe produk.
	 *
	 * @param keterangan teks keterangan baru
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * Menandai apakah tipe produk ini sedang aktif dan boleh dipilih saat membuat/mengubah
	 * {@link ProdukKoperasi} baru. Getter mengaplikasikan default lazy {@code true} bila field
	 * mentah masih {@code null} — konsisten dengan konvensi "aktif secara default" pada banyak
	 * entity master lain di codebase ini. Tipe nonaktif tetap tersimpan dan tetap dirujuk oleh
	 * produk-produk yang sudah ada, tapi disaring dari daftar pencarian default lewat filter
	 * {@code Restrictions.or(isNull("aktif"), eq("aktif", true))} pada
	 * {@code TipeProdukKoperasiAction.initCriteria}.
	 *
	 * @return {@code true} bila aktif (default); {@code false} bila dinonaktifkan eksplisit
	 */
	public Boolean getAktif() {
		return aktif == null ? true : aktif;
	}

	/**
	 * Menyimpan bendera aktif/nonaktif tipe produk.
	 *
	 * @param aktif bendera baru
	 */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}

	/**
	 * Menyimpan arah penambahan/pengurangan saldo untuk tipe produk ini. Dipanggil layar master
	 * ({@code TipeProdukKoperasiAction.onSave}) dengan nilai dari combobox eksplisit "Jenis"
	 * (Penambahan/Pengurangan) yang selalu memakai konstanta {@link #PENAMBAHAN}/{@link #PENGURANGAN};
	 * boleh {@code null} bila admin belum memilih apa pun pada combobox tersebut, dalam hal ini
	 * {@link #getJenis()} akan menebak nilainya saat berikutnya dipanggil.
	 *
	 * @param jenis arah baru; {@link #PENAMBAHAN}, {@link #PENGURANGAN}, atau {@code null}
	 */
	public void setJenis(Integer jenis) {
		this.jenis = jenis;
	}

	/**
	 * Mengembalikan arah penambahan/pengurangan saldo untuk transaksi yang memakai produk bertipe
	 * ini &mdash; nilainya dipakai langsung sebagai penentu arah debet/kredit jurnal otomatis oleh
	 * {@code ais.action.master.koperasi.TransaksiKoperasiAction} (lihat javadoc kelas untuk uraian
	 * lengkap).
	 *
	 * <p><b>Fallback tebak-nama dan efek samping penulisan balik.</b> Bila field mentah
	 * {@link #jenis} masih {@code null} (baris lama yang dibuat sebelum combobox "Jenis" eksplisit
	 * ada, atau baris yang disimpan lewat jalur lain tanpa mengisi field ini), method ini menebak
	 * nilainya berdasarkan <b>kecocokan nama persis</b> (case-insensitive, bukan substring) antara
	 * {@link #getNama()} (di-trim) dengan literal {@code "Pinjaman"}: cocok &rarr; {@link
	 * #PENGURANGAN}, tidak cocok (termasuk nama {@code null} atau variasi seperti "Pinjaman
	 * Anggota") &rarr; {@link #PENAMBAHAN} sebagai default. Hasil tebakan ini <b>ditulis balik</b>
	 * ke field {@link #jenis} in-memory sebelum dikembalikan — pemanggilan berikutnya pada object
	 * yang sama tidak menebak ulang, dan bila object ini kemudian di-flush oleh Hibernate, tebakan
	 * tersebut ikut tersimpan permanen ke kolom {@code jenis} di DB. Baris pemeriksaan
	 * {@code jenis == null} kedua yang seolah redundan pada ekspresi lain di kelas-kelas sejenis
	 * codebase ini <b>tidak</b> muncul di sini — method ini murni satu jalur if/else tanpa idiom
	 * ternary mubazir.
	 *
	 * @return arah saldo: {@link #PENAMBAHAN}, {@link #PENGURANGAN}, atau hasil tebakan bila field
	 *         mentah belum pernah diisi — tidak pernah {@code null}
	 */
	public Integer getJenis() {
		if (jenis == null) {
			if (nama != null && nama.trim().equalsIgnoreCase("Pinjaman")) {
				jenis = TipeProdukKoperasi.PENGURANGAN;
			} else {
				jenis = TipeProdukKoperasi.PENAMBAHAN;
			}
		}
		return jenis;
	}
}
