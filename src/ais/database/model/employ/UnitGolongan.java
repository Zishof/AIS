package ais.database.model.employ;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;




import org.hibernate.envers.Audited;



import ais.database.model.GeneralValueObject;

/**
 * Bank generated by hbm2java
 *
 * <p>Entity master data kepegawaian (tabel {@code employ.unit_golongan}) yang mengelompokkan
 * {@link Golongan} ke dalam unit/kelompok golongan yang lebih tinggi (mis. "Golongan PNS",
 * "Golongan Non-PNS/Honorer") lewat relasi {@link Golongan#getUnitGolongan()}. Javadoc lama "Bank
 * generated by hbm2java" adalah sisa salin-tempel generator hbm2java dari kelas lain (kelas ini
 * tidak berhubungan dengan entity Bank) — dibiarkan apa adanya sesuai aturan "jangan mengurangi
 * javadoc lama", diperkaya dengan javadoc ini.</p>
 *
 * <h2>Field {@code skor}: daftar id {@link SkorGolongan} yang relevan, bukan nilai skor</h2>
 * <p>{@link #getSkor()} BUKAN nilai skor itu sendiri — ia adalah teks CSV berisi PRIMARY KEY
 * ({@code id}, bukan {@code kode}) dari {@link SkorGolongan} yang RELEVAN untuk unit golongan ini,
 * dikonfirmasi dari pemakaiannya di {@code UnitGolonganAction} (method render baris tabel):
 * {@code unitGolongan.getSkor().split(",")} dipecah menjadi larik id, tiap id di-parse
 * {@code Long.parseLong(...)} dan dipakai untuk mengambil instance {@link SkorGolongan} lewat
 * {@code ConstantValues.ambil(SkorGolongan.class.getName(), id)} demi ditampilkan namanya di
 * layar. Dengan kata lain, field ini berfungsi sebagai DAFTAR PILIH komponen skor kepangkatan mana
 * saja yang berlaku untuk unit golongan ini — mekanisme pembatasan skor per-unit, TERPISAH dari
 * mesin evaluasi poin kenaikan golongan itu sendiri (yang berada di
 * {@code ais.action.master.employ.helper.GolonganUtil}, mengiterasi SELURUH {@link SkorGolongan}
 * aktif tanpa memandang unit golongan mana pun — lihat javadoc kelas {@link Golongan}). Perlu
 * diperhatikan: bila string CSV ini mengandung id yang parsing-nya gagal ({@link
 * NumberFormatException}) atau id yang sudah tidak merujuk {@link SkorGolongan} yang masih ada,
 * kode pemanggil ({@code UnitGolonganAction}) yang bertanggung jawab menanganinya — entity ini
 * sendiri tidak memvalidasi format maupun keberadaan referensinya.</p>
 *
 * <p>Layar CRUD-nya adalah {@code UnitGolonganAction}. Entity ini tidak membawa filter
 * tenant/satuan-kerja eksplisit di levelnya sendiri.</p>
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "employ", name = "unit_golongan")



public class UnitGolongan extends GeneralValueObject {

	/**
	 * Versi serialisasi entity ini untuk kompatibilitas Java serialization. Object entity AIS
	 * diserialkan ke cache MapDB dan session ZK, sehingga nilai ini jangan diubah tanpa alasan
	 * kuat — mengubahnya membuat data ter-cache lama tidak terbaca lagi
	 * ({@code InvalidClassException}) setelah deploy.
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	/** Primary key entity. Lihat {@link #getId()}. */
	private Long id;
	/**
	 * Nama pengguna terakhir yang mengubah entity ini. Field ini meng-hide field privat bernama
	 * sama di {@link GeneralValueObject}: karena kelas induk tersebut bukan
	 * {@code @Entity}/{@code @MappedSuperclass}, field yang dideklarasikannya tidak otomatis
	 * dipetakan Hibernate — setiap entity turunan wajib mendeklarasikan ulang field beserta
	 * getter/setter-nya sendiri agar benar-benar tersimpan sebagai kolom. Pola ini berulang di
	 * hampir seluruh entity AIS (kebutuhan teknis, bukan bug). Diisi otomatis lewat
	 * {@link #onUpdate()} setiap kali entity di-{@code UPDATE}.
	 */
	private String oleh;
	/**
	 * Id pengguna terakhir yang mengubah entity ini. Sama seperti {@link #oleh}, field ini
	 * meng-hide field privat bernama sama di {@link GeneralValueObject} demi pemetaan Hibernate.
	 * Diisi otomatis lewat {@link #onUpdate()}.
	 */
	private String olehId;

	/**
	 * Mengembalikan id pengguna yang terakhir mengubah entity ini.
	 *
	 * @return id pengguna pengubah terakhir, atau {@code null} bila belum pernah terisi
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menyetel id pengguna pengubah terakhir, dengan validasi non-trivial: nilai {@code null} atau
	 * string kosong/hanya-spasi diabaikan diam-diam. Konsekuensinya nilai lama tidak pernah bisa
	 * dihapus lewat setter ini — perilaku ini disengaja agar jejak audit yang sudah terisi tidak
	 * terhapus oleh jalur simpan yang kebetulan tidak membawa informasi pengguna.
	 *
	 * @param olehId id pengguna pengubah; diabaikan bila {@code null}/kosong
	 */
	public void setOlehId(String olehId) {
		if (olehId == null || olehId.trim().isEmpty()) {
			return;
		}
		this.olehId = olehId;
	}

	/**
	 * Menyetel nama pengguna pengubah terakhir, dengan validasi non-trivial yang sama seperti
	 * {@link #setOlehId(String)}: nilai {@code null}/kosong diabaikan diam-diam.
	 *
	 * @param oleh nama pengguna pengubah; diabaikan bila {@code null}/kosong
	 */
	public void setOleh(String oleh) {
		if (oleh == null || oleh.trim().isEmpty()) {
			return;
		}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan nama pengguna yang terakhir mengubah entity ini.
	 *
	 * @return nama pengguna pengubah terakhir, atau {@code null} bila belum pernah terisi
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Hook siklus-hidup JPA {@code @PreUpdate}: dipanggil otomatis oleh provider persistence tepat
	 * sebelum statement {@code UPDATE} dikirim ke database setiap kali entity ini diubah dan
	 * disimpan lewat session Hibernate yang aktif. Satu-satunya isi method ini adalah delegasi ke
	 * {@code ais.database.hibernate.AuditTimestampInterceptor.ubah(this)}, pola audit-timestamp
	 * standar yang diulang di hampir seluruh entity AIS. Efeknya: {@link #setTanggal_dirubah}
	 * diisi waktu server saat ini, dan {@link #setOleh}/{@link #setOlehId} diisi identitas
	 * pengguna yang sedang login.
	 */
	@javax.persistence.PreUpdate
	protected void onUpdate() {
		ais.database.hibernate.AuditTimestampInterceptor.ubah(this);
	}

	/**
	 * Stempel waktu perubahan terakhir. Diinisialisasi ke waktu pembuatan object memakai
	 * {@code WaktuUtil.getDate()} (jam server aplikasi, bukan jam database). Field ini juga
	 * meng-hide field bernama sama di {@link GeneralValueObject}.
	 */
	private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menyetel stempel waktu perubahan terakhir. Tanpa validasi.
	 *
	 * @param tanggal_dirubah waktu perubahan terakhir
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan stempel waktu perubahan terakhir, dipetakan sebagai kolom {@code TIMESTAMP}.
	 *
	 * @return waktu perubahan terakhir; tidak pernah {@code null} kecuali sengaja disetel demikian
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Representasi teks singkat entity ini untuk keperluan debug/log/tampilan combobox: gabungan
	 * {@link #getId()} dan {@code nama} mentah (bukan hasil {@link #getNama()} yang di-trim),
	 * dipisah tanda hubung.
	 *
	 * @return gabungan id dan nama mentah entity, mis. {@code "2-Golongan PNS"}
	 */
	public String toString() {
		return id + "-" + nama;
	}

	/** Kode ringkas unit golongan; lihat {@link #getKode()} untuk perilaku trim saat dibaca. */
	private String kode;
	/** Nama unit golongan; lihat {@link #getNama()} untuk perilaku trim saat dibaca. */
	private String nama;
	/** Keterangan bebas unit golongan ini; lihat {@link #getKeterangan()}. */
	private String keterangan;
	/**
	 * Daftar (CSV) primary key {@link SkorGolongan} yang relevan untuk unit golongan ini; lihat
	 * javadoc kelas dan {@link #getSkor()} untuk rincian pemakaiannya.
	 */
	private String skor;
	/** Status aktif/nonaktif baris ini; lihat {@link #getAktif()} untuk perilaku default saat {@code null}. */
	private Boolean aktif;

	/**
	 * Constructor default tanpa argumen, dibutuhkan Hibernate untuk membuat instance saat hidrasi
	 * entity dari hasil query.
	 */
	public UnitGolongan() {
	}

	/**
	 * Mengembalikan primary key entity, kolom identity ({@code insertable = false}, digenerasi
	 * database) dari tabel {@code employ.unit_golongan}.
	 *
	 * @return primary key, atau {@code null} bila entity belum pernah disimpan
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menyetel primary key entity. Tanpa validasi; normalnya tidak perlu dipanggil manual karena
	 * kolomnya {@code insertable = false}.
	 *
	 * @param id nilai primary key baru
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Mengembalikan kode ringkas unit golongan, sudah di-trim.
	 *
	 * @return kode yang sudah di-trim, tidak pernah {@code null} (string kosong bila kolom
	 *         {@code null})
	 */
	public String getKode() {
		return kode == null ? "" : kode.trim();
	}

	/**
	 * Menyetel kode ringkas unit golongan. Tanpa validasi/trim di sisi setter.
	 *
	 * @param kode kode baru
	 */
	public void setKode(String kode) {
		this.kode = kode;
	}

	/**
	 * Mengembalikan nama unit golongan, sudah di-trim.
	 *
	 * @return nama yang sudah di-trim, atau {@code null} bila kolom {@code null}
	 */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Menyetel nama unit golongan. Tanpa validasi/trim di sisi setter; kolom dipetakan
	 * {@code nullable = false} sehingga validasi wajib-isi ditegakkan di layar CRUD.
	 *
	 * @param nama nama baru
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Mengembalikan keterangan bebas unit golongan ini, apa adanya tanpa trim.
	 *
	 * @return keterangan, boleh {@code null}
	 */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * Menyetel keterangan bebas unit golongan ini.
	 *
	 * @param keterangan keterangan baru; boleh {@code null}
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * Mengembalikan status aktif/nonaktif unit golongan ini, dengan default {@code true} saat
	 * kolom database masih {@code null}.
	 *
	 * @return {@code true} bila aktif atau kolom {@code null}; {@code false} hanya bila kolom
	 *         eksplisit berisi {@code false}
	 */
	public Boolean getAktif() {
		return aktif == null ? true : aktif;
	}

	/**
	 * Menyetel status aktif/nonaktif unit golongan ini. Tanpa validasi.
	 *
	 * @param aktif status aktif baru; {@code null} akan kembali dibaca sebagai {@code true}
	 */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}

	/**
	 * Mengembalikan daftar (CSV) primary key {@link SkorGolongan} yang relevan untuk unit golongan
	 * ini, dengan perilaku GETTER DESTRUKTIF RINGAN: bila field masih {@code null}, method
	 * MENULISNYA MENJADI string kosong ({@code ""}) sebelum mengembalikan — memastikan pemanggil
	 * (mis. {@code unitGolongan.getSkor().split(",")} di {@code UnitGolonganAction}) selalu
	 * menerima string non-null yang aman dipanggil {@code split(...)} tanpa
	 * {@code NullPointerException}, dengan konsekuensi entity yang baru dibuat dan belum pernah
	 * disimpan akan punya field {@code skor} berisi {@code ""} (bukan lagi {@code null}) begitu
	 * getter ini sempat dipanggil sekali, walau belum ada perubahan data yang disengaja pengguna.
	 * TIDAK ada normalisasi format CSV lain di sini (berbeda dari {@link
	 * JenisJabatan#getJenisPengguna()}/{@link JenisJabatan#getUsernamePengguna()} yang membungkus
	 * dan mereduksi koma) — string dikembalikan apa adanya begitu bukan {@code null}.
	 *
	 * @return daftar id {@link SkorGolongan} berpemisah koma, tidak pernah {@code null} (string
	 *         kosong bila kolom {@code null})
	 */
	@Column(name = "skor", nullable = true, columnDefinition = "text")
	public String getSkor() {

		if (skor == null) {
			skor = "";
		}

		return skor;
	}

	/**
	 * Menyetel daftar (CSV) primary key {@link SkorGolongan} yang relevan untuk unit golongan ini.
	 * Tanpa validasi format maupun keberadaan id yang dirujuk.
	 *
	 * @param skor daftar id {@link SkorGolongan} berpemisah koma; boleh {@code null}
	 */
	public void setSkor(String skor) {
		this.skor = skor;
	}

}
