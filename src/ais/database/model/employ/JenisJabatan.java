package ais.database.model.employ;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

import ais.database.model.GeneralValueObject;

/**
 * Bank generated by hbm2java
 *
 * <p>Entity master data kepegawaian (tabel {@code employ.jenis_jabatan}) yang mengelompokkan
 * jabatan-jabatan (mis. {@link JabatanFungsional}) ke dalam kategori/grup jabatan yang lebih
 * tinggi (mis. "Fungsional Dosen", "Fungsional Umum", "Pejabat Struktural"). Javadoc lama "Bank
 * generated by hbm2java" adalah sisa salin-tempel generator hbm2java dari kelas lain (kelas ini
 * tidak berhubungan dengan entity Bank) — dibiarkan apa adanya sesuai aturan "jangan mengurangi
 * javadoc lama", diperkaya dengan javadoc ini.</p>
 *
 * <h2>Field {@code jenisPengguna}/{@code usernamePengguna}: filter akses, bukan tunjangan</h2>
 * <p>Berbeda dari {@link JabatanStruktural}/{@link JabatanFungsional}, entity ini TIDAK menyimpan
 * riwayat tunjangan dan TIDAK punya method {@code ambilTunjangan}/{@code ambilTunjangans} — kelas
 * ini murni klasifikasi/pengelompokan. Ia justru membawa dua field CSV pembatas akses,
 * {@link #getJenisPengguna()} dan {@link #getUsernamePengguna()}, dengan pola normalisasi dan
 * INTERAKSI ANTAR-FIELD YANG DESTRUKTIF — lihat javadoc kedua getter tersebut untuk rincian
 * lengkap perilaku ini, termasuk fakta bahwa membaca {@link #getUsernamePengguna()} dapat
 * MENGHAPUS PERMANEN nilai field tersebut bila {@link #getJenisPengguna()} kebetulan terisi.
 * Penelusuran kode sumber menunjukkan kedua field ini SAAT INI hanya dibaca/ditulis di layar
 * CRUD-nya sendiri ({@code ais.action.master.employ.JenisJabatanAction}) — belum ditemukan jalur
 * bisnis lain (mis. otorisasi surat/disposisi) yang mengonsumsi nilai
 * {@code jenisJabatan.getJenisPengguna()}/{@code jenisJabatan.getUsernamePengguna()} secara
 * langsung, berbeda dari field bernama serupa milik kelas-kelas lain yang tidak berkerabat (mis.
 * {@code AktorSop}, {@code Pejabat}, {@code OpsiSuratMasuk}) yang memang dikonsumsi jalur alur
 * surat/SOP. VERIFIKASI ulang sebelum mengandalkan kedua field ini sebagai mekanisme otorisasi
 * aktif di luar layar CRUD.</p>
 *
 * <h2>Field {@code grup}</h2>
 * <p>{@link #getGrup()} dikonsumsi jalur normalisasi grup disposisi surat masuk/keluar
 * ({@code SuratMasukAction}/{@code SuratKeluarAction}, method {@code normalisasiGrupDisposisi}),
 * menjadikan entity ini titik rujuk pengelompokan pejabat penerima disposisi surat — jalur
 * konsumsi nyata yang berbeda dari field {@code jenisPengguna}/{@code usernamePengguna} di atas.</p>
 *
 * <p>Layar CRUD-nya adalah {@code JenisJabatanAction}. Entity ini tidak membawa filter
 * tenant/satuan-kerja eksplisit di levelnya sendiri.</p>
 */
@Entity
@org.hibernate.annotations.Entity(dynamicInsert = true, dynamicUpdate = true)
@Audited
@Table(schema = "employ", name = "jenis_jabatan")
public class JenisJabatan extends GeneralValueObject {

	/**
	 * Versi serialisasi entity ini untuk kompatibilitas Java serialization. Object entity AIS
	 * diserialkan ke cache MapDB dan session ZK, sehingga nilai ini jangan diubah tanpa alasan
	 * kuat — mengubahnya membuat data ter-cache lama tidak terbaca lagi
	 * ({@code InvalidClassException}) setelah deploy.
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	/** Primary key entity. Lihat {@link #getId()}. */
	private Long id;
	/**
	 * Nama pengguna terakhir yang mengubah entity ini. Field ini meng-hide field privat bernama
	 * sama di {@link GeneralValueObject}: karena kelas induk tersebut bukan
	 * {@code @Entity}/{@code @MappedSuperclass}, field yang dideklarasikannya tidak otomatis
	 * dipetakan Hibernate — setiap entity turunan wajib mendeklarasikan ulang field beserta
	 * getter/setter-nya sendiri agar benar-benar tersimpan sebagai kolom. Pola ini berulang di
	 * hampir seluruh entity AIS (kebutuhan teknis, bukan bug). Diisi otomatis lewat
	 * {@link #onUpdate()} setiap kali entity di-{@code UPDATE}.
	 */
	private String oleh;
	/**
	 * Id pengguna terakhir yang mengubah entity ini. Sama seperti {@link #oleh}, field ini
	 * meng-hide field privat bernama sama di {@link GeneralValueObject} demi pemetaan Hibernate.
	 * Diisi otomatis lewat {@link #onUpdate()}.
	 */
	private String olehId;

	/**
	 * Mengembalikan id pengguna yang terakhir mengubah entity ini.
	 *
	 * @return id pengguna pengubah terakhir, atau {@code null} bila belum pernah terisi
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menyetel id pengguna pengubah terakhir, dengan validasi non-trivial: nilai {@code null} atau
	 * string kosong/hanya-spasi diabaikan diam-diam. Konsekuensinya nilai lama tidak pernah bisa
	 * dihapus lewat setter ini — perilaku ini disengaja agar jejak audit yang sudah terisi tidak
	 * terhapus oleh jalur simpan yang kebetulan tidak membawa informasi pengguna.
	 *
	 * @param olehId id pengguna pengubah; diabaikan bila {@code null}/kosong
	 */
	public void setOlehId(String olehId) {
		if (olehId == null || olehId.trim().isEmpty()) {
			return;
		}
		this.olehId = olehId;
	}

	/**
	 * Menyetel nama pengguna pengubah terakhir, dengan validasi non-trivial yang sama seperti
	 * {@link #setOlehId(String)}: nilai {@code null}/kosong diabaikan diam-diam.
	 *
	 * @param oleh nama pengguna pengubah; diabaikan bila {@code null}/kosong
	 */
	public void setOleh(String oleh) {
		if (oleh == null || oleh.trim().isEmpty()) {
			return;
		}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan nama pengguna yang terakhir mengubah entity ini.
	 *
	 * @return nama pengguna pengubah terakhir, atau {@code null} bila belum pernah terisi
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Hook siklus-hidup JPA {@code @PreUpdate}: dipanggil otomatis oleh provider persistence tepat
	 * sebelum statement {@code UPDATE} dikirim ke database setiap kali entity ini diubah dan
	 * disimpan lewat session Hibernate yang aktif. Satu-satunya isi method ini adalah delegasi ke
	 * {@code ais.database.hibernate.AuditTimestampInterceptor.ubah(this)}, pola audit-timestamp
	 * standar yang diulang di hampir seluruh entity AIS. Efeknya: {@link #setTanggal_dirubah}
	 * diisi waktu server saat ini, dan {@link #setOleh}/{@link #setOlehId} diisi identitas
	 * pengguna yang sedang login.
	 */
	@javax.persistence.PreUpdate
	protected void onUpdate() {
		ais.database.hibernate.AuditTimestampInterceptor.ubah(this);
	}

	/**
	 * Stempel waktu perubahan terakhir. Diinisialisasi ke waktu pembuatan object memakai
	 * {@code WaktuUtil.getDate()} (jam server aplikasi, bukan jam database). Field ini juga
	 * meng-hide field bernama sama di {@link GeneralValueObject}.
	 */
	private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menyetel stempel waktu perubahan terakhir. Tanpa validasi.
	 *
	 * @param tanggal_dirubah waktu perubahan terakhir
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan stempel waktu perubahan terakhir, dipetakan sebagai kolom {@code TIMESTAMP}.
	 *
	 * @return waktu perubahan terakhir; tidak pernah {@code null} kecuali sengaja disetel demikian
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Representasi teks singkat entity ini untuk keperluan debug/log/tampilan combobox: gabungan
	 * {@link #getId()} dan {@code nama} mentah (bukan hasil {@link #getNama()} yang di-trim),
	 * dipisah tanda hubung.
	 *
	 * @return gabungan id dan nama mentah entity, mis. {@code "5-Fungsional Dosen"}
	 */
	public String toString() {
		return id + "-" + nama;
	}

	/** Kode ringkas jenis jabatan; lihat {@link #getKode()} (TANPA perilaku generate otomatis, berbeda dari {@link JabatanStruktural#getKode()}). */
	private String kode;
	/** Nama jenis jabatan; lihat {@link #getNama()} untuk perilaku trim saat dibaca. */
	private String nama;
	/** Keterangan bebas jenis jabatan ini; lihat {@link #getKeterangan()}. */
	private String keterangan;
	/**
	 * Kunci teks turunan dari {@link #nama} (slug huruf kecil, spasi diganti {@code _}), DIHITUNG
	 * ULANG SETIAP KALI {@link #getKey()} dipanggil bila {@link #nama} terisi. Lihat javadoc
	 * {@link #getKey()}.
	 */
	private String key;
	/** Status aktif/nonaktif baris ini; lihat {@link #getAktif()} untuk perilaku default saat {@code null}. */
	private Boolean aktif;
	/** Nomor urut tampil jenis jabatan ini; lihat {@link #getNomorUrut()} untuk perilaku default saat {@code null}. */
	private Integer nomorUrut;
	/** Grup pengelompokan jenis jabatan; lihat {@link #getGrup()} untuk perilaku default dan konsumsinya oleh normalisasi grup disposisi surat. */
	private String grup;
//	private Boolean bukanJabatan;
	/**
	 * Daftar (CSV, dibungkus koma) username pengguna yang boleh memakai jenis jabatan ini. Lihat
	 * javadoc {@link #getUsernamePengguna()} — TERMASUK efek samping destruktif field ini bisa
	 * di-null-kan otomatis bila {@link #jenisPengguna} terisi.
	 */
	private String usernamePengguna;
	/**
	 * Daftar (CSV, dibungkus koma) jenis pengguna yang boleh memakai jenis jabatan ini. Lihat
	 * javadoc {@link #getJenisPengguna()}.
	 */
	private String jenisPengguna;

	/**
	 * Constructor default tanpa argumen, dibutuhkan Hibernate untuk membuat instance saat hidrasi
	 * entity dari hasil query.
	 */
	public JenisJabatan() {
	}

	/**
	 * Mengembalikan primary key entity, kolom identity ({@code insertable = false}, digenerasi
	 * database) dari tabel {@code employ.jenis_jabatan}.
	 *
	 * @return primary key, atau {@code null} bila entity belum pernah disimpan
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menyetel primary key entity. Tanpa validasi; normalnya tidak perlu dipanggil manual karena
	 * kolomnya {@code insertable = false}.
	 *
	 * @param id nilai primary key baru
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Mengembalikan nama jenis jabatan, sudah di-trim.
	 *
	 * @return nama yang sudah di-trim, atau {@code null} bila kolom {@code null}
	 */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Menyetel nama jenis jabatan. Tanpa validasi/trim di sisi setter; kolom dipetakan
	 * {@code nullable = false} sehingga validasi wajib-isi ditegakkan di layar CRUD.
	 *
	 * @param nama nama baru
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Mengembalikan keterangan bebas jenis jabatan ini, apa adanya tanpa trim.
	 *
	 * @return keterangan, boleh {@code null}
	 */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * Menyetel keterangan bebas jenis jabatan ini.
	 *
	 * @param keterangan keterangan baru; boleh {@code null}
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * Mengembalikan kunci teks (slug) turunan dari {@link #nama}, dengan perilaku GETTER
	 * DESTRUKTIF: setiap kali dipanggil DAN {@link #nama} tidak kosong, field {@link #key}
	 * DIHITUNG ULANG dan DITULIS BALIK sebagai {@code nama.trim().toLowerCase().replaceAll(" ",
	 * "_")} — mis. {@code "Fungsional Dosen"} menjadi {@code "fungsional_dosen"}. Bila
	 * {@link #nama} kosong/{@code null}, field {@link #key} TIDAK diubah dan nilai LAMA (bisa jadi
	 * sudah usang/tidak sinkron dengan {@link #nama} terkini) tetap dikembalikan apa adanya —
	 * berbeda dari pola getter destruktif lain di codebase AIS yang biasanya membangkitkan nilai
	 * baru hanya sekali saat field masih kosong (mis. {@link JabatanStruktural#getKode()}); di
	 * sini penghitungan ulang terjadi pada SETIAP pemanggilan selama {@link #nama} terisi, sehingga
	 * {@link #key} selalu mengikuti perubahan {@link #nama} terbaru tanpa perlu simpan ulang
	 * eksplisit — asalkan getter ini sempat dipanggil sebelum entity berikutnya di-{@code UPDATE}.
	 * Baris {@code System.out.println} debug pada kode asli dikomentari (bukan dihapus), sesuai
	 * kode sumber.
	 *
	 * @return kunci slug dari nama, atau nilai {@link #key} lama (mungkin usang) bila {@link #nama}
	 *         kosong/{@code null}
	 */
	public String getKey() {
		boolean ada = nama != null && !nama.trim().equals("");
		if (ada) {
			key = nama.trim().toLowerCase().replaceAll(" ", "_");
		}
//		System.out.println("key = " + key + ", nama = " + nama + ", ada = " + ada);
		return key;
	}

	/**
	 * Menyetel kunci slug secara manual. Nilai ini akan DITIMPA oleh {@link #getKey()} pada
	 * pemanggilan berikutnya selama {@link #nama} tidak kosong — lihat javadoc {@link #getKey()}.
	 *
	 * @param key kunci slug baru
	 */
	public void setKey(String key) {
		this.key = key;
	}

	/**
	 * Mengembalikan status aktif/nonaktif jenis jabatan ini, dengan default {@code true} saat
	 * kolom database masih {@code null}.
	 *
	 * @return {@code true} bila aktif atau kolom {@code null}; {@code false} hanya bila kolom
	 *         eksplisit berisi {@code false}
	 */
	public Boolean getAktif() {
		return aktif == null ? true : aktif;
	}

	/**
	 * Menyetel status aktif/nonaktif jenis jabatan ini. Tanpa validasi.
	 *
	 * @param aktif status aktif baru; {@code null} akan kembali dibaca sebagai {@code true}
	 */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}

	/**
	 * Mengembalikan nomor urut tampil jenis jabatan ini, dengan default {@code 1} saat kolom
	 * database masih {@code null}.
	 *
	 * @return nomor urut, {@code 1} bila kolom {@code null}
	 */
	public Integer getNomorUrut() {
		return nomorUrut == null ? 1 : nomorUrut;
	}

	/**
	 * Menyetel nomor urut tampil jenis jabatan ini. Tanpa validasi (tidak menolak duplikat/nilai
	 * negatif).
	 *
	 * @param nomorUrut nomor urut baru; boleh {@code null}
	 */
	public void setNomorUrut(Integer nomorUrut) {
		this.nomorUrut = nomorUrut;
	}

	/**
	 * Mengembalikan grup pengelompokan jenis jabatan ini, dengan default {@code "Pejabat"} saat
	 * kolom database masih {@code null}. Field ini dikonsumsi jalur normalisasi grup disposisi
	 * surat masuk/keluar ({@code SuratMasukAction}/{@code SuratKeluarAction}, method
	 * {@code normalisasiGrupDisposisi(jenisJabatan.getGrup())}) — lihat dokumentasi kelas.
	 *
	 * @return grup jenis jabatan, {@code "Pejabat"} bila kolom {@code null}
	 */
	public String getGrup() {
		return grup == null ? "Pejabat" : grup;
	}

	/**
	 * Menyetel grup pengelompokan jenis jabatan ini. Tanpa validasi terhadap nilai grup yang
	 * dikenal normalisasi grup disposisi surat.
	 *
	 * @param grup grup baru; {@code null} akan kembali dibaca sebagai {@code "Pejabat"}
	 */
	public void setGrup(String grup) {
		this.grup = grup;
	}

	/**
	 * Mengembalikan daftar jenis pengguna yang boleh memakai jenis jabatan ini, dengan perilaku
	 * GETTER DESTRUKTIF: setiap kali dipanggil, field {@link #jenisPengguna} DINORMALISASI dan
	 * DITULIS BALIK ke instance sebelum dikembalikan.
	 *
	 * <p>Langkah normalisasi: (1) bila kolom {@code null} atau (setelah trim, case-insensitive)
	 * sama dengan {@code ","} tunggal, mulai dari string kosong; selain itu bungkus nilai trim
	 * dengan koma di kedua sisi ({@code "," + nilai.trim() + ","}); (2) jalankan
	 * {@code replaceAll(",,", ",")} SEBANYAK TIGA KALI berturut-turut untuk meruntuhkan koma ganda
	 * berulang menjadi satu (tiga kali cukup untuk sampai empat koma beruntun tereduksi total,
	 * tetapi TIDAK menjamin tereduksi tuntas untuk deretan koma yang jauh lebih panjang — pola
	 * yang sama, dengan keterbatasan yang sama, dipakai {@link #getUsernamePengguna()}); (3)
	 * setelah reduksi, bila hasilnya PERSIS {@code ","}, {@code ",,"}, {@code ",,,"}, atau
	 * {@code ",,,,"}, nilai diset ulang jadi string kosong (menangani kasus tepi di mana isi asli
	 * hanya kumpulan koma tanpa data); (4) kasus khusus tambahan: bila hasilnya persis
	 * {@code ",-,"} (satu entri berisi tanda hubung tunggal — konvensi sentinel "kosongkan"),
	 * nilai juga diset jadi string kosong.</p>
	 *
	 * <p>Hasil akhir: {@code null} dikembalikan bila string ternormalisasi kosong/hanya-spasi;
	 * selain itu dikembalikan hasil trim dari string ternormalisasi (yang sudah dibungkus koma di
	 * kedua sisi, mis. {@code ",dosen,tendik,"}) — format terbungkus-koma ini memudahkan
	 * pencocokan {@code contains(",nilai,")} yang aman dari false-positive substring parsial di
	 * pemanggil.</p>
	 *
	 * @return daftar jenis pengguna terbungkus koma, atau {@code null} bila kosong
	 */
	@Column(name = "jenis_pengguna", nullable = true, columnDefinition = "text")
	public String getJenisPengguna() {

		jenisPengguna = (jenisPengguna == null || jenisPengguna.trim().equalsIgnoreCase(",") ? ""
				: "," + jenisPengguna.trim() + ",").replaceAll(",,", ",").replaceAll(",,", ",").replaceAll(",,", ",");

		if (jenisPengguna.equals(",")) {
			jenisPengguna = "";
		} else if (jenisPengguna.equals(",,")) {
			jenisPengguna = "";
		} else if (jenisPengguna.equals(",,,")) {
			jenisPengguna = "";
		} else if (jenisPengguna.equals(",,,,")) {
			jenisPengguna = "";
		}

		if (jenisPengguna.equals(",-,")) {
			jenisPengguna = "";
		}

		return jenisPengguna == null || jenisPengguna.trim().isEmpty() ? null : jenisPengguna.trim();
	}

	/**
	 * Menyetel daftar jenis pengguna secara manual. Tanpa validasi format; normalisasi baru
	 * terjadi saat {@link #getJenisPengguna()} berikutnya dipanggil.
	 *
	 * @param jenisPengguna daftar jenis pengguna baru (format bebas, akan dinormalisasi saat dibaca)
	 */
	public void setJenisPengguna(String jenisPengguna) {
		this.jenisPengguna = jenisPengguna;
	}

	/**
	 * Mengembalikan daftar username pengguna yang boleh memakai jenis jabatan ini, dengan perilaku
	 * GETTER DESTRUKTIF ganda: selain normalisasi format CSV-terbungkus-koma yang identik dengan
	 * {@link #getJenisPengguna()} (lihat javadoc method tersebut untuk rincian langkah reduksi
	 * koma dan penanganan sentinel {@code ",-,"}), method ini membawa EFEK SAMPING LINTAS-FIELD
	 * YANG DESTRUKTIF: di akhir, sebelum mengembalikan hasil, method memeriksa
	 * {@code getJenisPengguna() != null} — BILA jenis pengguna kebetulan TERISI (tidak kosong),
	 * field {@link #usernamePengguna} LANGSUNG DITIMPA MENJADI {@code null} SEBELUM dikembalikan,
	 * TERLEPAS dari apa pun nilai yang sebelumnya tersimpan di kolom database. Dengan kata lain,
	 * memanggil getter ini pada entity yang {@code jenisPengguna}-nya terisi akan MENGHAPUS nilai
	 * {@code usernamePengguna} dari state in-memory entity; bila entity tersebut kemudian ikut
	 * di-{@code UPDATE} lewat sesi Hibernate yang melacak perubahan, penghapusan ini akan
	 * TERSIMPAN PERMANEN ke database — padahal pemanggil getter mungkin hanya bermaksud MEMBACA
	 * nilai, bukan mengubahnya. Ini menegakkan aturan bisnis implisit "jenis pengguna dan username
	 * pengguna saling eksklusif" (hanya satu yang boleh aktif) TETAPI menegakkannya lewat efek
	 * samping getter, bukan lewat validasi eksplisit di setter/layar simpan — pola arsitektur
	 * "getter destruktif" yang berulang di banyak entity AIS, di sini bahkan MELINTASI DUA FIELD
	 * berbeda alih-alih hanya menormalisasi fieldnya sendiri.
	 *
	 * @return daftar username pengguna terbungkus koma, atau {@code null} bila kosong ATAU bila
	 *         {@link #getJenisPengguna()} kebetulan terisi (lihat efek samping di atas)
	 */
	@Column(name = "username_pengguna", nullable = true, columnDefinition = "text")
	public String getUsernamePengguna() {

		usernamePengguna = (usernamePengguna == null || usernamePengguna.trim().equalsIgnoreCase(",") ? ""
				: "," + usernamePengguna.trim() + ",").replaceAll(",,", ",").replaceAll(",,", ",")
				.replaceAll(",,", ",");

		if (usernamePengguna.equals(",")) {
			usernamePengguna = "";
		} else if (usernamePengguna.equals(",,")) {
			usernamePengguna = "";
		} else if (usernamePengguna.equals(",,,")) {
			usernamePengguna = "";
		} else if (usernamePengguna.equals(",,,,")) {
			usernamePengguna = "";
		}

		if (usernamePengguna.equals(",-,")) {
			usernamePengguna = "";
		}

		if (getJenisPengguna() != null) {
			usernamePengguna = null;
		}

		return usernamePengguna == null || usernamePengguna.trim().isEmpty() ? null : usernamePengguna.trim();
	}

	/**
	 * Menyetel daftar username pengguna secara manual. Tanpa validasi format. Perhatikan bahwa
	 * nilai yang disetel di sini bisa langsung "hilang" pada pembacaan berikutnya lewat
	 * {@link #getUsernamePengguna()} bila {@link #jenisPengguna} kebetulan terisi — lihat javadoc
	 * getter tersebut.
	 *
	 * @param usernamePengguna daftar username pengguna baru (format bebas, akan dinormalisasi saat
	 *                         dibaca, dan berpotensi di-null-kan oleh {@link #getUsernamePengguna()})
	 */
	public void setUsernamePengguna(String usernamePengguna) {
		this.usernamePengguna = usernamePengguna;
	}

	/**
	 * Mengembalikan kode ringkas jenis jabatan apa adanya, TANPA perilaku generate otomatis
	 * (berbeda dari {@link JabatanStruktural#getKode()}/{@link JabatanFungsional#getKode()} yang
	 * membangkitkan kode sintetis dari id saat kosong) dan TANPA trim.
	 *
	 * @return kode jenis jabatan, boleh {@code null}
	 */
	public String getKode() {
		return kode;
	}

	/**
	 * Menyetel kode ringkas jenis jabatan. Tanpa validasi.
	 *
	 * @param kode kode baru; boleh {@code null}
	 */
	public void setKode(String kode) {
		this.kode = kode;
	}

}
