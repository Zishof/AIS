package ais.database.model;

// Generated Dec 22, 2009 9:02:38 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

/**
 * StatusMahasiswa generated by hbm2java
 *
 * <p><b>Master status keaktifan kerja pegawai/dosen/guru</b> (mis. "Aktif", "Pensiun",
 * "Meninggal Dunia") &mdash; satu baris tabel {@code public.status_pegawai} mewakili satu nilai
 * status. Dipakai langsung oleh {@link Pegawai#getStatusPegawai()}, yang men-default-kan
 * {@code null} menjadi konstanta {@code ais.common.ConstantValues.AKTIF_PEGAWAI} dan memakai
 * kecocokan nama {@code "Pensiun"} (case-insensitive) untuk menandai pegawai pensiun (lihat baris
 * ~3599 pada {@link Pegawai}) &mdash; sehingga nilai literal "Pensiun" pada baris master ini
 * bersifat magic string yang dirujuk kode, bukan sekadar label bebas.</p>
 *
 * <h2>Berbeda dari {@link StatusKepegawaian} (diverifikasi dari kode)</h2>
 * <p>Entity ini adalah <b>sumbu independen</b> dari {@link StatusKepegawaian}: {@link Pegawai}
 * memiliki field {@code statusPegawai} dan {@code statusKepegawaian} sekaligus, masing-masing
 * dengan getter/setter sendiri yang tidak saling menurunkan nilai. {@code StatusPegawai} (kelas
 * ini) menjawab "apakah pegawai ini masih aktif bekerja atau sudah pensiun/meninggal", sedangkan
 * {@link StatusKepegawaian} menjawab "apa jenis ikatan kerjanya" (PNS/kontrak/honorer). Keduanya
 * dapat diisi bersamaan pada satu pegawai yang sama.</p>
 *
 * <h2>Peta anggota</h2>
 * <ul>
 *   <li><b>Identitas &amp; audit</b>: {@link #getId()}/{@link #setId(Long)},
 *       {@link #getOleh()}/{@link #setOleh(String)}, {@link #getOlehId()}/{@link #setOlehId(String)},
 *       {@link #getTanggal_dirubah()}/{@link #setTanggal_dirubah(Date)}, {@link #onUpdate()}.</li>
 *   <li><b>Deskriptif</b>: {@link #getNama()}/{@link #setNama(String)} (wajib, dirujuk oleh
 *       {@link Pegawai} lewat kecocokan literal "Pensiun"), {@link #getKeterangan()}/
 *       {@link #setKeterangan(String)}.</li>
 *   <li><b>Integrasi &amp; status</b>: {@link #getFeeder()}/{@link #setFeeder(String)} (kode
 *       pemetaan ke sistem feeder eksternal), {@link #getAktif()}/{@link #setAktif(Boolean)}
 *       (default {@code true} bila belum pernah diisi).</li>
 * </ul>
 *
 * @see StatusKepegawaian
 * @see Pegawai#getStatusPegawai()
 * @see GeneralValueObject
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "public", name = "status_pegawai")
public class StatusPegawai extends GeneralValueObject {

	/**
	 * Penanda versi serialisasi Java. Tidak berdampak fungsional.
	 */
	private static final long serialVersionUID = 6891282957373067916L;

	/** Kunci utama, lihat {@link #getId()}. */
	private Long id;

	/** Nama pengguna pengubah terakhir, lihat {@link #getOleh()}. */
	private String oleh;

	/** Id pengguna pengubah terakhir, lihat {@link #getOlehId()}. */
	private String olehId;

	/**
	 * Id pengguna pengubah terakhir.
	 *
	 * @return id pengguna pengubah terakhir; {@code null} bila belum pernah tercatat.
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menetapkan id pengguna pengubah terakhir. Setter penjaga: nilai {@code null}/kosong diabaikan
	 * diam-diam sehingga jejak audit yang sudah terisi tidak dapat dihapus lewat setter ini.
	 *
	 * @param olehId id pengguna; diabaikan bila {@code null} atau kosong.
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Menetapkan nama pengguna pengubah terakhir. Berperilaku sama dengan
	 * {@link #setOlehId(String)}: nilai {@code null}/kosong diabaikan diam-diam.
	 *
	 * @param oleh nama pengguna; diabaikan bila {@code null} atau kosong.
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Nama pengguna yang terakhir mengubah baris ini.
	 *
	 * @return nama pengguna pengubah terakhir; {@code null} bila belum pernah tercatat.
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Hook JPA {@code @PreUpdate} yang menyegarkan metadata audit sebelum baris ini di-{@code UPDATE},
	 * plus deklarasi field {@code tanggal_dirubah} yang menempel pada baris kode yang sama. Mendelegasikan
	 * ke {@code ais.database.hibernate.AuditTimestampInterceptor.ubah(this)}; dipanggil Hibernate saja.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menetapkan stempel waktu perubahan terakhir. Tidak menjaga terhadap {@code null}.
	 *
	 * @param tanggal_dirubah waktu perubahan terakhir; boleh {@code null}.
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Waktu terakhir baris ini diubah.
	 *
	 * @return waktu perubahan terakhir, dipetakan sebagai {@code TIMESTAMP}.
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Representasi teks baris ini &mdash; hanya {@link #getNama()} (mentah, bukan hasil
	 * {@code trim()}), berbeda dari kebanyakan lookup lain di paket ini yang menyertakan id.
	 *
	 * @return nama status pegawai.
	 */
	public String toString() {
		return nama;
	}

	/** Nama status pegawai, lihat {@link #getNama()}. */
	private String nama;

	/** Keterangan bebas, lihat {@link #getKeterangan()}. */
	private String keterangan;

	/** Kode pemetaan ke sistem feeder eksternal, lihat {@link #getFeeder()}. */
	private String feeder;

	/** Status aktif/nonaktif baris, lihat {@link #getAktif()}. */
	private Boolean aktif;

	/** Konstruktor tanpa argumen yang dibutuhkan Hibernate/JPA. */
	public StatusPegawai() {
	}

	/**
	 * Konstruktor kenyamanan untuk merujuk baris berdasarkan id yang sudah diketahui (mis. sebagai
	 * proxy referensi ringan tanpa memuat seluruh field dari database).
	 *
	 * @param id id baris yang dirujuk.
	 */
	public StatusPegawai(Long id) {
		this.id = id;
	}

	/**
	 * Konstruktor kenyamanan untuk membuat baris dengan nama langsung terisi (mis. saat seeding
	 * data referensi dari {@code InitData} atau perbandingan status seperti "Pensiun").
	 *
	 * @param nama nama status pegawai.
	 */
	public StatusPegawai(String nama) {
		this.nama = nama;
	}

	/**
	 * Kunci utama baris (kolom {@code id}, {@code IDENTITY} &mdash; dibangkitkan database).
	 *
	 * @return id baris; {@code null} selama object belum pernah disimpan.
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menetapkan kunci utama. Praktis hanya dipanggil Hibernate.
	 *
	 * @param id kunci utama baris.
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Nama status pegawai (kolom {@code nama}, wajib). Dikembalikan sudah di-{@code trim()}.
	 * Nilai "Pensiun" (case-insensitive) dirujuk langsung oleh {@link Pegawai#getStatusPegawai()}.
	 *
	 * @return nama; {@code null} bila belum diisi.
	 */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Menetapkan nama status pegawai.
	 *
	 * @param nama nama status.
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Keterangan bebas.
	 *
	 * @return keterangan; {@code null} bila belum diisi.
	 */
	public String getKeterangan() {
		return keterangan;
	}

	/**
	 * Menetapkan keterangan bebas.
	 *
	 * @param keterangan teks keterangan; boleh {@code null}.
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * Kode pemetaan baris ini ke sistem feeder eksternal (bukan kolom terpetakan JPA eksplisit,
	 * hanya field biasa yang dibaca/ditulis proses impor/ekspor feeder).
	 *
	 * @return kode feeder yang sudah di-{@code trim()}; {@code null} bila kosong atau belum diisi.
	 */
	public String getFeeder() {
		return feeder == null || feeder.trim().isEmpty() ? null : feeder.trim();
	}

	/**
	 * Menetapkan kode pemetaan feeder.
	 *
	 * @param feeder kode feeder; boleh {@code null}.
	 */
	public void setFeeder(String feeder) {
		this.feeder = feeder;
	}

	/**
	 * Status aktif/nonaktif baris ini pada layar pemilihan.
	 *
	 * @return {@code true} bila aktif atau belum pernah diisi (default aktif); nilai tersimpan
	 *         apa adanya bila sudah pernah diisi eksplisit.
	 */
	public Boolean getAktif() {
		return aktif == null ? true : aktif;
	}

	/**
	 * Menetapkan status aktif/nonaktif.
	 *
	 * @param aktif status aktif baru; boleh {@code null} (diperlakukan sebagai aktif oleh
	 *              {@link #getAktif()}).
	 */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}
}
