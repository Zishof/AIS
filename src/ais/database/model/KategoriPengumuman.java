package ais.database.model;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.Session;
import org.hibernate.criterion.Restrictions;
import org.hibernate.envers.Audited;

import ais.database.hibernate.HibernateUtil;

/**
 * Entity <b>KATEGORI PENGUMUMAN</b> — kategori pengelompokan pengumuman (dipakai
 * {@code PengumumanAkademis}/{@code PengumumanPerkuliahan} dan sejenisnya), dengan SATU kategori
 * yang diperlakukan istimewa sebagai "pengumuman utama" tampil di halaman depan/beranda. Dipetakan
 * ke tabel {@code public.kategori_pengumuman}.
 *
 * <p>Header berkas ini masih menyisakan komentar asli <i>"Bank generated by hbm2java"</i> dari
 * generator Hibernate Tools — sisa <i>copy-paste</i> template, tidak ada hubungannya dengan entity
 * {@code Bank}.</p>
 *
 * <h2>Peran dalam sistem</h2>
 * <p>{@link #getMerupakanPengumumanUtama() merupakanPengumumanUtama} menandai kategori yang
 * dianggap "utama"; nilainya dicerminkan ke variabel statis {@link #PENGUMUMAN_UTAMA}, yang dibaca
 * secara luas oleh puluhan layar {@code TampilanPengumuman*Action} (Akademis, Perkuliahan, Alumni,
 * PMB, PSB, Karir, Vendor, dsb.) untuk membedakan perlakuan tampilan: pengumuman berkategori utama
 * biasanya ditampilkan menonjol di beranda dan DIKECUALIKAN dari daftar "pengumuman lain". Nilai
 * ini juga dipakai {@code MainAction}/{@code MainAction2} untuk memutuskan konten beranda pengguna.</p>
 *
 * <h2>Mekanisme "get-or-create" singleton {@link #PENGUMUMAN_UTAMA}</h2>
 * <p>{@link #reloadDefault()} dipanggil SEKALI saat inisialisasi aplikasi lewat
 * {@code ais.common.InitData}: mencari baris {@code merupakanPengumumanUtama = true} di database;
 * bila tidak ada, method ini MEMBUAT baris baru ("Pengumuman Utama") lewat transaksi yang dibuka dan
 * ditutup sendiri pada session native, lalu MENUTUP session tersebut
 * ({@code HibernateUtil.closeSession()}) di akhir method — pola yang sama seperti
 * {@link ais.database.model.TemplateFormatBimbingan#createDefaultTemplateFormatBimbingan(Session,
 * ais.database.model.FormatNilaiProposalSkripsi)}, namun di sini hasilnya disimpan ke variabel STATIS
 * KELAS (bukan {@link java.util.Map} per-id) karena hanya ADA SATU kategori utama untuk seluruh
 * instalasi. Karena {@link #PENGUMUMAN_UTAMA} adalah referensi objek Hibernate yang dimuat SEKALI
 * saat startup, perubahan pada baris kategori utama lewat layar CRUD ({@code KategoriPengumumanAction})
 * TIDAK otomatis tercermin ke variabel statis ini sampai {@link #reloadDefault()} dipanggil ulang
 * (mis. lewat <i>restart</i> aplikasi) — nilai {@link #getNomorUrut()}/{@link #getNama()} yang dibaca
 * dari {@link #PENGUMUMAN_UTAMA} di layar lain bisa jadi bukan cerminan TERKINI dari database.</p>
 *
 * @see ais.database.model.TemplateFormatBimbingan
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "public", name = "kategori_pengumuman")
public class KategoriPengumuman extends GeneralValueObject {

	/**
	 * Referensi statis ke satu-satunya baris kategori "pengumuman utama" instalasi ini, dimuat/dibuat
	 * oleh {@link #reloadDefault()} saat inisialisasi aplikasi. {@code null} sebelum
	 * {@link #reloadDefault()} pernah dipanggil. Dibaca luas oleh keluarga layar
	 * {@code TampilanPengumuman*Action} untuk membedakan perlakuan tampilan pengumuman kategori
	 * utama — lihat penjelasan lengkap pada Javadoc kelas.
	 */
	public static KategoriPengumuman PENGUMUMAN_UTAMA = null;

	/**
	 * Memuat ulang {@link #PENGUMUMAN_UTAMA} dari database: mencari baris
	 * {@code merupakanPengumumanUtama = true}, atau MEMBUATNYA ("Pengumuman Utama") bila belum ada
	 * satu pun. Dipanggil sekali saat inisialisasi aplikasi lewat {@code ais.common.InitData}. Method
	 * ini membuka session native sendiri ({@code HibernateUtil.currentNativeSession()}), mengelola
	 * transaksinya sendiri pada jalur pembuatan baris baru, dan MENUTUP session tersebut di akhir
	 * eksekusi — lihat catatan kuirk pada Javadoc kelas mengenai implikasi pemanggilan ulang method
	 * ini di luar konteks startup.
	 */
	public static void reloadDefault() {
		Session session = HibernateUtil.currentNativeSession();
		PENGUMUMAN_UTAMA = (KategoriPengumuman) session.createCriteria(KategoriPengumuman.class)
				.add(Restrictions.eq("merupakanPengumumanUtama", true)).setMaxResults(1).uniqueResult();
		if (PENGUMUMAN_UTAMA == null) {
			PENGUMUMAN_UTAMA = new KategoriPengumuman();
			PENGUMUMAN_UTAMA.setMerupakanPengumumanUtama(true);
			PENGUMUMAN_UTAMA.setNama("Pengumuman Utama");
			PENGUMUMAN_UTAMA.setKeterangan("Pengumuman Utama");
			session.getTransaction().begin();
			session.save(PENGUMUMAN_UTAMA);
			session.getTransaction().commit();
		}
		HibernateUtil.closeSession();
	}

	/**
	 * Versi serialisasi Java untuk kompatibilitas antar build kelas ini.
	 */
	private static final long serialVersionUID = 2463821577548439808L;

	/** Kunci utama, di-<i>generate</i> database ({@code IDENTITY}). */
	private Long id;

	/** Nama pengguna terakhir yang mengubah baris ini; diisi otomatis lewat {@link #onUpdate()}. */
	private String oleh;

	/** Id pengguna terakhir yang mengubah baris ini; diisi otomatis lewat {@link #onUpdate()}. */
	private String olehId;

	/**
	 * Mengembalikan id pengguna pengubah terakhir.
	 *
	 * @return id pengguna pengubah terakhir, atau {@code null} bila belum pernah di-<i>update</i>
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menetapkan id pengguna pengubah terakhir. Nilai {@code null} atau kosong (hanya spasi)
	 * diabaikan diam-diam agar jejak audit tidak bisa dikosongkan lewat pemanggilan ini.
	 *
	 * @param olehId id pengguna pengubah; diabaikan bila {@code null} atau kosong
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Menetapkan nama pengguna pengubah terakhir. Sama seperti {@link #setOlehId(String)}, nilai
	 * {@code null} atau kosong diabaikan.
	 *
	 * @param oleh nama pengguna pengubah; diabaikan bila {@code null} atau kosong
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan nama pengguna terakhir yang mengubah baris ini.
	 *
	 * @return nama pengguna pengubah terakhir, atau {@code null} bila belum pernah di-<i>update</i>
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * <i>Callback</i> JPA {@code @PreUpdate}: mendelegasikan ke
	 * {@code ais.database.hibernate.AuditTimestampInterceptor.ubah(this)} untuk mengisi
	 * {@link #setTanggal_dirubah(Date)}, {@link #setOleh(String)}, {@link #setOlehId(String)} dari
	 * konteks pengguna aktif sebelum baris ini di-{@code UPDATE}.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menetapkan waktu perubahan terakhir baris ini.
	 *
	 * @param tanggal_dirubah waktu perubahan
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan waktu perubahan terakhir baris ini.
	 *
	 * @return waktu perubahan terakhir
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Representasi teks baris: {@code id-nama}.
	 *
	 * @return teks ringkas berisi id dan nama kategori
	 */
	public String toString() {
		return id + "-" + nama;
	}

	/** Nama tampilan kategori dalam bahasa default (Indonesia). Wajib diisi, maksimal 255 karakter. */
	private String nama;

	/** Nama tampilan kategori dalam Bahasa Inggris; lihat kuirk pada {@link #getNamaEn()}. */
	private String namaEn;

	/** Keterangan/deskripsi bebas kategori. */
	private String keterangan;

	/** Nomor urut tampilan kategori; lihat kuirk pada {@link #getNomorUrut()}. */
	private Integer nomorUrut;

	/** Menandai kategori ini sebagai "pengumuman utama" tunggal instalasi; lihat {@link #PENGUMUMAN_UTAMA}. */
	private Boolean merupakanPengumumanUtama;

	/**
	 * Konstruktor kosong yang diwajibkan Hibernate/JPA.
	 */
	public KategoriPengumuman() {
	}

	/**
	 * Mengembalikan kunci utama baris. Kolom {@code id} bersifat {@code insertable = false} karena
	 * nilainya dihasilkan database ({@code IDENTITY}).
	 *
	 * @return id baris, atau {@code null} bila baris belum disimpan
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menetapkan kunci utama. Normalnya hanya dipanggil Hibernate setelah {@code INSERT}.
	 *
	 * @param id kunci utama baris
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Mengembalikan nama kategori dalam bahasa default, sudah di-{@code trim}. Kolom wajib diisi,
	 * maksimal 255 karakter.
	 *
	 * @return nama kategori tanpa spasi di ujung, atau {@code null} bila belum diisi
	 */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Menetapkan nama kategori dalam bahasa default.
	 *
	 * @param nama nama kategori
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Mengembalikan keterangan/deskripsi bebas kategori.
	 *
	 * @return isi keterangan, bisa {@code null}
	 */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * Menetapkan keterangan/deskripsi bebas.
	 *
	 * @param keterangan teks keterangan
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * Mengembalikan nomor urut tampilan kategori.
	 *
	 * <p><b>Kuirk getter-menulis-balik:</b> bila {@link #getMerupakanPengumumanUtama()} bernilai
	 * {@code true}, field {@code nomorUrut} DIPAKSA menjadi {@link Integer#MIN_VALUE} sebelum
	 * dikembalikan — menjamin kategori utama SELALU berada di urutan paling depan pada pengurutan
	 * mana pun, TERLEPAS dari nilai yang pernah ditetapkan lewat {@link #setNomorUrut(Integer)}.
	 * Karena entity ini memakai akses properti, pada instance yang masih <i>attached</i>, kolom
	 * {@code nomor_urut} bisa ikut ter-<i>flush</i> menjadi {@code Integer.MIN_VALUE} hanya karena
	 * baris ini dibaca. Kolom ini juga dibatasi {@code unique = true} — dua kategori TIDAK boleh
	 * berbagi nomor urut yang sama, dan kuirk ini menjadi satu-satunya cara kategori utama dijamin
	 * tidak bentrok dengan nomor urut kategori lain.</p>
	 *
	 * @return {@link Integer#MIN_VALUE} bila kategori utama, {@code 1} bila belum diisi, atau nilai
	 *         yang ditetapkan sebelumnya
	 */
	@Column(nullable = false, unique = true)
	public Integer getNomorUrut() {
		if (getMerupakanPengumumanUtama()) {
			nomorUrut = Integer.MIN_VALUE;
		}
		return nomorUrut == null ? 1 : nomorUrut;
	}

	/**
	 * Menetapkan nomor urut tampilan kategori.
	 *
	 * @param nomorUrut nomor urut baru
	 */
	public void setNomorUrut(Integer nomorUrut) {
		this.nomorUrut = nomorUrut;
	}

	/**
	 * Mengembalikan status "kategori pengumuman utama". Nilai {@code null} diperlakukan sebagai
	 * {@code false}.
	 *
	 * @return {@code true} bila kategori ini adalah kategori utama, {@code false} bila
	 *         tidak/belum diisi
	 */
	public Boolean getMerupakanPengumumanUtama() {
		return merupakanPengumumanUtama == null ? false : merupakanPengumumanUtama;
	}

	/**
	 * Menetapkan status "kategori pengumuman utama".
	 *
	 * @param merupakanPengumumanUtama status baru
	 */
	public void setMerupakanPengumumanUtama(Boolean merupakanPengumumanUtama) {
		this.merupakanPengumumanUtama = merupakanPengumumanUtama;
	}

	/**
	 * Mengembalikan nama kategori dalam Bahasa Inggris.
	 *
	 * <p><b>Kuirk getter-menulis-balik:</b> bila field {@code null} atau kosong (hanya spasi),
	 * DITIMPA dengan nilai {@link #getNama()} (nama bahasa default) dan ditulis balik ke field
	 * {@code namaEn} — pada instance yang masih <i>attached</i>, kolom {@code nama_en} bisa terisi
	 * permanen dengan salinan {@code nama} hanya karena baris ini dibaca sebelum admin sempat mengisi
	 * terjemahannya secara eksplisit.</p>
	 *
	 * @return nama Bahasa Inggris, atau salinan {@link #getNama()} bila belum pernah diisi
	 */
	public String getNamaEn() {
		if (namaEn == null || namaEn.trim().isEmpty()) {
			namaEn = getNama();
		}
		return namaEn;
	}

	/**
	 * Menetapkan nama kategori dalam Bahasa Inggris.
	 *
	 * @param namaEn nama Bahasa Inggris baru
	 */
	public void setNamaEn(String namaEn) {
		this.namaEn = namaEn;
	}
}
