package ais.database.model;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

/**
 * Bank generated by hbm2java
 *
 * <p><b>Catatan "siapa terakhir login dari mana", berkunci NAMA (bukan id numerik).</b> Berbeda
 * dari {@link OnlineUsers} yang menyimpan snapshot pengguna yang SEDANG online (banyak baris,
 * relasi ke berbagai jenis akun), tabel ini hanya menyimpan SATU baris per nilai {@link #getNama()}
 * -- kolom {@code nama} adalah primary key sekaligus unique. Baris baru dengan nama yang sama akan
 * MENIMPA baris lama (host dan waktu login terbaru), bukan menambah riwayat.</p>
 *
 * <h3>Data yang tersimpan</h3>
 * <p>Hanya {@link #getNama()} (identitas login, apa adanya -- tidak jelas dari kelas ini apakah ini
 * NIM/NIS/username atau nama tampilan, tergantung pemanggil), {@link #getHost()} (alamat/host asal
 * login), dan {@link #getWaktu()} (kapan). Tidak ada token sesi, password, atau data kredensial
 * lain yang tersimpan di sini -- entity ini murni catatan kehadiran/jejak login, bukan penyimpan
 * rahasia otentikasi.</p>
 *
 * <h3>Visibilitas: siapa bisa melihat siapa</h3>
 * <p>Kelas model ini sendiri TIDAK mendeklarasikan kolom kepemilikan/tenant apa pun (tidak ada
 * satuanKerja, sekolah, atau yayasan) -- siapa yang boleh melihat daftar baris di tabel ini
 * sepenuhnya ditentukan oleh action/query pemanggil di luar berkas ini, bukan oleh entity.
 * Pembaca yang perlu memastikan cakupan akses harus menelusuri kelas action yang melakukan query
 * atas {@code AccessedUsers} secara terpisah.</p>
 *
 * @see OnlineUsers
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Table(schema = "public", name = "accessed_users")
public class AccessedUsers extends GeneralValueObject {

	/**
	 *
	 */
	private static final long serialVersionUID = 2463821577548439808L;

	/** Identitas login (primary key). Lihat {@link #getNama()}. */
	private String nama;
	/** Alamat/host asal login terakhir. Lihat {@link #getHost()}. */
	private String host;
	/** Jejak "siapa yang menulis baris ini", diisi otomatis lewat {@link #setOleh(String)}. */
	private String oleh;
	/** Pendamping {@link #oleh}. Lihat {@link #getOlehId()}. */
	private String olehId;

	/**
	 * Mengembalikan rantai jejak pemanggil yang tersimpan bersama {@link #oleh}, diisi otomatis
	 * oleh {@code AuditTimestampInterceptor}.
	 *
	 * @return jejak pemanggil; boleh {@code null} bila belum pernah diisi
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menyetel jejak pemanggil {@link #getOlehId()}. Setter ini <b>mengabaikan diam-diam</b>
	 * nilai {@code null}/kosong sehingga jejak lama tidak dapat ditimpa nilai hampa.
	 *
	 * @param olehId jejak pemanggil baru; {@code null}/kosong diabaikan tanpa peringatan
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/** Waktu login terakhir tercatat, diinisialisasi ke waktu server saat objek dibuat. */
	private Date waktu = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menyetel identitas/nama pelaku penyimpanan baris ini. Sama seperti {@link #setOlehId(String)},
	 * nilai {@code null}/kosong diabaikan diam-diam agar jejak lama tidak tertimpa nilai hampa.
	 *
	 * @param oleh nama/identitas pelaku; {@code null}/kosong diabaikan tanpa peringatan
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan identitas pelaku yang terakhir menyimpan baris ini.
	 *
	 * @return nama/identitas pelaku; boleh {@code null} bila belum pernah diisi
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * <i>Callback</i> JPA {@link javax.persistence.PreUpdate} yang memperbarui
	 * {@link #getTanggal_dirubah()}/{@link #getOleh()}/{@link #getOlehId()} lewat
	 * {@code AuditTimestampInterceptor} pada setiap UPDATE baris.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     /**
	 * Stempel waktu terakhir baris ini diubah (berbeda dari {@link #waktu}, yang menyatakan kapan
	 * pengguna login -- ini menyatakan kapan BARIS DATABASE-nya terakhir disentuh).
	 */
	private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menyetel stempel waktu perubahan terakhir. Umumnya diurus otomatis oleh {@link #onUpdate()}.
	 *
	 * @param tanggal_dirubah stempel waktu baru
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan stempel waktu perubahan terakhir baris ini (presisi TIMESTAMP).
	 *
	 * @return waktu terakhir baris disimpan/diubah
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/** Konstruktor tanpa argumen yang diwajibkan JPA/Hibernate. */
	public AccessedUsers() {
	}

	/**
	 * Mengembalikan identitas login yang menjadi primary key baris ini, dengan spasi tepi
	 * dipangkas. Karena kolom ini {@code unique} dan sekaligus {@link Id}, menyimpan baris baru
	 * dengan {@code nama} yang sudah ada akan MENIMPA baris lama (host &amp; waktu login
	 * sebelumnya hilang), bukan menambah baris riwayat baru.
	 *
	 * @return identitas login yang sudah di-<i>trim</i>; {@code null} bila field belum diisi
	 */
	@Id
	@Column(name = "nama", nullable = false, unique = true, length = 250)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Menyetel identitas login (primary key). Karena berfungsi sebagai id, ubah nilai ini hanya
	 * saat membuat baris baru -- mengubahnya pada baris yang sudah tersimpan akan diperlakukan
	 * Hibernate sebagai identitas berbeda, bukan sebagai pembaruan baris yang sama.
	 *
	 * @param nama identitas login baru
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Menyetel alamat/host asal login terakhir.
	 *
	 * @param host alamat/host; boleh {@code null}
	 */
	public void setHost(String host) {
		this.host = host;
	}

	/**
	 * Mengembalikan alamat/host asal login terakhir.
	 *
	 * @return host; boleh {@code null} bila belum diisi
	 */
	public String getHost() {
		return host;
	}

	/**
	 * Mengembalikan waktu login terakhir tercatat (presisi TIMESTAMP).
	 *
	 * @return waktu login; terisi waktu server saat objek dibuat bila tidak disetel
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getWaktu() {
		return waktu;
	}

	/**
	 * Menyetel waktu login yang tercatat.
	 *
	 * @param waktu waktu login baru
	 */
	public void setWaktu(Date waktu) {
		this.waktu = waktu;
	}

}
