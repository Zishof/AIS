package ais.database.model.library;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;




import org.hibernate.envers.Audited;

import ais.database.model.GeneralValueObject;



/**
 * Master data status <b>siklus terbit/publikasi</b> satu judul koleksi {@link
 * ais.database.model.library.Item} &mdash; berbeda konsep dari {@link StatusItem} (kondisi fisik
 * eksemplar) maupun {@link ItemHasStatus} (riwayat status bertanggal, tidak dipakai nyata).
 *
 * <p><b>Konstanta well-known.</b> {@code ais.action.master.library.util.LibraryUtil.DRAFT} adalah
 * baris "Draft" yang dicari/dibuat otomatis saat aplikasi start (pola
 * <i>find-or-create singleton lookup row</i> yang umum di AIS untuk nilai enum-seperti yang
 * disimpan sebagai tabel, bukan Java enum, supaya bisa ditambah lewat UI tanpa deploy ulang).
 * {@link ais.database.model.library.Item#getStatusTerbitItem()} mengembalikan {@code DRAFT} sebagai
 * default bila field mentahnya {@code null} &mdash; sehingga item baru yang belum pernah disetel
 * status terbitnya dianggap "Draft" secara implisit.</p>
 *
 * <p><b>Snapshot historis.</b> {@link ItemKomentar#getStatusTerbitItemPadaSaatKomentar()} menyimpan
 * <i>salinan referensi</i> ke baris status terbit yang berlaku saat komentar/ulasan dibuat &mdash;
 * bukan status terbit item saat ini. Karena baris {@code StatusTerbitItem} sendiri adalah master
 * data yang jarang berubah maknanya (hanya nama/keterangan yang bisa diedit lewat CRUD), snapshot
 * ini pada praktiknya lebih berguna sebagai jejak "status apa yang terlihat pengunjung saat itu"
 * daripada sebagai riwayat versi yang independen.</p>
 *
 * <p>Tidak ada {@code StatusTerbitItemAction} maupun {@code StatusTerbitItemDao} khusus pada saat
 * dokumentasi ini ditulis &mdash; berbeda dari {@link StatusItem} yang punya action/DAO sendiri.
 * Pengelolaan baris master data ini (bila ada) kemungkinan lewat mekanisme CRUD generik/dinamis,
 * bukan layar khusus. Entity-nya sendiri dipakai luas sebagai <i>referensi baca</i> pada modul
 * repositori/OPAC ({@code LibraryMarcApi}, {@code LibraryItemDetailService},
 * {@code LibraryMemberApi}, {@code PustakaApi}) untuk menyaring item yang sudah "Published"/terbit
 * dari yang masih "Draft".</p>
 *
 * <p>Trio field {@code oleh}/{@code olehId}/{@code tanggal_dirubah} adalah audit shadow standar AIS
 * (kolom biasa yang diperbarui manual lewat {@code onUpdate()}), terpisah dari mekanisme Hibernate
 * Envers yang diaktifkan lewat {@link Audited} pada kelas ini.</p>
 *
 * <p>Bank generated by hbm2java (komentar asli generator, dipertahankan).</p>
 *
 * @see ais.database.model.library.Item#getStatusTerbitItem()
 * @see ItemKomentar#getStatusTerbitItemPadaSaatKomentar()
 * @see StatusItem
 * @see ItemHasStatus
 * @see GeneralValueObject
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "library", name = "status_terbit_item")



public class StatusTerbitItem extends GeneralValueObject {

	/**
	 *
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	/** Primary key baris {@code status_terbit_item}, dibangkitkan basis data (IDENTITY). */
	private Long id;
	/** Username/login pembuat baris (audit shadow, bukan Envers). */
	private String oleh;
	/** Identifier pembuat baris (audit shadow). */
	private String olehId;

	/**
	 * Mengembalikan identifier pembuat baris untuk keperluan audit ringan.
	 *
	 * @return identifier pembuat, bisa {@code null} bila belum pernah disetel.
	 */
	public String getOlehId() {return olehId;}

	/**
	 * Menyetel identifier pembuat baris. Nilai kosong/hanya-spasi/{@code null} diabaikan diam-diam
	 * (nilai lama dipertahankan), sama seperti {@link #setOleh(String)}.
	 *
	 * @param olehId identifier pembuat; diabaikan bila {@code null}/kosong.
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}this.olehId = olehId;}

	/**
	 * Menyetel nama/username pembuat baris.
	 *
	 * <p>Guard kosong-diabaikan: input {@code null}/hanya-spasi tidak pernah menimpa nilai lama.
	 * Pola berulang pada seluruh field audit shadow AIS untuk mencegah proses batch/import yang
	 * lupa mengisi actor tidak sengaja mengosongkan jejak audit yang sudah ada.</p>
	 *
	 * @param oleh nama/username pembuat; diabaikan bila {@code null}/kosong.
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan nama/username pembuat baris.
	 *
	 * @return nama pembuat, bisa {@code null} bila belum pernah disetel.
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Callback JPA {@code @PreUpdate}: memperbarui {@link #tanggal_dirubah} melalui
	 * {@code AuditTimestampInterceptor.ubah(this)} setiap kali baris di-{@code UPDATE}. Dipanggil
	 * otomatis oleh provider JPA/Hibernate.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}
	/** Timestamp perubahan terakhir (audit shadow); default konstruksi objek = waktu saat ini. */
	private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menyetel timestamp perubahan terakhir secara manual. Jarang dipanggil langsung karena
	 * {@link #onUpdate()} sudah menanganinya otomatis pada setiap {@code UPDATE}.
	 *
	 * @param tanggal_dirubah timestamp baru.
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan timestamp perubahan terakhir baris ini.
	 *
	 * @return timestamp perubahan terakhir.
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Representasi string singkat &mdash; nama status terbit (mis. "Draft", "Published"). Dipakai
	 * combobox/label ZK yang menampilkan entity ini langsung tanpa formatter khusus.
	 *
	 * @return {@link #nama}, bisa {@code null} bila belum diisi.
	 */
	public String toString() {
		return nama;
	}

	/** Nama status terbit, mis. "Draft", "Published". Kolom wajib ({@code nullable=false}). */
	private String nama;
	/** Keterangan bebas, opsional. */
	private String keterangan;

	/**
	 * Konstruktor kosong yang dibutuhkan Hibernate untuk instansiasi via refleksi.
	 */
	public StatusTerbitItem() {
	}

	/**
	 * Mengembalikan primary key baris ini.
	 *
	 * @return id baris, atau {@code null} untuk entity yang belum tersimpan.
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menyetel primary key. Normalnya tidak dipanggil manual karena kolom {@code id} bersifat
	 * {@code insertable=false} (dibangkitkan basis data via IDENTITY).
	 *
	 * @param id primary key baris.
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Mengembalikan nama status terbit, di-{@code trim()} setiap pembacaan.
	 *
	 * @return nama ter-trim, atau {@code null} bila field mentah {@code null}.
	 */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Menyetel nama status terbit. Tidak melakukan trim/validasi di sisi setter &mdash; trimming
	 * hanya terjadi di {@link #getNama()}.
	 *
	 * @param nama nama status terbit baru.
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Mengembalikan keterangan bebas untuk baris ini.
	 *
	 * @return keterangan, boleh {@code null}.
	 */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * Menyetel keterangan bebas.
	 *
	 * @param keterangan teks keterangan baru; boleh {@code null}.
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

}
