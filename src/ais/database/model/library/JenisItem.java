package ais.database.model.library;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;




import org.hibernate.envers.Audited;

import ais.database.model.GeneralValueObject;



/**
 * Master data <b>jenis</b> (format/genre) koleksi &mdash; satu dari dua sumbu klasifikasi bebas
 * yang dipasang pada {@link ais.database.model.library.Item}, sumbu satunya adalah {@link
 * TipeItem}. Keduanya <b>bukan sinonim</b>: verifikasi lapangan (bukan asumsi dari nama kelas)
 * menunjukkan pemakaian yang konsisten dan berbeda pada {@code ais.action.master.library.ItemAction}
 * dan {@code ais.action.master.library.util.LibraryUtil}:
 *
 * <ul>
 *   <li>{@code JenisItem} = bentuk/format fisik-konseptual koleksi, mis. "Text" (baris well-known
 *   {@code LibraryUtil.TEXT}, dibuat otomatis saat start bila belum ada). Dipetakan ke metadata
 *   Dublin Core {@code dc.format} saat ekspor ke DSpace ({@code item.getJenisItem().getNama()}
 *   pada {@code ItemAction}).</li>
 *   <li>{@link TipeItem} = jenis <i>karya/terbitan</i>, mis. "Textbook", "Skripsi", "Thesis",
 *   "Disertasi", "Karya Ilmiah", "Jurnal Nasional yang terakreditasi". Dipetakan ke metadata
 *   {@code dc.type}.</li>
 * </ul>
 *
 * <p>Satu {@code Item} punya tepat satu {@code jenisItem} dan tepat satu {@code tipeItem} secara
 * independen &mdash; keduanya boleh dikombinasikan bebas (mis. Skripsi yang jenisnya "Text").
 * Jangan gabungkan dua konsep ini saat menambah fitur baru; keduanya punya tabel, DAO
 * ({@code JenisItemDao}/{@code TipeItemDao}), dan layar admin ({@code JenisItemAction}/
 * {@code TipeItemAction}) terpisah.</p>
 *
 * <p><b>Flag {@link #defaultItem}.</b> Menandai baris yang dipakai sebagai jenis bawaan saat item
 * baru dibuat tanpa jenis eksplisit. Berbeda dari {@link TipeItem#getAktif() TipeItem.aktif} (flag
 * aktif/nonaktif untuk menyaring pilihan combobox) &mdash; {@code JenisItem} tidak punya padanan
 * flag aktif/nonaktif sendiri; ini salah satu contoh dua entity yang tampak paralel tetapi punya
 * set flag konfigurasi yang tidak simetris.</p>
 *
 * <p>Trio field {@code oleh}/{@code olehId}/{@code tanggal_dirubah} adalah audit shadow standar AIS
 * (kolom biasa yang diperbarui manual lewat {@code onUpdate()}), terpisah dari Hibernate Envers
 * yang diaktifkan lewat {@link Audited} pada kelas ini.</p>
 *
 * <p>Bank generated by hbm2java (komentar asli generator, dipertahankan).</p>
 *
 * @see TipeItem
 * @see ais.database.model.library.Item#getJenisItem()
 * @see GeneralValueObject
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "library", name = "jenis_item")



public class JenisItem extends GeneralValueObject {

	/**
	 *
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	/** Primary key baris {@code jenis_item}, dibangkitkan basis data (IDENTITY). */
	private Long id;
	/** Username/login pembuat baris (audit shadow, bukan Envers). */
	private String oleh;
	/** Identifier pembuat baris (audit shadow). */
	private String olehId;

	/**
	 * Mengembalikan identifier pembuat baris untuk keperluan audit ringan.
	 *
	 * @return identifier pembuat, bisa {@code null} bila belum pernah disetel.
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menyetel identifier pembuat baris. Nilai kosong/hanya-spasi/{@code null} diabaikan diam-diam
	 * (nilai lama dipertahankan), sama seperti {@link #setOleh(String)}.
	 *
	 * @param olehId identifier pembuat; diabaikan bila {@code null}/kosong.
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Menyetel nama/username pembuat baris.
	 *
	 * <p>Guard kosong-diabaikan: input {@code null}/hanya-spasi tidak pernah menimpa nilai lama.
	 * Pola berulang pada seluruh field audit shadow AIS.</p>
	 *
	 * @param oleh nama/username pembuat; diabaikan bila {@code null}/kosong.
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan nama/username pembuat baris.
	 *
	 * @return nama pembuat, bisa {@code null} bila belum pernah disetel.
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Callback JPA {@code @PreUpdate}: memperbarui {@link #tanggal_dirubah} melalui
	 * {@code AuditTimestampInterceptor.ubah(this)} setiap kali baris di-{@code UPDATE}. Dipanggil
	 * otomatis oleh provider JPA/Hibernate.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}
	/** Timestamp perubahan terakhir (audit shadow); default konstruksi objek = waktu saat ini. */
	private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menyetel timestamp perubahan terakhir secara manual. Jarang dipanggil langsung karena
	 * {@link #onUpdate()} sudah menanganinya otomatis pada setiap {@code UPDATE}.
	 *
	 * @param tanggal_dirubah timestamp baru.
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan timestamp perubahan terakhir baris ini.
	 *
	 * @return timestamp perubahan terakhir.
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Representasi string singkat &mdash; nama jenis (mis. "Text"). Dipakai combobox/label ZK
	 * yang menampilkan entity ini langsung tanpa formatter khusus.
	 *
	 * @return {@link #nama}, bisa {@code null} bila belum diisi.
	 */
	public String toString() {
		return nama;
	}

	/** Nama jenis/format koleksi, mis. "Text". Kolom wajib ({@code nullable=false}). */
	private String nama;
	/** Keterangan bebas, opsional. */
	private String keterangan;
	/** Flag "jenis bawaan" untuk item baru tanpa jenis eksplisit. Lihat {@link #getDefaultItem()}. */
	private Boolean defaultItem = false;

	/**
	 * Konstruktor kosong yang dibutuhkan Hibernate untuk instansiasi via refleksi.
	 */
	public JenisItem() {
	}

	/**
	 * Mengembalikan primary key baris ini.
	 *
	 * @return id baris, atau {@code null} untuk entity yang belum tersimpan.
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menyetel primary key. Normalnya tidak dipanggil manual karena kolom {@code id} bersifat
	 * {@code insertable=false} (dibangkitkan basis data via IDENTITY).
	 *
	 * @param id primary key baris.
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Mengembalikan nama jenis, di-{@code trim()} setiap pembacaan.
	 *
	 * @return nama ter-trim, atau {@code null} bila field mentah {@code null}.
	 */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Menyetel nama jenis. Tidak melakukan trim/validasi di sisi setter &mdash; trimming hanya
	 * terjadi di {@link #getNama()}.
	 *
	 * @param nama nama jenis baru.
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Mengembalikan keterangan bebas untuk baris ini.
	 *
	 * @return keterangan, boleh {@code null}.
	 */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * Menyetel keterangan bebas.
	 *
	 * @param keterangan teks keterangan baru; boleh {@code null}.
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * Mengembalikan flag "jenis bawaan", dengan default lunak: bila field mentah {@code null}
	 * (baris lama sebelum kolom ini ada, atau hasil deserialisasi tanpa nilai), method ini
	 * menuliskan {@code false} ke field sebelum mengembalikannya sehingga pemanggil berikutnya
	 * selalu melihat nilai boolean non-null yang konsisten.
	 *
	 * @return {@code true} bila baris ini dipakai sebagai jenis bawaan; {@code false} sebagai
	 *         default bila belum pernah disetel.
	 */
	public Boolean getDefaultItem() {
		if (defaultItem == null) {
			defaultItem = false;
		}
		return defaultItem;
	}

	/**
	 * Menyetel flag "jenis bawaan".
	 *
	 * @param defaultItem nilai flag baru; {@code null} akan kembali diganti default {@code false}
	 *                    pada pembacaan berikutnya lewat {@link #getDefaultItem()}.
	 */
	public void setDefaultItem(Boolean defaultItem) {
		this.defaultItem = defaultItem;
	}

}
