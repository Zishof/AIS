package ais.database.model.library;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;




import org.hibernate.envers.Audited;

import ais.database.model.GeneralValueObject;



/**
 * Bank generated by hbm2java
 *
 * <p><b>Master edisi DDC</b> (tabel {@code library.versi_ddc_item}, mis. "DDC Edisi 21", "DDC
 * Edisi 22 Ringkas"). Entity ini murni tabel lookup datar (tanpa pohon/hierarki) yang dirujuk oleh
 * {@link DdcItem#getVersiDdcItem()}: setiap kode DDC ({@link DdcItem}) dapat diskop ke satu edisi
 * tertentu lewat relasi tersebut. Kelas ini sendiri tidak memuat logika untuk memvalidasi bahwa
 * kode-kode di bawah satu edisi konsisten satu sama lain &mdash; itu sepenuhnya tanggung jawab
 * pemanggil.</p>
 *
 * <p><b>Field audit bayangan.</b> {@code oleh}, {@code olehId}, {@code tanggal_dirubah} beserta
 * {@link #onUpdate()} diduplikasi di hampir semua entity karena {@link GeneralValueObject} bukan
 * {@code @Entity}/{@code @MappedSuperclass} sehingga tidak bisa mewariskan pemetaan kolom &mdash;
 * ini keharusan teknis, bukan salinan-tempel yang lalai.</p>
 *
 * @see DdcItem
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "library", name = "versi_ddc_item")



public class VersiDdcItem extends GeneralValueObject {

	/**
	 * Versi serialisasi Java. Nilai ini sengaja sama dengan hampir semua entity hasil generate
	 * hbm2java di aplikasi ini; jangan dijadikan penanda identitas kelas.
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	/** Kunci utama tabel {@code library.versi_ddc_item}, identity/auto-increment DB. */
	private Long id;
	/** Field audit bayangan: nama/identitas pengguna terakhir yang menyentuh baris ini. */
	private String oleh;
	/** Field audit bayangan: ID pengguna terakhir yang menyentuh baris ini (pasangan {@link #oleh}). */
	private String olehId;

	/**
	 * Mengembalikan ID pengguna terakhir yang menyentuh baris ini.
	 *
	 * @return ID pengguna, atau {@code null} bila belum pernah diisi.
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menetapkan ID pengguna penyentuh terakhir, dengan penjaga satu arah: masukan kosong/
	 * {@code null} diabaikan diam-diam sehingga nilai yang sudah terisi tidak bisa dikosongkan
	 * kembali lewat setter ini.
	 *
	 * @param olehId ID pengguna; nilai kosong/{@code null} diabaikan.
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Menetapkan identitas pengguna penyentuh terakhir, dengan penjaga satu arah yang sama seperti
	 * {@link #setOlehId(String)}.
	 *
	 * @param oleh identitas pengguna; nilai kosong/{@code null} diabaikan.
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan identitas pengguna terakhir yang menyentuh baris ini.
	 *
	 * @return identitas pengguna, atau {@code null} bila belum pernah diisi.
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Kait daur hidup JPA yang dijalankan tepat sebelum {@code UPDATE} baris ini, sekaligus deklarasi
	 * field audit bayangan {@link #tanggal_dirubah} pada baris fisik yang sama (peninggalan generator
	 * hbm2java). Mendelegasikan ke {@code AuditTimestampInterceptor.ubah(this)} yang mengisi ulang
	 * {@link #getTanggal_dirubah()} beserta identitas pengguna aktif. Hanya terpicu pada UPDATE, bukan
	 * INSERT.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menetapkan waktu perubahan terakhir secara manual.
	 *
	 * @param tanggal_dirubah waktu perubahan.
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan waktu perubahan terakhir baris ini.
	 *
	 * @return waktu perubahan terakhir; diisi awal saat objek dibuat dan dimutakhirkan oleh
	 *         {@link #onUpdate()} pada setiap UPDATE.
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Representasi teks singkat untuk log/debug: {@code id-nama}.
	 *
	 * @return string gabungan id dan nama edisi.
	 */
	public String toString() {
		return id + "-" + nama;
	}

	/** Nama edisi DDC (mis. {@code "DDC Edisi 23"}). */
	private String nama;
	/** Keterangan bebas mengenai edisi ini. */
	private String keterangan;

	/** Konstruktor tanpa argumen, dibutuhkan Hibernate. */
	public VersiDdcItem() {
	}

	/**
	 * Mengembalikan kunci utama baris ini.
	 *
	 * @return id, atau {@code null} bila belum tersimpan.
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menetapkan kunci utama. Umumnya hanya dipanggil Hibernate sendiri karena kolomnya
	 * {@code insertable = false} (identity DB).
	 *
	 * @param id kunci utama.
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Mengembalikan nama edisi DDC, di-trim.
	 *
	 * @return nama edisi yang sudah dipangkas spasi, atau {@code null} bila belum diisi.
	 */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Menetapkan nama edisi DDC.
	 *
	 * @param nama nama baru; disimpan apa adanya (pemangkasan spasi terjadi saat pembacaan lewat
	 *             {@link #getNama()}, bukan di setter ini).
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Mengembalikan keterangan bebas mengenai edisi ini.
	 *
	 * @return keterangan, atau {@code null} bila belum diisi.
	 */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * Menetapkan keterangan bebas mengenai edisi ini.
	 *
	 * @param keterangan keterangan baru.
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

}
