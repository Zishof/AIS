package ais.database.model.library;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;




import org.hibernate.envers.Audited;



import ais.database.model.GeneralValueObject;

/**
 * Master data <b>tipe karya/terbitan</b> koleksi &mdash; satu dari dua sumbu klasifikasi bebas
 * yang dipasang pada {@link ais.database.model.library.Item}, sumbu satunya adalah {@link
 * JenisItem}. Lihat javadoc {@link JenisItem} untuk perbandingan lengkap; ringkasnya: baris kelas
 * ini mewakili nilai seperti "Textbook", "Skripsi", "Thesis", "Disertasi", "Karya Ilmiah", "Jurnal
 * Nasional yang terakreditasi", "Jurnal International", "Prosiding" (baris well-known dicari/
 * dibuat otomatis lewat {@code LibraryUtil.KARYA_ILMIAH/SKRIPSI/THESIS/DISERTASI/TEXTBOOK} saat
 * start), dan dipetakan ke metadata Dublin Core {@code dc.type} saat ekspor ke DSpace.
 *
 * <p><b>Flag {@link #getAktif()}.</b> Menyaring pilihan combobox layar entri item
 * ({@code Common.insertComboDanSemua(..., TipeItem.class, Restrictions.or(eq("aktif", true),
 * isNull("aktif")))}): baris nonaktif ({@code aktif=false}) disembunyikan dari combobox tetapi
 * baris lama dengan {@code aktif=null} tetap tampil (diperlakukan sama seperti {@code true}).
 * Berbeda dari {@link JenisItem#getDefaultItem()} yang menandai baris <i>bawaan</i>, bukan
 * aktif/nonaktif &mdash; {@code TipeItem} tidak punya padanan flag "bawaan" aktif (field
 * {@code defaultItem} lama sudah dikomentari, lihat catatan pada {@link #getKode()}).</p>
 *
 * <p><b>Field {@link #kode}.</b> Kode singkat 2 digit (default {@code "01"} bila kosong lewat
 * {@link #getKode()}); dipakai sebagai bagian pembentuk nomor/kode koleksi pada beberapa generator
 * barcode ({@code YtbBarcodeGenerator}, {@code UMJBarcodeGenerator},
 * {@code IAINBatusangkarBarcodeGenerator}).</p>
 *
 * <p><b>Kode mati.</b> Field {@code defaultItem} (beserta getter/setter-nya) sengaja dikomentari
 * di kelas ini &mdash; sisa refactor lama yang meniru pola {@link JenisItem#defaultItem}, kemudian
 * ditinggalkan tanpa dihapus. Jangan diaktifkan kembali tanpa memverifikasi ulang apakah kolom
 * {@code default_item} masih ada di tabel {@code tipe_item}.</p>
 *
 * <p>Trio field {@code oleh}/{@code olehId}/{@code tanggal_dirubah} adalah audit shadow standar AIS
 * (kolom biasa yang diperbarui manual lewat {@code onUpdate()}), terpisah dari Hibernate Envers
 * yang diaktifkan lewat {@link Audited} pada kelas ini.</p>
 *
 * <p>Bank generated by hbm2java (komentar asli generator, dipertahankan).</p>
 *
 * @see JenisItem
 * @see ais.database.model.library.Item#getTipeItem()
 * @see GeneralValueObject
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "library", name = "tipe_item")



public class TipeItem extends GeneralValueObject {

	/**
	 *
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	/** Primary key baris {@code tipe_item}, dibangkitkan basis data (IDENTITY). */
	private Long id;
	/** Username/login pembuat baris (audit shadow, bukan Envers). */
	private String oleh;
	/** Identifier pembuat baris (audit shadow). */
	private String olehId;

	/**
	 * Mengembalikan identifier pembuat baris untuk keperluan audit ringan.
	 *
	 * @return identifier pembuat, bisa {@code null} bila belum pernah disetel.
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menyetel identifier pembuat baris. Nilai kosong/hanya-spasi/{@code null} diabaikan diam-diam
	 * (nilai lama dipertahankan), sama seperti {@link #setOleh(String)}.
	 *
	 * @param olehId identifier pembuat; diabaikan bila {@code null}/kosong.
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Menyetel nama/username pembuat baris.
	 *
	 * <p>Guard kosong-diabaikan: input {@code null}/hanya-spasi tidak pernah menimpa nilai lama.
	 * Pola berulang pada seluruh field audit shadow AIS.</p>
	 *
	 * @param oleh nama/username pembuat; diabaikan bila {@code null}/kosong.
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan nama/username pembuat baris.
	 *
	 * @return nama pembuat, bisa {@code null} bila belum pernah disetel.
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Callback JPA {@code @PreUpdate}: memperbarui {@link #tanggal_dirubah} melalui
	 * {@code AuditTimestampInterceptor.ubah(this)} setiap kali baris di-{@code UPDATE}. Dipanggil
	 * otomatis oleh provider JPA/Hibernate.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}
	/** Timestamp perubahan terakhir (audit shadow); default konstruksi objek = waktu saat ini. */
	private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menyetel timestamp perubahan terakhir secara manual. Jarang dipanggil langsung karena
	 * {@link #onUpdate()} sudah menanganinya otomatis pada setiap {@code UPDATE}.
	 *
	 * @param tanggal_dirubah timestamp baru.
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan timestamp perubahan terakhir baris ini.
	 *
	 * @return timestamp perubahan terakhir.
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Representasi string singkat &mdash; nama tipe (mis. "Skripsi", "Textbook"). Dipakai
	 * combobox/label ZK yang menampilkan entity ini langsung tanpa formatter khusus.
	 *
	 * @return {@link #nama}, bisa {@code null} bila belum diisi.
	 */
	public String toString() {
		return nama;
	}

	/** Kode singkat (default "01" bila kosong, lihat {@link #getKode()}); komponen kode barcode. */
	private String kode;
	/** Nama tipe karya/terbitan, mis. "Skripsi", "Textbook". Kolom wajib ({@code nullable=false}). */
	private String nama;
	/** Keterangan bebas, opsional. */
	private String keterangan;
	// private Boolean defaultItem = false;
	/** Flag aktif/nonaktif untuk menyaring pilihan combobox. Lihat {@link #getAktif()}. */
	private Boolean aktif;

	/**
	 * Konstruktor kosong yang dibutuhkan Hibernate untuk instansiasi via refleksi.
	 */
	public TipeItem() {
	}

	/**
	 * Konstruktor pintasan untuk membangun referensi "hanya-id" tanpa memuat baris dari basis
	 * data &mdash; berguna saat kode pemanggil hanya perlu meneruskan id ke setter relasi
	 * {@code @ManyToOne} (Hibernate akan memperlakukannya sebagai proxy yang cukup untuk
	 * keperluan FK, tanpa query {@code SELECT} tambahan) atau ke pembanding {@code equals()}/
	 * {@code compareTo()} berbasis id yang diwarisi {@link GeneralValueObject}. Field lain
	 * ({@code nama}, {@code kode}, dst.) tetap {@code null} sampai entity benar-benar dimuat.
	 *
	 * @param id primary key yang ingin direferensikan.
	 */
	public TipeItem(Long id) {
		this.id = id;
	}

	/**
	 * Mengembalikan primary key baris ini.
	 *
	 * @return id baris, atau {@code null} untuk entity yang belum tersimpan.
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menyetel primary key. Normalnya tidak dipanggil manual karena kolom {@code id} bersifat
	 * {@code insertable=false} (dibangkitkan basis data via IDENTITY); dipakai juga oleh
	 * konstruktor {@link #TipeItem(Long)}.
	 *
	 * @param id primary key baris.
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Mengembalikan kode singkat tipe, dengan default lunak: bila field mentah {@code null} atau
	 * kosong-setelah-trim, method ini menuliskan {@code "01"} ke field sebelum mengembalikannya
	 * (sehingga pemanggil berikutnya selalu melihat kode yang konsisten), lalu selalu
	 * mengembalikan hasil {@code trim()}.
	 *
	 * @return kode ter-trim, tidak pernah {@code null} (default {@code "01"}).
	 */
	public String getKode() {
		if (kode == null || kode.trim().isEmpty()) {
			kode = "01";
		}
		return kode.trim();
	}

	/**
	 * Menyetel kode singkat tipe. Tidak melakukan trim/validasi di sisi setter &mdash; trimming
	 * dan default hanya terjadi di {@link #getKode()}.
	 *
	 * @param kode kode baru; boleh {@code null}/kosong (akan diganti default pada pembacaan
	 *             berikutnya).
	 */
	public void setKode(String kode) {
		this.kode = kode;
	}

	/**
	 * Mengembalikan nama tipe karya/terbitan, di-{@code trim()} setiap pembacaan.
	 *
	 * @return nama ter-trim, atau {@code null} bila field mentah {@code null}.
	 */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Menyetel nama tipe karya/terbitan. Tidak melakukan trim/validasi di sisi setter &mdash;
	 * trimming hanya terjadi di {@link #getNama()}.
	 *
	 * @param nama nama tipe baru.
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Mengembalikan keterangan bebas untuk baris ini.
	 *
	 * @return keterangan, boleh {@code null}.
	 */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * Menyetel keterangan bebas.
	 *
	 * @param keterangan teks keterangan baru; boleh {@code null}.
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}
	//
	// public Boolean getDefaultItem() {
	// if (defaultItem == null) {
	// defaultItem = false;
	// }
	// return defaultItem;
	// }
	//
	// public void setDefaultItem(Boolean defaultItem) {
	// this.defaultItem = defaultItem;
	// }

	/**
	 * Mengembalikan flag aktif/nonaktif tipe ini, dengan default lunak: field mentah {@code null}
	 * (baris lama sebelum kolom ini ada) diperlakukan sebagai {@code true} (aktif) <b>tanpa</b>
	 * menuliskan nilai default ke field &mdash; berbeda dari kebanyakan getter boolean-default di
	 * model AIS lain (mis. {@link JenisItem#getDefaultItem()}) yang menulis-balik default ke
	 * field. Konsekuensinya: baris dengan {@code aktif=null} di basis data tetap {@code null}
	 * selamanya kecuali diset eksplisit; ini dipakai langsung oleh filter combobox
	 * ({@code Restrictions.or(eq("aktif", true), isNull("aktif"))}) sehingga perbedaan
	 * "null vs false" tetap penting di level query, bukan hanya di level getter Java.
	 *
	 * @return {@code true} bila aktif atau belum pernah disetel; {@code false} hanya bila field
	 *         eksplisit disetel {@code false}.
	 */
	public Boolean getAktif() {
		return aktif == null ? true : aktif;
	}

	/**
	 * Menyetel flag aktif/nonaktif tipe ini.
	 *
	 * @param aktif nilai flag baru; {@code null} akan diperlakukan sebagai aktif oleh
	 *              {@link #getAktif()} tanpa menulis-balik field.
	 */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}

}
