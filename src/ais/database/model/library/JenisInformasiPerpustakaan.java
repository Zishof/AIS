package ais.database.model.library;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;




import org.hibernate.envers.Audited;

import ais.database.model.GeneralValueObject;



/**
 * Bank generated by hbm2java
 *
 * <p>
 * <b>Tabel jenis/kategori pengumuman perpustakaan</b> (<code>library.jenis_informasi_perpustakaan</code>).
 * Setiap baris adalah satu label sederhana ({@link #getNama()} + {@link #getKeterangan()}) yang
 * dipasangkan ke satu {@link InformasiPerpustakaan} lewat
 * {@link InformasiPerpustakaan#getJenisInformasiPerpustakaan()}. Kelas ini sendiri tidak
 * mengandung logika bisnis apa pun — murni tabel referensi/lookup.
 * </p>
 *
 * <h3>Baris bawaan di-auto-seed, bukan dikelola manual</h3>
 * <p>
 * <code>ais.action.master.library.util.LibraryUtil</code> mem-bootstrap tiga baris tetap
 * ("Informasi", "Pengumuman", "Peringatan") ke dalam field statis
 * <code>LibraryUtil.INFORMASI</code>/<code>PENGUMUMAN</code>/<code>PERINGATAN</code>: setiap
 * baris dicari lebih dulu lewat <code>Restrictions.eq("nama", ...)</code>, dan hanya dibuat lewat
 * <code>session.save(...)</code> bila belum ada. Pola ini identik dengan auto-seed konfigurasi di
 * bagian lain AIS — konsekuensinya, mengganti {@link #getNama()} salah satu baris ini lewat layar
 * CRUD akan membuat <code>LibraryUtil</code> membuat baris baru dengan nama bawaan pada
 * bootstrap berikutnya (baris lama tidak dihapus, hanya ditinggalkan), dan
 * {@link InformasiPerpustakaan#getJenisInformasiPerpustakaan()} yang berdefault ke
 * <code>LibraryUtil.INFORMASI</code> akan mengikuti instance mana pun yang terakhir dipegang
 * field statis tersebut.
 * </p>
 *
 * @see InformasiPerpustakaan
 * @see ais.action.master.library.util.LibraryUtil
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "library", name = "jenis_informasi_perpustakaan")



public class JenisInformasiPerpustakaan extends GeneralValueObject {

	/**
	 * Penanda versi serialisasi Java. Nilai ini dipakai ulang secara identik oleh hampir seluruh
	 * entity di paket {@code ais.database.model.library} karena semuanya lahir dari template
	 * hbm2java yang sama; duplikasi tidak berbahaya selama objek tidak dideserialisasi lintas
	 * kelas.
	 */
	private static final long serialVersionUID = 2463821577548439808L;

	/** Kunci primer teknis, dipetakan ke kolom {@code id} (identity/auto increment PostgreSQL). */
	private Long id;

	/**
	 * Nama tampilan pengguna terakhir yang mengubah baris ini (field audit shadow). Lihat
	 * {@link #setOleh(String)} untuk penjelasan penjaga satu arahnya.
	 */
	private String oleh;

	/**
	 * ID login ({@code tbmuser.user_id}) pengguna terakhir yang mengubah baris ini (field audit
	 * shadow), disimpan sebagai String lepas dan bukan foreign key.
	 */
	private String olehId;

	/**
	 * Mengembalikan ID login pengguna terakhir yang mengubah baris ini.
	 *
	 * @return ID login pengubah terakhir, atau {@code null} bila baris belum pernah disunting
	 *         sejak dibuat.
	 */
	public String getOlehId() {return olehId;}

	/**
	 * Menetapkan ID login pengguna pengubah terakhir, dengan penjaga satu arah: nilai
	 * {@code null} atau kosong/whitespace ditolak diam-diam sehingga field tidak pernah kembali
	 * kosong lewat API publik. Ini keharusan teknis, bukan bug — mencegah binder ZK yang selalu
	 * memanggil setter kolom audit dengan {@code null} pada setiap simpan menghapus jejak audit
	 * yang baru ditulis {@link #onUpdate()}.
	 *
	 * @param olehId ID login pengubah; {@code null}, kosong, atau whitespace diabaikan tanpa
	 *               mengubah state.
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}this.olehId = olehId;}

	/**
	 * Menetapkan nama tampilan pengguna pengubah terakhir, dengan penjaga satu arah yang identik
	 * dengan {@link #setOlehId(String)}.
	 *
	 * @param oleh nama tampilan pengubah; {@code null}, kosong, atau whitespace diabaikan.
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan nama tampilan pengguna terakhir yang mengubah baris ini.
	 *
	 * @return nama tampilan pengubah terakhir, atau {@code null}.
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Callback JPA yang dijalankan tepat sebelum statement UPDATE dikirim ke database.
	 * Mendelegasikan ke {@link ais.database.hibernate.AuditTimestampInterceptor#ubah(Object)} yang
	 * mengisi trio field audit shadow ({@link #setOleh(String)}, {@link #setOlehId(String)}, dan
	 * {@link #setTanggal_dirubah(Date)}) berdasarkan pengguna yang sedang login pada thread saat
	 * ini. Hanya {@code @PreUpdate}, tidak ada {@code @PrePersist}: baris baru tidak membawa
	 * identitas pembuatnya di field audit shadow ini.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}

	/**
	 * Cap waktu perubahan terakhir (field audit shadow), diinisialisasi ke waktu server saat
	 * objek dibuat di memori lewat {@link ais.ui.util.WaktuUtil#getDate()}.
	 */
	private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menetapkan cap waktu perubahan terakhir. Tidak ada penjaga: {@code null} diterima dan
	 * mengosongkan field. Dalam alur normal hanya dipanggil oleh {@link #onUpdate()}.
	 *
	 * @param tanggal_dirubah cap waktu perubahan; {@code null} diterima.
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan cap waktu perubahan terakhir, dipetakan sebagai TIMESTAMP.
	 *
	 * @return cap waktu perubahan terakhir; bisa {@code null} bila belum pernah diubah sejak
	 *         dibuat.
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Representasi teks objek: {@link #getNama()} apa adanya (tanpa {@code trim()}), dipakai
	 * sebagai label combobox jenis informasi pada layar {@code InformasiPerpustakaanAction}.
	 *
	 * @return {@link #nama}, mungkin {@code null} bila belum diisi.
	 */
	public String toString() {
		return nama;
	}

	/** Nama jenis informasi (mis. "Informasi", "Pengumuman", "Peringatan"). */
	private String nama;

	/** Keterangan bebas tentang jenis informasi ini. */
	private String keterangan;

	/**
	 * Konstruktor tanpa argumen yang diwajibkan JPA/Hibernate untuk instansiasi lewat refleksi,
	 * dan dipakai layar CRUD saat operator menekan tombol Tambah.
	 */
	public JenisInformasiPerpustakaan() {
	}

	/**
	 * Mengembalikan kunci primer teknis baris ini.
	 *
	 * @return kunci primer, atau {@code null} bila objek belum tersimpan.
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menetapkan kunci primer secara manual. Praktis hanya dipakai Hibernate saat menghidupkan
	 * objek dari baris database, karena kolom dipetakan {@code insertable = false}.
	 *
	 * @param id kunci primer.
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Mengembalikan nama jenis informasi, di-{@code trim()} pada setiap pembacaan.
	 *
	 * @return {@link #nama} setelah di-{@code trim()}, atau {@code null} bila belum diisi.
	 */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Menetapkan nama jenis informasi. Setter mentah, tidak memangkas whitespace — pemangkasan
	 * baru terjadi saat {@link #getNama()} dipanggil.
	 *
	 * @param nama nama jenis informasi; kolom {@code nullable = false} di database sehingga
	 *             seharusnya tidak diisi {@code null} saat disimpan.
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Mengembalikan keterangan bebas tentang jenis informasi ini.
	 *
	 * @return {@link #keterangan}, atau {@code null} bila belum diisi.
	 */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * Menetapkan keterangan bebas tentang jenis informasi ini.
	 *
	 * @param keterangan teks keterangan; {@code null} diperbolehkan.
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

}
