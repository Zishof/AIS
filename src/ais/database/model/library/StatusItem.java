package ais.database.model.library;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;




import org.hibernate.envers.Audited;

import ais.database.model.GeneralValueObject;



/**
 * Master data lookup untuk <b>kondisi fisik</b> satu eksemplar koleksi perpustakaan &mdash; bukan
 * status peminjaman/ketersediaan. Contoh nilai {@link #getNama() nama} yang dipakai di lapangan:
 * "Baik", "Rusak", "Hilang", dan sejenisnya.
 *
 * <p><b>Cakupan pemakaian &mdash; sempit dan spesifik.</b> Meski nama kelas terdengar generik
 * ("status item"), satu-satunya pemakai relasi kelas ini di seluruh basis kode adalah
 * {@code ais.database.model.library.SaldoAwalDetailDetail#getStatusItem()}: kolom {@code nullable}
 * yang merekam keadaan fisik eksemplar <i>pada saat pendataan saldo awal</i> (pencatatan koleksi
 * lama yang belum tentu semuanya "Baik"). Di luar konteks pendataan saldo awal itu, kelas ini
 * TIDAK dipakai sebagai gerbang peminjaman ataupun sebagai bagian dari siklus status eksemplar yang
 * didokumentasikan pada {@code ItemPunyaBarcode}/{@code BatchItemPunyaBarcode} (barcode + status
 * "boleh dipinjam" ada di jalur lain, terpisah dari entity ini). Jangan mengasumsikan kelas ini
 * dipakai sebagai FK dari {@link ais.database.model.library.Item} secara langsung &mdash; tidak
 * ada relasi seperti itu.</p>
 *
 * <p>Diakses lewat CRUD generik {@code ais.action.master.library.StatusItemAction} (layar admin
 * "Status Item") dan {@code ais.database.dao.library.StatusItemDao}/{@code StatusItemDaoImpl}.
 * Bandingkan dengan {@link StatusTerbitItem} (status siklus terbit/publikasi item, dipakai luas di
 * {@link ais.database.model.library.Item#getStatusTerbitItem()}) dan {@link ItemHasStatus} (riwayat
 * status bertanggal per item, entity terpisah yang saat ini tidak punya pemakai nyata) &mdash;
 * ketiganya bernama mirip "status item" tetapi mewakili tiga konsep berbeda yang tidak saling
 * menggantikan.</p>
 *
 * <p>Field {@code oleh}/{@code olehId}/{@code tanggal_dirubah} adalah trio audit shadow standar
 * AIS: bukan hasil Hibernate Envers (meski kelas ditandai {@link Audited}, yang menulis riwayat ke
 * tabel {@code _AUD} terpisah), melainkan kolom biasa pada tabel {@code status_item} sendiri yang
 * diperbarui manual lewat {@code onUpdate()} setiap sebelum update. Ini pola berulang di seluruh
 * model AIS, bukan bug.</p>
 *
 * <p>Bank generated by hbm2java (komentar asli generator, dipertahankan).</p>
 *
 * @see ais.database.model.library.SaldoAwalDetailDetail
 * @see StatusTerbitItem
 * @see ItemHasStatus
 * @see GeneralValueObject
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "library", name = "status_item")



public class StatusItem extends GeneralValueObject {

	/**
	 *
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	/** Primary key baris {@code status_item}, dibangkitkan basis data (IDENTITY). */
	private Long id;
	/** Username/login pembuat baris (audit shadow, bukan Envers). Lihat {@link #setOleh(String)}. */
	private String oleh;
	/** Identifier pembuat baris (audit shadow), umumnya sama dengan {@link #oleh} untuk data lama. Lihat {@link #setOlehId(String)}. */
	private String olehId;

	/**
	 * Mengembalikan identifier pembuat baris untuk keperluan audit ringan.
	 *
	 * @return identifier pembuat, bisa {@code null} bila belum pernah disetel.
	 */
	public String getOlehId() {return olehId;}

	/**
	 * Menyetel identifier pembuat baris. Nilai kosong/hanya-spasi/{@code null} diabaikan diam-diam
	 * (nilai lama dipertahankan) &mdash; lihat catatan yang sama pada {@link #setOleh(String)}.
	 *
	 * @param olehId identifier pembuat; diabaikan bila {@code null}/kosong.
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}this.olehId = olehId;}

	/**
	 * Menyetel nama/username pembuat baris.
	 *
	 * <p>Guard kosong-diabaikan: bila {@code oleh} bernilai {@code null} atau hanya berisi spasi,
	 * method ini kembali tanpa melakukan apa pun sehingga nilai field yang sudah tersimpan
	 * sebelumnya <b>tidak pernah ditimpa</b> oleh input kosong. Ini pola berulang di seluruh field
	 * audit shadow AIS ({@code oleh}/{@code olehId}); tujuannya mencegah proses batch/import yang
	 * lupa mengisi actor secara tidak sengaja mengosongkan jejak audit yang sudah ada.</p>
	 *
	 * @param oleh nama/username pembuat; diabaikan bila {@code null}/kosong.
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan nama/username pembuat baris.
	 *
	 * @return nama pembuat, bisa {@code null} bila belum pernah disetel.
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Callback JPA {@code @PreUpdate}: memperbarui {@link #tanggal_dirubah} melalui
	 * {@code AuditTimestampInterceptor.ubah(this)} setiap kali baris di-{@code UPDATE}. Dipanggil
	 * otomatis oleh provider JPA/Hibernate, tidak dipanggil manual dari kode aplikasi.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}
	/** Timestamp perubahan terakhir (audit shadow); default konstruksi objek = waktu saat ini. */
	private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menyetel timestamp perubahan terakhir secara manual. Jarang dipanggil langsung karena
	 * {@link #onUpdate()} sudah menanganinya otomatis pada setiap {@code UPDATE}.
	 *
	 * @param tanggal_dirubah timestamp baru.
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan timestamp perubahan terakhir baris ini.
	 *
	 * @return timestamp perubahan terakhir.
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Representasi string singkat &mdash; nama kondisi fisik (mis. "Baik", "Rusak"). Dipakai
	 * combobox/label ZK yang menampilkan entity ini langsung tanpa formatter khusus.
	 *
	 * @return {@link #nama}, bisa {@code null} bila belum diisi.
	 */
	public String toString() {
		return nama;
	}

	/** Nama kondisi fisik, mis. "Baik", "Rusak", "Hilang". Kolom wajib ({@code nullable=false}). */
	private String nama;
	/** Keterangan bebas, opsional. */
	private String keterangan;
	/** Flag aktif/nonaktif bergaya integer (1 = aktif). Lihat {@link #getStatus()} untuk default. */
	private Integer status = 1;

	/**
	 * Konstruktor kosong yang dibutuhkan Hibernate untuk instansiasi via refleksi.
	 */
	public StatusItem() {
	}

	/**
	 * Mengembalikan primary key baris ini.
	 *
	 * @return id baris, atau {@code null} untuk entity yang belum tersimpan.
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menyetel primary key. Normalnya tidak dipanggil manual karena kolom {@code id} bersifat
	 * {@code insertable=false} (dibangkitkan basis data via IDENTITY); dipakai Hibernate saat
	 * memuat entity dari hasil query.
	 *
	 * @param id primary key baris.
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Mengembalikan nama kondisi fisik, di-{@code trim()} setiap pembacaan untuk menyamarkan
	 * spasi tersisa hasil input pengguna/import lama.
	 *
	 * @return nama ter-trim, atau {@code null} bila field mentah {@code null}.
	 */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Menyetel nama kondisi fisik. Tidak melakukan trim/validasi di sisi setter &mdash; trimming
	 * hanya terjadi di {@link #getNama()}.
	 *
	 * @param nama nama kondisi fisik baru.
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Mengembalikan keterangan bebas untuk baris ini.
	 *
	 * @return keterangan, boleh {@code null}.
	 */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * Menyetel keterangan bebas.
	 *
	 * @param keterangan teks keterangan baru; boleh {@code null}.
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * Mengembalikan flag status baris, dengan default lunak: bila field mentah {@code null}
	 * (baris lama sebelum kolom ini ada, atau hasil deserialisasi tanpa nilai), method ini
	 * menuliskan {@code 1} ke field sebelum mengembalikannya sehingga pemanggil berikutnya (dan
	 * potensi flush Hibernate berikutnya bila entity ini managed) selalu melihat nilai konsisten
	 * "aktif". Kolom ini <b>tidak</b> dipetakan dengan anotasi {@code @Column} eksplisit di kelas
	 * ini &mdash; pemetaannya mengikuti konvensi nama properti JPA default.
	 *
	 * @return {@code 1} (aktif) sebagai default bila belum pernah disetel, atau nilai tersimpan.
	 */
	public Integer getStatus() {
		if (status == null) {
			status = 1;
		}
		return status;
	}

	/**
	 * Menyetel flag status baris.
	 *
	 * @param status nilai status baru; {@code null} akan kembali diganti default {@code 1} pada
	 *               pembacaan berikutnya lewat {@link #getStatus()}.
	 */
	public void setStatus(Integer status) {
		this.status = status;
	}

}
