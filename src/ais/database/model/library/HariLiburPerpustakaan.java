package ais.database.model.library;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;




import org.hibernate.envers.Audited;



import ais.database.model.GeneralValueObject;

/**
 * Bank generated by hbm2java
 *
 * <p>
 * <b>Daftar hari libur khusus perpustakaan</b> (tabel <code>library.hari_libur_perpustakaan</code>).
 * Setiap baris adalah satu tanggal libur ({@link #getTanggal()}) dengan nama ({@link #getNama()})
 * dan keterangan bebas ({@link #getKeterangan()}). Dikelola lewat
 * <code>ais.action.master.library.HariLiburPerpustakaanAction</code>.
 * </p>
 *
 * <h3>Satu sumber kebenaran — berbeda dari {@code rab.HariLibur}/{@code Kalender}</h3>
 * <p>
 * Berbeda dari pasangan {@code rab.HariLibur}/{@code Kalender} yang terbukti punya dua sumber
 * kebenaran yang tidak sinkron, entity ini <b>hanya dikonsumsi lewat satu jalur</b>: seluruh
 * baris dimuat ke cache statis in-memory <code>ais.common.Common#hariLiburPerpustakaans</code>
 * (peta <code>Date &rarr; HariLiburPerpustakaan</code>) oleh
 * <code>CommonMaintenanceHelper.reInitHariLibur()</code>, dan tidak ada jalur baca lain (query
 * langsung ke tabel ini di luar refresh cache) yang ditemukan di kode. Cache dimuat ulang saat
 * startup aplikasi maupun secara eksplisit oleh
 * <code>HariLiburPerpustakaanAction</code> setiap kali operator menyimpan atau menghapus baris,
 * sehingga risiko cache basi jauh lebih kecil dibanding pola auto-seed/cache lain yang hanya
 * dimuat sekali saat startup.
 * </p>
 *
 * <h3>Konsumen: menggeser tenggat pengembalian pinjaman, berlaku global lintas perpustakaan</h3>
 * <p>
 * Konsumen utama cache ini adalah
 * {@link ais.database.model.library.PeminjamanPengadaanItemDetail#hitungBatasWaktupengembalian()}
 * (dan padanannya pada {@code sirkulasisurat.PeminjamanSuratItemDetail}): bila konfigurasi
 * <code>libur_nasional_hari_libur_tanggal_kembali_mundur</code> aktif, tanggal jatuh tempo
 * peminjaman digeser maju melewati setiap tanggal yang muncul di
 * <code>Common.hariLiburPerpustakaans</code> (dibatasi 29 iterasi). Sebagaimana didokumentasikan
 * pada method tersebut, pembacaan itu memakai sumber <b>global</b> — parameter perpustakaan pada
 * pemanggilnya sama sekali tidak dipakai — sehingga meski namanya menyebut "perpustakaan", daftar
 * libur ini berlaku seragam untuk <b>semua</b> perpustakaan dalam satu instalasi; tidak ada cara
 * memberi satu perpustakaan kalender liburnya sendiri. Menambah atau menghapus baris di sini juga
 * dapat menggeser tenggat peminjaman yang <em>sudah berjalan</em> begitu baris peminjamannya
 * dibaca ulang, karena tenggat dihitung ulang setiap pembacaan, bukan disimpan sebagai nilai
 * final saat peminjaman terjadi.
 * </p>
 *
 * @see ais.common.Common#hariLiburPerpustakaans
 * @see ais.action.master.library.HariLiburPerpustakaanAction
 * @see ais.database.model.library.PeminjamanPengadaanItemDetail#hitungBatasWaktupengembalian()
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "library", name = "hari_libur_perpustakaan")



public class HariLiburPerpustakaan extends GeneralValueObject {

	/**
	 * Penanda versi serialisasi Java, dipakai ulang secara identik oleh hampir seluruh entity di
	 * paket {@code ais.database.model.library}.
	 */
	private static final long serialVersionUID = 2463821577548439808L;

	/** Kunci primer teknis, dipetakan ke kolom {@code id} (identity/auto increment PostgreSQL). */
	private Long id;

	/** Nama tampilan pengguna terakhir yang mengubah baris ini (field audit shadow). */
	private String oleh;

	/**
	 * ID login ({@code tbmuser.user_id}) pengguna terakhir yang mengubah baris ini (field audit
	 * shadow), disimpan sebagai String lepas dan bukan foreign key.
	 */
	private String olehId;

	/**
	 * Mengembalikan ID login pengguna terakhir yang mengubah baris ini.
	 *
	 * @return ID login pengubah terakhir, atau {@code null} bila baris belum pernah disunting
	 *         sejak dibuat.
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menetapkan ID login pengguna pengubah terakhir, dengan penjaga satu arah: nilai
	 * {@code null} atau kosong/whitespace ditolak diam-diam. Ini keharusan teknis, bukan bug —
	 * mencegah binder ZK yang selalu memanggil setter kolom audit dengan {@code null} pada setiap
	 * simpan menghapus jejak audit yang baru ditulis {@link #onUpdate()}.
	 *
	 * @param olehId ID login pengubah; {@code null}, kosong, atau whitespace diabaikan.
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Menetapkan nama tampilan pengguna pengubah terakhir, dengan penjaga satu arah yang identik
	 * dengan {@link #setOlehId(String)}.
	 *
	 * @param oleh nama tampilan pengubah; {@code null}, kosong, atau whitespace diabaikan.
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan nama tampilan pengguna terakhir yang mengubah baris ini.
	 *
	 * @return nama tampilan pengubah terakhir, atau {@code null}.
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Callback JPA yang dijalankan tepat sebelum statement UPDATE dikirim ke database.
	 * Mendelegasikan ke {@link ais.database.hibernate.AuditTimestampInterceptor#ubah(Object)} yang
	 * mengisi trio field audit shadow. Hanya {@code @PreUpdate}, tidak ada {@code @PrePersist}.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menetapkan cap waktu perubahan terakhir. Tidak ada penjaga: {@code null} diterima dan
	 * mengosongkan field.
	 *
	 * @param tanggal_dirubah cap waktu perubahan; {@code null} diterima.
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan cap waktu perubahan terakhir, dipetakan sebagai TIMESTAMP.
	 *
	 * @return cap waktu perubahan terakhir; bisa {@code null} bila belum pernah diubah sejak
	 *         dibuat.
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Representasi teks objek: {@link #getId()} digabung dengan {@link #nama} (tanpa
	 * {@code trim()}), dipisah tanda hubung. Dipakai sebagai label baris pada layar CRUD.
	 *
	 * @return {@code "<id>-<nama>"}; bagian nama bisa berupa literal "null" bila belum diisi.
	 */
	public String toString() {
		return id + "-" + nama;
	}

	/** Nama/label hari libur ini (mis. "Libur Semester", "Hari Jadi Perpustakaan"). */
	private String nama;

	/** Tanggal hari libur. Diisi nilai bawaan oleh {@link #getTanggal()} bila masih null. */
	private Date tanggal;

	/** Keterangan bebas tentang hari libur ini. */
	private String keterangan;

	/**
	 * Konstruktor tanpa argumen yang diwajibkan JPA/Hibernate untuk instansiasi lewat refleksi,
	 * dan dipakai layar CRUD saat operator menekan tombol Tambah.
	 */
	public HariLiburPerpustakaan() {
	}

	/**
	 * Mengembalikan kunci primer teknis baris ini.
	 *
	 * @return kunci primer, atau {@code null} bila objek belum tersimpan.
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menetapkan kunci primer secara manual. Praktis hanya dipakai Hibernate saat menghidupkan
	 * objek dari baris database.
	 *
	 * @param id kunci primer.
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Mengembalikan nama hari libur, di-{@code trim()} pada setiap pembacaan.
	 *
	 * @return {@link #nama} setelah di-{@code trim()}, atau {@code null} bila belum diisi.
	 */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Menetapkan nama hari libur. Setter mentah, tidak memangkas whitespace — pemangkasan baru
	 * terjadi saat {@link #getNama()} dipanggil.
	 *
	 * @param nama nama hari libur; kolom {@code nullable = false} di database.
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Mengembalikan keterangan bebas tentang hari libur ini.
	 *
	 * @return {@link #keterangan}, atau {@code null} bila belum diisi.
	 */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * Menetapkan keterangan bebas tentang hari libur ini.
	 *
	 * @param keterangan teks keterangan; {@code null} diperbolehkan.
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * Mengembalikan tanggal hari libur, mengisi tanggal server hari ini bila kosong.
	 *
	 * <p>
	 * <b>Getter dengan nilai bawaan, tidak destruktif ke field.</b> Berbeda dari getter destruktif
	 * pada entity lain di paket ini, method ini <b>tidak menuliskan</b> nilai bawaan ke field
	 * {@link #tanggal} — ia hanya mengembalikan {@link ais.ui.util.WaktuUtil#getDate()} secara
	 * langsung tanpa efek samping saat {@link #tanggal} masih {@code null}. Karena kolom
	 * dipetakan {@code nullable = false} di database, kondisi ini pada praktiknya hanya
	 * teramati pada objek baru yang belum disimpan, sebelum {@link #setTanggal(Date)} dipanggil
	 * oleh layar CRUD.
	 * </p>
	 *
	 * @return {@link #tanggal}, atau tanggal server saat ini bila field masih kosong.
	 */
	@Temporal(TemporalType.DATE)
	public Date getTanggal() {
		return tanggal == null ? ais.ui.util.WaktuUtil.getDate() : tanggal;
	}

	/**
	 * Menetapkan tanggal hari libur. Setter mentah tanpa validasi duplikasi — dua baris dengan
	 * tanggal yang sama diterima begitu saja; cache
	 * {@code Common.hariLiburPerpustakaans} yang dikunci per tanggal akan menyimpan hanya satu di
	 * antara baris-baris duplikat tersebut (baris terakhir yang diproses saat refresh cache).
	 *
	 * @param tanggal tanggal hari libur; {@code null} diterima tetapi akan digantikan tanggal
	 *                server saat ini oleh {@link #getTanggal()} pada pembacaan berikutnya.
	 */
	public void setTanggal(Date tanggal) {
		this.tanggal = tanggal;
	}

}
