package ais.database.model.kpi;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;




import org.hibernate.envers.Audited;



import ais.database.model.GeneralValueObject;

/**
 * Entity JPA/Hibernate untuk tabel {@code public.kategori_kpi} — master kategori (dimensi)
 * penilaian KPI, misalnya "Kuantitas", "Kualitas", atau "Ketepatan Waktu".
 *
 * <p>Setiap {@link ais.database.model.kpi.MasaKpi item KPI} pada dasarnya dikelompokkan ke salah
 * satu baris kategori ini agar penilaian kinerja pegawai dapat direkap per dimensi. Kelas ini
 * murni master/referensi: hanya berisi kode, nama, keterangan, dan flag aktif — tidak ada relasi
 * ke entity lain di kelas ini sendiri (relasi dipegang oleh entity KPI yang mereferensikannya).</p>
 *
 * <p>Kelas ini dihasilkan oleh hbm2java (komentar asli "Bank generated by hbm2java" adalah sisa
 * template generator dan tidak relevan dengan domain KPI) lalu ditambah field audit
 * ({@link #getOleh() oleh}/{@link #getOlehId() olehId}/{@link #getTanggal_dirubah() tanggal_dirubah})
 * secara manual — pola yang sama diulang pada seluruh entity KPI lain di paket ini. Field-field
 * audit ini adalah <strong>redundansi yang disengaja</strong> terhadap tabel audit Envers
 * ({@link org.hibernate.envers.Audited @Audited} di kelas ini): mereka bukan bug melainkan
 * kebutuhan teknis agar "siapa mengubah terakhir" bisa ditampilkan cepat di UI tanpa query
 * terpisah ke tabel revisi Envers.</p>
 *
 * @see ais.database.model.kpi.SatuanKpi
 * @see ais.database.model.kpi.MasaKpi
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "public", name = "kategori_kpi")



public class KategoriKpi extends GeneralValueObject {

	/**
	 *
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	private Long id;
	private String oleh;
	private String olehId;

	/**
	 * @return id pengguna (username/kode) yang terakhir menyimpan baris ini, sebagaimana
	 *         tercatat pada field audit shadow {@link #oleh}/{@link #olehId}.
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menyetel id pengguna yang menyimpan baris ini. Nilai {@code null} atau string kosong/spasi
	 * diabaikan (guard fail-safe) sehingga nilai audit yang sudah tersimpan tidak pernah
	 * tertimpa oleh input kosong.
	 *
	 * @param olehId id pengguna; diabaikan bila {@code null} atau blank
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Menyetel nama pengguna yang menyimpan baris ini. Nilai {@code null} atau string kosong/spasi
	 * diabaikan (guard fail-safe), sama seperti {@link #setOlehId(String)}.
	 *
	 * @param oleh nama pengguna; diabaikan bila {@code null} atau blank
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * @return nama pengguna yang terakhir menyimpan baris ini (field audit shadow, lihat
	 *         dokumentasi kelas).
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Callback JPA {@code @PreUpdate}: dipanggil otomatis oleh Hibernate sebelum setiap
	 * {@code UPDATE} untuk memperbarui {@link #tanggal_dirubah} via
	 * {@link ais.database.hibernate.AuditTimestampInterceptor#ubah(Object)}.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * @param tanggal_dirubah waktu perubahan terakhir baris ini; biasanya diisi otomatis oleh
	 *                        {@link #onUpdate()}, bukan dipanggil manual
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/** @return waktu perubahan terakhir baris ini; default saat konstruksi adalah waktu sekarang. */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/** @return representasi ringkas {@code "<id>-<nama>"} untuk keperluan log/debug. */
	public String toString() {
		return id + "-" + nama;
	}

	private String kode;

	private String nama;
	private String keterangan;
	private Boolean aktif;

	/** Konstruktor default (dibutuhkan Hibernate). */
	public KategoriKpi() {
	}

	/** @return id baris (primary key, auto increment identity). */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/** @param id id baris; biasanya tidak perlu diset manual karena kolom bersifat identity. */
	public void setId(Long id) {
		this.id = id;
	}

	/** @return kode kategori KPI, di-trim; string kosong ({@code ""}) bila belum diisi. */
	public String getKode() {
		return kode == null ? "" : kode.trim();
	}

	/** @param kode kode kategori KPI (belum di-trim/divalidasi saat disimpan). */
	public void setKode(String kode) {
		this.kode = kode;
	}

	/** @return nama kategori KPI, di-trim; {@code null} bila belum diisi. */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/** @param nama nama kategori KPI (kolom wajib/{@code NOT NULL} di database). */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/** @return keterangan/deskripsi tambahan kategori KPI; boleh {@code null}. */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/** @param keterangan keterangan/deskripsi tambahan kategori KPI. */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * @return status aktif kategori ini. Bila belum pernah diset ({@code null}) dianggap
	 *         <strong>aktif</strong> — pola "default aktif" yang konsisten dipakai di seluruh
	 *         entity master KPI pada paket ini, sehingga baris lama yang belum mengisi kolom
	 *         {@code aktif} tetap tampil/terpakai secara wajar.
	 */
	public Boolean getAktif() {
		return aktif == null ? true : aktif;
	}

	/** @param aktif status aktif/nonaktif kategori KPI ini. */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}

}
