package ais.database.model;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

/**
 * Bank generated by hbm2java
 *
 * <p><b>Master status pemenuhan kewajiban beban kerja dosen (BKD)</b> (mis. "Memenuhi", "Tidak
 * Memenuhi") &mdash; satu baris tabel {@code public.status_kewajiban_beban_dosen} mewakili satu
 * nilai status. Dirujuk oleh {@link KewajibanBebanDosen#getStatusKewajibanBebanDosen()} sebagai
 * hasil evaluasi apakah SKS beban kerja dosen memenuhi ambang minimal/maksimal yang ditetapkan.</p>
 *
 * <h2>Jebakan pada {@link #getKodeUnik()} (diverifikasi dari kode)</h2>
 * <p>{@link #getKodeUnik()} adalah <b>getter-mutasi</b>: setiap dipanggil, ia menugaskan ulang
 * field {@code kodeUnik} dari hasil {@link #getKode()} saat itu juga, lalu mengembalikannya.
 * Kolom ini dipetakan {@code @Column(unique = true)}, sehingga <b>setiap kali baris ini
 * dipersiskan (flush/save) setelah {@code getKodeUnik()} dipanggil, nilai unik yang tersimpan di
 * database akan selalu disamakan dengan {@link #getKode()} terkini</b> &mdash; bila dua baris
 * master ini kebetulan diberi {@code kode} yang sama, constraint unik pada {@code kode_unik}
 * akan menolak salah satunya saat disimpan (bukan gagal senyap).</p>
 *
 * <h2>Peta anggota</h2>
 * <ul>
 *   <li><b>Identitas &amp; audit</b>: {@link #getId()}/{@link #setId(Long)},
 *       {@link #getOleh()}/{@link #setOleh(String)}, {@link #getOlehId()}/{@link #setOlehId(String)},
 *       {@link #getTanggal_dirubah()}/{@link #setTanggal_dirubah(Date)}, {@link #onUpdate()}.</li>
 *   <li><b>Deskriptif</b>: {@link #getKode()}/{@link #setKode(String)}, {@link #getNama()}/
 *       {@link #setNama(String)} (wajib, dipakai {@link #toString()}),
 *       {@link #getKeterangan()}/{@link #setKeterangan(String)},
 *       {@link #getKodeUnik()}/{@link #setKodeUnik(String)} (lihat jebakan di atas).</li>
 *   <li><b>Status</b>: {@link #getAktif()}/{@link #setAktif(Boolean)} (default {@code true} bila
 *       belum pernah diisi, sekaligus disimpan ke field pada pemanggilan pertama).</li>
 * </ul>
 *
 * @see KewajibanBebanDosen
 * @see GeneralValueObject
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "public", name = "status_kewajiban_beban_dosen")

public class StatusKewajibanBebanDosen extends GeneralValueObject {

	/**
	 * Penanda versi serialisasi Java. Tidak berdampak fungsional.
	 */
	private static final long serialVersionUID = 2463821577548439808L;

	/** Kunci utama, lihat {@link #getId()}. */
	private Long id;

	/** Nama pengguna pengubah terakhir, lihat {@link #getOleh()}. */
	private String oleh;

	/** Id pengguna pengubah terakhir, lihat {@link #getOlehId()}. */
	private String olehId;

	/**
	 * Id pengguna pengubah terakhir.
	 *
	 * @return id pengguna pengubah terakhir; {@code null} bila belum pernah tercatat.
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menetapkan id pengguna pengubah terakhir. Setter penjaga: nilai {@code null}/kosong diabaikan
	 * diam-diam sehingga jejak audit yang sudah terisi tidak dapat dihapus lewat setter ini.
	 *
	 * @param olehId id pengguna; diabaikan bila {@code null} atau kosong.
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Menetapkan nama pengguna pengubah terakhir. Berperilaku sama dengan
	 * {@link #setOlehId(String)}: nilai {@code null}/kosong diabaikan diam-diam.
	 *
	 * @param oleh nama pengguna; diabaikan bila {@code null} atau kosong.
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Nama pengguna yang terakhir mengubah baris ini.
	 *
	 * @return nama pengguna pengubah terakhir; {@code null} bila belum pernah tercatat.
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Hook JPA {@code @PreUpdate} yang menyegarkan metadata audit sebelum baris ini di-{@code UPDATE},
	 * plus deklarasi field {@code tanggal_dirubah} yang menempel pada baris kode yang sama. Mendelegasikan
	 * ke {@code ais.database.hibernate.AuditTimestampInterceptor.ubah(this)}; dipanggil Hibernate saja.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menetapkan stempel waktu perubahan terakhir. Tidak menjaga terhadap {@code null}.
	 *
	 * @param tanggal_dirubah waktu perubahan terakhir; boleh {@code null}.
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Waktu terakhir baris ini diubah.
	 *
	 * @return waktu perubahan terakhir, dipetakan sebagai {@code TIMESTAMP}.
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Representasi teks baris ini: id digabung nama.
	 *
	 * @return string {@code "<id>-<nama>"}.
	 */
	public String toString() {
		return id + "-" + nama;
	}

	/** Kode status, lihat {@link #getKode()}. */
	private String kode;

	/** Nama status pemenuhan kewajiban beban dosen, lihat {@link #getNama()}. */
	private String nama;

	/** Keterangan bebas, lihat {@link #getKeterangan()}. */
	private String keterangan;

	/** Status aktif/nonaktif baris, lihat {@link #getAktif()}. */
	private Boolean aktif;

	/** Kode unik turunan dari {@link #getKode()}, lihat {@link #getKodeUnik()}. */
	private String kodeUnik;

	/** Konstruktor tanpa argumen yang dibutuhkan Hibernate/JPA. */
	public StatusKewajibanBebanDosen() {
	}

	/**
	 * Konstruktor kenyamanan untuk merujuk baris berdasarkan id yang sudah diketahui.
	 *
	 * @param id id baris yang dirujuk.
	 */
	public StatusKewajibanBebanDosen(Long id) {
		this.id = id;
	}

	/**
	 * Kunci utama baris (kolom {@code id}, {@code IDENTITY} &mdash; dibangkitkan database).
	 *
	 * @return id baris; {@code null} selama object belum pernah disimpan.
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menetapkan kunci utama. Praktis hanya dipanggil Hibernate.
	 *
	 * @param id kunci utama baris.
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Nama status pemenuhan kewajiban beban dosen (kolom {@code nama}, wajib). Dikembalikan
	 * sudah di-{@code trim()}.
	 *
	 * @return nama; {@code null} bila belum diisi.
	 */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Menetapkan nama status.
	 *
	 * @param nama nama status.
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Keterangan bebas (kolom {@code keterangan}, {@code nullable}).
	 *
	 * @return keterangan; {@code null} bila belum diisi.
	 */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * Menetapkan keterangan bebas.
	 *
	 * @param keterangan teks keterangan; boleh {@code null}.
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * Kode status (bukan kolom terpetakan JPA eksplisit, hanya field biasa; getter murni tanpa
	 * efek samping). Dipakai sebagai sumber turunan oleh {@link #getKodeUnik()}.
	 *
	 * @return kode status; {@code null} bila belum diisi.
	 */
	public String getKode() {
		return kode;
	}

	/**
	 * Menetapkan kode status.
	 *
	 * @param kode kode status; boleh {@code null}.
	 */
	public void setKode(String kode) {
		this.kode = kode;
	}

	/**
	 * Status aktif/nonaktif baris ini pada layar pemilihan.
	 *
	 * @return {@code true} bila aktif atau belum pernah diisi; berbeda dari kebanyakan master
	 *         lain di paket ini, nilai default {@code true} di sini juga <b>ditugaskan kembali
	 *         ke field {@code aktif}</b> pada pemanggilan pertama, bukan hanya dikembalikan.
	 */
	public Boolean getAktif() {
		if (aktif == null) {
			aktif = true;
		}
		return aktif;
	}

	/**
	 * Menetapkan status aktif/nonaktif.
	 *
	 * @param aktif status aktif baru; boleh {@code null} (akan dinormalisasi menjadi {@code true}
	 *              oleh pemanggilan {@link #getAktif()} berikutnya).
	 */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}

	/**
	 * Kode unik baris ini. <b>Getter-mutasi</b>: setiap dipanggil, field {@code kodeUnik}
	 * ditugaskan ulang dari {@link #getKode()} saat itu juga sebelum dikembalikan. Kolom ini
	 * dipetakan {@code @Column(unique = true)}, sehingga nilai yang benar-benar tersimpan ke
	 * database selalu mengikuti {@link #getKode()} terkini pada saat entity di-flush, dan dua
	 * baris master dengan {@code kode} yang sama akan ditolak constraint unik saat disimpan.
	 *
	 * @return kode unik, sama dengan {@link #getKode()} saat ini.
	 */
	@Column(unique = true)
	public String getKodeUnik() {
		kodeUnik = getKode();
		return kodeUnik;
	}

	/**
	 * Menetapkan kode unik secara manual. Nilai ini akan ditimpa oleh {@link #getKodeUnik()}
	 * pada pemanggilan berikutnya dengan hasil {@link #getKode()} saat itu.
	 *
	 * @param kodeUnik kode unik; boleh {@code null}.
	 */
	public void setKodeUnik(String kodeUnik) {
		this.kodeUnik = kodeUnik;
	}

}
