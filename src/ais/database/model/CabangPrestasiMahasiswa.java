package ais.database.model;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

/**
 * Bank generated by hbm2java
 *
 * <p><b>Master cabang/bidang prestasi mahasiswa</b> (mis. "Seni", "Olah Raga", "Kejuaraan
 * Ilmiah", "Lain-Lain") &mdash; satu baris tabel {@code public.cabang_prestasi_mahasiswa}
 * mewakili satu bidang capaian prestasi. Berbeda dari {@link KategoriPrestasiMahasiswa} yang
 * mewakili <b>tingkat</b> prestasi (Internasional/Nasional/dst.), entity ini mewakili <b>bidang</b>
 * prestasi (Seni/Olah Raga/dst.) &mdash; keduanya dipakai berdampingan, biasanya sebagai dua kolom
 * terpisah pada satu baris riwayat prestasi mahasiswa.</p>
 *
 * <h2>Jebakan pada {@link #getKode()} (diverifikasi dari kode)</h2>
 * <p>{@link #getKode()} <b>bukan getter murni</b>: setiap dipanggil, ia mencocokkan
 * {@link #getNama()} (case-insensitive) terhadap empat nama baku ("Seni"&rarr;2, "Olah Raga"&rarr;3,
 * "Kejuaraan Ilmiah"&rarr;1, "Lain-Lain"&rarr;9 &mdash; perhatikan urutan kode <b>tidak berurutan</b>
 * dengan urutan deklarasi cabang) dan <b>menugaskan hasilnya kembali ke field {@code kode}</b>
 * sebelum mengembalikannya. Nama di luar keempat nama baku ini mempertahankan nilai {@code kode}
 * sebelumnya apa adanya.</p>
 *
 * <h2>Peta anggota</h2>
 * <ul>
 *   <li><b>Identitas &amp; audit</b>: {@link #getId()}/{@link #setId(Long)},
 *       {@link #getOleh()}/{@link #setOleh(String)}, {@link #getOlehId()}/{@link #setOlehId(String)},
 *       {@link #getTanggal_dirubah()}/{@link #setTanggal_dirubah(Date)}, {@link #onUpdate()}.</li>
 *   <li><b>Deskriptif</b>: {@link #getKode()}/{@link #setKode(String)} (lihat jebakan di atas),
 *       {@link #getNama()}/{@link #setNama(String)} (wajib, dipakai {@link #toString()}),
 *       {@link #getKeterangan()}/{@link #setKeterangan(String)}.</li>
 *   <li><b>Integrasi</b>: {@link #getFeeder()}/{@link #setFeeder(String)} (kode pemetaan ke
 *       sistem feeder eksternal).</li>
 * </ul>
 *
 * @see KategoriPrestasiMahasiswa
 * @see GeneralValueObject
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "public", name = "cabang_prestasi_mahasiswa")

public class CabangPrestasiMahasiswa extends GeneralValueObject {

	/**
	 * Penanda versi serialisasi Java. Tidak berdampak fungsional.
	 */
	private static final long serialVersionUID = 2463821577548439808L;

	/** Kunci utama, lihat {@link #getId()}. */
	private Long id;

	/** Nama pengguna pengubah terakhir, lihat {@link #getOleh()}. */
	private String oleh;

	/** Id pengguna pengubah terakhir, lihat {@link #getOlehId()}. */
	private String olehId;

	/**
	 * Id pengguna pengubah terakhir.
	 *
	 * @return id pengguna pengubah terakhir; {@code null} bila belum pernah tercatat.
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menetapkan id pengguna pengubah terakhir. Setter penjaga: nilai {@code null}/kosong diabaikan
	 * diam-diam sehingga jejak audit yang sudah terisi tidak dapat dihapus lewat setter ini.
	 *
	 * @param olehId id pengguna; diabaikan bila {@code null} atau kosong.
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Menetapkan nama pengguna pengubah terakhir. Berperilaku sama dengan
	 * {@link #setOlehId(String)}: nilai {@code null}/kosong diabaikan diam-diam.
	 *
	 * @param oleh nama pengguna; diabaikan bila {@code null} atau kosong.
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Nama pengguna yang terakhir mengubah baris ini.
	 *
	 * @return nama pengguna pengubah terakhir; {@code null} bila belum pernah tercatat.
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Hook JPA {@code @PreUpdate} yang menyegarkan metadata audit sebelum baris ini di-{@code UPDATE},
	 * plus deklarasi field {@code tanggal_dirubah} yang menempel pada baris kode yang sama. Mendelegasikan
	 * ke {@code ais.database.hibernate.AuditTimestampInterceptor.ubah(this)}; dipanggil Hibernate saja.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menetapkan stempel waktu perubahan terakhir. Tidak menjaga terhadap {@code null}.
	 *
	 * @param tanggal_dirubah waktu perubahan terakhir; boleh {@code null}.
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Waktu terakhir baris ini diubah.
	 *
	 * @return waktu perubahan terakhir, dipetakan sebagai {@code TIMESTAMP}.
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Representasi teks baris ini: id digabung nama.
	 *
	 * @return string {@code "<id>-<nama>"}.
	 */
	public String toString() {
		return id + "-" + nama;
	}

	/** Kode cabang prestasi, lihat {@link #getKode()}. */
	private String kode;

	/** Nama cabang/bidang prestasi, lihat {@link #getNama()}. */
	private String nama;

	/** Keterangan bebas, lihat {@link #getKeterangan()}. */
	private String keterangan;

	/** Kode pemetaan ke sistem feeder eksternal, lihat {@link #getFeeder()}. */
	private String feeder;

	/** Konstruktor tanpa argumen yang dibutuhkan Hibernate/JPA. */
	public CabangPrestasiMahasiswa() {
	}

	/**
	 * Kunci utama baris (kolom {@code id}, {@code IDENTITY} &mdash; dibangkitkan database).
	 *
	 * @return id baris; {@code null} selama object belum pernah disimpan.
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menetapkan kunci utama. Praktis hanya dipanggil Hibernate.
	 *
	 * @param id kunci utama baris.
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Kode cabang prestasi. <b>Getter-mutasi</b>: memetakan {@link #getNama()} ke kode tetap
	 * (Seni=2, Olah Raga=3, Kejuaraan Ilmiah=1, Lain-Lain=9) dan menugaskan hasilnya kembali ke
	 * field {@code kode} setiap dipanggil; nama yang tidak cocok mempertahankan nilai
	 * {@code kode} sebelumnya.
	 *
	 * @return kode cabang prestasi; bisa {@code null} bila nama tidak cocok satupun nama baku
	 *         dan {@code kode} belum pernah diisi manual.
	 */
	public String getKode() {
		if (nama != null) {
			if (nama.equalsIgnoreCase("Seni")) {
				kode = "2";
			} else if (nama.equalsIgnoreCase("Olah Raga")) {
				kode = "3";
			} else if (nama.equalsIgnoreCase("Kejuaraan Ilmiah")) {
				kode = "1";
			} else if (nama.equalsIgnoreCase("Lain-Lain")) {
				kode = "9";
			}
		}
		return kode;
	}

	/**
	 * Menetapkan kode cabang prestasi secara manual. Nilai ini akan ditimpa oleh
	 * {@link #getKode()} pada pemanggilan berikutnya jika {@link #getNama()} cocok salah satu
	 * dari empat nama baku.
	 *
	 * @param kode kode cabang prestasi.
	 */
	public void setKode(String kode) {
		this.kode = kode;
	}

	/**
	 * Nama cabang/bidang prestasi (kolom {@code nama}, wajib). Dikembalikan sudah
	 * di-{@code trim()}.
	 *
	 * @return nama; {@code null} bila belum diisi.
	 */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Menetapkan nama cabang/bidang prestasi.
	 *
	 * @param nama nama cabang prestasi.
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Keterangan bebas (kolom {@code keterangan}, {@code nullable}).
	 *
	 * @return keterangan; {@code null} bila belum diisi.
	 */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * Menetapkan keterangan bebas.
	 *
	 * @param keterangan teks keterangan; boleh {@code null}.
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * Kode pemetaan baris ini ke sistem feeder eksternal (bukan kolom terpetakan JPA eksplisit,
	 * hanya field biasa; getter murni tanpa efek samping, berbeda dari
	 * {@link #getKode()}/{@link #getFeeder()} pada beberapa master feeder lain di paket ini yang
	 * melakukan trim/normalisasi).
	 *
	 * @return kode feeder; {@code null} bila belum diisi.
	 */
	public String getFeeder() {
		return feeder;
	}

	/**
	 * Menetapkan kode pemetaan feeder.
	 *
	 * @param feeder kode feeder; boleh {@code null}.
	 */
	public void setFeeder(String feeder) {
		this.feeder = feeder;
	}

}
