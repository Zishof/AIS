package ais.database.model;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

/**
 * Bank generated by hbm2java
 *
 * <h2>Integrasi Google Drive — kode OAuth MENTAH, AKTIF DIPAKAI (bukan yatim)</h2>
 * <p>
 * Sama seperti {@link GCalendarCode} (bukan seperti pola yang sudah dikonfirmasi
 * yatim pada entitas SharePoint/Azure): {@link #getKeterangan()} menyimpan
 * authorization code OAuth Google Drive MENTAH milik pengguna {@link #getNama()},
 * ditulis oleh {@code ais.common.gdrive.GDriveUtilPerPengguna#simpanCodeDrive}
 * dan dibaca kembali oleh implementasi {@code waitForCode()} di kelas yang sama
 * untuk ditukar menjadi access/refresh token. Refresh token hasil pertukaran
 * disimpan terpisah (di luar entitas ini) lewat penyimpanan kredensial lokal
 * per pengguna.
 * </p>
 * <p>
 * <b>Catatan keamanan:</b> kode OAuth disimpan TANPA ENKRIPSI. Berbeda dari
 * {@link GCalendarCode}, di sini ADA method pembersihan eksplisit
 * ({@code hapusCodeDrive}) yang menghapus baris ini dari tabel — namun dipanggil
 * di AWAL proses koneksi baru (agar timer tidak bereaksi ke sisa percobaan lama),
 * BUKAN setelah kode berhasil ditukar jadi token. Akibatnya kode OAuth mentah
 * tetap tersisa di DB sejak berhasil dipakai sampai percobaan koneksi berikutnya
 * (jendela paparan data-at-rest yang sama seperti {@link GCalendarCode}).
 * </p>
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "public", name = "gdrive_code")

public class GDriveCode extends GeneralValueObject {

	/** 
	 * 
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	private Long id;
	private String oleh;
	private String olehId;

	/**
	 * @return id akun yang membuat/mengubah baris ini.
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menyimpan id pembuat/pengubah. Nilai kosong/null diabaikan (tidak menimpa
	 * nilai lama) — write-guard satu-arah, konsisten dengan pola arsip lain.
	 *
	 * @param olehId id akun pembuat/pengubah.
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Menyimpan nama pembuat/pengubah. Nilai kosong/null diabaikan (tidak menimpa
	 * nilai lama) — write-guard satu-arah, konsisten dengan pola arsip lain.
	 *
	 * @param oleh nama akun pembuat/pengubah.
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * @return nama akun yang membuat/mengubah baris ini.
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Hook Envers/JPA: memperbarui timestamp audit shadow {@link #tanggal_dirubah}
	 * setiap kali baris ini di-update. Field ini adalah kebutuhan teknis, bukan bug.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * @param tanggal_dirubah waktu perubahan terakhir (audit shadow field).
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * @return waktu perubahan terakhir baris ini, diisi otomatis oleh {@link #onUpdate()}.
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * @return representasi ringkas "{id}-{nama}", dipakai untuk keperluan log/debug.
	 */
	public String toString() {
		return id + "-" + nama;
	}

	private String nama;
	private String keterangan;

	/**
	 * Konstruktor kosong (dipakai Hibernate untuk instansiasi via reflection).
	 */
	public GDriveCode() {
	}

	/**
	 * @return id unik baris (surrogate key, auto-increment).
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * @param id id unik baris.
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * @return username pengguna pemilik baris ini (kunci pencarian upsert di
	 *         {@code GDriveUtilPerPengguna}), di-trim saat dibaca.
	 */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * @param nama username pengguna pemilik baris ini.
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * @return authorization code OAuth Google Drive MENTAH (tanpa enkripsi) milik
	 *         pengguna {@link #getNama()} — lihat catatan keamanan pada Javadoc
	 *         kelas untuk konteks lengkap (entitas ini AKTIF dipakai, bukan yatim).
	 */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * @param keterangan authorization code OAuth mentah untuk disimpan.
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

}
