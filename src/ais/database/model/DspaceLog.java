package ais.database.model;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

/**
 * Entity <b>DSPACE LOG</b> — jejak audit satu pemanggilan HTTP ke repositori digital eksternal
 * DSpace (lewat {@code ais.common.URLCommon}), dicatat TERLEPAS dari hasil pemanggilan (sukses
 * maupun gagal). Dipetakan ke tabel {@code public.dspace_log}.
 *
 * <p>Header berkas ini masih menyisakan komentar asli <i>"Bank generated by hbm2java"</i> dari
 * generator Hibernate Tools — sisa <i>copy-paste</i> template, tidak ada hubungannya dengan entity
 * {@code Bank}.</p>
 *
 * <h2>Peran dalam sistem</h2>
 * <p>Setiap method integrasi di {@code ais.common.URLCommon} ({@code post}/{@code put}/
 * {@code delete}/{@code upload}) membuat satu baris {@code DspaceLog} sebelum memanggil endpoint
 * DSpace ({@code ConstantValues.DSPACE_URL_PRIVATE}), mengisi {@link #getAction() action} dengan
 * salah satu konstanta {@link #UPLOAD}/{@link #SIMPAN}/{@link #UBAH}/{@link #HAPUS}, lalu setelah
 * respons diterima mengisi {@link #getStatus() status} (kode status HTTP), {@link #getHandle()
 * handle} (identitas objek DSpace dari field {@code retrieveLink}/{@code handle} pada respons), dan
 * bila terjadi kegagalan {@link #getError() error} (pesan {@code exception}). Baris ini kemudian
 * disimpan lewat {@code URLCommon.simpanDspaceLog(DspaceLog)} dan bisa dilihat lewat layar
 * {@code ais.action.master.DspaceLogAction}.</p>
 *
 * <p>Berbeda dari {@link ais.database.model.DspaceLog}'s rekan {@code DspaceInformation} (klien
 * DSpace eksternal via REST, sudah didokumentasikan sebelumnya): entity itu menyimpan KONFIGURASI
 * koneksi DSpace, sedangkan entity ini menyimpan RIWAYAT PEMANGGILANnya.</p>
 *
 * @see ais.common.URLCommon
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "public", name = "dspace_log")

public class DspaceLog extends GeneralValueObject {

	/** Nilai {@link #getAction() action} untuk pemanggilan unggah berkas binari ke DSpace. */
	public static final String UPLOAD = "Upload";

	/** Nilai {@link #getAction() action} untuk pemanggilan penyimpanan data (POST) ke DSpace. */
	public static final String SIMPAN = "Simpan";

	/** Nilai {@link #getAction() action} untuk pemanggilan pembaruan data (PUT) ke DSpace. */
	public static final String UBAH = "Ubah";

	/** Nilai {@link #getAction() action} untuk pemanggilan penghapusan data (DELETE) ke DSpace. */
	public static final String HAPUS = "Hapus";

	/**
	 * Versi serialisasi Java untuk kompatibilitas antar build kelas ini.
	 */
	private static final long serialVersionUID = 2463821577548439808L;

	/** Kunci utama, di-<i>generate</i> database ({@code IDENTITY}). */
	private Long id;

	/** Nama pengguna terakhir yang mengubah baris ini; diisi otomatis lewat {@link #onUpdate()}. */
	private String oleh;

	/** Id pengguna terakhir yang mengubah baris ini; diisi otomatis lewat {@link #onUpdate()}. */
	private String olehId;

	/**
	 * Mengembalikan id pengguna pengubah terakhir.
	 *
	 * @return id pengguna pengubah terakhir, atau {@code null} bila belum pernah di-<i>update</i>
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menetapkan id pengguna pengubah terakhir. Nilai {@code null} atau kosong (hanya spasi)
	 * diabaikan diam-diam agar jejak audit tidak bisa dikosongkan lewat pemanggilan ini.
	 *
	 * @param olehId id pengguna pengubah; diabaikan bila {@code null} atau kosong
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Menetapkan nama pengguna pengubah terakhir. Sama seperti {@link #setOlehId(String)}, nilai
	 * {@code null} atau kosong diabaikan.
	 *
	 * @param oleh nama pengguna pengubah; diabaikan bila {@code null} atau kosong
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan nama pengguna terakhir yang mengubah baris ini.
	 *
	 * @return nama pengguna pengubah terakhir, atau {@code null} bila belum pernah di-<i>update</i>
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * <i>Callback</i> JPA {@code @PreUpdate}: mendelegasikan ke
	 * {@code ais.database.hibernate.AuditTimestampInterceptor.ubah(this)} untuk mengisi
	 * {@link #setTanggal_dirubah(Date)}, {@link #setOleh(String)}, {@link #setOlehId(String)} dari
	 * konteks pengguna aktif sebelum baris ini di-{@code UPDATE}.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menetapkan waktu perubahan terakhir baris ini.
	 *
	 * @param tanggal_dirubah waktu perubahan
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan waktu perubahan terakhir baris ini.
	 *
	 * @return waktu perubahan terakhir
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Representasi teks baris: {@code id-keterangan}.
	 *
	 * @return teks ringkas berisi id dan keterangan pemanggilan
	 */
	public String toString() {
		return id + "-" + keterangan;
	}

	/** Kode status HTTP hasil pemanggilan; baku {@code 0} sebelum respons diterima/dicatat. */
	private Integer status = 0;

	/** Jenis aksi pemanggilan; nilai baku salah satu dari {@link #UPLOAD}/{@link #SIMPAN}/{@link #UBAH}/{@link #HAPUS}. */
	private String action;

	/** Ringkasan/isi permintaan (mis. payload JSON atau nama berkas+deskripsi) yang dikirim ke DSpace. */
	private String keterangan;

	/** Pesan galat ({@code exception.getMessage()}) bila pemanggilan gagal; {@code null} bila sukses. */
	private String error;

	/** Identitas objek DSpace yang terpengaruh, diambil dari field {@code retrieveLink}/{@code handle} pada respons. */
	private String handle;

	/**
	 * Konstruktor kosong yang diwajibkan Hibernate/JPA.
	 */
	public DspaceLog() {
	}

	/**
	 * Mengembalikan kunci utama baris. Kolom {@code id} bersifat {@code insertable = false} karena
	 * nilainya dihasilkan database ({@code IDENTITY}).
	 *
	 * @return id baris, atau {@code null} bila baris belum disimpan
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menetapkan kunci utama. Normalnya hanya dipanggil Hibernate setelah {@code INSERT}.
	 *
	 * @param id kunci utama baris
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Mengembalikan ringkasan/isi permintaan yang dikirim ke DSpace.
	 *
	 * @return isi keterangan, bisa {@code null}
	 */
	@Column(name = "keterangan", columnDefinition = "text")
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * Menetapkan ringkasan/isi permintaan yang dikirim ke DSpace.
	 *
	 * @param keterangan teks ringkasan/payload
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * Mengembalikan kode status HTTP hasil pemanggilan.
	 *
	 * @return kode status HTTP, atau {@code 0} bila respons belum/tidak pernah dicatat
	 */
	public Integer getStatus() {
		return status;
	}

	/**
	 * Menetapkan kode status HTTP hasil pemanggilan.
	 *
	 * @param status kode status HTTP baru
	 */
	public void setStatus(Integer status) {
		this.status = status;
	}

	/**
	 * Mengembalikan jenis aksi pemanggilan apa adanya.
	 *
	 * @return salah satu dari {@link #UPLOAD}/{@link #SIMPAN}/{@link #UBAH}/{@link #HAPUS}, atau
	 *         {@code null} bila belum diisi
	 */
	public String getAction() {
		return action;
	}

	/**
	 * Menetapkan jenis aksi pemanggilan.
	 *
	 * @param action salah satu konstanta aksi kelas ini
	 */
	public void setAction(String action) {
		this.action = action;
	}

	/**
	 * Mengembalikan pesan galat pemanggilan.
	 *
	 * @return pesan {@code exception.getMessage()}, atau {@code null} bila pemanggilan sukses
	 */
	@Column(name = "error", columnDefinition = "text")
	public String getError() {
		return error;
	}

	/**
	 * Menetapkan pesan galat pemanggilan.
	 *
	 * @param error pesan galat baru
	 */
	public void setError(String error) {
		this.error = error;
	}

	/**
	 * Mengembalikan identitas objek DSpace yang terpengaruh pemanggilan ini.
	 *
	 * @return handle/link objek DSpace, bisa {@code null} bila tidak tersedia pada respons
	 */
	public String getHandle() {
		return handle;
	}

	/**
	 * Menetapkan identitas objek DSpace yang terpengaruh.
	 *
	 * @param handle handle/link objek DSpace
	 */
	public void setHandle(String handle) {
		this.handle = handle;
	}

}
