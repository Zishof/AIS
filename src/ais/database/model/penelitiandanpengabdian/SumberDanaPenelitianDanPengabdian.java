package ais.database.model.penelitiandanpengabdian;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;




import org.hibernate.envers.Audited;



import ais.database.model.GeneralValueObject;

/**
 * Master <b>sumber pendanaan</b> kegiatan penelitian &amp; pengabdian kepada masyarakat —
 * daftar pilihan yang dicentang pengusul untuk menyatakan dari mana biaya kegiatannya berasal.
 *
 * <p><b>Cara dipakai.</b> Relasinya banyak-ke-banyak dari sisi proposal:
 * {@link PengajuanPenelitianDanPengabdian#getSumberDanaPenelitianDanPengabdianes()} memegang
 * himpunan baris master ini lewat tabel penghubung
 * {@code penelitiandanpengabdian.pengajuan_has_sumber_dana}. Satu proposal karenanya boleh
 * mencantumkan lebih dari satu sumber dana (mis. dana internal perguruan tinggi ditambah dana
 * kementerian), dan satu sumber dana dipakai bersama oleh banyak proposal. Pada penyimpanan,
 * {@code PengajuanPenelitianDanPengabdianHelper} mengosongkan lalu mengisi ulang himpunan itu
 * dari kotak centang di layar — bukan menambahkannya secara bertahap.
 *
 * <p><b>Bukan kolom naratif bernama sama.</b> {@link PenelitianDanPengabdian#getSumberDana()}
 * adalah naskah HTML panduan yang menerangkan sumber dana skema secara bebas; master inilah
 * yang menjadi data terstruktur dan bisa direkap. Keduanya tidak saling tersinkron.
 *
 * <p><b>Konsumen laporan.</b> Nilai yang tercatat lewat master ini dijumlahkan pada laporan
 * dana SAPTO {@code LaporanDana_A_6_2_2} (penelitian) dan {@code LaporanDana_A_6_2_3}
 * (pengabdian), yang menelusuri himpunan sumber dana tiap proposal untuk mengelompokkan
 * anggaran. Karena pengelompokan itu bertumpu pada identitas baris master, mengganti nama
 * sebuah sumber dana akan mengubah label seluruh rekap historis yang menunjuknya — sedangkan
 * membuat baris baru yang bermakna sama justru memecah rekap menjadi dua kelompok.
 *
 * <p><b>Baris bawaan.</b> Bila tabel masih kosong, {@code DetailArtikelHelper} menyemai lima
 * baris mengikuti kategori isian borang akreditasi: pembiayaan sendiri oleh peneliti; PT/yayasan
 * yang bersangkutan; Kemdiknas/kementerian lain terkait; institusi dalam negeri di luar
 * Kemdiknas; dan institusi luar negeri. Tidak ada penanda "data bawaan" seperti pada
 * {@link TipePenelitianDanPengabdian}, jadi kelima baris ini boleh disunting maupun dihapus
 * administrator lewat {@code SumberDanaPenelitianDanPengabdianAction}.
 *
 * <p><b>Pemetaan.</b> Skema {@code penelitiandanpengabdian}, tabel
 * {@code sumber_dana_penelitian_dan_pengabdian}, beranotasi {@code @Audited} (Envers) dengan
 * {@code dynamicInsert}/{@code dynamicUpdate}. Komentar pembangkit "Bank generated by hbm2java"
 * adalah sisa kerangka Hibernate Tools dan tidak menggambarkan isi kelas.
 *
 * @see PengajuanPenelitianDanPengabdian#getSumberDanaPenelitianDanPengabdianes()
 * @see PenelitianDanPengabdian#getSumberDana()
 * @see ais.database.model.GeneralValueObject
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "penelitiandanpengabdian", name = "sumber_dana_penelitian_dan_pengabdian")



public class SumberDanaPenelitianDanPengabdian extends GeneralValueObject {

	/**
	 * Penanda versi serialisasi Java, bernilai sama dengan entitas lain sepaket karena kerangka
	 * kelasnya disalin dari sumber yang sama.
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	/** Kunci utama sekuensial dari basis data. Lihat {@link #getId()}. */
	private Long id;
	/** Nama tampil pengguna terakhir yang mengubah baris ini. Lihat {@link #getOleh()}. */
	private String oleh;
	/** Identitas pengguna terakhir yang mengubah baris ini. Lihat {@link #getOlehId()}. */
	private String olehId;

	/**
	 * Mengembalikan identitas pengguna terakhir yang menyimpan baris master ini.
	 *
	 * <p>Bersama {@link #getOleh()} dan {@link #getTanggal_dirubah()} ini blok field audit
	 * bayangan yang diulang di hampir semua entitas AIS — keharusan teknis karena
	 * {@link ais.database.model.GeneralValueObject} adalah POJO abstrak biasa, bukan
	 * {@code @MappedSuperclass}, sehingga propertinya tidak ikut dipetakan ke kolom turunan.
	 *
	 * @return identitas pengguna pengubah terakhir, atau {@code null} bila belum pernah terisi
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menetapkan identitas pengguna pengubah terakhir dengan penjaga satu arah: argumen
	 * {@code null} atau berisi spasi saja diabaikan sehingga jejak audit tidak dapat
	 * dikosongkan lewat setter ini.
	 *
	 * @param olehId identitas pengguna pengubah; {@code null}/kosong diabaikan diam-diam
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Menetapkan nama tampil pengguna pengubah terakhir, dengan penjaga satu arah yang sama
	 * seperti {@link #setOlehId(String)}.
	 *
	 * @param oleh nama tampil pengguna pengubah; {@code null}/kosong diabaikan diam-diam
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan nama tampil pengguna terakhir yang menyimpan baris master ini.
	 *
	 * @return nama pengguna pengubah terakhir, atau {@code null} bila belum pernah terisi
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Kait daur hidup JPA sebelum setiap {@code UPDATE}; mendelegasikan pencatatan stempel waktu
	 * dan identitas pengubah ke {@code AuditTimestampInterceptor.ubah(this)}. Deklarasi field
	 * {@code tanggal_dirubah} sengaja ditempelkan pada baris yang sama — pola penyisipan blok
	 * audit yang dipakai di seluruh basis kode AIS.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menetapkan stempel waktu perubahan terakhir; biasanya sudah diurus {@link #onUpdate()}.
	 *
	 * @param tanggal_dirubah waktu perubahan yang ingin dicatat
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan stempel waktu perubahan terakhir baris ini.
	 *
	 * @return waktu perubahan terakhir; untuk objek baru berisi waktu objek dibuat
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Mengembalikan nama sumber dana sebagai representasi teks objek — nilai inilah yang tampil
	 * sebagai label kotak centang pada layar pengajuan dan sebagai nama kelompok pada rekap dana
	 * SAPTO.
	 *
	 * @return nama sumber dana apa adanya, boleh {@code null} bila belum diisi
	 */
	public String toString() {
		return nama;
	}

	/** Nama sumber dana yang tampil ke pengusul, mis. "Institusi luar negeri". Lihat {@link #getNama()}. */
	private String nama;
	/** Keterangan bebas untuk administrator. Lihat {@link #getKeterangan()}. */
	private String keterangan;
	/** Penanda sumber dana masih ditawarkan; bawaan {@code true}. Lihat {@link #getAktif()}. */
	private Boolean aktif;

	/**
	 * Konstruktor tanpa argumen yang diwajibkan Hibernate; juga dipakai {@code DetailArtikelHelper}
	 * saat menyemai lima baris bawaan pada basis data yang masih kosong.
	 */
	public SumberDanaPenelitianDanPengabdian() {
	}

	/**
	 * Mengembalikan kunci utama sumber dana. Kolomnya {@code insertable = false} karena nilainya
	 * dibangkitkan basis data.
	 *
	 * <p>Id inilah yang tersimpan di tabel penghubung {@code pengajuan_has_sumber_dana}, jadi ia
	 * merupakan satu-satunya pengikat antara proposal dan sumber dananya: menghapus baris master
	 * memutus kaitan itu untuk seluruh proposal historis yang menunjuknya. Master ini tidak punya
	 * penanda data bawaan yang melindunginya dari penghapusan.
	 *
	 * @return id baris, atau {@code null} bila objek belum pernah disimpan
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menetapkan kunci utama; dipakai Hibernate dan proses impor.
	 *
	 * @param id kunci utama baris
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Mengembalikan nama sumber dana apa adanya, tanpa normalisasi.
	 *
	 * <p>Kolom dideklarasikan {@code nullable = false}, namun getter tidak memberi nilai
	 * pengganti sehingga objek yang belum diisi tetap mengembalikan {@code null} sebelum sempat
	 * ditolak basis data. Tidak ada penjaga keunikan nama: dua baris "Dana internal PT" dapat
	 * hidup berdampingan dan akan memecah rekap dana SAPTO menjadi dua kelompok yang sebenarnya
	 * sama.
	 *
	 * @return nama sumber dana, boleh {@code null}
	 */
	@Column(name = "nama", nullable = false, columnDefinition = "text")
	public String getNama() {
		return this.nama;
	}

	/**
	 * Menetapkan nama sumber dana.
	 *
	 * <p>Perubahan nama berlaku surut: karena proposal hanya menyimpan rujukan ke baris ini,
	 * seluruh rekap historis akan tampil dengan nama yang baru.
	 *
	 * @param nama nama sumber dana
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Mengembalikan keterangan bebas sumber dana ini, apa adanya.
	 *
	 * @return keterangan, boleh {@code null}
	 */
	@Column(name = "keterangan", nullable = true, columnDefinition = "text")
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * Menetapkan keterangan bebas sumber dana ini.
	 *
	 * @param keterangan catatan bebas; boleh {@code null}
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * Mengembalikan penanda apakah sumber dana masih ditawarkan, dengan bawaan {@code true} bila
	 * kolom masih {@code null}; nilai bawaan itu ditulis balik ke field sehingga pembacaan biasa
	 * dapat memunculkan {@code UPDATE} pada entitas {@code dynamicUpdate} ini.
	 *
	 * <p>Penanda ini benar-benar ditegakkan pada daftar pilihan pengajuan, dan penyaringnya
	 * ditulis dengan benar untuk data lama:
	 * {@code Restrictions.or(Restrictions.isNull("aktif"), Restrictions.eq("aktif", true))} —
	 * artinya baris warisan yang kolomnya masih {@code NULL} tetap ikut ditawarkan, selaras
	 * dengan bawaan getter ini. Bandingkan dengan penyaring pada
	 * {@link PenelitianDanPengabdian#getAktif()} yang hanya menguji kesamaan dengan {@code true}
	 * sehingga membuang baris ber-{@code NULL}; pola pada master ini yang lebih aman untuk
	 * ditiru.
	 *
	 * <p>Menonaktifkan sumber dana hanya menyembunyikannya dari kotak centang proposal baru;
	 * proposal lama yang sudah menunjuknya tetap utuh dan tetap ikut terekap.
	 *
	 * @return {@code true} bila sumber dana masih ditawarkan; tidak pernah {@code null}
	 */
	public Boolean getAktif() {
		if (aktif == null) {
			aktif = true;
		}
		return aktif;
	}

	/**
	 * Menetapkan penanda aktif sumber dana. Menonaktifkan lebih aman daripada menghapus, sebab
	 * penghapusan memutus kaitan seluruh proposal historis yang menunjuk baris ini.
	 *
	 * @param aktif {@code true} bila sumber dana masih ditawarkan
	 */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}

}
