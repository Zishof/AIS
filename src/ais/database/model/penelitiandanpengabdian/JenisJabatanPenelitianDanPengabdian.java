package ais.database.model.penelitiandanpengabdian;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;




import org.hibernate.envers.Audited;



import ais.database.model.GeneralValueObject;

/**
 * Master <b>jabatan/peran anggota tim</b> penelitian &amp; pengabdian (ketua, anggota, dan
 * seterusnya) — namun perlu diketahui sejak awal: <b>entitas ini yatim, belum benar-benar
 * terpakai</b>.
 *
 * <p><b>Status pemakaian, hasil penelusuran menyeluruh.</b> Tidak ada satu pun entitas di basis
 * kode ini yang memegang kunci asing ke tabel {@code jenis_jabatan_penelitian_dan_pengabdian}.
 * Peran anggota tim justru dicatat sebagai teks bebas pada kolom {@code keterangan} milik
 * {@link AnggotaPengajuanPenelitianDanPengabdian} (untuk tim proposal) dan
 * {@link AnggotaArtikel} (untuk tim penulis artikel). Seluruh rujukan yang ada ke kelas ini
 * hanya tiga macam:
 * <ul>
 * <li>{@code JenisJabatanPenelitianDanPengabdianAction} — layar CRUD masternya, terpasang di
 * menu sebagai "Jabatan Penelitian"
 * ({@code /pages/master/penelitiandanpengabdian/jenis_jabatan_penelitian_dan_pengabdian.zul});</li>
 * <li>penyemaian dua baris bawaan "Ketua" dan "Anggota" yang dilakukan di dua tempat terpisah —
 * layar CRUD tersebut dan {@code DetailArtikelHelper} — keduanya dijaga syarat "tabel masih
 * kosong";</li>
 * <li>deklarasi kelas ini sendiri.</li>
 * </ul>
 * Dengan kata lain administrator dapat menambah, menyunting, dan menghapus daftar jabatan, tetapi
 * daftar itu tidak pernah ditawarkan sebagai pilihan di layar mana pun. Ini bentuk "entitas
 * tidur" — infrastruktur yang sudah dibangun (tabel, layar, menu, data bawaan) untuk fitur yang
 * penyambungannya ke alur kerja belum dikerjakan.
 *
 * <p><b>Implikasi bagi pengembangan.</b> Bila kelak peran anggota tim ingin dibuat terstruktur —
 * misalnya agar rekap "berapa kali seorang dosen menjadi ketua" dapat dihitung untuk BKD atau
 * borang akreditasi — kelas inilah kandidat rujukannya, dan pekerjaannya meliputi penambahan FK
 * pada kedua entitas anggota berikut migrasi nilai {@code keterangan} lama yang berupa teks
 * bebas. Sebaliknya, bila fitur itu diputuskan tidak diperlukan, kelas beserta layar dan menunya
 * dapat dipensiunkan. Yang jelas: menghapus baris di layar "Jabatan Penelitian" saat ini tidak
 * berdampak apa pun pada data proposal maupun artikel, karena tidak ada yang menunjuknya.
 *
 * <p><b>Pemetaan.</b> Skema {@code penelitiandanpengabdian}, tabel
 * {@code jenis_jabatan_penelitian_dan_pengabdian}, beranotasi {@code @Audited} (Envers) dengan
 * {@code dynamicInsert}/{@code dynamicUpdate}. Berbeda dari master lain sepaket, kolom
 * {@code nama} dan {@code keterangan} di sini dipetakan sebagai kolom teks berpanjang bawaan
 * (tanpa {@code columnDefinition = "text"}). Komentar pembangkit "Bank generated by hbm2java"
 * adalah sisa kerangka Hibernate Tools.
 *
 * @see AnggotaPengajuanPenelitianDanPengabdian#getKeterangan()
 * @see AnggotaArtikel
 * @see ais.database.model.GeneralValueObject
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "penelitiandanpengabdian", name = "jenis_jabatan_penelitian_dan_pengabdian")



public class JenisJabatanPenelitianDanPengabdian extends GeneralValueObject {

	/**
	 * Penanda versi serialisasi Java, bernilai sama dengan entitas lain sepaket karena kerangka
	 * kelasnya disalin dari sumber yang sama.
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	/** Kunci utama sekuensial dari basis data. Lihat {@link #getId()}. */
	private Long id;
	/**
	 * Blok audit bayangan yang dipadatkan ke satu baris — pola penyisipan otomatis yang dipakai
	 * di seluruh basis kode AIS agar dapat ditempelkan ke entitas lama tanpa mengubah struktur
	 * berkas. Isinya: field {@code oleh} (nama tampil pengubah terakhir, lihat
	 * {@link #getOleh()}), field {@code olehId} beserta getter-nya (identitas pengubah terakhir),
	 * dan setter {@code setOlehId} yang berpenjaga satu arah — argumen {@code null} atau berisi
	 * spasi saja diabaikan sehingga jejak audit yang sudah terisi tidak dapat dikosongkan
	 * kembali lewat setter.
	 *
	 * <p>Pengulangan blok ini di hampir setiap entitas adalah keharusan teknis, bukan cacat:
	 * {@link ais.database.model.GeneralValueObject} merupakan POJO abstrak biasa dan bukan
	 * {@code @MappedSuperclass}, sehingga properti yang dideklarasikan di sana tidak ikut
	 * dipetakan Hibernate ke kolom tabel turunannya.
	 */
	private String oleh;private String olehId;public String getOlehId() {return olehId;}public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}this.olehId = olehId;}

	/**
	 * Menetapkan nama tampil pengguna pengubah terakhir dengan penjaga satu arah: argumen
	 * {@code null} atau berisi spasi saja diabaikan.
	 *
	 * @param oleh nama tampil pengguna pengubah; {@code null}/kosong diabaikan diam-diam
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan nama tampil pengguna terakhir yang menyimpan baris master ini.
	 *
	 * @return nama pengguna pengubah terakhir, atau {@code null} bila belum pernah terisi
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Kait daur hidup JPA sebelum setiap {@code UPDATE}; mendelegasikan pencatatan stempel waktu
	 * dan identitas pengubah ke {@code AuditTimestampInterceptor.ubah(this)}. Deklarasi field
	 * {@code tanggal_dirubah} sengaja ditempelkan pada baris yang sama, mengikuti pola blok
	 * audit yang sama seperti pada baris {@code oleh}/{@code olehId} di atas.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menetapkan stempel waktu perubahan terakhir; biasanya sudah diurus {@link #onUpdate()}.
	 *
	 * @param tanggal_dirubah waktu perubahan yang ingin dicatat
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan stempel waktu perubahan terakhir baris ini.
	 *
	 * @return waktu perubahan terakhir; untuk objek baru berisi waktu objek dibuat
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Mengembalikan nama jabatan sebagai representasi teks objek, mis. "Ketua" atau "Anggota".
	 *
	 * <p>Karena entitas ini belum dirujuk relasi mana pun, hasil {@code toString()} praktis
	 * hanya terlihat pada layar master dan pada keluaran log.
	 *
	 * @return nama jabatan apa adanya, boleh {@code null}
	 */
	public String toString() {
		return nama;
	}

	/** Nama jabatan/peran anggota tim, mis. "Ketua". Lihat {@link #getNama()}. */
	private String nama;
	/** Keterangan bebas untuk administrator. Lihat {@link #getKeterangan()}. */
	private String keterangan;
	/** Penanda jabatan masih dipakai; bawaan {@code true}. Lihat {@link #getAktif()}. */
	private Boolean aktif;

	/**
	 * Konstruktor tanpa argumen yang diwajibkan Hibernate; juga dipakai kedua jalur penyemaian
	 * saat membuat dua baris bawaan "Ketua" dan "Anggota".
	 */
	public JenisJabatanPenelitianDanPengabdian() {
	}

	/**
	 * Mengembalikan kunci utama jabatan. Kolomnya {@code insertable = false} karena nilainya
	 * dibangkitkan basis data.
	 *
	 * <p>Berbeda dari master lain sepaket, id ini belum pernah disimpan sebagai kunci asing di
	 * tabel mana pun — lihat catatan pada javadoc kelas mengenai status entitas yang masih
	 * yatim.
	 *
	 * @return id baris, atau {@code null} bila objek belum pernah disimpan
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menetapkan kunci utama; dipakai Hibernate dan proses impor.
	 *
	 * @param id kunci utama baris
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Mengembalikan nama jabatan apa adanya, tanpa normalisasi.
	 *
	 * <p>Kolom {@code nullable = false} dan — berbeda dari master lain sepaket — dipetakan
	 * sebagai kolom karakter berpanjang bawaan, bukan {@code text}, sehingga nama yang terlalu
	 * panjang dapat ditolak basis data. Tidak ada penjaga keunikan nama.
	 *
	 * @return nama jabatan, boleh {@code null} untuk objek yang belum diisi
	 */
	@Column(name = "nama", nullable = false)
	public String getNama() {
		return this.nama;
	}

	/**
	 * Menetapkan nama jabatan.
	 *
	 * @param nama nama jabatan, mis. "Ketua"
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Mengembalikan keterangan bebas jabatan ini, apa adanya.
	 *
	 * @return keterangan, boleh {@code null}
	 */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * Menetapkan keterangan bebas jabatan ini.
	 *
	 * @param keterangan catatan bebas; boleh {@code null}
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * Mengembalikan penanda apakah jabatan masih dipakai, dengan bawaan {@code true} bila kolom
	 * masih {@code null}; nilai bawaan itu ditulis balik ke field sehingga pembacaan biasa dapat
	 * memunculkan {@code UPDATE} pada entitas {@code dynamicUpdate} ini.
	 *
	 * <p>Selama entitas ini belum dirujuk relasi apa pun, penanda ini tidak menyaring apa-apa —
	 * ia baru bermakna bila kelak daftar jabatan ditawarkan sebagai pilihan pada layar tim
	 * proposal atau tim artikel.
	 *
	 * @return {@code true} bila jabatan dianggap aktif; tidak pernah {@code null}
	 */
	public Boolean getAktif() {
		if (aktif == null) {
			aktif = true;
		}
		return aktif;
	}

	/**
	 * Menetapkan penanda aktif jabatan.
	 *
	 * @param aktif {@code true} bila jabatan masih dipakai
	 */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}

}
