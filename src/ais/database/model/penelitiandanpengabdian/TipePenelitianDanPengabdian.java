package ais.database.model.penelitiandanpengabdian;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;




import org.hibernate.envers.Audited;



import ais.database.model.GeneralValueObject;

/**
 * Master <b>kategori kegiatan</b> penelitian &amp; pengabdian — sumbu yang secara de-facto
 * memisahkan penelitian ilmiah, pengabdian kepada masyarakat, dan kegiatan lainnya di seluruh
 * modul AIS.
 *
 * <p><b>Tiga baris istimewa.</b> Isi tabel ini praktis tetap: {@code InitDataHelper} memastikan
 * ada baris berkode {@code 001.000} ("Penelitian Ilmiah"), {@code 002.000} ("Pengabdian
 * Masyarakat"), dan {@code 003.000} ("Lainnya"), lalu menyimpannya sebagai singleton
 * {@code ConstantValues.PENELITIAN}, {@code ConstantValues.PENGABDIAN}, dan
 * {@code ConstantValues.PENELITIAN_LAINNYA}. Ketiga singleton itulah yang diedarkan sebagai
 * parameter ke helper pengajuan dan penyaring laporan, sehingga kelas ini berperan sebagai
 * <i>enum yang disimpan di basis data</i>. Pemakainya antara lain layar asesmen dan kinerja
 * BKD, profil serta biodata dosen, dasbor dosen, dan laporan LKPS akreditasi 3.a.2 (penelitian)
 * dan 4.a.2 (pengabdian).
 *
 * <p><b>Tipe versus Jenis — dua sumbu bebas, diverifikasi dari pemakaian.</b> Saudaranya,
 * {@link JenisPenelitianDanPengabdian}, adalah sumbu klasifikasi berjenjang (punya
 * {@code parent}) yang hanya dipakai melabeli dan mengelompokkan skema. Kelas ini justru rata
 * (tanpa {@code parent}) namun dibaca mesin. Keduanya jelas berangkat dari daftar yang sama —
 * kode semai persis serupa ({@code 001.000}-{@code 003.000}) dengan dua isi pertama yang
 * identik — lalu berkembang ke arah berbeda: kelas ini menumbuhkan penanda
 * {@link #getDefaultData() defaultData} untuk melindungi baris bawaannya, saudaranya
 * menumbuhkan hierarki. Karena itu jangan menganggap keduanya saling menggantikan.
 *
 * <p>{@link PenelitianDanPengabdian} merujuk kelas ini lewat FK yang {@code nullable = true},
 * sedangkan FK ke master jenis justru {@code nullable = false} — pembalikan kewajiban yang
 * kontra-intuitif mengingat sumbu inilah yang menentukan makna fungsional skema.
 *
 * <p><b>Pemetaan.</b> Skema {@code penelitiandanpengabdian}, tabel
 * {@code tipe_penelitian_dan_pengabdian}, beranotasi {@code @Audited} (Envers) dengan
 * {@code dynamicInsert}/{@code dynamicUpdate}. Terdaftar pula pada senarai
 * {@code InitData.initClasses(...)} sehingga isinya dipanaskan ke cache saat aplikasi mulai.
 * Komentar pembangkit "Bank generated by hbm2java" adalah sisa kerangka Hibernate Tools.
 *
 * @see JenisPenelitianDanPengabdian
 * @see PenelitianDanPengabdian
 * @see ais.database.model.GeneralValueObject
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "penelitiandanpengabdian", name = "tipe_penelitian_dan_pengabdian")
public class TipePenelitianDanPengabdian extends GeneralValueObject {

	/**
	 * Penanda versi serialisasi Java, bernilai sama dengan entitas lain sepaket karena kerangka
	 * kelasnya disalin dari sumber yang sama.
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	/** Kunci utama sekuensial dari basis data. Lihat {@link #getId()}. */
	private Long id;
	/** Nama tampil pengguna terakhir yang mengubah baris ini. Lihat {@link #getOleh()}. */
	private String oleh;
	/** Identitas pengguna terakhir yang mengubah baris ini. Lihat {@link #getOlehId()}. */
	private String olehId;

	/**
	 * Mengembalikan identitas pengguna terakhir yang menyimpan baris master ini.
	 *
	 * <p>Bersama {@link #getOleh()} dan {@link #getTanggal_dirubah()} ini blok field audit
	 * bayangan yang diulang di hampir semua entitas AIS — keharusan teknis karena
	 * {@link ais.database.model.GeneralValueObject} adalah POJO abstrak biasa, bukan
	 * {@code @MappedSuperclass}, sehingga propertinya tidak ikut dipetakan ke kolom turunan.
	 *
	 * @return identitas pengguna pengubah terakhir, atau {@code null} bila belum pernah terisi
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menetapkan identitas pengguna pengubah terakhir dengan penjaga satu arah: argumen
	 * {@code null} atau berisi spasi saja diabaikan sehingga jejak audit tidak dapat
	 * dikosongkan lewat setter ini.
	 *
	 * @param olehId identitas pengguna pengubah; {@code null}/kosong diabaikan diam-diam
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Menetapkan nama tampil pengguna pengubah terakhir, dengan penjaga satu arah yang sama
	 * seperti {@link #setOlehId(String)}.
	 *
	 * @param oleh nama tampil pengguna pengubah; {@code null}/kosong diabaikan diam-diam
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan nama tampil pengguna terakhir yang menyimpan baris master ini.
	 *
	 * @return nama pengguna pengubah terakhir, atau {@code null} bila belum pernah terisi
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Kait daur hidup JPA sebelum setiap {@code UPDATE}; mendelegasikan pencatatan stempel waktu
	 * dan identitas pengubah ke {@code AuditTimestampInterceptor.ubah(this)}. Deklarasi field
	 * {@code tanggal_dirubah} sengaja ditempelkan pada baris yang sama — pola penyisipan blok
	 * audit yang dipakai di seluruh basis kode AIS.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menetapkan stempel waktu perubahan terakhir; biasanya sudah diurus {@link #onUpdate()}.
	 *
	 * @param tanggal_dirubah waktu perubahan yang ingin dicatat
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan stempel waktu perubahan terakhir baris ini.
	 *
	 * @return waktu perubahan terakhir; untuk objek baru berisi waktu objek dibuat
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Mengembalikan label kategori sebagai representasi teks objek — nilai inilah yang muncul
	 * pada combobox tipe, judul tab BKD, dan judul skema yang dibuat otomatis helper pengajuan
	 * (yang merangkai judul dari {@code tipe + " " + tahun}).
	 *
	 * <p>Karena membaca field langsung tanpa nilai pengganti, hasilnya {@code null} untuk baris
	 * yang labelnya belum terisi — dan pada perangkaian judul otomatis di atas, {@code null} itu
	 * akan ikut terbaca sebagai teks "null".
	 *
	 * @return label kategori apa adanya, boleh {@code null}
	 */
	public String toString() {
		return isi;
	}

	/**
	 * Kode kategori. Untuk tiga baris bawaan bernilai {@code 001.000}/{@code 002.000}/
	 * {@code 003.000} dan dipakai program untuk menemukannya kembali. Lihat {@link #getKode()}.
	 */
	private String kode;

	/** Label kategori yang tampil ke pengguna, mis. "Penelitian Ilmiah". Lihat {@link #getIsi()}. */
	private String isi;
	/** Keterangan bebas untuk administrator. Lihat {@link #getKeterangan()}. */
	private String keterangan;
	/** Penanda kategori masih dipakai; bawaan {@code true}. Lihat {@link #getAktif()}. */
	private Boolean aktif;
	/** Penanda baris bawaan sistem yang dilindungi dari hapus/ubah kode. Lihat {@link #getDefaultData()}. */
	private Boolean defaultData;

	/**
	 * Konstruktor tanpa argumen yang diwajibkan Hibernate; juga dipakai {@code InitDataHelper}
	 * saat membuat tiga baris bawaan bila belum ada.
	 */
	public TipePenelitianDanPengabdian() {
	}

	/**
	 * Mengembalikan kunci utama kategori. Kolomnya {@code insertable = false} karena nilainya
	 * dibangkitkan basis data.
	 *
	 * <p>Id di sini tidak boleh dianggap tetap antarinstalasi — kode aplikasi mencari baris
	 * bawaan lewat {@link #getKode()}, bukan lewat id, justru karena urutan penyemaian bisa
	 * berbeda di tiap basis data.
	 *
	 * @return id baris, atau {@code null} bila objek belum pernah disimpan
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menetapkan kunci utama; dipakai Hibernate dan proses impor.
	 *
	 * @param id kunci utama baris
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Mengembalikan label kategori apa adanya, tanpa normalisasi.
	 *
	 * <p>Berbeda dari {@link JenisPenelitianDanPengabdian#getIsi()} yang memetakan {@code null}
	 * menjadi string kosong dan memangkas spasi, getter di sini mengembalikan nilai mentah —
	 * termasuk {@code null} untuk baris yang labelnya belum terisi meski kolom dideklarasikan
	 * {@code nullable = false}. Pemanggil yang merangkai teks dari nilai ini perlu berjaga
	 * sendiri.
	 *
	 * @return label kategori, boleh {@code null}
	 */
	@Column(name = "isi", nullable = false, columnDefinition = "text")
	public String getIsi() {
		return this.isi;
	}

	/**
	 * Menetapkan label kategori.
	 *
	 * <p>Mengubah label baris bawaan tidak memutus pencarian program, sebab pencarian dilakukan
	 * atas {@link #getKode()} — jadi perguruan tinggi boleh menyesuaikan istilah tampilannya
	 * tanpa merusak penyaringan BKD maupun laporan akreditasi.
	 *
	 * @param isi label kategori
	 */
	public void setIsi(String isi) {
		this.isi = isi;
	}

	/**
	 * Mengembalikan keterangan bebas kategori ini, apa adanya.
	 *
	 * @return keterangan, boleh {@code null}
	 */
	@Column(name = "keterangan", nullable = true, columnDefinition = "text")
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * Menetapkan keterangan bebas kategori ini.
	 *
	 * @param keterangan catatan bebas; boleh {@code null}
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * Mengembalikan kode kategori — <b>kunci fungsional</b> master ini.
	 *
	 * <p>{@code InitDataHelper} mencari baris bawaan dengan {@code Restrictions.eq("kode", ...)}
	 * atas nilai {@code 001.000}, {@code 002.000}, dan {@code 003.000}, lalu menyimpan hasilnya
	 * ke singleton {@code ConstantValues}. Karena itu kode inilah yang harus dijaga tetap,
	 * bukan id maupun labelnya.
	 *
	 * <p>Meski demikian tidak ada batasan {@code unique} pada kolomnya dan tidak ada penjaga
	 * tabrakan kode di entitas ini. Bila dua baris memakai kode yang sama, pencarian bawaan
	 * yang memakai {@code setMaxResults(1)} akan memilih salah satunya tanpa peringatan.
	 * Layar master melindungi hal ini secara terbatas dengan menonaktifkan kotak isian kode
	 * untuk baris yang {@link #getDefaultData()}-nya bernilai benar.
	 *
	 * @return kode kategori, boleh {@code null} untuk baris tambahan buatan pengguna
	 */
	public String getKode() {
		return kode;
	}

	/**
	 * Menetapkan kode kategori. Mengubah kode salah satu baris bawaan akan memutus kaitannya
	 * dengan singleton {@code ConstantValues} dan membuat {@code InitDataHelper} membuat baris
	 * bawaan baru pada penyalaan berikutnya — karena itu layar master mengunci isian kode untuk
	 * baris bawaan.
	 *
	 * @param kode kode kategori
	 */
	public void setKode(String kode) {
		this.kode = kode;
	}

	/**
	 * Mengembalikan penanda apakah kategori masih dipakai, dengan bawaan {@code true} bila kolom
	 * masih {@code null} (nilai bawaan ditulis balik ke field, sehingga pembacaan biasa dapat
	 * memunculkan {@code UPDATE} pada entitas {@code dynamicUpdate} ini).
	 *
	 * <p>Perlu diingat bahwa singleton {@code ConstantValues.PENELITIAN}/{@code PENGABDIAN}
	 * diambil tanpa menyaring penanda ini, jadi menonaktifkan baris bawaan tidak menghentikan
	 * pemakaiannya oleh modul BKD dan laporan akreditasi.
	 *
	 * @return {@code true} bila kategori dianggap aktif; tidak pernah {@code null}
	 */
	public Boolean getAktif() {
		if (aktif == null) {
			aktif = true;
		}
		return aktif;
	}

	/**
	 * Menetapkan penanda aktif kategori.
	 *
	 * @param aktif {@code true} bila kategori masih dipakai
	 */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}

	/**
	 * Mengembalikan penanda "baris bawaan sistem", yaitu baris yang tidak boleh dihapus dan
	 * kodenya tidak boleh diubah pengguna.
	 *
	 * <p><b>Penanda yang menyembuhkan diri sendiri:</b> alih-alih semata membaca kolom, method
	 * ini lebih dulu memeriksa {@link #getKode()}. Bila kodenya salah satu dari {@code 001.000},
	 * {@code 002.000}, atau {@code 003.000} (dibandingkan tanpa peduli besar-kecil huruf dan
	 * setelah dipangkas spasi), field dipaksa bernilai {@code true} — walaupun kolom di basis
	 * data menyimpan {@code false}. Baris lain yang kolomnya {@code null} diturunkan menjadi
	 * {@code false}. Dengan begitu perlindungan tiga baris bawaan tetap berlaku pada instalasi
	 * lama yang kolom penandanya belum pernah diisi, dan tidak dapat dilucuti hanya dengan
	 * mengubah kolom.
	 *
	 * <p>Karena kedua cabang menulis ke field, getter ini punya efek samping: nilai hasil
	 * penyembuhan ikut tersimpan pada flush berikutnya bila objek sedang terkelola.
	 *
	 * <p>Konsumennya adalah {@code TipePenelitianDanPengabdianAction}, yang menyembunyikan
	 * tombol hapus dan mengunci kotak isian kode untuk baris ber-penanda benar. Perlindungan ini
	 * karenanya berlaku di lapisan layar — jalur penyimpanan lain (impor massal, CRUD generik)
	 * tidak ikut memeriksanya.
	 *
	 * @return {@code true} bila baris ini data bawaan sistem; tidak pernah {@code null}
	 */
	public Boolean getDefaultData() {
		if (getKode() != null && (getKode().trim().equalsIgnoreCase("001.000")
				|| getKode().trim().equalsIgnoreCase("002.000") || getKode().trim().equalsIgnoreCase("003.000"))) {
			defaultData = true;
		}
		if (defaultData == null) {
			defaultData = false;
		}
		return defaultData;
	}

	/**
	 * Menetapkan penanda data bawaan.
	 *
	 * <p>Menyetel {@code false} pada baris yang kodenya termasuk tiga kode bawaan tidak
	 * berpengaruh: {@link #getDefaultData()} akan mengembalikannya menjadi {@code true} pada
	 * pembacaan berikutnya. Untuk benar-benar melepas perlindungan, kode barisnya yang harus
	 * diubah.
	 *
	 * @param defaultData {@code true} bila baris ditandai sebagai data bawaan
	 */
	public void setDefaultData(Boolean defaultData) {
		this.defaultData = defaultData;
	}

}
