package ais.database.model;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

import ais.ui.util.WaktuUtil;

/**
 * Entity <b>NOTIFIKASI WA</b> — jejak audit satu pesan WhatsApp yang dikirim/diproses lewat integrasi
 * WhatsApp Business API di {@code ais.action.servlet.Wa} (dan API pendamping
 * {@code ais.action.servlet.api.WaApi}). Dipetakan ke tabel {@code public.notifikasi_wa}.
 *
 * <p>Header berkas ini masih menyisakan komentar asli <i>"Bank generated by hbm2java"</i> dari
 * generator Hibernate Tools — sisa <i>copy-paste</i> template, tidak ada hubungannya dengan entity
 * {@code Bank}.</p>
 *
 * <h2>Peran dalam sistem</h2>
 * <p>Baris baru dibuat oleh {@code Wa.java} setiap kali balasan <i>chatbot</i> WhatsApp terkirim
 * (atau dicoba kirim): {@link #getWas() was} menyimpan nomor WhatsApp pengirim/lawan bicara
 * ({@code from}), {@link #getKeterangan() keterangan} menyimpan payload JSON permintaan yang dikirim
 * ke Graph API Facebook (isi pesan balasan lengkap dengan metadata pengiriman), dan
 * {@link #getHasil() hasil} menyimpan keluaran mentah proses {@code curl} yang memanggil endpoint
 * tersebut (atau {@code "Tidak di kirmkan"} bila fitur balasan otomatis dinonaktifkan lewat
 * konfigurasi {@code aktifkan_reply_chatbot}). {@link #getBuka() buka} menandai status baca pada
 * layar {@code ais.action.master.NotifikasiWaAction}.</p>
 *
 * <h2>Verifikasi relasi integrasi WhatsApp (diminta, dibandingkan dengan {@code WaProfile})</h2>
 * <p>{@code WaProfile.java} (sudah didokumentasikan sebelumnya) dikonfirmasi AMAN — nol token API
 * mentah tersimpan di entity itu. Entity {@code NotifikasiWa} ini SENDIRI juga tidak memiliki kolom
 * token/kredensial — field-fieldnya (nomor WA, payload pesan, hasil kirim) adalah data operasional,
 * bukan rahasia otentikasi. <b>Namun</b>, penelusuran jalur yang MENGISI entity ini
 * ({@code ais.action.servlet.Wa}, method yang membuat {@code NotifikasiWa} di sekitar pemanggilan
 * Graph API) menemukan bahwa token akses Graph API Facebook (bearer token) DIAMBIL lewat
 * {@code Common.getKonfigurasi("token_wa", "&lt;token panjang&gt;")} — argumen kedua adalah
 * <b>token asli hardcoded sebagai nilai default</b> di kode sumber {@code Wa.java}, yang otomatis
 * ditulis ke tabel {@code konfigurasi} pada instalasi mana pun yang belum mengisi {@code token_wa}
 * secara eksplisit (pola auto-seed {@code getKonfigurasi} yang sama seperti dijelaskan di catatan
 * proyek "Konfigurasi auto-seed"). Pola hardcode token serupa juga ditemukan di
 * {@code ais.action.servlet.api.WaApi} ({@code token_ultramsg_baru}, {@code token_instance_id_baru}).
 * Temuan ini BUKAN pada entity {@code NotifikasiWa} ini, melainkan pada kode pemanggil di paket
 * {@code ais.action.servlet}; dilaporkan terpisah sebagai temuan keamanan baru (lihat tugas
 * terlampir) karena berbeda lokasi dan pola dari kluster kerentanan {@code LogLogin} yang sudah
 * tercatat.</p>
 *
 * @see ais.action.servlet.Wa
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "public", name = "notifikasi_wa")
public class NotifikasiWa extends GeneralValueObject {

	/**
	 * Versi serialisasi Java untuk kompatibilitas antar build kelas ini.
	 */
	private static final long serialVersionUID = 2463821577548439808L;

	/** Kunci utama, di-<i>generate</i> database ({@code IDENTITY}). */
	private Long id;

	/** Nama pengguna terakhir yang mengubah baris ini; diisi otomatis lewat {@link #onUpdate()}. */
	private String oleh;

	/** Id pengguna terakhir yang mengubah baris ini; diisi otomatis lewat {@link #onUpdate()}. */
	private String olehId;

	/**
	 * Mengembalikan id pengguna pengubah terakhir.
	 *
	 * @return id pengguna pengubah terakhir, atau {@code null} bila belum pernah di-<i>update</i>
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menetapkan id pengguna pengubah terakhir. Nilai {@code null} atau kosong (hanya spasi)
	 * diabaikan diam-diam agar jejak audit tidak bisa dikosongkan lewat pemanggilan ini.
	 *
	 * @param olehId id pengguna pengubah; diabaikan bila {@code null} atau kosong
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Menetapkan nama pengguna pengubah terakhir. Sama seperti {@link #setOlehId(String)}, nilai
	 * {@code null} atau kosong diabaikan.
	 *
	 * @param oleh nama pengguna pengubah; diabaikan bila {@code null} atau kosong
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan nama pengguna terakhir yang mengubah baris ini.
	 *
	 * @return nama pengguna pengubah terakhir, atau {@code null} bila belum pernah di-<i>update</i>
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * <i>Callback</i> JPA {@code @PreUpdate}: mendelegasikan ke
	 * {@code ais.database.hibernate.AuditTimestampInterceptor.ubah(this)} untuk mengisi
	 * {@link #setTanggal_dirubah(Date)}, {@link #setOleh(String)}, {@link #setOlehId(String)} dari
	 * konteks pengguna aktif sebelum baris ini di-{@code UPDATE}.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menetapkan waktu perubahan terakhir baris ini.
	 *
	 * @param tanggal_dirubah waktu perubahan
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan waktu perubahan terakhir baris ini.
	 *
	 * @return waktu perubahan terakhir
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Representasi teks baris: {@code id-keterangan}.
	 *
	 * @return teks ringkas berisi id dan payload pesan
	 */
	public String toString() {
		return id + "-" + keterangan;
	}

	/** Nomor WhatsApp pengirim/lawan bicara ({@code from}). */
	private String was;

	/** Payload JSON permintaan pesan yang dikirim ke Graph API Facebook. */
	private String keterangan;

	/** Keluaran mentah proses {@code curl} pemanggil Graph API, atau penanda "tidak dikirim" bila fitur nonaktif. */
	private String hasil;

	/** Waktu pesan diproses. */
	private Date waktu;

	/** Status sudah/belum dibaca pada layar {@code NotifikasiWaAction}; {@code null} diperlakukan sebagai {@code false}. */
	private Boolean buka;

	/**
	 * Konstruktor kosong yang diwajibkan Hibernate/JPA.
	 */
	public NotifikasiWa() {
	}

	/**
	 * Mengembalikan kunci utama baris. Kolom {@code id} bersifat {@code insertable = false} karena
	 * nilainya dihasilkan database ({@code IDENTITY}).
	 *
	 * @return id baris, atau {@code null} bila baris belum disimpan
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menetapkan kunci utama. Normalnya hanya dipanggil Hibernate setelah {@code INSERT}.
	 *
	 * @param id kunci utama baris
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Mengembalikan payload JSON permintaan pesan. Field kosong (hanya spasi) diperlakukan sama
	 * seperti {@code null}.
	 *
	 * @return payload JSON, atau {@code null} bila belum diisi/kosong
	 */
	@Column(name = "keterangan", nullable = true, columnDefinition = "text")
	public String getKeterangan() {
		return this.keterangan == null || this.keterangan.trim().isEmpty() ? null : this.keterangan;
	}

	/**
	 * Menetapkan payload JSON permintaan pesan.
	 *
	 * @param keterangan payload JSON baru
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * Mengembalikan nomor WhatsApp pengirim/lawan bicara. Field kosong (hanya spasi) diperlakukan
	 * sama seperti {@code null}.
	 *
	 * @return nomor WhatsApp, atau {@code null} bila belum diisi/kosong
	 */
	@Column(name = "was", nullable = true, columnDefinition = "text")
	public String getWas() {
		return this.was == null || this.was.trim().isEmpty() ? null : this.was;
	}

	/**
	 * Menetapkan nomor WhatsApp pengirim/lawan bicara.
	 *
	 * @param was nomor WhatsApp baru
	 */
	public void setWas(String was) {
		this.was = was;
	}

	/**
	 * Mengembalikan waktu pesan diproses. Bila field {@code null}, dikembalikan waktu SAAT INI dari
	 * {@link WaktuUtil#getDate()} sebagai cadangan — bukan waktu peristiwa yang sebenarnya.
	 *
	 * @return waktu pesan diproses, atau waktu sekarang bila belum pernah diisi
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getWaktu() {
		return waktu == null ? WaktuUtil.getDate() : waktu;
	}

	/**
	 * Menetapkan waktu pesan diproses.
	 *
	 * @param waktu waktu baru
	 */
	public void setWaktu(Date waktu) {
		this.waktu = waktu;
	}

	/**
	 * Mengembalikan keluaran mentah proses pemanggil Graph API, dijaga dari {@code null} (kembalian
	 * string kosong bila belum diisi).
	 *
	 * @return isi hasil, tidak pernah {@code null}
	 */
	@Column(name = "hasil", nullable = true, columnDefinition = "text")
	public String getHasil() {
		return hasil == null ? "" : hasil;
	}

	/**
	 * Menetapkan keluaran mentah proses pemanggil Graph API.
	 *
	 * @param hasil teks hasil baru
	 */
	public void setHasil(String hasil) {
		this.hasil = hasil;
	}

	/**
	 * Mengembalikan status sudah/belum dibaca. Nilai {@code null} diperlakukan sebagai
	 * {@code false} (belum dibaca).
	 *
	 * @return {@code true} bila sudah dibaca, {@code false} bila belum/tidak diisi
	 */
	public Boolean getBuka() {
		return buka == null ? false : buka;
	}

	/**
	 * Menetapkan status sudah/belum dibaca.
	 *
	 * @param buka status baru
	 */
	public void setBuka(Boolean buka) {
		this.buka = buka;
	}

}
