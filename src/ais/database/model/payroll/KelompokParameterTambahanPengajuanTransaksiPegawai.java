package ais.database.model.payroll;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.Session;
import org.hibernate.criterion.Restrictions;
import org.hibernate.envers.Audited;

import ais.database.hibernate.HibernateUtil;
import ais.database.model.GeneralValueObject;

/**
 * Bank generated by hbm2java
 *
 * <p>Entity master <b>kategori (seksi) field kustom</b> untuk modul <i>Pengajuan Transaksi
 * Pegawai</i> (payroll) &mdash; tabel {@code payroll.kelompok_parameter_tambahan_pengajuan_transaksi_pegawai}.
 * Satu baris di sini adalah satu <b>judul seksi</b> yang tercetak sebagai label tebal pemisah
 * antar-kelompok isian dinamis pada formulir {@link PengajuanTransaksiPegawai}; bukan field/isian
 * itu sendiri. Field/isian sesungguhnya hidup di {@link ais.database.model.ParameterTambahan} dan
 * dipasang ke seksi ini lewat baris penghubung {@link ParameterTambahanPengajuanTransaksiPegawai}
 * (kolom FK {@code kelompok_parameter_tambahan_pengajuan_pegawai} pada tabel
 * {@code parameter_tambahan_pengajuan_transaksi_pegawai} &mdash; lihat catatan penamaan kolom di
 * {@link ParameterTambahanPengajuanTransaksiPegawai#getKelompokParameterTambahanPengajuanTransaksiPegawai()}).</p>
 *
 * <h3>Domain "Pengajuan" yang mana? (keluarga {@code KelompokParameterTambahan*} yang mirip)</h3>
 * <p>Kelas ini melayani <b>pengajuan transaksi payroll pegawai</b> ({@link PengajuanTransaksiPegawai},
 * tabel {@code payroll.pengajuan_transaksi_pegawai}, dirender
 * {@code ais.action.master.payroll.PengajuanTransaksiPegawaiAction}). Ia <b>bukan</b> varian yang
 * sama dengan sepupu-sepupunya yang namanya sangat mirip:</p>
 * <ul>
 *   <li>{@code ais.database.model.KelompokParameterTambahanPengajuanPegawai} (paket
 *   {@code ais.database.model}, <b>tanpa</b> "Transaksi") &mdash; melayani pengajuan/permohonan
 *   umum pegawai, modul terpisah dengan tabel sendiri {@code kelompok_parameter_tambahan_pengajuan_pegawai}.
 *   Kemiripan nama inilah akar dari cacat salin-tempel {@code task_fe6517bf}: kode Action modul
 *   payroll ({@code PengajuanTransaksiPegawaiAction.populateParameterTambahan}, didokumentasikan di
 *   {@link PengajuanTransaksiPegawai#populateParameterTambahan(java.util.List)}) sempat/masih
 *   membaca atribut baris ZK dengan kunci dan tipe milik kelas <i>itu</i>, bukan kelas <b>ini</b>,
 *   sehingga {@code getAttribute(...)} selalu {@code null} dan penyimpanan dokumen diam-diam
 *   mengosongkan seluruh isian dinamis. Bahkan nama kolom FK di
 *   {@link ParameterTambahanPengajuanTransaksiPegawai} (lihat catatan pada getter relasinya)
 *   mewarisi jejak salin-tempel yang sama: nama kolomnya masih {@code ..._pengajuan_pegawai},
 *   bukan {@code ..._pengajuan_transaksi_pegawai} &mdash; internal konsisten (Hibernate hanya
 *   memakai nama kolom itu untuk dirinya sendiri) tapi menegaskan modul ini memang hasil
 *   penyalinan dari varian {@code PengajuanPegawai} yang penggantian namanya tidak tuntas.</li>
 *   <li>{@link ais.database.model.KelompokParameterTambahanPengajuan} (paket
 *   {@code ais.database.model}) &mdash; melayani pengajuan layanan peserta didik
 *   ({@code PengajuanMahasiswa}/{@code PengajuanSiswa}), domain akademik, sama sekali berbeda.</li>
 *   <li>{@code KelompokParameterTambahanGajiPegawai} (paket {@code ais.database.model.payroll},
 *   sepupu <b>satu paket</b>) &mdash; melayani <i>rencana/pembayaran gaji</i> ({@code GajiPegawai}),
 *   bukan pengajuan transaksi. Strukturnya identik field-demi-field dengan kelas ini (hasil
 *   generator yang sama), termasuk pola auto-seed {@link #checkCreateDefault()}; bedanya murni
 *   pada tabel target dan konteks pemakaian. Pasangannya,
 *   {@code ParameterTambahanGajiPegawai}, menamai kolom FK-nya secara <b>konsisten</b>
 *   ({@code kelompok_parameter_tambahan_gaji_pegawai}, sesuai nama tabel gaji sendiri) &mdash;
 *   kontras dengan inkonsistensi penamaan di sepupu transaksi ini.</li>
 * </ul>
 * <p>Pola "kelompok/kategori dinamis + baris penghubung ke {@code ParameterTambahan} + registrasi
 * per-jenis lewat {@code @ManyToMany}" ini berulang secara konsisten di banyak modul pengajuan/
 * catatan AIS (pegawai, mahasiswa, siswa, alumni, gaji, transaksi payroll, dst.) &mdash; lihat
 * javadoc {@link ais.database.model.KelompokParameterTambahanPengajuan} untuk uraian rantai
 * 4 lapisnya yang berlaku sama di sini.</p>
 *
 * <h3>Rantai konfigurasi &mdash; 4 lapis, khusus untuk modul ini</h3>
 * <ol>
 *   <li>{@link ais.database.model.ParameterTambahan} &mdash; definisi field kustom (label, tipe
 *   input, pilihan, bendera {@code aktif}, wajib-diisi, wajib-lampiran, dsb).</li>
 *   <li>{@link ParameterTambahanPengajuanTransaksiPegawai} &mdash; baris penghubung yang menempelkan
 *   sebuah {@code ParameterTambahan} ke <b>kategori ini</b> (kolom FK
 *   {@code kelompok_parameter_tambahan_pengajuan_pegawai}).</li>
 *   <li><b>Kelas ini</b> &mdash; kategori/seksi, dengan bendera {@link #getAktif()} dan
 *   {@link #getNomorUrut()} sebagai urutan seksi pada layar.</li>
 *   <li>{@link PengajuanTransaksiPegawai#getKelompokParameterTambahanPengajuanTransaksiPegawais()}
 *   di {@code JenisPengajuanTransaksiPegawai} (relasi {@code @ManyToMany} lewat tabel
 *   {@code jenis_pengajuan_transaksi_pegawai_has_parameter}) &mdash; lapis terakhir: kategori baru
 *   <b>tidak otomatis</b> muncul di formulir. Admin harus mencentang kategori itu per jenis
 *   pengajuan transaksi pada layar master "Jenis Pengajuan Transaksi Pegawai".</li>
 * </ol>
 *
 * <h3>Tidak ada validasi anti-data-yatim di level entity/DB</h3>
 * <p>Kelas ini <b>tidak</b> mendeklarasikan constraint unik apa pun (nama boleh berulang, tidak
 * ada index unik pada kombinasi apa pun) dan <b>tidak</b> memiliki referensi balik wajib ke
 * dokumen induk mana pun &mdash; ia adalah master data berdiri sendiri, dapat eksis tanpa satu pun
 * {@link ParameterTambahanPengajuanTransaksiPegawai} yang menunjuknya (kelompok "yatim", kosong
 * isi) maupun tanpa dicentang di {@code JenisPengajuanTransaksiPegawai} manapun (kelompok tidak
 * pernah tampil). Sebaliknya, {@link ParameterTambahanPengajuanTransaksiPegawai} <b>mewajibkan</b>
 * {@code ParameterTambahan} (kolom {@code nullable = false}) tetapi <b>tidak mewajibkan</b>
 * kelompok ini (kolom {@code nullable = true}) &mdash; baris penghubung tanpa kelompok bisa
 * tersimpan, meski UI ({@code ParameterTambahanPengajuanTransaksiPegawaiAction}) selalu memaksa
 * pemilihan kelompok sebelum simpan. Tidak ada {@code CascadeType.REMOVE}/{@code DELETE} pada
 * relasi manapun ke kelas ini, sehingga menghapus kelompok yang masih dirujuk baris penghubung
 * akan gagal karena pelanggaran FK (bukan silently orphan) &mdash; berbeda dari risiko kehilangan
 * data pada {@code task_fe6517bf}, yang murni bug logika aplikasi (kunci atribut salah), bukan
 * data yatim di database: baris {@link ParameterTambahanPengajuanTransaksiPegawai} dan kelompok
 * ini sendiri tetap utuh, yang hilang hanya <i>jawaban pengguna</i> yang tersimpan sebagai teks di
 * kolom {@code parameterTambahanInds}/{@code parameterTambahan} milik {@link PengajuanTransaksiPegawai}.</p>
 *
 * <p>Seluruh field audit ({@link #getOleh()}, {@link #getOlehId()}, {@link #getTanggal_dirubah()})
 * adalah <b>shadow field</b> yang wajib dideklarasikan ulang di sini secara teknis: kelas dasar
 * {@link GeneralValueObject} <b>bukan</b> {@code @MappedSuperclass}, sehingga field-field itu di
 * sana hanya state Java biasa tanpa pemetaan kolom (lihat javadoc {@link GeneralValueObject}) —
 * ini bukan duplikasi yang bisa dihapus, melainkan keharusan agar Hibernate memetakan kolom
 * {@code oleh}/{@code oleh_id}/{@code tanggal_dirubah} pada tabel kelompok ini.</p>
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "payroll", name = "kelompok_parameter_tambahan_pengajuan_transaksi_pegawai")
public class KelompokParameterTambahanPengajuanTransaksiPegawai extends GeneralValueObject {

	/**
	 * ID versi serialisasi Java untuk kompatibilitas {@link java.io.Serializable} lintas build.
	 * Nilainya konstan hasil generator (tidak pernah diubah manual sejak dibuat), sama persis
	 * dengan {@code serialVersionUID} pada banyak entity lain hasil hbm2java di codebase ini
	 * (nilai generator, bukan hash isi kelas) — kebetulan penomoran, bukan indikasi relasi.
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	/** ID baris (primary key), diisi otomatis oleh DB (kolom {@code IDENTITY}) — lihat {@link #getId()}. */
	private Long id;
	/** Nama akun/username pembuat-atau-pengubah terakhir baris ini; lihat {@link #getOleh()}. */
	private String oleh;
	/** ID pengguna pembuat-atau-pengubah terakhir baris ini; lihat {@link #getOlehId()}. */
	private String olehId;

	/**
	 * Mengembalikan ID pengguna (bukan nama tampilan) yang terakhir membuat/mengubah baris
	 * kelompok ini, hasil pencatatan {@link ais.database.hibernate.AuditTimestampInterceptor}.
	 *
	 * @return ID pengguna audit terakhir, atau {@code null} bila belum pernah diisi
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menyimpan ID pengguna audit. Guard di awal method membuat setter ini <b>diam-diam
	 * menolak</b> nilai {@code null} atau string kosong/whitespace — memanggilnya dengan nilai
	 * semacam itu adalah no-op senyap yang mempertahankan {@link #olehId} lama, bukan
	 * mengosongkannya. Pola guard identik dipakai di {@link #setOleh(String)} dan di kelas
	 * pasangan {@link ParameterTambahanPengajuanTransaksiPegawai}.
	 *
	 * @param olehId ID pengguna baru; diabaikan bila kosong
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Menyimpan nama/username audit. Guard di awal method membuat setter ini <b>diam-diam
	 * menolak</b> nilai {@code null} atau string kosong/whitespace, mempertahankan {@link #oleh}
	 * lama alih-alih mengosongkannya — sama seperti {@link #setOlehId(String)}.
	 *
	 * @param oleh nama/username baru; diabaikan bila kosong
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan nama/username pengguna yang terakhir membuat/mengubah baris kelompok ini.
	 *
	 * @return nama/username audit terakhir, atau {@code null} bila belum pernah diisi
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Callback JPA {@code @PreUpdate}: dipanggil otomatis oleh Hibernate tepat sebelum baris
	 * kelompok ini di-{@code UPDATE}, mendelegasikan pencatatan stempel waktu perubahan ke
	 * {@link ais.database.hibernate.AuditTimestampInterceptor#ubah(GeneralValueObject)}, yang
	 * pada gilirannya memutakhirkan {@link #tanggal_dirubah}. Tidak dipanggil pada
	 * {@code INSERT} pertama — field diinisialisasi saat konstruksi lewat
	 * {@link ais.ui.util.WaktuUtil#getDate()} (lihat inisialisasi {@link #tanggal_dirubah}).
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menyimpan stempel waktu terakhir baris ini dirubah.
	 *
	 * @param tanggal_dirubah stempel waktu baru
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan stempel waktu terakhir baris ini dirubah, dipetakan sebagai kolom
	 * {@code @Temporal(TIMESTAMP)}. Diinisialisasi ke waktu-sekarang saat object dikonstruksi dan
	 * dimutakhirkan otomatis oleh {@link #onUpdate()} setiap kali baris di-{@code UPDATE}.
	 *
	 * @return stempel waktu perubahan terakhir
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Representasi teks singkat baris kelompok ini, berformat {@code "<id>-<nama>"} (mis.
	 * {@code "3-Form Tambahan"}). Dipakai lewat pemanggilan implisit {@code toString()} pada log,
	 * combobox rendering, dan pesan debug; <b>tidak</b> di-override lewat {@link #compareTo}
	 * meski keduanya memakai {@code nama} — lihat catatan pada {@link #compareTo(GeneralValueObject)}
	 * yang justru mengurutkan berdasarkan {@link #getNomorUrut()}, bukan {@code nama}.
	 *
	 * @return teks {@code "id-nama"}; bagian {@code id} bisa muncul sebagai {@code "null"} untuk
	 *         object yang belum tersimpan
	 */
	public String toString() {
		return id + "-" + nama;
	}

	/** Nama kelompok/seksi, tercetak sebagai judul pemisah di formulir; lihat {@link #getNama()}. */
	private String nama;
	/** Keterangan/deskripsi opsional kelompok; lihat {@link #getKeterangan()}. */
	private String keterangan;
	/**
	 * Penanda kelompok bawaan (default) yang di-auto-seed oleh {@link #checkCreateDefault()};
	 * lihat {@link #getDefaultData()}.
	 */
	private Boolean defaultData;
	/** Bendera aktif/nonaktif kelompok — hanya kelompok aktif yang ditampilkan; lihat {@link #getAktif()}. */
	private Boolean aktif;
	/** Urutan tampil kelompok relatif terhadap kelompok lain; lihat {@link #getNomorUrut()}. */
	private Integer nomorUrut;

	/**
	 * Menjamin tersedianya <b>satu</b> baris kelompok bawaan bernama {@code "Form Tambahan"}
	 * ({@link #defaultData} = {@code true}), membuatnya bila belum ada, lalu mengembalikannya.
	 *
	 * <p><b>Pola auto-seed dengan commit langsung ke DB.</b> Method ini mencari baris dengan
	 * {@code defaultData = true} lewat {@code Criteria}; bila tidak ditemukan, ia membangun
	 * instance baru, mengisi {@code nama}/{@code keterangan} = {@code "Form Tambahan"}, lalu
	 * <b>membuka dan meng-commit transaksinya sendiri</b> ({@code session.getTransaction().begin()}
	 * / {@code commit()}) sebelum menutup session lewat {@link HibernateUtil#closeSession()}. Ini
	 * adalah pola auto-seed yang sama seperti pada
	 * {@link ais.database.model.KelompokParameterTambahanPengajuan#checkCreateDefault()} dan
	 * sepupu {@code KelompokParameterTambahanGajiPegawai.checkCreateDefault()}: dipanggil dari
	 * layar master ({@code ParameterTambahanPengajuanTransaksiPegawaiAction}, sebelum combobox
	 * kategori diisi) semata-mata agar dropdown kategori tidak pernah kosong pada instalasi baru
	 * — bukan bug, melainkan keharusan UX yang sengaja menembus lapisan transaksi UI normal.</p>
	 *
	 * <p><b>Efek samping penting:</b> karena transaksinya dibuka/ditutup sendiri dan session
	 * ditutup di akhir, pemanggilan method ini <b>di tengah</b> transaksi/session Hibernate lain
	 * yang sedang berjalan berisiko mengganggu unit-of-work pemanggil (session yang sedang dipakai
	 * pemanggil bisa ikut tertutup bila memakai session "current" yang sama). Method ini juga
	 * <b>tidak idempoten terhadap race</b>: dua pemanggilan konkuren yang sama-sama menemukan hasil
	 * {@code uniqueResult() == null} dapat masing-masing membuat baris "Form Tambahan" sendiri
	 * (tidak ada constraint unik pada {@code defaultData} atau {@code nama} di level DB/entity —
	 * lihat catatan level-kelas soal ketiadaan validasi anti-duplikat).</p>
	 *
	 * @return baris kelompok bawaan yang sudah ada, atau baris baru yang baru saja dibuat dan
	 *         di-commit
	 */
	public static KelompokParameterTambahanPengajuanTransaksiPegawai checkCreateDefault() {
		Session session = HibernateUtil.currentNativeSession();
		KelompokParameterTambahanPengajuanTransaksiPegawai kelompokParameterTambahanCatatanAdministrasi = (KelompokParameterTambahanPengajuanTransaksiPegawai) session
				.createCriteria(KelompokParameterTambahanPengajuanTransaksiPegawai.class)
				.add(Restrictions.eq("defaultData", true)).setMaxResults(1).uniqueResult();
		if (kelompokParameterTambahanCatatanAdministrasi == null) {
			kelompokParameterTambahanCatatanAdministrasi = new KelompokParameterTambahanPengajuanTransaksiPegawai();
			kelompokParameterTambahanCatatanAdministrasi.setDefaultData(true);
			kelompokParameterTambahanCatatanAdministrasi.setNama("Form Tambahan");
			kelompokParameterTambahanCatatanAdministrasi.setKeterangan("Form Tambahan");
			session.getTransaction().begin();
			session.save(kelompokParameterTambahanCatatanAdministrasi);
			session.getTransaction().commit();
		}

		HibernateUtil.closeSession();
		return kelompokParameterTambahanCatatanAdministrasi;
	}

	/** Konstruktor kosong wajib Hibernate/JavaBean; field diisi belakangan lewat setter atau reflection. */
	public KelompokParameterTambahanPengajuanTransaksiPegawai() {
	}

	/**
	 * Primary key baris kelompok, dipetakan {@code IDENTITY} (auto-increment DB) sehingga
	 * {@code insertable = false} — nilainya baru terisi <b>setelah</b> {@code INSERT} berhasil,
	 * bukan sebelum.
	 *
	 * @return ID baris, atau {@code null} untuk object baru yang belum tersimpan
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menyimpan ID baris. Dipanggil Hibernate saat hidrasi dari DB, atau manual saat memuat ulang
	 * proxy by-id; jarang dipanggil aplikasi secara langsung karena kolomnya {@code insertable = false}.
	 *
	 * @param id ID baru
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Nama kelompok/seksi, tercetak sebagai judul pemisah antar-kelompok isian dinamis di layar
	 * pengajuan transaksi pegawai. Kolom wajib ({@code nullable = false}, maks 255 karakter);
	 * nilai yang dikembalikan selalu di-{@code trim()} agar spasi tepi yang tersimpan tidak
	 * bocor ke tampilan/combobox — perbandingan {@code equals}/pencarian teks di layar admin tetap
	 * memakai nilai yang sudah rapi ini.
	 *
	 * @return nama kelompok, sudah di-trim; {@code null} bila field mentah {@code null}
	 */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Menyimpan nama kelompok apa adanya (tanpa trim) — perataan spasi baru terjadi saat dibaca
	 * lewat {@link #getNama()}.
	 *
	 * @param nama nama kelompok baru
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Keterangan/deskripsi bebas untuk kelompok ini, kolom opsional ({@code nullable = true},
	 * tanpa batas panjang eksplisit). Berbeda dari {@link #getNama()}, nilainya dikembalikan
	 * apa adanya <b>tanpa</b> {@code trim()}.
	 *
	 * @return keterangan kelompok, bisa {@code null}
	 */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * Menyimpan keterangan kelompok.
	 *
	 * @param keterangan teks keterangan baru
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * Menandai apakah baris ini adalah kelompok bawaan hasil auto-seed
	 * {@link #checkCreateDefault()}. Getter mengaplikasikan default lazy {@code false} bila field
	 * mentah masih {@code null} (baris lama sebelum kolom ini ada, atau baris yang dibuat manual
	 * tanpa mengisi field ini) — pola default-lazy yang konsisten dengan {@link #getAktif()} dan
	 * banyak entity bertipe {@code Boolean} lain di codebase ini. <b>Tidak</b> dipetakan
	 * {@code @Column} eksplisit; Hibernate memakai konvensi penamaan bawaannya (properti
	 * {@code defaultData} &rarr; kolom {@code default_data}) untuk field ini.
	 *
	 * @return {@code true} bila kelompok bawaan; {@code false} (setelah dinormalisasi) bila bukan
	 */
	public Boolean getDefaultData() {
		if (defaultData == null) {
			defaultData = false;
		}
		return defaultData;
	}

	/**
	 * Menyimpan bendera kelompok-bawaan. Dipanggil {@link #checkCreateDefault()} saat membuat
	 * baris seed; jarang/tidak pernah di-set eksplisit lewat UI admin biasa.
	 *
	 * @param defaultData bendera baru
	 */
	public void setDefaultData(Boolean defaultData) {
		this.defaultData = defaultData;
	}

	/**
	 * Menandai apakah kelompok ini sedang aktif dan boleh ditampilkan di formulir pengajuan
	 * transaksi pegawai. Getter mengaplikasikan default lazy {@code true} bila field mentah masih
	 * {@code null} — konsisten dengan konvensi "aktif secara default" pada banyak entity master
	 * lain di codebase (mis. {@link ais.database.model.ParameterTambahan#getAktif()}). Kelompok
	 * nonaktif tetap tersimpan di DB dan tetap bisa dirujuk baris
	 * {@link ParameterTambahanPengajuanTransaksiPegawai} yang ada, tetapi disaring dari daftar
	 * yang dimuat listener dinamis (lihat filter {@code kelompok...aktif = true} pada
	 * {@code ParameterTambahanPengajuanTransaksiPegawaiListener.onEvent}).
	 *
	 * @return {@code true} bila aktif (default); {@code false} bila dinonaktifkan eksplisit
	 */
	public Boolean getAktif() {
		if (aktif == null) {
			aktif = true;
		}
		return aktif;
	}

	/**
	 * Menyimpan bendera aktif/nonaktif kelompok.
	 *
	 * @param aktif bendera baru
	 */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}

	/**
	 * Urutan tampil kelompok ini relatif terhadap kelompok lain pada layar pengajuan transaksi
	 * pegawai (angka lebih kecil tampil lebih dulu). Getter mengaplikasikan default lazy
	 * {@code 1} bila field mentah masih {@code null} — dipakai langsung oleh
	 * {@link #compareTo(GeneralValueObject)} untuk mengurutkan koleksi kelompok (mis.
	 * {@code TreeSet} di {@code PengajuanTransaksiPegawai.getKelompokParameterTambahanPengajuanTransaksiPegawais()}).
	 * Ekspresi kondisional pada baris {@code return} bersifat mubazir: normalisasi sudah tuntas
	 * dua baris di atasnya, sehingga cabang {@code nomorUrut == null} pada {@code return} tidak
	 * pernah tercapai lagi — pola idiom yang berulang di banyak getter serupa pada codebase ini
	 * (bukan bug, sekadar sisa refactor yang tidak dirapikan).
	 *
	 * @return urutan tampil, minimal {@code 1} (default)
	 */
	public Integer getNomorUrut() {
		if (nomorUrut == null) {
			nomorUrut = 1;
		}
		return nomorUrut == null ? 1 : nomorUrut;
	}

	/**
	 * Menyimpan urutan tampil kelompok.
	 *
	 * @param nomorUrut urutan tampil baru
	 */
	public void setNomorUrut(Integer nomorUrut) {
		this.nomorUrut = nomorUrut;
	}

	/**
	 * Mengurutkan dua kelompok berdasarkan {@link #getNomorUrut()} secara menaik. Dipakai
	 * Hibernate/koleksi terurut (mis. {@code TreeSet<KelompokParameterTambahanPengajuanTransaksiPegawai>}
	 * pada {@code PengajuanTransaksiPegawai}) untuk menentukan urutan render kelompok di layar.
	 * Ber-{@code cast} langsung ke tipe konkret kelas ini tanpa pengecekan {@code instanceof};
	 * memanggilnya dengan {@code arg0} bertipe {@link GeneralValueObject} lain (mis. tertukar
	 * dengan sepupu {@code KelompokParameterTambahanGajiPegawai} atau
	 * {@code ais.database.model.KelompokParameterTambahanPengajuanPegawai} — kesalahan tipe yang
	 * sama persis dengan pola {@code task_fe6517bf}) akan melempar {@link ClassCastException}
	 * saat runtime, bukan gagal senyap. Ini justru kontras positif dibanding bug attribute-key
	 * pada {@code populateParameterTambahan()}: kesalahan tipe di sini <b>berisik</b> (exception),
	 * bukan diam-diam kehilangan data.
	 *
	 * @param arg0 kelompok pembanding, harus bertipe {@code KelompokParameterTambahanPengajuanTransaksiPegawai}
	 * @return negatif/nol/positif sesuai kontrak {@link Comparable#compareTo}, berbasis selisih
	 *         {@link #getNomorUrut()}
	 * @throws ClassCastException bila {@code arg0} bukan instance kelas ini
	 */
	@Override
	public int compareTo(GeneralValueObject arg0) {
		return getNomorUrut().compareTo(((KelompokParameterTambahanPengajuanTransaksiPegawai) arg0).getNomorUrut());
	}
}
