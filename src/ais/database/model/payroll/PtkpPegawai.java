package ais.database.model.payroll;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

import ais.database.model.GeneralValueObject;



/**
 * <h1>PtkpPegawai — master data status PTKP (Penghasilan Tidak Kena Pajak) pegawai</h1>
 *
 * <p>Baris kode "generated by hbm2java" ini bukan sekadar boilerplate kosong: kolom {@code tarif}
 * dipakai NYATA sebagai <b>masukan langsung mesin formula gaji</b> di {@code GajiPegawaiAction},
 * lewat placeholder teks {@code " PTKP_PEGAWAI "} yang ada di formula {@code ItemGaji}. Saat
 * formula memuat token tersebut, mesin menggantinya dengan {@code pegawai.getPtkpPegawai().getTarif()}
 * — nilai PTKP pegawai yang bersangkutan — sebelum formula dievaluasi oleh {@code exp4j}. Dengan
 * begitu setiap {@code ItemGaji} (mis. rumus PPh 21 gaji bulanan) dapat merujuk PTKP pegawai tanpa
 * hardcode nominal, cukup menuliskan token {@code PTKP_PEGAWAI} di teks formulanya.</p>
 *
 * <p><b>Relasi:</b> {@link ais.database.model.Pegawai#getPtkpPegawai()} — relasi {@code @ManyToOne}
 * pada kolom {@code ptkp_pegawai} — menjadikan satu baris {@code PtkpPegawai} berlaku untuk banyak
 * pegawai sekaligus (mis. semua pegawai berstatus "TK/0" memakai baris PTKP yang sama). Field ini
 * diisi manual oleh admin lewat combobox di layar {@code GajiPegawaiAction} (kombinasi dengan
 * {@code AsuransiPegawai} 1–4 yang berbagi bentuk & pola field yang persis sama di kelas ini).
 * Bandingkan dengan {@link ais.database.model.Pegawai#getNpwp()} yang menjadi identitas pajak
 * pegawai sedangkan {@code PtkpPegawai} adalah <i>status/golongan</i> PTKP (analog TK/0, K/1, K/2,
 * dsb pada ketentuan PPh 21 Indonesia) — kelas ini TIDAK mengunci nilai golongan ke daftar tetap;
 * {@code kode}/{@code nama} bebas diisi admin, sehingga validitas nominal {@code tarif} terhadap
 * aturan PPh 21 resmi sepenuhnya tanggung jawab operasional, bukan dijamin oleh kode ini.</p>
 *
 * <p><b>Master data ter-cache penuh:</b> kelas ini terdaftar di {@code InitData} sebagai salah satu
 * {@code ConstantValues} — seluruh baris tabel {@code payroll.ptkp_pegawai} dimuat ke memori JVM dan
 * dipakai ulang lewat {@link ais.database.model.GeneralValueObject#check(Object)} (dipanggil dari
 * getter relasi seperti {@link ais.database.model.Pegawai#getPtkpPegawai()}), bukan query database
 * per akses. Implikasinya: perubahan tarif PTKP di database HANYA terlihat setelah cache
 * {@code ConstantValues} dimuat ulang (mis. restart aplikasi atau mekanisme refresh cache internal),
 * bukan seketika saat baris diubah lewat SQL langsung.</p>
 *
 * <p><b>CRUD:</b> layar pemeliharaan ada di {@code ais.action.master.payroll.PtkpPegawaiAction} dan
 * ringkasannya tampil di dashboard kepegawaian ({@code DasboardKepegawaianPtkpPegawai},
 * {@code DasboardKepegawaian}).</p>
 *
 * <p>Seperti kelas {@code GeneralValueObject} turunan hbm2java lain di modul ini, field
 * {@code oleh}/{@code olehId}/{@code tanggal_dirubah} adalah <b>field audit shadow</b> — bukan bug,
 * melainkan keharusan teknis karena {@link ais.database.model.GeneralValueObject} sendiri BUKAN
 * {@code @Entity} sehingga tidak bisa mendeklarasikan kolom audit generik yang diwariskan lewat
 * pemetaan JPA; tiap entity turunan mengulang field yang sama.</p>
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "payroll", name = "ptkp_pegawai")
public class PtkpPegawai extends GeneralValueObject {

	/**
	 *
	 */
	private static final long serialVersionUID = 2463821577548439808L;

	/** ID baris (identity/auto-increment), primary key tabel {@code payroll.ptkp_pegawai}. */
	private Long id;

	/**
	 * Nama pengguna yang membuat/mengubah baris ini (field audit shadow, diisi oleh lapisan
	 * aplikasi — lihat catatan kelas). Tidak dipetakan ke kolom lewat anotasi JPA eksplisit di
	 * kelas ini; nilainya disetel manual oleh pemanggil (mis. {@code Common.refreshSaveOrUpdate}).
	 */
	private String oleh;

	/** ID pengguna yang membuat/mengubah baris ini (pasangan {@link #oleh}, lihat catatan kelas). */
	private String olehId;

	/**
	 * @return ID pengguna yang terakhir mengubah baris ini, atau {@code null} bila belum pernah
	 * diisi
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Mengisi ID pengguna pengubah. <b>Menolak diam-diam</b> nilai {@code null} atau string kosong
	 * (setelah {@code trim()}) — pemanggilan dengan nilai kosong tidak melempar exception maupun
	 * mengubah field, sehingga nilai lama tetap dipertahankan alih-alih ditimpa kosong.
	 *
	 * @param olehId ID pengguna pengubah; diabaikan bila kosong/{@code null}
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Mengisi nama pengguna pengubah. Sama seperti {@link #setOlehId(String)}, nilai kosong/
	 * {@code null} diabaikan diam-diam agar nilai lama tidak tertimpa kosong.
	 *
	 * @param oleh nama pengguna pengubah; diabaikan bila kosong/{@code null}
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * @return nama pengguna yang terakhir mengubah baris ini, atau {@code null} bila belum pernah
	 * diisi
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Hook siklus-hidup JPA {@code @PreUpdate} — dipanggil otomatis oleh Hibernate tepat sebelum
	 * baris ini di-UPDATE ke database. Mendelegasikan ke
	 * {@link ais.database.hibernate.AuditTimestampInterceptor#ubah(Object)} yang mengisi ulang
	 * {@link #tanggal_dirubah} (dan field audit sejenis bila ada) dengan waktu saat ini, sehingga
	 * jejak "kapan terakhir diubah" selalu akurat tanpa perlu pemanggil menyetelnya manual di setiap
	 * titik simpan.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Mengisi tanggal/jam terakhir baris ini diubah. Biasanya tidak perlu dipanggil manual karena
	 * {@link #onUpdate()} sudah mengisinya otomatis pada setiap UPDATE; nilai awal (saat objek baru
	 * dibuat) sudah terisi waktu saat itu lewat inisialisasi field.
	 *
	 * @param tanggal_dirubah tanggal/jam perubahan
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * @return tanggal/jam terakhir baris ini diubah; diinisialisasi ke waktu pembuatan objek dan
	 * diperbarui otomatis oleh {@link #onUpdate()} pada tiap UPDATE
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * @return representasi ringkas untuk log/debug/tampilan combobox: {@code id} digabung
	 * {@link #nama} dengan pemisah {@code "-"} (mis. {@code "3-TK/0"})
	 */
	public String toString() {
		return id + "-" + nama;
	}

	/** Kode singkat status PTKP (mis. "TK0", "K1"), bebas diisi admin — tidak dikunci ke daftar baku. */
	private String kode;

	/**
	 * Nominal/persentase tarif PTKP. Inilah nilai yang disuntikkan mesin formula {@code ItemGaji}
	 * ke token {@code PTKP_PEGAWAI} — lihat catatan kelas.
	 */
	private Double tarif;

	/** Nama/label status PTKP yang tampil di UI (mis. "TK/0 - Tidak Kawin"). Wajib diisi ({@code nullable = false}). */
	private String nama;

	/** Keterangan bebas, opsional. */
	private String keterangan;

	/** Status aktif/nonaktif baris; default efektif {@code true} bila kolom kosong — lihat {@link #getAktif()}. */
	private Boolean aktif;

	/** Konstruktor kosong wajib JPA/Hibernate. Semua field diisi lewat setter setelah instansiasi. */
	public PtkpPegawai() {
	}

	/**
	 * @return ID baris (primary key), atau {@code null} untuk entity yang belum pernah disimpan
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Mengisi ID baris. Kolom bertanda {@code insertable = false} pada anotasi {@link #getId()},
	 * jadi nilai yang disetel di sini tidak ikut dikirim pada INSERT — ID sebenarnya dihasilkan
	 * server (strategi {@code IDENTITY}/auto-increment) dan hanya relevan dipakai untuk kasus
	 * seperti {@code session.load(PtkpPegawai.class, id)}.
	 *
	 * @param id ID baris
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * @return {@link #kode}, di-{@code trim()}; string kosong {@code ""} (BUKAN {@code null}) bila
	 * kolom belum diisi — beda dengan {@link #getNama()} yang mengembalikan {@code null} pada kasus
	 * serupa
	 */
	public String getKode() {
		return kode == null ? "" : kode.trim();
	}

	/**
	 * Mengisi kode status PTKP.
	 *
	 * @param kode kode singkat, bebas format
	 */
	public void setKode(String kode) {
		this.kode = kode;
	}

	/**
	 * @return nama/label status PTKP, di-{@code trim()}; {@code null} bila kolom kosong (meski
	 * kolom database ditandai {@code nullable = false} — validasi "wajib isi" sesungguhnya
	 * ditegakkan di layar CRUD, bukan di getter ini)
	 */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Mengisi nama/label status PTKP.
	 *
	 * @param nama nama status PTKP; maksimal 255 karakter di database
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * @return keterangan bebas, apa adanya (TANPA {@code trim()}, berbeda dari {@link #getKode()}
	 * dan {@link #getNama()}); {@code null} bila belum diisi
	 */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * Mengisi keterangan bebas.
	 *
	 * @param keterangan teks keterangan, opsional
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}


	/**
	 * @return status aktif baris ini; {@code true} bila kolom {@code null} (default AKTIF, bukan
	 * default nonaktif) — pola "flag aktif" umum di modul ini: baris lama yang belum pernah diisi
	 * kolom {@code aktif} tetap dianggap aktif alih-alih tersembunyi tiba-tiba dari daftar pilihan
	 */
	public Boolean getAktif() {
		return aktif == null ? true : aktif;
	}

	/**
	 * Mengisi status aktif/nonaktif. Layar CRUD {@code PtkpPegawaiAction} memakai kolom ini untuk
	 * menyaring pilihan combobox PTKP yang ditawarkan ke admin saat mengisi data gaji pegawai
	 * ({@code Restrictions.eq("aktif", true)}), sehingga status PTKP lama yang sudah tidak berlaku
	 * bisa disembunyikan dari pilihan baru tanpa menghapus baris (dan riwayat pegawai yang sudah
	 * memakainya tetap utuh).
	 *
	 * @param aktif {@code true} aktif, {@code false} nonaktif, {@code null} diperlakukan sebagai
	 * aktif oleh {@link #getAktif()}
	 */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}

	/**
	 * @return nominal/persentase tarif PTKP; {@code 0.0} (BUKAN {@code null}) bila kolom kosong,
	 * sehingga aman langsung dipakai dalam ekspresi aritmatika oleh mesin formula {@code ItemGaji}
	 * tanpa perlu null-check tambahan di sisi pemanggil — lihat catatan kelas soal token
	 * {@code PTKP_PEGAWAI}
	 */
	public Double getTarif() {
		return tarif == null ? 0.0 : tarif;
	}

	/**
	 * Mengisi nominal/persentase tarif PTKP.
	 *
	 * @param tarif nilai tarif; {@code null} diperlakukan sebagai {@code 0.0} oleh {@link #getTarif()}
	 */
	public void setTarif(Double tarif) {
		this.tarif = tarif;
	}

}
