package ais.database.model.payroll;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

import ais.database.model.GeneralValueObject;

/**
 * Master/template "kop" penyesuaian gaji yang dipakai untuk membuat sekumpulan baris
 * {@link GajiTabahan} sekaligus untuk banyak pegawai dalam satu periode. Satu baris
 * {@code AdjusVariablePenggajian} bukan komponen gaji itu sendiri — ia hanya menyimpan
 * {@link #kode}/{@link #nama}/{@link #keterangan}/{@link #mulai}/{@link #sampai} yang menjadi
 * NILAI DEFAULT bagi banyak baris {@link GajiTabahan} yang dibuat lewat layar
 * {@code AdjusVariablePenggajianAction} (lihat tombol "Tambah" yang melakukan iterasi atas daftar
 * pegawai dan membuat satu {@code GajiTabahan} per pegawai, masing-masing menunjuk balik ke
 * instance {@code AdjusVariablePenggajian} yang sama lewat {@code GajiTabahan#setAdjusVariablePenggajian}).
 *
 * <p><b>Relasi ke mesin formula gaji ({@code ItemGaji}).</b> Entity ini sendiri TIDAK langsung
 * menjadi variabel exp4j. Yang menjadi variabel adalah {@code kode} milik {@link GajiTabahan}
 * (lihat javadoc kelas {@code ItemGaji}, bagian daftar sumber variabel: {@code Konstanta},
 * {@code ParameterTambahanGajiPegawai}, {@code GajiTabahan}, {@code PenilaianKpi},
 * {@code JenisTransaksiPegawai}). Keterkaitannya: {@link GajiTabahan#getKode()},
 * {@link GajiTabahan#getKeterangan()}, {@link GajiTabahan#getNama()}, {@link GajiTabahan#getMulai()}
 * dan {@link GajiTabahan#getSampai()} semuanya memakai POLA GETTER DESTRUKTIF FALLBACK: bila field
 * milik baris {@code GajiTabahan} itu sendiri kosong/{@code null}, getter tersebut MENIMPA field
 * instance dengan nilai yang diambil dari {@link AdjusVariablePenggajian} yang ditunjuknya (lihat
 * {@code AdjusVariablePenggajian.getKode()} dkk. di bawah). Konsekuensinya: {@code kode} yang dipakai
 * mesin formula gaji sebagai nama variabel BISA berasal dari kelas ini secara tidak langsung —
 * mengubah {@link #kode} pada baris {@code AdjusVariablePenggajian} yang sudah dipakai banyak
 * {@code GajiTabahan} (dan yang baris {@code GajiTabahan}-nya sendiri tidak mengisi {@code kode})
 * akan mengubah nama variabel formula untuk SEMUA baris tersebut sekaligus, termasuk baris yang
 * sudah lama tersimpan.</p>
 *
 * <p><b>Bukan bagian dari shift/absensi.</b> Berdasarkan penelusuran referensi di codebase, kelas
 * ini hanya dipakai oleh {@link GajiTabahan}, {@code GajiTabahanAction}, dan
 * {@code AdjusVariablePenggajianAction} (CRUD + laporan cetak/upload) — tidak ada relasi ke modul
 * shift ({@code DetailJenisShiftPegawai}) atau absensi harian ({@code StatuskehadiranKaryawanHarian},
 * {@code AbsenPegawaiDetail}).</p>
 *
 * <p>Field audit {@link #oleh}/{@link #olehId}/{@link #tanggal_dirubah} adalah pola shadow-audit
 * standar: karena {@link GeneralValueObject} bukan {@code @Entity} JPA, setiap entity anak menyalin
 * sendiri triplet field ini plus hook {@link #onUpdate()}. Ini keharusan teknis pada arsitektur AIS,
 * bukan duplikasi yang keliru.</p>
 *
 * Bank generated by hbm2java
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "payroll", name = "adjus_variable_penggajian")
public class AdjusVariablePenggajian extends GeneralValueObject {

	/**
	 * Versi serialisasi tetap untuk kompatibilitas {@link java.io.Serializable} antar build. Nilai
	 * ini warisan generator hbm2java dan sengaja tidak diubah kecuali struktur field berubah
	 * tak-kompatibel.
	 */
	private static final long serialVersionUID = 2463821577548439808L;

	/** Primary key baris penyesuaian gaji ini, di-generate database (identity) — lihat {@link #getId()}. */
	private Long id;

	/** Nama/username user yang terakhir membuat atau mengubah baris ini (field audit shadow). */
	private String oleh;

	/** ID user yang terakhir membuat atau mengubah baris ini (field audit shadow), pasangan {@link #oleh}. */
	private String olehId;

	/**
	 * Mengembalikan ID user yang terakhir mengubah baris ini.
	 *
	 * @return ID user audit, atau {@code null} bila belum pernah diisi
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Mengisi ID user audit ({@link #olehId}). Guard: nilai {@code null} atau string kosong/hanya-spasi
	 * diabaikan diam-diam agar baris audit yang sudah ada tidak tertimpa kosong oleh pemanggil yang lupa
	 * menyertakan identitas user (mis. proses batch/background).
	 *
	 * @param olehId ID user yang melakukan perubahan; diabaikan bila kosong
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Mengisi nama/username user audit ({@link #oleh}). Guard sama seperti {@link #setOlehId(String)}:
	 * nilai {@code null} atau kosong/hanya-spasi diabaikan diam-diam supaya nilai audit lama tidak
	 * tertimpa kosong.
	 *
	 * @param oleh username user yang melakukan perubahan; diabaikan bila kosong
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan username user yang terakhir mengubah baris ini.
	 *
	 * @return username user audit, atau {@code null} bila belum pernah diisi
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Hook siklus-hidup JPA {@code @PreUpdate} — dipanggil otomatis oleh Hibernate tepat sebelum
	 * statement {@code UPDATE} dikirim ke database untuk entity ini. Mendelegasikan ke
	 * {@link ais.database.hibernate.AuditTimestampInterceptor#ubah(Object)} yang mengisi
	 * {@link #tanggal_dirubah} dengan waktu saat ini, memastikan jejak "kapan terakhir diubah" selalu
	 * konsisten tanpa bergantung pada pemanggil yang mengeset manual.
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}

	/**
	 * Timestamp perubahan terakhir baris ini (field audit shadow). Diinisialisasi ke waktu saat
	 * object dibuat di JVM dan ditimpa otomatis oleh {@link #onUpdate()} setiap kali baris
	 * di-{@code UPDATE}.
	 */
	private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Mengisi timestamp perubahan terakhir secara manual. Biasanya tidak perlu dipanggil langsung
	 * karena {@link #onUpdate()} sudah mengelolanya otomatis pada setiap update.
	 *
	 * @param tanggal_dirubah timestamp baru
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan timestamp perubahan terakhir baris ini, dipetakan ke kolom {@code TIMESTAMP}.
	 *
	 * @return waktu perubahan terakhir
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/**
	 * Representasi teks ringkas baris ini untuk log/debug/tampilan combo-box, berformat
	 * {@code "<id>-<nama>"}. Tidak melakukan resolusi lazy atau efek samping apa pun — murni
	 * menggabungkan {@link #id} dan {@link #nama} apa adanya (bila {@link #nama} {@code null},
	 * hasilnya memuat literal {@code "null"} dari konkatenasi string Java).
	 *
	 * @return string deskriptif baris penyesuaian gaji ini
	 */
	public String toString() {
		return id + "-" + nama;
	}

	/**
	 * Kode singkat penyesuaian gaji ini. Nilai inilah yang menjadi NAMA VARIABEL pada mesin formula
	 * exp4j ({@code ItemGaji}) secara tidak langsung — lewat fallback destruktif
	 * {@link GajiTabahan#getKode()} pada baris {@link GajiTabahan} yang menunjuk ke instance ini dan
	 * tidak mengisi {@code kode}-nya sendiri. Lihat javadoc kelas untuk penjelasan lengkap dampaknya.
	 */
	private String kode;

	/**
	 * Tanggal mulai berlaku penyesuaian ini, default waktu saat object dibuat di JVM
	 * ({@link ais.ui.util.WaktuUtil#getDate()}). Dipakai sebagai nilai default
	 * {@link GajiTabahan#getMulai()} lewat pola fallback destruktif yang sama.
	 */
	private Date mulai = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Tanggal berakhirnya masa berlaku penyesuaian ini; {@code null} berarti tidak ada batas akhir
	 * eksplisit. Dipakai sebagai nilai default {@link GajiTabahan#getSampai()} lewat pola fallback
	 * destruktif yang sama.
	 */
	private Date sampai;

	/** Nama deskriptif penyesuaian gaji ini, dipakai sebagai nilai default {@link GajiTabahan#getNama()}. */
	private String nama;

	/** Keterangan/deskripsi bebas penyesuaian gaji ini, dipakai sebagai nilai default {@link GajiTabahan#getKeterangan()}. */
	private String keterangan;

	/**
	 * Flag aktif/nonaktif baris ini. Lihat {@link #getAktif()} untuk perilaku default saat kolom
	 * database masih {@code NULL}.
	 */
	private Boolean aktif;

	/** Konstruktor kosong yang dibutuhkan Hibernate untuk instansiasi entity lewat reflection. */
	public AdjusVariablePenggajian() {
	}

	/**
	 * Mengembalikan primary key baris ini.
	 *
	 * @return id, atau {@code null} untuk instance baru yang belum disimpan
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Mengisi primary key baris ini. Kolom dipetakan {@code insertable = false} sehingga pengisian
	 * manual di sini tidak dikirim balik saat {@code INSERT}; nilai sebenarnya berasal dari
	 * identity/auto-increment database.
	 *
	 * @param id primary key baris
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Mengembalikan kode penyesuaian gaji ini, sudah di-{@code trim()}. Nilai ini yang secara tidak
	 * langsung menjadi nama variabel pada mesin formula gaji lewat {@link GajiTabahan#getKode()} —
	 * lihat javadoc kelas.
	 *
	 * @return kode ter-trim, atau string kosong bila belum diisi ({@code null}/kosong)
	 */
	public String getKode() {
		return kode == null || kode.isEmpty() ? "" : kode.trim();
	}

	/**
	 * Mengisi kode penyesuaian gaji ini. Tidak ada validasi keunikan atau format di level entity —
	 * pengecekan duplikasi (bila ada) dilakukan oleh layar/action pemanggil.
	 *
	 * @param kode kode baru
	 */
	public void setKode(String kode) {
		this.kode = kode;
	}

	/**
	 * Mengembalikan nama penyesuaian gaji ini, sudah di-{@code trim()}.
	 *
	 * @return nama ter-trim, atau {@code null} bila belum pernah diisi
	 */
	@Column(name = "nama", columnDefinition = "text")
	public String getNama() {
		return this.nama == null ? null : this.nama.trim();
	}

	/**
	 * Mengisi nama penyesuaian gaji ini.
	 *
	 * @param nama nama baru
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Mengembalikan keterangan/deskripsi bebas baris ini apa adanya (tanpa trim).
	 *
	 * @return keterangan, atau {@code null} bila belum diisi
	 */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * Mengisi keterangan/deskripsi bebas baris ini.
	 *
	 * @param keterangan keterangan baru
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

	/**
	 * Mengembalikan status aktif/nonaktif baris ini. Kolom {@code aktif} pada database tidak
	 * ber-{@code NOT NULL}; getter ini memperlakukan nilai {@code NULL} sebagai AKTIF (default
	 * "fail-open") — baris lama yang dibuat sebelum kolom ini ada tetap tampil/terpakai sebagai
	 * baris aktif tanpa perlu migrasi data.
	 *
	 * @return {@code true} bila aktif atau belum pernah diisi, {@code false} bila eksplisit dinonaktifkan
	 */
	public Boolean getAktif() {
		return aktif == null ? true : aktif;
	}

	/**
	 * Mengisi status aktif/nonaktif baris ini.
	 *
	 * @param aktif status baru; {@code null} akan diperlakukan sebagai aktif oleh {@link #getAktif()}
	 */
	public void setAktif(Boolean aktif) {
		this.aktif = aktif;
	}

	/**
	 * Mengembalikan tanggal mulai berlaku penyesuaian ini, dipetakan ke kolom {@code DATE} (tanpa
	 * komponen jam).
	 *
	 * @return tanggal mulai
	 */
	@Temporal(TemporalType.DATE)
	public Date getMulai() {
		return mulai;
	}

	/**
	 * Mengisi tanggal mulai berlaku penyesuaian ini.
	 *
	 * @param mulai tanggal mulai baru
	 */
	public void setMulai(Date mulai) {
		this.mulai = mulai;
	}

	/**
	 * Mengembalikan tanggal berakhirnya masa berlaku penyesuaian ini, dipetakan ke kolom
	 * {@code DATE} (tanpa komponen jam).
	 *
	 * @return tanggal sampai, atau {@code null} bila tidak ada batas akhir eksplisit
	 */
	@Temporal(TemporalType.DATE)
	public Date getSampai() {
		return sampai;
	}

	/**
	 * Mengisi tanggal berakhirnya masa berlaku penyesuaian ini.
	 *
	 * @param sampai tanggal sampai baru, atau {@code null} untuk tanpa batas akhir
	 */
	public void setSampai(Date sampai) {
		this.sampai = sampai;
	}
}
