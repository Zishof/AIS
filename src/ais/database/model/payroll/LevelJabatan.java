package ais.database.model.payroll;

// Generated Apr 16, 2010 2:27:16 PM by Hibernate Tools 3.2.4.CR1

import static javax.persistence.GenerationType.IDENTITY;

import java.util.Date;

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.Temporal;
import javax.persistence.TemporalType;

import org.hibernate.envers.Audited;

import ais.database.model.GeneralValueObject;

/**
 * Bank generated by hbm2java
 *
 * <p>Komentar generator di atas adalah artefak salin-tempel dari template hbm2java Hibernate
 * Tools (lihat juga komentar berkas {@code // Generated Apr 16, 2010 ... by Hibernate Tools}) dan
 * <b>tidak merujuk ke entity {@code Bank}</b> &mdash; kelas ini murni memodelkan <b>level
 * jabatan</b>, salah satu dimensi master organisasi pada modul payroll.</p>
 *
 * <h2>Peran dalam struktur organisasi</h2>
 * <p>{@code LevelJabatan} adalah tabel master sederhana ({@code payroll.level_jabatan}) berisi
 * daftar tingkatan/level jabatan (mis. "Staf", "Supervisor", "Manajer") yang independen dari
 * {@code JabatanStruktural} dan {@code JabatanFungsional}. Bersama {@code Cabang} dan {@link
 * Departemen}, entity ini membentuk tiga dimensi <b>opsional</b> yang dilekatkan langsung pada
 * {@code ais.database.model.Pegawai} (lihat {@code Pegawai#getLevelJabatan()}) untuk keperluan
 * <b>penyaringan pencarian dan label tampilan saja</b> &mdash; berdasarkan penelusuran kode
 * langsung, entity ini BUKAN bagian dari rantai kalkulasi tunjangan: mesin formula gaji ({@code
 * ais.database.model.payroll.ItemGaji}) menghubungkan tunjangan struktural/fungsional/jabatan ke
 * {@code KodeTunjangan} melalui {@code JabatanStruktural}/{@code JabatanFungsional}, bukan
 * melalui {@code LevelJabatan}. Entity ini juga dipakai sebagai kolom filter/label opsional pada
 * {@code FormatItemGaji} dan {@code GajiTabahan} (format/tambahan gaji yang dapat dibatasi
 * berlaku hanya untuk level jabatan tertentu).</p>
 *
 * <h2>Siklus hidup &amp; UI</h2>
 * <p>CRUD dikelola oleh {@code ais.action.master.payroll.LevelJabatanAction} (layar ZK generik
 * dengan dua kolom form: Nama dan Keterangan) yang menegakkan larangan nama duplikat lewat
 * {@code checkNamaLevelJabatan()} sebelum simpan. Daftar pegawai yang tergabung dalam satu level
 * jabatan ditampilkan lewat panel detail {@code
 * ais.action.master.payroll.detail.LevelJabatanPunyaPegawaiAction}, yang juga menyediakan operasi
 * "lepas dari level jabatan" &mdash; baik per pegawai ({@code pegawai.setLevelJabatan(null)})
 * maupun massal lewat SQL native {@code UPDATE pegawai SET level_jabatan = NULL WHERE
 * level_jabatan = <id>}. Operasi ini TIDAK menghapus baris {@code LevelJabatan} maupun baris
 * {@code Pegawai}, hanya melepas relasinya; penghapusan baris {@code LevelJabatan} itu sendiri
 * mengikuti perilaku hapus generik {@code GenericAutowireComposer} dan akan gagal (foreign key)
 * selama masih ada pegawai yang mengacu ke baris tersebut.</p>
 *
 * <h2>Startup</h2>
 * <p>Kelas ini didaftarkan pada {@code ais.common.InitData#doInitData()} lewat pemanggilan
 * {@code initClasses(..., LevelJabatan.class, ...)}, yang pada setiap startup aplikasi memicu
 * {@code InitDataHelper.initData(LevelJabatan.class)} untuk menyelaraskan skema tabel audit
 * Envers &mdash; BUKAN mengisi baris data (lihat catatan pada {@code InitData#doInitData()}
 * bahwa kolom tabel utama diserahkan sepenuhnya ke {@code hbm2ddl.auto=update}, dan hanya skema
 * tabel audit {@code new_audit.*} yang diselaraskan manual di sana).</p>
 *
 * <h2>Audit</h2>
 * <p>Diberi anotasi {@code @Audited} (Hibernate Envers) sehingga setiap INSERT/UPDATE/DELETE
 * tercatat pada tabel bayangan audit. Kelas ini juga mewarisi field audit generik {@code oleh}/
 * {@code olehId}/{@code tanggal_dirubah} dari {@link GeneralValueObject} yang di-<i>shadow</i>
 * ulang (dideklarasikan kembali) di sini &mdash; ini adalah kebutuhan teknis arsitektur, bukan
 * cacat, karena {@code GeneralValueObject} bukan {@code @Entity} sehingga field yang perlu
 * dipetakan ke kolom harus dideklarasikan ulang pada tiap subclass {@code @Entity} agar
 * Hibernate dapat memetakannya. Lihat {@link #onUpdate()} untuk mekanisme pembaruan otomatis
 * {@link #tanggal_dirubah} pada setiap {@code UPDATE}.</p>
 *
 * @see GeneralValueObject
 * @see Departemen
 */
@Entity
@org.hibernate.annotations.Entity(
    dynamicInsert = true,
    dynamicUpdate = true
)
@Audited
@Table(schema = "payroll", name = "level_jabatan")
public class LevelJabatan extends GeneralValueObject {

	/**
	 * Versi serialisasi tetap untuk kompatibilitas {@link java.io.Serializable}. Diwarisi dari
	 * kontrak {@link GeneralValueObject}; nilainya tidak berubah sejak entity ini dibuat sehingga
	 * aman diabaikan kecuali struktur field berubah signifikan.
	 */
	private static final long serialVersionUID = 2463821577548439808L;
	/** Primary key auto-generated (identity) tabel {@code payroll.level_jabatan}. Lihat {@link #getId()}. */
	private Long id;
	/** Shadow field audit: nama pengguna yang terakhir mengubah baris ini. Lihat {@link #getOleh()}. */
	private String oleh;
	/** Shadow field audit: id pengguna yang terakhir mengubah baris ini. Lihat {@link #getOlehId()}. */
	private String olehId;

	/**
	 * Mengembalikan id pengguna (shadow audit field) yang terakhir mengubah baris {@code
	 * LevelJabatan} ini. Field ini di-<i>shadow</i> ulang dari {@link GeneralValueObject} karena
	 * kelas induk bukan {@code @Entity} &mdash; lihat javadoc kelas ini bagian "Audit".
	 *
	 * @return id pengguna terakhir, atau {@code null} bila belum pernah diisi.
	 */
	public String getOlehId() {
		return olehId;
	}

	/**
	 * Menetapkan id pengguna (shadow audit field) yang mengubah baris ini.
	 *
	 * <p><b>Perilaku penting:</b> method ini secara sengaja mengabaikan input {@code null} atau
	 * string kosong/hanya-whitespace &mdash; pemanggilan {@code setOlehId(null)} atau {@code
	 * setOlehId("")} TIDAK mengosongkan {@link #olehId}, melainkan diam-diam tidak melakukan
	 * apa-apa (early return) dan mempertahankan nilai lama apa adanya. Pemanggil yang berharap
	 * dapat "membersihkan" audit id lewat setter ini akan salah duga; satu-satunya cara
	 * mengosongkan field ini adalah menulis langsung ke kolom lewat HQL/SQL, karena tidak ada
	 * jalur setter yang menerima nilai kosong.</p>
	 *
	 * @param olehId id pengguna baru; diabaikan (no-op) bila {@code null} atau kosong setelah
	 *               {@code trim()}.
	 */
	public void setOlehId(String olehId) {if (olehId == null || olehId.trim().isEmpty()) {return;}
		this.olehId = olehId;
	}

	/**
	 * Representasi string entity ini untuk keperluan tampilan (mis. dropdown/label komponen ZK),
	 * berupa nama level jabatan apa adanya. Override ini menimpa format default {@code
	 * "kode - nama"} milik {@link GeneralValueObject#toString()} karena {@code LevelJabatan}
	 * tidak memiliki kolom {@code kode}.
	 *
	 * @return nilai {@link #nama}; dapat berupa {@code null} bila belum diisi (tidak ada
	 *         null-guard pada method ini).
	 */
	public String toString() {
		return nama;
	}

	/**
	 * Menetapkan nama pengguna (shadow audit field) yang mengubah baris ini. Memakai guard yang
	 * sama seperti {@link #setOlehId(String)}: input {@code null} atau kosong/hanya-whitespace
	 * diabaikan secara diam-diam, nilai lama dipertahankan.
	 *
	 * @param oleh nama pengguna baru; diabaikan (no-op) bila {@code null} atau kosong setelah
	 *             {@code trim()}.
	 */
	public void setOleh(String oleh) {if (oleh == null || oleh.trim().isEmpty()) {return;}
		this.oleh = oleh;
	}

	/**
	 * Mengembalikan nama pengguna (shadow audit field) yang terakhir mengubah baris ini.
	 *
	 * @return nama pengguna terakhir, atau {@code null} bila belum pernah diisi.
	 */
	public String getOleh() {
		return oleh;
	}

	/**
	 * Hook siklus hidup JPA {@code @PreUpdate}: dipanggil oleh Hibernate tepat sebelum statement
	 * {@code UPDATE} untuk baris ini dieksekusi. Implementasi mendelegasikan sepenuhnya ke {@code
	 * ais.database.hibernate.AuditTimestampInterceptor#ubah(GeneralValueObject)}, yang
	 * bertanggung jawab menuliskan waktu perubahan terkini ke {@link #tanggal_dirubah} sebelum
	 * baris disimpan. Method ini adalah implementasi kontrak {@code abstract} {@link
	 * GeneralValueObject#onUpdate()} yang diwajibkan bagi seluruh subclass entity.
	 *
	 * <p><b>Efek samping:</b> memutasi state entity ini sendiri (in-place) di tengah siklus flush
	 * Hibernate. Jangan dipanggil manual di luar lifecycle JPA/Hibernate karena dapat menimbulkan
	 * penulisan ganda atau state yang tidak konsisten dengan transaksi yang sedang berjalan.</p>
	 */
	@javax.persistence.PreUpdate protected void onUpdate() { ais.database.hibernate.AuditTimestampInterceptor.ubah(this);}     /** Timestamp perubahan terakhir baris ini; diperbarui otomatis oleh {@link #onUpdate()} pada tiap {@code UPDATE}, dan diinisialisasi ke waktu saat instance dibuat (bukan waktu commit transaksi). */ private Date tanggal_dirubah = ais.ui.util.WaktuUtil.getDate();

	/**
	 * Menetapkan timestamp perubahan terakhir secara manual. Dalam alur normal aplikasi, nilai ini
	 * ditulis otomatis oleh {@link #onUpdate()}; setter ini tersedia untuk kasus khusus (mis.
	 * migrasi data, restorasi dari backup) di mana timestamp perlu diatur eksplisit.
	 *
	 * @param tanggal_dirubah timestamp baru.
	 */
	public void setTanggal_dirubah(Date tanggal_dirubah) {
		this.tanggal_dirubah = tanggal_dirubah;
	}

	/**
	 * Mengembalikan timestamp perubahan terakhir baris ini.
	 *
	 * @return nilai {@link #tanggal_dirubah}.
	 */
	@Temporal(TemporalType.TIMESTAMP)
	public Date getTanggal_dirubah() {
		return tanggal_dirubah;
	}

	/** Nama level jabatan; wajib diisi (kolom {@code nama}, {@code NOT NULL}, maks. 255 karakter)
	 *  dan divalidasi unik oleh {@code LevelJabatanAction#checkNamaLevelJabatan()} pada layar
	 *  CRUD sebelum disimpan. Lihat {@link #getNama()}/{@link #setNama(String)}. */
	private String nama;
	/** Keterangan/deskripsi bebas untuk level jabatan ini (kolom {@code keterangan}, opsional,
	 *  boleh {@code null}). Lihat {@link #getKeterangan()}/{@link #setKeterangan(String)}. */
	private String keterangan;

	/**
	 * Konstruktor default, wajib bagi entity Hibernate (dipakai reflektif saat memuat baris dari
	 * database). Tidak melakukan inisialisasi eksplisit selain nilai default field {@link
	 * #tanggal_dirubah} yang sudah diisi pada deklarasi field (waktu saat instance dibuat, bukan
	 * waktu commit transaksi).
	 */
	public LevelJabatan() {
	}

	/**
	 * Mengembalikan primary key baris ini.
	 *
	 * <p>Kolom {@code id} dipetakan {@code insertable = false} karena nilainya sepenuhnya
	 * diserahkan ke strategi {@code IDENTITY} database (auto-increment); Hibernate membaca nilai
	 * yang dihasilkan database setelah INSERT, bukan menuliskannya sendiri.</p>
	 *
	 * @return id baris ini, atau {@code null} untuk instance yang belum pernah disimpan (transient).
	 */
	@Id
	@GeneratedValue(strategy = IDENTITY)
	@Column(name = "id", insertable = false, unique = true, nullable = false)
	public Long getId() {
		return this.id;
	}

	/**
	 * Menetapkan primary key secara manual. Dalam alur normal, id diisi otomatis oleh Hibernate
	 * setelah INSERT (lihat catatan {@code insertable = false} pada {@link #getId()}); setter ini
	 * umumnya hanya relevan untuk memuat ulang referensi ke baris yang sudah ada (mis. {@code
	 * session.load(LevelJabatan.class, id)}).
	 *
	 * @param id primary key baru.
	 */
	public void setId(Long id) {
		this.id = id;
	}

	/**
	 * Mengembalikan nama level jabatan.
	 *
	 * @return nilai {@link #nama}.
	 */
	@Column(name = "nama", nullable = false, length = 255)
	public String getNama() {
		return this.nama;
	}

	/**
	 * Menetapkan nama level jabatan. Tidak ada validasi/normalisasi pada level entity ini
	 * (trim, larangan kosong, dan larangan duplikat ditegakkan pada layar UI oleh {@code
	 * LevelJabatanAction#onSave(Event)} dan {@code checkNamaLevelJabatan()}, bukan di sini)
	 * &mdash; pemanggil lain (mis. skrip migrasi atau service) yang langsung memakai entity ini
	 * bertanggung jawab menegakkan aturan yang sama sendiri.
	 *
	 * @param nama nama level jabatan baru.
	 */
	public void setNama(String nama) {
		this.nama = nama;
	}

	/**
	 * Mengembalikan keterangan/deskripsi level jabatan.
	 *
	 * @return nilai {@link #keterangan}, dapat {@code null}.
	 */
	@Column(name = "keterangan", nullable = true)
	public String getKeterangan() {
		return this.keterangan;
	}

	/**
	 * Menetapkan keterangan/deskripsi level jabatan. Tidak ada validasi pada level entity ini.
	 *
	 * @param keterangan keterangan baru, boleh {@code null}.
	 */
	public void setKeterangan(String keterangan) {
		this.keterangan = keterangan;
	}

}
