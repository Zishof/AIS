# Implementation evidence ledger

Dokumen ini mencatat evidence aktual selama Fase 0–19. `IMPLEMENTED_UNVERIFIED` bukan `PASS`.

## Current run — 23 Agustus 2026, setelah spesifikasi 12–14

| Evidence | Hasil aktual |
|---|---|
| Checkout | Folder bukan working copy Git/SVN; SHA/revision VCS `UNVERIFIED_ON_CURRENT_CHECKOUT` |
| Compile | `mvn -DskipTests clean package`, 7.066 source pada clean build exit 0; perubahan jurnal setelahnya dikompilasi incremental tanpa error, tree canonical terakhir 7.068 source |
| Final package | targeted compile perubahan jurnal dan `war:war` current checkout exit 0; WAR 737.920.653 byte, 72.115 entry, SHA-256 `23763B38132EF3026F20D0C00C9AC0D9936517C4C35233840DF5AF83237DCB71`; ZIP valid dan memuat portal login, kontrol admin/OJS/sample, self-test, serta JSP jurnal terbaru. Full compile pernah exit 0 sebelum perubahan paralel terbaru; percobaan ulang final tertahan di luar scope jurnal oleh enam pemanggilan helper `ambilNilaiData` yang hilang pada `FeederExporter.java` |
| Secret scan | Exact-value scan source dan exploded WAR: 0 match; password tetap external-only |
| Schema SIT | main 3.322 tabel dengan tepat 12 tabel jurnal; streaming 83 tabel dengan tepat satu `lampiran_jurnal`, 19 kolom, dua unique constraint |
| File lifecycle | `JurnalStreamingDatabaseSelfTest` dan `JurnalFileEndToEndSelfTest` exit 0; 0 baris `lampiran_jurnal` tersisa setelah rollback/cleanup |
| Route/RBAC | 28/28 route positive dan 28/28 negative lulus pada schema akses versi 2 |
| Storage | `LampiranLain` untuk jurnal berstatus `SUPERSEDED`; canonical adalah entity mandiri `LampiranJurnal` |
| UI | design token, focus-visible, touch target, reduced-motion, dan mobile card table minimum diterapkan; visual matrix penuh belum dijalankan |
| Menu database SIT | Preflight seluruh `menu` menemukan 29 reserved journal rows dan 0 collision eksternal; 28 legacy label/route diperbarui atomik, verifikasi/rerun menghasilkan 29 unchanged. Production tidak dimutasi |
| Importer current | OJS 3.5 preflight/dry-run/execute tepat 134/905, domain transform, cancel/resume, retry FAILED file ke `LampiranJurnal`, SHA-256/anonymous stream/final reconciliation lulus; legacy tepat 7 tabel/37 field lulus |
| Demo current | 500 jurnal, 50.000 artikel dan 50.000 contributor tersedia idempoten; 5.500 published, 6.000 draft, status workflow lain terisi, author display `Prof. Dr. ASROFI RIDHO S.AG., M.SI., M.H, M.Pd, M.Psi` dengan reference 245 |
| Login portal jurnal | PASS container/browser: form login AIS tampil pada landing page, POST+CSRF, lockout namespace jurnal, pesan kredensial generik tanpa password reflection, layout desktop dan 390 px tanpa overflow. Setelah login tampil ruang kerja/logout; `Import dari OJS` dan kontrol sample hanya dirender bila `Common.getApakahAdmin()==true`, sementara route tujuan tetap memakai authorization service |
| Data sample admin | PASS SIT: kontrol admin membuat idempotent 50 jurnal × 100 artikel memakai namespace `sample-50x100`; self-test namespace terpisah membuktikan 50 jurnal/5.000 artikel/5.000 contributor dibuat lalu seluruhnya dihapus dan tersisa 0. Cleanup hanya menerima key berawalan `sample-`, menghapus usage event yang tepat terkait item sample, fail-closed untuk dependensi bisnis lain, dan menolak percobaan menghapus dataset demo 500×100; verifikasi akhir `0|0|50000` (item sample, collection sample, item demo yang dipertahankan) |
| Performance awal | Characterization-only pada 50.000 artikel: 180 operasi, 3 query/loop, p50 31,30 ms, p95 78,44 ms, throughput 22,42 ops/s, heap used 1.509.496.960 byte; threshold/full dataset/soak masih terbuka |
| Full performance/load/soak | `PASS_LOCAL_SIT_V1`: dedicated production-shaped dataset 100 jurnal/100k artikel/1m metadata file/10m usage/10k user (2.673 MB); final run 8 thread × 300 detik, 1.608.291 operasi, warm p95 2,23 ms, analytic p95 1.652,22 ms, load p95 7,18 ms, 5.344,88 ops/s, error 0, heap peak 203.459.480 byte. Schema test sudah di-drop; production HTTP/WAL/long-soak/SLA owner belum diklaim |
| Self-test current | 37/37 class jurnal dalam inventory current lulus pada environment clone/fixture yang sesuai, termasuk full-scale performance dan `JurnalPortalLoginSelfTest`; `JurnalDemoDataSelfTest` kini juga menutup create/delete sample 50×100. Smoke `JurnalCatalogSelfTest` dari build final exit 0. Warning JNDI/RMI CLI non-fatal karena registry container tidak aktif |
| Secret scan current | Exact-value scan source dan exploded WAR: 0 match; credential hanya diinjeksi melalui environment saat test |

Seluruh evidence di bawah bagian ini yang memakai angka database lain, storage `LampiranLain`, route lama, atau hash artefak lama berstatus `SUPERSEDED`; barisnya dipertahankan untuk audit historis.

## Addendum historis komputer lanjutan — 23 Agustus 2026 (`SUPERSEDED`)

Addendum ini adalah evidence terbaru pada `C:\opt\AIS\ais` dan mengoreksi angka snapshot komputer lama bila berbeda. Database baseline tidak dimutasi.

| Evidence | Environment/artifact | Hasil aktual komputer ini |
|---|---|---|
| Backup sebelum test | `tmp/jurnal-baseline/ais-before-jurnal-fase-0-19.dump` | PASS: custom dump 47.517.041 byte, SHA-256 `C1B19EFCB4EECC150A4557248DE8DBCCC47CB7A0619D4CFE4E50BD600539F80E` |
| Backup streaming sebelum test | `tmp/jurnal-baseline/streaming-ais-before-jurnal-fase-0-19.dump` | PASS: custom dump 850 byte, SHA-256 `516C47DB696389332A1841EED240F8F08C4ED488F212F2490004622371A5B864`; baseline `streaming_ais` pada komputer ini kosong (0 tabel/0 large object), berbeda dari snapshot lama |
| Clone main SIT/UAT | `ais_jurnal_sit`, `ais_jurnal_uat` | PASS migration+verification: baseline 3.303 tabel menjadi 3.323, terdiri dari 4 bootstrap repository-modern yang belum ada pada baseline ini, 4 prerequisite repository jurnal, dan tepat 12 tabel jurnal; rerun migration/seed idempoten; parent+28 menu dan 29 assignment admin terverifikasi |
| Clone streaming SIT/UAT | `streaming_ais_jurnal_sit`, `streaming_ais_jurnal_uat` | PASS untuk technical test: schema-only disposable dibuat dari mapping `hibernate.streaming.cfg.xml` aplikasi melalui Hibernate, menghasilkan 82 tabel termasuk Envers/`REVINFO`; ini bukan klaim restore-equivalence terhadap baseline streaming lama yang tidak tersedia |
| Regression dependency jsoup | 184 JAR fisik `WEB-INF/lib` | PASS targeted: hanya `jsoup-1.23.1.jar` yang memiliki entry `org/jsoup/*`; fat JAR Docear aktif tidak lagi membawa class jsoup; viewer HTML/JATS/XXE self-test lulus |
| Self-test jurnal agregat | clone SIT dan fixture importer disposable | PASS 28/28: 21 contract/service/database/security/workflow/file/health test dan 7 importer test; OJS 3.5 tepat 134 tabel/905 field, dry-run/execute/idempotency/cancel-resume/file checksum/final reconciliation lulus; legacy tepat 7 tabel/37 field lulus |
| Two-database lifecycle | main+streaming SIT | PASS: BLOB round-trip rollback, upload/stream/authorization/checksum/cleanup, health/readiness, serta schema mutation disabled lulus |
| Technical UAT smoke | main+streaming UAT | PASS: streaming rollback, file end-to-end+cleanup, dan health/readiness lulus terhadap build/class hasil clean build yang sama; bukan business-owner sign-off |
| Admin demo-data generator | `masterJurnal` admin action + `ais_jurnal_sit` | PASS targeted: admin-only+CSRF, confirmation `GENERATE-DEMO-500X100`, environment gate demo/SIT/UAT, bounded 500 jurnal × 100–200 artikel, idempotency key, dan fallback author authority. Run `demo-500x100` menghasilkan tepat 500 jurnal, 50.000 artikel, 50.000 contributor, 5.500 published, 6.000 draft, dan masing-masing 5.500 pada submitted/screening/review/copyediting/production/proof/scheduled dalam 43.698 ms; rerun membuat 0 row duplikat. Dosen ID 245 tidak ada pada clone ini sehingga nama fallback digunakan; deployment demo akan memakai nama record ID 245 bila tersedia |
| Post-test clone hygiene terbaru | SIT/UAT main+streaming | PASS: main `ais` tetap 3.303 tabel; clone SIT/UAT masing-masing 3.323 tabel; streaming baseline tetap 0 tabel/0 large object; clone streaming SIT/UAT masing-masing 82 tabel/0 large object setelah rollback/cleanup |
| Final offline package | `build/maven/ais.war` | PASS clean package pada 03:08 WIB: 7.047 source dikompilasi dengan Java source/target 1.8 dan 46 resource disalin; WAR 737.796.422 byte, 72.084 entry, SHA-256 `419AF20A47981ECFC293C0685F4095C68AE1008149398A21842BD377E9EC13A9`; SQL prerequisite, generator demo, self-test, dan kedua JAR jsoup/Docear terpaket |
| Secret scan terarah | Java/XML/properties/JSP/POM + exploded WAR | PARTIAL_PASS: satu password plaintext legacy pada `META-INF/context.xml` diganti placeholder `${AIS_DB_PASSWORD}` dan dua `tempfile.tmp` duplikat berisi konfigurasi lama dihapus; exact-value scan source dan exploded WAR final menghasilkan 0 match. Deployment wajib menyuntikkan property tersebut dan cold-container/JNDI validation ulang masih diperlukan |
| Fase 16 | dependency/security/performance/accessibility | **BLOCKED**: laporan Dependency-Check lama tidak ikut tersalin dan inventaris lama masih mempunyai Severity 1/2 terbuka; targeted jsoup regression tidak menutup triage seluruh dependency, SBOM/license, load/soak, accessibility, maupun security suite penuh |
| Fase 17 | technical SIT | **TECHNICAL_PARTIAL_PASS**: migration, 28/28 self-test, importer reconciliation, streaming/file, dan health lulus; minimum scenario formal 100%, external sandbox, performance/security/accessibility, regression AIS luas, dan tanda tangan SIT belum terpenuhi |
| Fase 18 | UAT | **TECHNICAL_SMOKE_PASS / FORMAL_NOT_RUN**: smoke clone UAT lulus; 12-area actor journey dan business-owner go/no-go belum dapat ditandatangani oleh automated test |
| Fase 19 | pilot/cutover | **NOT_RUN / NOT_AUTHORIZED**: entry gate Fase 16–18 belum terpenuhi serta endpoint pilot, owner, freeze window, alert routing, dan persetujuan cutover belum tersedia |

Warning JNDI/RMI `localhost` pada eksekusi CLI dicatat sebagai non-fatal: registry container tidak dijalankan, sedangkan setiap proses test berakhir exit code 0. Auto-DDL hanya dipakai satu kali untuk membangun schema **fixture streaming disposable** dari mapping resmi; runtime SIT/UAT tetap memakai `hibernate.hbm2ddl.auto=none` dan migration SQL eksplisit.

| Evidence | Environment/artifact | Hasil saat ini |
|---|---|---|
| Baseline backup | `tmp/jurnal-baseline/ais-before-jurnal-fase-0-19.dump` | custom dump tersedia; SHA-256 `99E31D3117B587ADB5DBB2AB483E2AC22F2BCF3CF2F12B6F66BF90CE4CECE95F` |
| Restore drill | database `ais_jurnal_restore_drill` | PASS |
| Isolated clones | `ais_jurnal_sit`, `ais_jurnal_uat` | tersedia; baseline `ais` tidak dimutasi |
| Streaming baseline backup | `tmp/jurnal-baseline/streaming-ais-before-jurnal-fase-0-19.dump` | custom dump 3,410,318 byte; SHA-256 `162B558542B20A487CB443C3639B94A7D89BAC088D24A939E85FFC02BE973263` |
| Streaming restore drill | database `streaming_ais_jurnal_restore_drill` | PASS: 80 tabel dan 4 baris `lampiran_lain`, sama dengan baseline |
| Streaming isolated clones | `streaming_ais_jurnal_sit`, `streaming_ais_jurnal_uat` | tersedia; baseline `streaming_ais` tidak dimutasi |
| Streaming Hibernate/BLOB gate | `streaming_ais_jurnal_sit` | PASS: environment override, explicit schema contract, OID payload round-trip, rollback; tetap 80 tabel, 4 baris `lampiran_lain`, 5 large object |
| Journal file lifecycle | `ais_jurnal_sit` + `streaming_ais_jurnal_sit` | PASS parsial: raw-body upload bounded, exact declared/actual size, MIME/stage allowlist, main metadata + streaming OID BLOB, SHA-256/size verified download, object authorization, repair by exact `ref`/`jenis`, dan test cleanup; setelah test tetap 3.293 tabel main, 4 baris/5 large object streaming, tanpa fixture tersisa |
| Submit-to-publish journey | `ais_jurnal_sit`, rollback-only service test | PARTIAL_PASS: create journal, draft, external contributor, submit/screening, double-anonymous reviewer invite/accept/complete, final decision, copyedit/production/proof, DOI/URN, issue/TOC, publish dan idempotent `Artikel` projection lulus; fixture rollback dan jumlah tabel tetap 3.293. Multi-round exception/anonymity DTO/UI suites belum lengkap |
| Invitation/discussion journey | `ais_jurnal_sit`, rollback-only service test | PASS parsial: cryptographic token terikat email, wrong-email denial, one-time accept, revoked-token denial, scoped assignment, existing `Diskusi`/`DiskusiKomentar`, indexed participant dan participant comment lulus; journal path memakai scalar actor ID agar tidak tergantung relasi ORM `Tbmuser` legacy yang luas |
| Baseline compile | Maven compile sebelum perubahan jurnal | PASS |
| Journal schema | `ais_jurnal_sit` | PASS: clone dibangun ulang dari baseline, migration eksplisit menghasilkan total 3.293 tabel = 3.277 baseline + 4 repository prerequisite + 12 jurnal; bootstrap Hibernate `hbm2ddl.auto=none` tidak mengubah jumlah tabel |
| Repository prerequisite | `ais_jurnal_sit` | empat tabel repository missing dibuat; extensions diterapkan idempoten; committed importer test menemukan dan menutup missing Envers columns pada `repo_collection__audit`/`repo_item__audit`, lalu verification gate lulus |
| Menu hierarchy | `ais_jurnal_sit` | parent + 28 child, 29 assignment admin; seed ulang 0 insert |
| RBAC/catalog/profile contract | Java self-test | PASS: fail-closed RBAC, 28 menu, 73 email key, 45 plugin, 134 OJS tables, immutable published review form dan access-policy validation |
| New UI route/RBAC matrix | Java self-test + `NewUiRouteRegistry` | PASS contract: 28/28 canonical route, 28 positive read, 28 default-deny negative, unknown route ditolak. Cold-container smoke dari WAR final membuktikan public home/search/feed dan protected API guard; authenticated 28-menu DOM journey tetap menjadi gate SIT |
| Admin workspace/read-command model | `JurnalWorkspaceService`, `JurnalAdminApi`, `admin.jsp` | IMPLEMENTED_UNVERIFIED: paged item list, journal selector, responsive/accessibility basics, dan semantic command forms untuk workflow/domain services. Importer UI/API menyediakan preflight/register/dry-run/execute/resume/cancel hanya melalui named environment `connectionReference`; secret tidak diterima dari browser atau disimpan di tabel. Non-admin journal set berasal dari assignment aktif dan explicit journal filter melewati object-scope gate. HTTP container/DOM/CSRF suite tiap modul belum lengkap |
| Hibernate mapping/bootstrap | clone SIT | PASS: environment override menunjuk clone dan 12 tabel terdeteksi; Hibernate 3/JDK 25 mengeluarkan warning proxy legacy |
| Object-level authorization | service layer + clone SIT | PARTIAL_PASS: admin active-role override dan non-admin assignment gate teruji negative/positive termasuk stage isolation; fixture rollback terbukti (`penugasan_tahap_jurnal=0`); gates diterapkan pada administration, workflow, files, publication, identifier, contributor, discussion, invitation, subscription/payment, email, integration dan importer; seluruh cross-tenant/IDOR route matrix belum lengkap |
| Email template/delivery hardening | `ais_jurnal_sit`, capture-mode rollback test | PARTIAL_PASS: journal-scope/capability, versioned locale/variable policy, unknown-variable dan unsafe-HTML rejection, recipient cap/deduplication, immutable snapshot, capture mode dan journal-scoped idempotency lulus; database test membuktikan batch seed tepat 146 dan rerun 0. Existing `RepoUserPreference` menyimpan self-service per-journal email/in-app channel, immediate/daily/weekly/none digest, per-template unsubscribe dan mandatory security override. Preference-aware per-user dispatch, email opt-out menjadi `IN_APP_ONLY`, bounded daily/weekly digest release, unique active preference, dan endpoint CSRF `/jurnal/preferences` lulus rollback test. Sandbox transport SMTP riil masih menjadi release gate |
| Payment callback security | `ais_jurnal_sit`, rollback/sandbox resolver | PASS_BEHAVIOR: existing `LogPembayaran` dan `LanggananJurnal`; provider allowlist, secret environment-only, canonical HMAC-SHA256 constant-time compare, batas timestamp anti-replay, exact reference/amount/currency snapshot, audit actor callback dan duplicate delivery idempoten. Signature palsu dan callback kedaluwarsa ditolak; endpoint `/jurnal-payment-callback` tidak menerima secret dari HTTP. Sandbox provider eksternal riil tetap diperlukan sebelum cutover |
| Native plugin exports/recommendations | `ais_jurnal_sit`, published workflow rollback test | PASS_SCHEMA: satu artikel published diekspor sebagai AIS native XML, DOAJ XML, PubMed XML dan JATS XML; seluruh hasil well-formed/XML-safe, DOAJ Articles XSD 1.3 dan NLM PubMed DTD 3.0 lulus. Integrity facts dan bounded same-author/similarity recommendation tersedia melalui route publik. Endpoint deposit sandbox tetap menjadi release gate |
| Existing payment reuse | `LanggananJurnal` + `LogPembayaran` pada `ais_jurnal_sit` | PARTIAL_PASS: rollback-only test membuktikan reference mismatch ditolak, amount/currency mengikuti immutable policy snapshot, settlement mengaktifkan langganan dan callback ulang idempoten pada payment yang sama; tidak ada fixture tersisa. Validasi signature adapter provider dan sandbox reconciliation eksternal belum dijalankan |
| Access/subscription/IP evaluator | `ais_jurnal_sit`, rollback-only service test | PARTIAL_PASS: open access, user subscription, institution IPv4 dan IPv6 lower/upper boundary, outside deny, normalized numeric overlap rejection dan hostname/DNS rejection lulus; expiry reminder, cache invalidation dan full policy matrix belum lengkap |
| Usage aggregation/reports | `ais_jurnal_sit`, rollback-only service tests | PARTIAL_PASS: vocabulary fisik existing `VIEW`/`DOWNLOAD`, total/country/referrer daily dimensions, bot exclusion, journal scope, site-admin-only global rebuild, delete-and-rebuild idempotency lulus. COUNTER Release 5 JSON menghasilkan Total Item Investigations/Requests dari aggregate existing; paged bounded CSV article/review/subscription memakai capability/object scope dan formula-injection guard melalui `/jurnal-report`. Workflow test memverifikasi 3 export. COUNTER validator resmi, late-event/privacy retention dan large-dataset memory measurement belum lengkap |
| Production usage capture | `RepoUsageEvent` existing + rollback integration | PASS_BEHAVIOR: article VIEW dan successful file DOWNLOAD route menulis event; HMAC-SHA256 visitor hash berganti harian dan tidak menyimpan IP, user-agent dibatasi/diklasifikasi bot, referrer hanya hostname, DNT/GPC opt-out, duplicate 30 detik ditolak. Capture nyata mengalir ke aggregate/COUNTER idempoten tanpa tabel baru |
| Official export schema validation | published workflow rollback + schema/DTD HTTPS resmi | PASS_SCHEMA: satu artikel published yang sama lulus Crossref Metadata Input Schema 5.3.1, DataCite Metadata Schema 4.5, DOAJ Articles XSD 1.3, dan NLM PubMed DTD 3.0 dengan heap 1,5 GB. Crossref/DOAJ memakai URL absolut dari `AIS_JURNAL_PUBLIC_BASE_URL`; author terurut diproyeksikan dari `RepoItemContributor`; journal title/publisher/ISSN memakai `RepoCollection.metadataProfileJson` existing; base, bahasa, ISSN, dan nama invalid fail-closed. Sandbox deposit ke provider eksternal tetap belum dijalankan |
| Public plugin behavior | profile JSON existing, JSP/servlet + rollback workflow | PARTIAL_PASS: journal/issue/subject browse, information reader/author/librarian, submission CTA, subscription info, bounded unique custom blocks, static pages, announcement feeds dan first-party usage tersedia tanpa tabel baru. GA4 ID tervalidasi dan hanya dimuat setelah POST consent ber-CSRF/cookie HttpOnly SameSite/CSP nonce; DNT/GPC mencegah load. Authenticated/browser consent journey tetap gate SIT |
| Galley viewers | `RepoBitstream` + `LampiranJurnal`, targeted security + cold-container test | PARTIAL_PASS: article hanya menampilkan galley `LINKED` production/publication; HTML memakai Jsoup allowlist tanpa script/style/image/event URL, JATS menolak DTD/entity dan menjadi HTML semantik escaped, PDF kembali ke stream entitlement existing. Render aktif dibatasi 2 MiB dan CSP default-none. Cold WAR membuktikan ID tidak ada = 404 tanpa redirect/`Location` + `nosniff`, dan mode invalid = 422; HTTP fixture untuk galley valid HTML/JATS/PDF masih wajib |
| Public discovery/citation metadata | public service/JSP + rollback publication test | PARTIAL_PASS: canonical article route, Scholar/DC/OpenGraph tags, Atom discovery link, BibTeX/RIS/APA/Vancouver/IEEE/CSL-JSON dan unknown-format fail-closed telah diimplementasikan; enam formatter lulus terhadap artikel yang benar-benar published. Container crawl, schema/SEO validator dan accessibility browser test belum dijalankan |
| Importer staging/provenance | disposable OJS 3.5 + legacy source fixtures -> disposable main/streaming target clones | PARTIAL_PASS: external JDBC read-only preflight tepat OJS `3.5.0-5`, 134 tabel/905 kolom; executable catalog tepat 69 MERGED + 37 ALTER + 11 NEW_MODEL + 4 DERIVED + 13 N/A; dry-run menghasilkan 134 checkpoint, 905 provenance, 905 explicit target outcomes, `STAGING_COMPLETED`, dan rerun idempotency job yang sama. Execute-mode menghasilkan 770 domain-linked field + 135 explicit derived/N/A, seluruh core root/relasi dan 1 PDF galley. Pada fixture yang direfresh 2026-08-22, secure `files_dir` reconciliation melakukan bounded stream 46 byte ke `LampiranJurnal`, memverifikasi read-back/SHA-256, rerun tanpa duplikasi, anonymous entitlement, lalu final gate lulus dengan `complete134Table905FieldReconciliation=true`. Adapter legacy mengenali tepat 7 tabel/37 field (`LEGACY`), mengimpor article/issue/title/TOC relation, rerun idempoten dan final status `COMPLETED`. In-flight cancel diamati setelah 2 committed checkpoint, berakhir `CANCELLED`, lalu resume memakai checkpoint dan menutup 134 tabel. Long-tail family semantics dan manual collision resolution UI tetap belum lengkap |
| Importer legacy compatibility | `JurnalPenelitianAction` | PASS: body writer jurnal/artikel OJS lama beserta seluruh import/call `OjsHibernateUtil` dihapus dari action; kedua tombol dan compatibility method lama hanya mengarah ke `/jurnal/admin/importOjs`. Class `OjsHibernateUtil` masih dipakai integrasi akun OJS non-jurnal dan baru dapat didekomisi setelah compatibility period Fase 19 |
| OAI metadata formats | `JurnalMetadataFormatService` + Java/container self-test | PARTIAL_PASS: serializer `oai_dc`, OAI MARC, MARCXML, OAI JATS dan RFC1807 tersedia, XML escaping dan unknown-format fail-closed lulus; resumption token opaque bertanda tangan HMAC, tamper/expiry/bounds fail-closed. Cold-container `Identify` dari WAR final 200 XML dengan CSP `default-src 'none'`, `nosniff`, dan `no-referrer`. Validator skema/protokol eksternal seluruh verb tetap wajib |
| Active-content galley hardening | `JurnalFile` | IMPLEMENTED_UNVERIFIED: HTML/XML/SVG tidak dirender inline; response dipaksa attachment dengan CSP sandbox, sementara PDF/media pasif dapat inline setelah entitlement. HTTP header/browser regression test belum dijalankan |
| Domain workflows/public portal | source + migration | IMPLEMENTED_UNVERIFIED; automated database/UI/protocol suites belum lengkap |
| Full Maven/WAR build | `build/maven/ais.war` | PASS package 2026-08-23 00:10 WIB: 7.043 source dikompilasi dan 47 non-Java resource disalin dari `src/main/src`; WAR 739.616.271 byte, SHA-256 `BC8466E47832D4A645A43B903E030DB30A8BC94E76FC9E32186B28A746ABF4C6`. Pemeriksaan ZIP membuktikan `hibernate.cfg.xml` existing, `web.xml`, JSP public/admin, servlet galley, profile/public projection, serializer/validator resmi, viewer, dan usage capture terpaket. Test suite legacy Maven tidak tersedia sehingga self-test jurnal dijalankan eksplisit |
| Dependency vulnerability inventory | OWASP Dependency-Check 13.0.0, NVD 2.0 + CISA KEV, `build/maven/dependency-check-report.json` | BLOCKED: 184 JAR fisik AIS dianalisis menjadi 1.762 dependency/komponen tertanam; 72 komponen memiliki 302 catatan kerentanan (53 critical, 127 high, 116 medium, 4 low berdasarkan skor CVSS yang tersedia). Temuan utama berada pada dependency legacy/bundled antara lain Spring 3.1.4, Log4j lama, PostgreSQL 42.2.10, Commons FileUpload, Axis, XStream/Jackson tertanam. SHA-256 laporan `465E84CB378FA1C790A183BBB592D133ACCFCF236F066AF1F837DEAE88C33716`. Angka adalah inventory scanner, bukan bukti exploitability; triage reachability, false-positive/suppression beralasan, lisensi/SBOM dan rencana upgrade kompatibel wajib sebelum RC. Karena masih ada Severity 1/2 terbuka, exit gate Fase 16 belum lulus |
| Cold WAR container smoke | Tomcat 9.0.89/JDK 25, clone SIT main+streaming, port terisolasi 18089 | PASS: `webapps` dibersihkan secara recoverable sehingga hanya WAR dengan hash final yang dideploy; startup 366.228 ms tanpa `SEVERE` baru. `/jurnal`, `/jurnal/browse`, search, Atom dan OAI `Identify` = 200 dengan marker/body benar; OAI CSP `default-src 'none'`; galley tidak ada = 404 tanpa redirect, mode invalid = 422; consent GET = 405 dan POST tanpa CSRF = 403; admin anonim ditahan filter login existing sebagai 302. Tomcat dihentikan tanpa proses tersisa. Debt non-blocking tetap: DBCP2 mengabaikan nama properti pool legacy dan `ErrorAuditUtil` ThreadLocal dilaporkan saat undeploy lama |
| Aggregated journal self-test run | 28 class test dari `build/maven/classes`, clone SIT dan disposable importer fixtures | PASS 28/28 pada 2026-08-23: 20 contract/database/authorization/workflow/payment/email/file/galley/usage test serta 8 importer/mapping test. Workflow yang sama lulus empat validator resmi; coverage tepat 134/134 tabel dan 905/905 field; OJS 3.5 read-only, dry-run, execute, cancel/resume, file reconciliation/finalisasi serta legacy 7 tabel/37 field lulus. Mapping coverage CLI menerima `02-OJS-3505-TABLE-MAPPING.md`; JVM legacy dijalankan dengan `--add-opens=java.base/java.lang=ALL-UNNAMED`; credential hanya melalui environment |
| Post-test clone hygiene | main/streaming SIT clones | PASS: main tetap 3.293 tabel, tanpa collection self-test/assignment/import job; streaming tetap 80 tabel, 4 `lampiran_lain`, 5 large object |
| SIT/UAT sign-off | clone terpisah | NOT_RUN; hanya dijalankan setelah seluruh feature gate siap |

Credential tidak dicatat di ledger, log, Markdown, source, atau XML. Semua proses test memperoleh secret melalui environment `AIS_JURNAL_DB_*` dan `AIS_JURNAL_STREAMING_DB_*`.

